# ML Test Score mapping for `_codex_` (scaffolding).
#
# categories -> description + tests
categories:
  infrastructure:
    description: "Core tools sanity (env snapshot, experiment index, config validate)"
    tests:
      - tests/tools
  codex_ml:
    description: "Core codex_ml scaffolding tests"
    tests:
      - tests/codex_ml
  data:
    description: "Placeholder for future data tests"
    tests:
      - tests/data
