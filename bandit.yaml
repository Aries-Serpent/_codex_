# Bandit security linter configuration
# https://bandit.readthedocs.io/en/latest/config.html

skips:
  # B101: assert statements are acceptable in tests and non-production guards
  - B101
  # B603/B607: subprocess without shell is fine — we ban shell=True via custom hook
  - B603
  - B607

assert_used:
  skips:
    - "tests/*"
    - "src/*/test_*.py"
