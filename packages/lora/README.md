# codex-ml-lora

Dependency-free LoRA configuration and backend contracts. The PEFT and legacy
Codex ML adapters resolve their heavy dependencies only when invoked.

## Installation

This alpha package is not published on public PyPI. From the repository root,
install it from the checked-out source tree:

```console
python -m pip install ./packages/lora
```
