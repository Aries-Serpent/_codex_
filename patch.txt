--- .github/workflows/copilot-setup-steps.yml
+++ .github/workflows/copilot-setup-steps.yml
@@ -445,17 +445,2 @@
           if [ -f .codex/agent_context.json ]; then
             echo "Injecting repo variable context from .codex/agent_context.json"
-            python3 -c "import json, os
-            try:
-                with open('.codex/agent_context.json', encoding='utf-8') as handle:
-                    context = json.load(handle)
-                with open(os.environ['GITHUB_ENV'], 'a', encoding='utf-8') as github_env:
-                    injected = 0
-                    for key, value in context.items():
-                        if not key.startswith('_') and value:
-                            github_env.write(f'{key}={value}\n')
-                            injected += 1
-                print(f'Injected {injected} variables into GITHUB_ENV')
-            except Exception as exc:
-                print(f'Warning: {exc}')"
+            python3 -c "import json, os; ctx = json.load(open('.codex/agent_context.json', encoding='utf-8')); [open(os.environ['GITHUB_ENV'], 'a', encoding='utf-8').write(f'{k}={v}\n') for k, v in ctx.items() if not k.startswith('_') and v]; print('Injected variables into GITHUB_ENV')"
           else
