# ML Lite requirements - CPU-only PyTorch
# Total estimated size: ~200-300 MB
# NO CUDA dependencies (saves 3.5 GB)

# CPU-only PyTorch index
--extra-index-url https://download.pytorch.org/whl/cpu

# CPU-only PyTorch (much smaller than CUDA version)
# Minimum 2.6.0: fixes torch.load RCE with weights_only=True (CVE-2025-32434)
# Must stay consistent with pyproject.toml torch>=2.6.0,<3.0.0
torch>=2.6.0

# Transformers (without model downloads)
transformers>=5.8.0

# Tokenization
sentencepiece>=0.1.99

# Deterministic numeric kernels
numpy>=1.26

# Note: Do NOT include:
# - accelerate (pulls CUDA dependencies)
# - Any nvidia-* packages
# - torchvision (not needed for our tests)
# - torchaudio (not needed for our tests)
