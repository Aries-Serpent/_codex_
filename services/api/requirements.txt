# BEGIN: CODEX_API_REQS
# fastapi>=0.115 pulls in a compatible starlette via transitive dependency.
# Keep this service requirements file unpinned for starlette so FastAPI manages
# its compatible range; repository-wide starlette policy remains defined in the
# root pyproject.toml for root-package environments.
fastapi==0.115.12
uvicorn==0.30.1
pydantic==2.8.2
