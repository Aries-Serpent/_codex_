# Normalize text; keep LF endings in the repository
* text=auto eol=lf

# Explicit text normalization for common source/assets
*.md      text eol=lf
*.txt     text eol=lf
*.json    text eol=lf
*.yaml    text eol=lf
*.yml     text eol=lf
*.py      text eol=lf
*.sh      text eol=lf
*.vocab   text eol=lf
*.sha256  text eol=lf
SHA256SUMS text eol=lf

# Binary artifacts — keep raw bytes and skip diffs (use LFS selectively)
*.model       binary
*.bin         binary
*.safetensors binary
*.zip         binary
*.7z          binary
*.tar         binary
*.tar.*       binary
*.png         binary
*.jpg         binary
*.jpeg        binary
*.gif         binary
*.webp        binary
*.ico         binary
*.pdf         binary
*.mp4         binary
*.mov         binary
*.mp3         binary
*.wav         binary
*.ttf         binary
*.otf         binary
*.woff        binary
*.woff2       binary
*.dll         binary
*.so          binary
*.dylib       binary

# Generated/minified assets: suppress diffs to avoid noisy reviews
*.min.js   -diff
*.min.css  -diff
*.map      -diff
dist/**    -diff
build/**   -diff
out/**     -diff
public/**  -diff

# Heavy notebook JSON diffs rarely help reviewers
*.ipynb    -diff

# Lockfiles are generated; mark them so GitHub collapses them by default
package-lock.json linguist-generated=true
yarn.lock         linguist-generated=true
pnpm-lock.yaml    linguist-generated=true

# NOTE: Track large binaries with Git LFS on a per-path basis when needed.

