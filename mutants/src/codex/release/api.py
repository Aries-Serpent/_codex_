"""
Api Module

This module provides functionality for api.

Usage:
    from release.api import ...

Classes:
    [To be documented]

Functions:
    [To be documented]

Author: Codex Team
"""

from __future__ import annotations
import logging
logger = logging.getLogger(__name__)

import contextlib
import hashlib
import io
import json
import os
import shutil
import tarfile
from pathlib import Path

from codex.archive.api import restore
from codex.archive.dal import ArchiveDAL
from codex.evidence.core import evidence_append
from codex.release.manifest import dump_manifest_locked, load_manifest
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


def x__set_mode__mutmut_orig(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_1(path: Path, mode_str: str) -> None:
    mode = None
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_2(path: Path, mode_str: str) -> None:
    mode = int(None, 8)
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_3(path: Path, mode_str: str) -> None:
    mode = int(mode_str, None)
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_4(path: Path, mode_str: str) -> None:
    mode = int(8)
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_5(path: Path, mode_str: str) -> None:
    mode = int(mode_str, )
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_6(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 9)
    os.chmod(path, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_7(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(None, (path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_8(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, None)


def x__set_mode__mutmut_9(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod((path.stat().st_mode & ~0o777) | mode)


def x__set_mode__mutmut_10(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, )


def x__set_mode__mutmut_11(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, (path.stat().st_mode & ~0o777) & mode)


def x__set_mode__mutmut_12(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, (path.stat().st_mode | ~0o777) | mode)


def x__set_mode__mutmut_13(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, (path.stat().st_mode & 0o777) | mode)


def x__set_mode__mutmut_14(path: Path, mode_str: str) -> None:
    mode = int(mode_str, 8)
    os.chmod(path, (path.stat().st_mode & ~512) | mode)

x__set_mode__mutmut_mutants : ClassVar[MutantDict] = {
'x__set_mode__mutmut_1': x__set_mode__mutmut_1, 
    'x__set_mode__mutmut_2': x__set_mode__mutmut_2, 
    'x__set_mode__mutmut_3': x__set_mode__mutmut_3, 
    'x__set_mode__mutmut_4': x__set_mode__mutmut_4, 
    'x__set_mode__mutmut_5': x__set_mode__mutmut_5, 
    'x__set_mode__mutmut_6': x__set_mode__mutmut_6, 
    'x__set_mode__mutmut_7': x__set_mode__mutmut_7, 
    'x__set_mode__mutmut_8': x__set_mode__mutmut_8, 
    'x__set_mode__mutmut_9': x__set_mode__mutmut_9, 
    'x__set_mode__mutmut_10': x__set_mode__mutmut_10, 
    'x__set_mode__mutmut_11': x__set_mode__mutmut_11, 
    'x__set_mode__mutmut_12': x__set_mode__mutmut_12, 
    'x__set_mode__mutmut_13': x__set_mode__mutmut_13, 
    'x__set_mode__mutmut_14': x__set_mode__mutmut_14
}

def _set_mode(*args, **kwargs):
    result = _mutmut_trampoline(x__set_mode__mutmut_orig, x__set_mode__mutmut_mutants, args, kwargs)
    return result 

_set_mode.__signature__ = _mutmut_signature(x__set_mode__mutmut_orig)
x__set_mode__mutmut_orig.__name__ = 'x__set_mode'


def x__templated_bytes__mutmut_orig(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_1(data: bytes, vars: dict) -> bytes:
    if vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_2(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = None
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_3(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode(None, "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_4(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", None)
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_5(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_6(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", )
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_7(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("XXutf-8XX", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_8(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("UTF-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_9(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "XXignoreXX")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_10(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "IGNORE")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_11(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning(None, exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_12(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=None)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_13(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning(exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_14(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", )
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_15(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("XXException occurredXX", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_16(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_17(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("EXCEPTION OCCURRED", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_18(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=False)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_19(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning(None, exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_20(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=None)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_21(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning(exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_22(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", )
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_23(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("XXException occurredXX", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_24(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_25(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("EXCEPTION OCCURRED", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_26(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=False)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_27(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = None
    return s.encode("utf-8")


def x__templated_bytes__mutmut_28(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace(None, str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_29(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", None)
    return s.encode("utf-8")


def x__templated_bytes__mutmut_30(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace(str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_31(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", )
    return s.encode("utf-8")


def x__templated_bytes__mutmut_32(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) - "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_33(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" - str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_34(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("XX{{XX" + str(k) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_35(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(None) + "}}", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_36(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "XX}}XX", str(v))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_37(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(None))
    return s.encode("utf-8")


def x__templated_bytes__mutmut_38(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode(None)


def x__templated_bytes__mutmut_39(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("XXutf-8XX")


def x__templated_bytes__mutmut_40(data: bytes, vars: dict) -> bytes:
    if not vars:
        return data
    try:
        s = data.decode("utf-8", "ignore")
    except Exception:
        logger.warning("Exception occurred", exc_info=True)
        logger.warning("Exception occurred", exc_info=True)
        return data
    for k, v in vars.items():
        s = s.replace("{{" + str(k) + "}}", str(v))
    return s.encode("UTF-8")

x__templated_bytes__mutmut_mutants : ClassVar[MutantDict] = {
'x__templated_bytes__mutmut_1': x__templated_bytes__mutmut_1, 
    'x__templated_bytes__mutmut_2': x__templated_bytes__mutmut_2, 
    'x__templated_bytes__mutmut_3': x__templated_bytes__mutmut_3, 
    'x__templated_bytes__mutmut_4': x__templated_bytes__mutmut_4, 
    'x__templated_bytes__mutmut_5': x__templated_bytes__mutmut_5, 
    'x__templated_bytes__mutmut_6': x__templated_bytes__mutmut_6, 
    'x__templated_bytes__mutmut_7': x__templated_bytes__mutmut_7, 
    'x__templated_bytes__mutmut_8': x__templated_bytes__mutmut_8, 
    'x__templated_bytes__mutmut_9': x__templated_bytes__mutmut_9, 
    'x__templated_bytes__mutmut_10': x__templated_bytes__mutmut_10, 
    'x__templated_bytes__mutmut_11': x__templated_bytes__mutmut_11, 
    'x__templated_bytes__mutmut_12': x__templated_bytes__mutmut_12, 
    'x__templated_bytes__mutmut_13': x__templated_bytes__mutmut_13, 
    'x__templated_bytes__mutmut_14': x__templated_bytes__mutmut_14, 
    'x__templated_bytes__mutmut_15': x__templated_bytes__mutmut_15, 
    'x__templated_bytes__mutmut_16': x__templated_bytes__mutmut_16, 
    'x__templated_bytes__mutmut_17': x__templated_bytes__mutmut_17, 
    'x__templated_bytes__mutmut_18': x__templated_bytes__mutmut_18, 
    'x__templated_bytes__mutmut_19': x__templated_bytes__mutmut_19, 
    'x__templated_bytes__mutmut_20': x__templated_bytes__mutmut_20, 
    'x__templated_bytes__mutmut_21': x__templated_bytes__mutmut_21, 
    'x__templated_bytes__mutmut_22': x__templated_bytes__mutmut_22, 
    'x__templated_bytes__mutmut_23': x__templated_bytes__mutmut_23, 
    'x__templated_bytes__mutmut_24': x__templated_bytes__mutmut_24, 
    'x__templated_bytes__mutmut_25': x__templated_bytes__mutmut_25, 
    'x__templated_bytes__mutmut_26': x__templated_bytes__mutmut_26, 
    'x__templated_bytes__mutmut_27': x__templated_bytes__mutmut_27, 
    'x__templated_bytes__mutmut_28': x__templated_bytes__mutmut_28, 
    'x__templated_bytes__mutmut_29': x__templated_bytes__mutmut_29, 
    'x__templated_bytes__mutmut_30': x__templated_bytes__mutmut_30, 
    'x__templated_bytes__mutmut_31': x__templated_bytes__mutmut_31, 
    'x__templated_bytes__mutmut_32': x__templated_bytes__mutmut_32, 
    'x__templated_bytes__mutmut_33': x__templated_bytes__mutmut_33, 
    'x__templated_bytes__mutmut_34': x__templated_bytes__mutmut_34, 
    'x__templated_bytes__mutmut_35': x__templated_bytes__mutmut_35, 
    'x__templated_bytes__mutmut_36': x__templated_bytes__mutmut_36, 
    'x__templated_bytes__mutmut_37': x__templated_bytes__mutmut_37, 
    'x__templated_bytes__mutmut_38': x__templated_bytes__mutmut_38, 
    'x__templated_bytes__mutmut_39': x__templated_bytes__mutmut_39, 
    'x__templated_bytes__mutmut_40': x__templated_bytes__mutmut_40
}

def _templated_bytes(*args, **kwargs):
    result = _mutmut_trampoline(x__templated_bytes__mutmut_orig, x__templated_bytes__mutmut_mutants, args, kwargs)
    return result 

_templated_bytes.__signature__ = _mutmut_signature(x__templated_bytes__mutmut_orig)
x__templated_bytes__mutmut_orig.__name__ = 'x__templated_bytes'


def x__tar_add_bytes__mutmut_orig(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_1(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 421) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_2(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = None
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_3(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(None)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_4(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = None
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_5(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = None
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_6(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 1
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_7(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = None
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_8(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 1
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_9(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = None
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_10(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = "XXXX"
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_11(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = None
    tar.addfile(ti, io.BytesIO(b))


def x__tar_add_bytes__mutmut_12(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(None, io.BytesIO(b))


def x__tar_add_bytes__mutmut_13(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, None)


def x__tar_add_bytes__mutmut_14(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(io.BytesIO(b))


def x__tar_add_bytes__mutmut_15(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, )


def x__tar_add_bytes__mutmut_16(tar: tarfile.TarFile, arcname: str, b: bytes, mode: int = 0o644) -> None:
    ti = tarfile.TarInfo(arcname)
    ti.size = len(b)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.mode = mode
    tar.addfile(ti, io.BytesIO(None))

x__tar_add_bytes__mutmut_mutants : ClassVar[MutantDict] = {
'x__tar_add_bytes__mutmut_1': x__tar_add_bytes__mutmut_1, 
    'x__tar_add_bytes__mutmut_2': x__tar_add_bytes__mutmut_2, 
    'x__tar_add_bytes__mutmut_3': x__tar_add_bytes__mutmut_3, 
    'x__tar_add_bytes__mutmut_4': x__tar_add_bytes__mutmut_4, 
    'x__tar_add_bytes__mutmut_5': x__tar_add_bytes__mutmut_5, 
    'x__tar_add_bytes__mutmut_6': x__tar_add_bytes__mutmut_6, 
    'x__tar_add_bytes__mutmut_7': x__tar_add_bytes__mutmut_7, 
    'x__tar_add_bytes__mutmut_8': x__tar_add_bytes__mutmut_8, 
    'x__tar_add_bytes__mutmut_9': x__tar_add_bytes__mutmut_9, 
    'x__tar_add_bytes__mutmut_10': x__tar_add_bytes__mutmut_10, 
    'x__tar_add_bytes__mutmut_11': x__tar_add_bytes__mutmut_11, 
    'x__tar_add_bytes__mutmut_12': x__tar_add_bytes__mutmut_12, 
    'x__tar_add_bytes__mutmut_13': x__tar_add_bytes__mutmut_13, 
    'x__tar_add_bytes__mutmut_14': x__tar_add_bytes__mutmut_14, 
    'x__tar_add_bytes__mutmut_15': x__tar_add_bytes__mutmut_15, 
    'x__tar_add_bytes__mutmut_16': x__tar_add_bytes__mutmut_16
}

def _tar_add_bytes(*args, **kwargs):
    result = _mutmut_trampoline(x__tar_add_bytes__mutmut_orig, x__tar_add_bytes__mutmut_mutants, args, kwargs)
    return result 

_tar_add_bytes.__signature__ = _mutmut_signature(x__tar_add_bytes__mutmut_orig)
x__tar_add_bytes__mutmut_orig.__name__ = 'x__tar_add_bytes'


def x__tar_add_symlink__mutmut_orig(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_1(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = None
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_2(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(None)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_3(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = None
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_4(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 1
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_5(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = None
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_6(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 1
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_7(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = None
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_8(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = "XXXX"
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_9(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = None
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_10(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = None
    ti.mode = 0o777
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_11(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = None
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_12(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 512
    tar.addfile(ti)


def x__tar_add_symlink__mutmut_13(tar: tarfile.TarFile, linkname: str, target: str) -> None:
    ti = tarfile.TarInfo(linkname)
    ti.mtime = 0
    ti.uid = ti.gid = 0
    ti.uname = ti.gname = ""
    ti.type = tarfile.SYMTYPE
    ti.linkname = target
    ti.mode = 0o777
    tar.addfile(None)

x__tar_add_symlink__mutmut_mutants : ClassVar[MutantDict] = {
'x__tar_add_symlink__mutmut_1': x__tar_add_symlink__mutmut_1, 
    'x__tar_add_symlink__mutmut_2': x__tar_add_symlink__mutmut_2, 
    'x__tar_add_symlink__mutmut_3': x__tar_add_symlink__mutmut_3, 
    'x__tar_add_symlink__mutmut_4': x__tar_add_symlink__mutmut_4, 
    'x__tar_add_symlink__mutmut_5': x__tar_add_symlink__mutmut_5, 
    'x__tar_add_symlink__mutmut_6': x__tar_add_symlink__mutmut_6, 
    'x__tar_add_symlink__mutmut_7': x__tar_add_symlink__mutmut_7, 
    'x__tar_add_symlink__mutmut_8': x__tar_add_symlink__mutmut_8, 
    'x__tar_add_symlink__mutmut_9': x__tar_add_symlink__mutmut_9, 
    'x__tar_add_symlink__mutmut_10': x__tar_add_symlink__mutmut_10, 
    'x__tar_add_symlink__mutmut_11': x__tar_add_symlink__mutmut_11, 
    'x__tar_add_symlink__mutmut_12': x__tar_add_symlink__mutmut_12, 
    'x__tar_add_symlink__mutmut_13': x__tar_add_symlink__mutmut_13
}

def _tar_add_symlink(*args, **kwargs):
    result = _mutmut_trampoline(x__tar_add_symlink__mutmut_orig, x__tar_add_symlink__mutmut_mutants, args, kwargs)
    return result 

_tar_add_symlink.__signature__ = _mutmut_signature(x__tar_add_symlink__mutmut_orig)
x__tar_add_symlink__mutmut_orig.__name__ = 'x__tar_add_symlink'


def x__is_safe_member__mutmut_orig(member: tarfile.TarInfo) -> bool:
    name = Path(member.name)
    return not name.is_absolute() and ".." not in name.parts


def x__is_safe_member__mutmut_1(member: tarfile.TarInfo) -> bool:
    name = None
    return not name.is_absolute() and ".." not in name.parts


def x__is_safe_member__mutmut_2(member: tarfile.TarInfo) -> bool:
    name = Path(None)
    return not name.is_absolute() and ".." not in name.parts


def x__is_safe_member__mutmut_3(member: tarfile.TarInfo) -> bool:
    name = Path(member.name)
    return not name.is_absolute() or ".." not in name.parts


def x__is_safe_member__mutmut_4(member: tarfile.TarInfo) -> bool:
    name = Path(member.name)
    return name.is_absolute() and ".." not in name.parts


def x__is_safe_member__mutmut_5(member: tarfile.TarInfo) -> bool:
    name = Path(member.name)
    return not name.is_absolute() and "XX..XX" not in name.parts


def x__is_safe_member__mutmut_6(member: tarfile.TarInfo) -> bool:
    name = Path(member.name)
    return not name.is_absolute() and ".." in name.parts

x__is_safe_member__mutmut_mutants : ClassVar[MutantDict] = {
'x__is_safe_member__mutmut_1': x__is_safe_member__mutmut_1, 
    'x__is_safe_member__mutmut_2': x__is_safe_member__mutmut_2, 
    'x__is_safe_member__mutmut_3': x__is_safe_member__mutmut_3, 
    'x__is_safe_member__mutmut_4': x__is_safe_member__mutmut_4, 
    'x__is_safe_member__mutmut_5': x__is_safe_member__mutmut_5, 
    'x__is_safe_member__mutmut_6': x__is_safe_member__mutmut_6
}

def _is_safe_member(*args, **kwargs):
    result = _mutmut_trampoline(x__is_safe_member__mutmut_orig, x__is_safe_member__mutmut_mutants, args, kwargs)
    return result 

_is_safe_member.__signature__ = _mutmut_signature(x__is_safe_member__mutmut_orig)
x__is_safe_member__mutmut_orig.__name__ = 'x__is_safe_member'


def x__ensure_within__mutmut_orig(base: Path, candidate: Path, what: str) -> None:
    candidate_abs = candidate.resolve(strict=False)
    try:
        candidate_abs.relative_to(base)
    except ValueError as exc:  # pragma: no cover - defensive guard
        raise ValueError(f"{what} escapes destination: {candidate}") from exc


def x__ensure_within__mutmut_1(base: Path, candidate: Path, what: str) -> None:
    candidate_abs = None
    try:
        candidate_abs.relative_to(base)
    except ValueError as exc:  # pragma: no cover - defensive guard
        raise ValueError(f"{what} escapes destination: {candidate}") from exc


def x__ensure_within__mutmut_2(base: Path, candidate: Path, what: str) -> None:
    candidate_abs = candidate.resolve(strict=None)
    try:
        candidate_abs.relative_to(base)
    except ValueError as exc:  # pragma: no cover - defensive guard
        raise ValueError(f"{what} escapes destination: {candidate}") from exc


def x__ensure_within__mutmut_3(base: Path, candidate: Path, what: str) -> None:
    candidate_abs = candidate.resolve(strict=True)
    try:
        candidate_abs.relative_to(base)
    except ValueError as exc:  # pragma: no cover - defensive guard
        raise ValueError(f"{what} escapes destination: {candidate}") from exc


def x__ensure_within__mutmut_4(base: Path, candidate: Path, what: str) -> None:
    candidate_abs = candidate.resolve(strict=False)
    try:
        candidate_abs.relative_to(None)
    except ValueError as exc:  # pragma: no cover - defensive guard
        raise ValueError(f"{what} escapes destination: {candidate}") from exc


def x__ensure_within__mutmut_5(base: Path, candidate: Path, what: str) -> None:
    candidate_abs = candidate.resolve(strict=False)
    try:
        candidate_abs.relative_to(base)
    except ValueError as exc:  # pragma: no cover - defensive guard
        raise ValueError(None) from exc

x__ensure_within__mutmut_mutants : ClassVar[MutantDict] = {
'x__ensure_within__mutmut_1': x__ensure_within__mutmut_1, 
    'x__ensure_within__mutmut_2': x__ensure_within__mutmut_2, 
    'x__ensure_within__mutmut_3': x__ensure_within__mutmut_3, 
    'x__ensure_within__mutmut_4': x__ensure_within__mutmut_4, 
    'x__ensure_within__mutmut_5': x__ensure_within__mutmut_5
}

def _ensure_within(*args, **kwargs):
    result = _mutmut_trampoline(x__ensure_within__mutmut_orig, x__ensure_within__mutmut_mutants, args, kwargs)
    return result 

_ensure_within.__signature__ = _mutmut_signature(x__ensure_within__mutmut_orig)
x__ensure_within__mutmut_orig.__name__ = 'x__ensure_within'


def x__safe_extract__mutmut_orig(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_1(tar: tarfile.TarFile, dest: Path) -> None:
    base = None
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_2(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_3(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(None):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_4(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(None)
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_5(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() and member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_6(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() and member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_7(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(None)
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_8(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = None
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_9(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest * member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_10(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(None, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_11(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, None, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_12(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, None)
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_13(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_14(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_15(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, )
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_16(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "XXmember pathXX")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_17(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "MEMBER PATH")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_18(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = None
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_19(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(None)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_20(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(None)
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_21(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                None,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_22(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                None,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_23(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                None,
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_24(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_25(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_26(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_27(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent * link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_28(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                None,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_29(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                None,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_30(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                None,
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_31(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_32(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_33(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_34(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest * member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_35(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=None, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_36(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=None)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_37(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_38(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, )
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_39(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=False, exist_ok=True)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_40(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=False)
        tar.extract(member, dest.as_posix())


def x__safe_extract__mutmut_41(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(None, dest.as_posix())


def x__safe_extract__mutmut_42(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, None)


def x__safe_extract__mutmut_43(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(dest.as_posix())


def x__safe_extract__mutmut_44(tar: tarfile.TarFile, dest: Path) -> None:
    base = dest.resolve()
    for member in tar.getmembers():
        if not _is_safe_member(member):
            raise ValueError(f"unsafe path in archive: {member.name}")
        if member.ischr() or member.isblk() or member.isfifo():
            raise ValueError(f"unsupported special file in archive: {member.name}")
        target = dest / member.name
        _ensure_within(base, target, "member path")
        if member.issym():
            link_target = Path(member.linkname)
            if link_target.is_absolute():
                raise ValueError(f"absolute symlink target not allowed: {member.linkname}")
            _ensure_within(
                base,
                target.parent / link_target,
                f"symlink target for {member.name}",
            )
        elif member.islnk():
            _ensure_within(
                base,
                dest / member.linkname,
                f"hardlink target for {member.name}",
            )
        target.parent.mkdir(parents=True, exist_ok=True)
        tar.extract(member, )

x__safe_extract__mutmut_mutants : ClassVar[MutantDict] = {
'x__safe_extract__mutmut_1': x__safe_extract__mutmut_1, 
    'x__safe_extract__mutmut_2': x__safe_extract__mutmut_2, 
    'x__safe_extract__mutmut_3': x__safe_extract__mutmut_3, 
    'x__safe_extract__mutmut_4': x__safe_extract__mutmut_4, 
    'x__safe_extract__mutmut_5': x__safe_extract__mutmut_5, 
    'x__safe_extract__mutmut_6': x__safe_extract__mutmut_6, 
    'x__safe_extract__mutmut_7': x__safe_extract__mutmut_7, 
    'x__safe_extract__mutmut_8': x__safe_extract__mutmut_8, 
    'x__safe_extract__mutmut_9': x__safe_extract__mutmut_9, 
    'x__safe_extract__mutmut_10': x__safe_extract__mutmut_10, 
    'x__safe_extract__mutmut_11': x__safe_extract__mutmut_11, 
    'x__safe_extract__mutmut_12': x__safe_extract__mutmut_12, 
    'x__safe_extract__mutmut_13': x__safe_extract__mutmut_13, 
    'x__safe_extract__mutmut_14': x__safe_extract__mutmut_14, 
    'x__safe_extract__mutmut_15': x__safe_extract__mutmut_15, 
    'x__safe_extract__mutmut_16': x__safe_extract__mutmut_16, 
    'x__safe_extract__mutmut_17': x__safe_extract__mutmut_17, 
    'x__safe_extract__mutmut_18': x__safe_extract__mutmut_18, 
    'x__safe_extract__mutmut_19': x__safe_extract__mutmut_19, 
    'x__safe_extract__mutmut_20': x__safe_extract__mutmut_20, 
    'x__safe_extract__mutmut_21': x__safe_extract__mutmut_21, 
    'x__safe_extract__mutmut_22': x__safe_extract__mutmut_22, 
    'x__safe_extract__mutmut_23': x__safe_extract__mutmut_23, 
    'x__safe_extract__mutmut_24': x__safe_extract__mutmut_24, 
    'x__safe_extract__mutmut_25': x__safe_extract__mutmut_25, 
    'x__safe_extract__mutmut_26': x__safe_extract__mutmut_26, 
    'x__safe_extract__mutmut_27': x__safe_extract__mutmut_27, 
    'x__safe_extract__mutmut_28': x__safe_extract__mutmut_28, 
    'x__safe_extract__mutmut_29': x__safe_extract__mutmut_29, 
    'x__safe_extract__mutmut_30': x__safe_extract__mutmut_30, 
    'x__safe_extract__mutmut_31': x__safe_extract__mutmut_31, 
    'x__safe_extract__mutmut_32': x__safe_extract__mutmut_32, 
    'x__safe_extract__mutmut_33': x__safe_extract__mutmut_33, 
    'x__safe_extract__mutmut_34': x__safe_extract__mutmut_34, 
    'x__safe_extract__mutmut_35': x__safe_extract__mutmut_35, 
    'x__safe_extract__mutmut_36': x__safe_extract__mutmut_36, 
    'x__safe_extract__mutmut_37': x__safe_extract__mutmut_37, 
    'x__safe_extract__mutmut_38': x__safe_extract__mutmut_38, 
    'x__safe_extract__mutmut_39': x__safe_extract__mutmut_39, 
    'x__safe_extract__mutmut_40': x__safe_extract__mutmut_40, 
    'x__safe_extract__mutmut_41': x__safe_extract__mutmut_41, 
    'x__safe_extract__mutmut_42': x__safe_extract__mutmut_42, 
    'x__safe_extract__mutmut_43': x__safe_extract__mutmut_43, 
    'x__safe_extract__mutmut_44': x__safe_extract__mutmut_44
}

def _safe_extract(*args, **kwargs):
    result = _mutmut_trampoline(x__safe_extract__mutmut_orig, x__safe_extract__mutmut_mutants, args, kwargs)
    return result 

_safe_extract.__signature__ = _mutmut_signature(x__safe_extract__mutmut_orig)
x__safe_extract__mutmut_orig.__name__ = 'x__safe_extract'


def x__evidence_append_release__mutmut_orig(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_1(action: str, payload: dict) -> None:
    actor = None
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_2(action: str, payload: dict) -> None:
    actor = os.getenv(None, "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_3(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", None)
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_4(action: str, payload: dict) -> None:
    actor = os.getenv("codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_5(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", )
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_6(action: str, payload: dict) -> None:
    actor = os.getenv("XXCODEX_ACTORXX", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_7(action: str, payload: dict) -> None:
    actor = os.getenv("codex_actor", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_8(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "XXcodexXX")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_9(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "CODEX")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_10(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=None, actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_11(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=None, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_12(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool=None, repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_13(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo=None, context=payload)


def x__evidence_append_release__mutmut_14(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", context=None)


def x__evidence_append_release__mutmut_15(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(actor=actor, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_16(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, tool="release", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_17(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_18(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", context=payload)


def x__evidence_append_release__mutmut_19(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_codex_", )


def x__evidence_append_release__mutmut_20(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="XXreleaseXX", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_21(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="RELEASE", repo="_codex_", context=payload)


def x__evidence_append_release__mutmut_22(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="XX_codex_XX", context=payload)


def x__evidence_append_release__mutmut_23(action: str, payload: dict) -> None:
    actor = os.getenv("CODEX_ACTOR", "codex")
    evidence_append(action=action, actor=actor, tool="release", repo="_CODEX_", context=payload)

x__evidence_append_release__mutmut_mutants : ClassVar[MutantDict] = {
'x__evidence_append_release__mutmut_1': x__evidence_append_release__mutmut_1, 
    'x__evidence_append_release__mutmut_2': x__evidence_append_release__mutmut_2, 
    'x__evidence_append_release__mutmut_3': x__evidence_append_release__mutmut_3, 
    'x__evidence_append_release__mutmut_4': x__evidence_append_release__mutmut_4, 
    'x__evidence_append_release__mutmut_5': x__evidence_append_release__mutmut_5, 
    'x__evidence_append_release__mutmut_6': x__evidence_append_release__mutmut_6, 
    'x__evidence_append_release__mutmut_7': x__evidence_append_release__mutmut_7, 
    'x__evidence_append_release__mutmut_8': x__evidence_append_release__mutmut_8, 
    'x__evidence_append_release__mutmut_9': x__evidence_append_release__mutmut_9, 
    'x__evidence_append_release__mutmut_10': x__evidence_append_release__mutmut_10, 
    'x__evidence_append_release__mutmut_11': x__evidence_append_release__mutmut_11, 
    'x__evidence_append_release__mutmut_12': x__evidence_append_release__mutmut_12, 
    'x__evidence_append_release__mutmut_13': x__evidence_append_release__mutmut_13, 
    'x__evidence_append_release__mutmut_14': x__evidence_append_release__mutmut_14, 
    'x__evidence_append_release__mutmut_15': x__evidence_append_release__mutmut_15, 
    'x__evidence_append_release__mutmut_16': x__evidence_append_release__mutmut_16, 
    'x__evidence_append_release__mutmut_17': x__evidence_append_release__mutmut_17, 
    'x__evidence_append_release__mutmut_18': x__evidence_append_release__mutmut_18, 
    'x__evidence_append_release__mutmut_19': x__evidence_append_release__mutmut_19, 
    'x__evidence_append_release__mutmut_20': x__evidence_append_release__mutmut_20, 
    'x__evidence_append_release__mutmut_21': x__evidence_append_release__mutmut_21, 
    'x__evidence_append_release__mutmut_22': x__evidence_append_release__mutmut_22, 
    'x__evidence_append_release__mutmut_23': x__evidence_append_release__mutmut_23
}

def _evidence_append_release(*args, **kwargs):
    result = _mutmut_trampoline(x__evidence_append_release__mutmut_orig, x__evidence_append_release__mutmut_mutants, args, kwargs)
    return result 

_evidence_append_release.__signature__ = _mutmut_signature(x__evidence_append_release__mutmut_orig)
x__evidence_append_release__mutmut_orig.__name__ = 'x__evidence_append_release'


def x__clean_path__mutmut_orig(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_1(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_2(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(None):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_3(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(None, 0o700)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_4(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, None)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_5(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(0o700)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_6(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, )
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_7(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 449)
            func(p)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_8(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(None)

        shutil.rmtree(path, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_9(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(None, onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_10(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(path, onerror=None)
        return

    path.unlink()


def x__clean_path__mutmut_11(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(onerror=_onerror)
        return

    path.unlink()


def x__clean_path__mutmut_12(path: Path) -> None:
    """Remove ``path`` regardless of whether it is a file, directory, or symlink."""

    if path.is_symlink():
        path.unlink()
        return

    if not path.exists():
        return

    if path.is_file():
        path.unlink()
        return

    if path.is_dir():

        def _onerror(func, p, exc_info):  # pragma: no cover - defensive cleanup
            with contextlib.suppress(OSError):
                os.chmod(p, 0o700)
            func(p)

        shutil.rmtree(path, )
        return

    path.unlink()

x__clean_path__mutmut_mutants : ClassVar[MutantDict] = {
'x__clean_path__mutmut_1': x__clean_path__mutmut_1, 
    'x__clean_path__mutmut_2': x__clean_path__mutmut_2, 
    'x__clean_path__mutmut_3': x__clean_path__mutmut_3, 
    'x__clean_path__mutmut_4': x__clean_path__mutmut_4, 
    'x__clean_path__mutmut_5': x__clean_path__mutmut_5, 
    'x__clean_path__mutmut_6': x__clean_path__mutmut_6, 
    'x__clean_path__mutmut_7': x__clean_path__mutmut_7, 
    'x__clean_path__mutmut_8': x__clean_path__mutmut_8, 
    'x__clean_path__mutmut_9': x__clean_path__mutmut_9, 
    'x__clean_path__mutmut_10': x__clean_path__mutmut_10, 
    'x__clean_path__mutmut_11': x__clean_path__mutmut_11, 
    'x__clean_path__mutmut_12': x__clean_path__mutmut_12
}

def _clean_path(*args, **kwargs):
    result = _mutmut_trampoline(x__clean_path__mutmut_orig, x__clean_path__mutmut_mutants, args, kwargs)
    return result 

_clean_path.__signature__ = _mutmut_signature(x__clean_path__mutmut_orig)
x__clean_path__mutmut_orig.__name__ = 'x__clean_path'


def x_pack_release__mutmut_orig(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_1(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = None
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_2(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(None)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_3(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(None)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_4(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=None, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_5(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=None)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_6(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_7(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, )
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_8(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=False, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_9(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=False)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_10(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = None
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_11(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(None)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_12(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = None
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_13(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir * c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_14(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=None, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_15(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=None)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_16(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_17(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, )
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_18(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=False, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_19(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=False)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_20(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = None
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_21(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(None, c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_22(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], None)
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_23(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_24(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], )
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_25(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["XXbytesXX"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_26(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["BYTES"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_27(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars and {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_28(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(None)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_29(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(None, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_30(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, None)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_31(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_32(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, )
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_33(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = None
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_34(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir * s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_35(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=None, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_36(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=None)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_37(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_38(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, )
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_39(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=False, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_40(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=False)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_41(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() and p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_42(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(None):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_43(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(None, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_44(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, None)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_45(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_46(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, )
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_47(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = None
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_48(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(None)
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_49(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding=None))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_50(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="XXutf-8XX"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_51(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="UTF-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_52(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = None
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_53(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(None)
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_54(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(None))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_55(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = None
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_56(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get(None, {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_57(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", None)
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_58(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get({})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_59(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", )
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_60(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("XXchecksXX", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_61(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("CHECKS", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_62(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) or "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_63(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "XXsha256_manifestXX" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_64(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "SHA256_MANIFEST" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_65(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" not in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_66(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = None
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_67(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(None)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_68(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop(None, None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_69(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop(None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_70(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", )
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_71(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("XXsha256_manifestXX", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_72(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("SHA256_MANIFEST", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_73(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = None
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_74(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["XXchecksXX"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_75(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["CHECKS"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_76(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop(None, None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_77(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop(None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_78(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", )
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_79(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("XXchecksXX", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_80(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("CHECKS", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_81(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = None
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_82(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(None, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_83(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=None, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_84(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=None).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_85(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_86(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_87(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, ).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_88(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=False, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_89(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=("XX,XX", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_90(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", "XX:XX")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_91(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = None
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_92(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(None).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_93(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = None
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_94(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault(None, {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_95(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", None)["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_96(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault({})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_97(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", )["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_98(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("XXchecksXX", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_99(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("CHECKS", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_100(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["XXsha256_manifestXX"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_101(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["SHA256_MANIFEST"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_102(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(None, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_103(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, None)
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_104(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_105(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, )
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_106(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path(None))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_107(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("XXdist/release.manifest.lock.jsonXX"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_108(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("DIST/RELEASE.MANIFEST.LOCK.JSON"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_109(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=None, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_110(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=None)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_111(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_112(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, )
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_113(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=False, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_114(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=False)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_115(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        None, "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_116(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), None, compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_117(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=None, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_118(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=None
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_119(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_120(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_121(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_122(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_123(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "XXw:gzXX", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_124(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "W:GZ", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_125(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=10, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_126(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(None):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_127(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob(None)):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_128(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("XX*XX")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_129(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f != staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_130(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                break
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_131(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = None
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_132(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(None).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_133(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(None, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_134(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, None, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_135(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, None)
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_136(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_137(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_138(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, )
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_139(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(None))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_140(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = None
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_141(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = None
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_142(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode | 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_143(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 512
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_144(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(None, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_145(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, None, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_146(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, None, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_147(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, None)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_148(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_149(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_150(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_151(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, )
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_152(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = None
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_153(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path(None)
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_154(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("XXdist/release.manifest.lock.jsonXX")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_155(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("DIST/RELEASE.MANIFEST.LOCK.JSON")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_156(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(None, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_157(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, None, locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_158(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", None, 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_159(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), None)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_160(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes("release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_161(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_162(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_163(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), )
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_164(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "XXrelease.manifest.lock.jsonXX", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_165(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "RELEASE.MANIFEST.LOCK.JSON", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_166(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 421)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_167(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        None,
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_168(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        None,
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_169(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_170(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_171(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "XXPACKXX",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_172(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "pack",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_173(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"XXrelease_idXX": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_174(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"RELEASE_ID": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_175(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "XXversionXX": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_176(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "VERSION": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_177(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "XXbundleXX": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_178(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "BUNDLE": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_179(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = None
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_180(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = None
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_181(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=None,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_182(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=None,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_183(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=None,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_184(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=None,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_185(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata=None,
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_186(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_187(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_188(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_189(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_190(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_191(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "XXtargetXX": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_192(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "TARGET": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_193(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "XXchecksXX": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_194(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "CHECKS": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_195(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get(None, {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_196(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", None),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_197(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get({}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_198(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", ),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_199(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("XXchecksXX", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_200(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("CHECKS", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_201(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = None
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_202(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get(None, "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_203(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", None)
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_204(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_205(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", )
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_206(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("XXidXX", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_207(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("ID", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_208(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "XXXX")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_209(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = None
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_210(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(None)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_211(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = None
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_212(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning(None, exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_213(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=None)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_214(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning(exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_215(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", )
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_216(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("XXException occurredXX", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_217(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_218(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_219(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=False)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_220(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(None, exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_221(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=None)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_222(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_223(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", )
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_224(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("XXException occurredXX", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_225(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_226(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_227(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=False)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_228(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = ""
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_229(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=None,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_230(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=None,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_231(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=None,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_232(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=None,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_233(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=None,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_234(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=None,
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_235(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_236(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_237(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_238(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_239(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_240(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_241(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars and {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_242(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            None, {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_243(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", None
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_244(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_245(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_246(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "XXRELEASE_PERSISTXX", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_247(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "release_persist", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_248(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"XXrelease_idXX": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_249(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"RELEASE_ID": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_250(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "XXmeta_idXX": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_251(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "META_ID": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_252(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            None,
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_253(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            None,
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_254(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_255(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_256(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "XXRELEASE_PERSIST_FAILXX",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_257(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "release_persist_fail",
            {"release_id": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_258(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"XXrelease_idXX": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_259(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"RELEASE_ID": m.release_id, "error": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_260(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "XXerrorXX": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_261(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "ERROR": str(exc)},
        )
    return bundle_path, manifest_dict


def x_pack_release__mutmut_262(manifest_path: Path, staging_dir: Path, bundle_path: Path) -> tuple[Path, dict]:
    """
    Build a deterministic tar.gz bundle from a manifest referencing archive tombstones.
    Returns (bundle_path, locked_manifest_dict).
    """

    m = load_manifest(manifest_path)
    _clean_path(staging_dir)
    staging_dir.mkdir(parents=True, exist_ok=True)
    # hydrate components
    for c in m.components:
        obj = restore(c.tombstone)
        outp = staging_dir / c.dest_path
        outp.parent.mkdir(parents=True, exist_ok=True)
        data = _templated_bytes(obj["bytes"], c.template_vars or {})
        outp.write_bytes(data)
        _set_mode(outp, c.mode)
    # symlinks
    for s in m.symlinks:
        p = staging_dir / s.link_path
        p.parent.mkdir(parents=True, exist_ok=True)
        if p.exists() or p.is_symlink():
            with contextlib.suppress(OSError):
                p.unlink()
        os.symlink(s.target, p)
    # lock manifest with sha256 of canonical JSON
    manifest_dict = json.loads(manifest_path.read_text(encoding="utf-8"))
    manifest_for_hash = json.loads(json.dumps(manifest_dict))
    checks = manifest_for_hash.get("checks", {})
    if isinstance(checks, dict) and "sha256_manifest" in checks:
        checks = dict(checks)
        checks.pop("sha256_manifest", None)
        if checks:
            manifest_for_hash["checks"] = checks
        else:
            manifest_for_hash.pop("checks", None)
    manifest_bytes = json.dumps(manifest_for_hash, sort_keys=True, separators=(",", ":")).encode()
    sha = hashlib.sha256(manifest_bytes).hexdigest()
    manifest_dict.setdefault("checks", {})["sha256_manifest"] = sha
    dump_manifest_locked(manifest_dict, Path("dist/release.manifest.lock.json"))
    # build deterministic tar.gz
    bundle_path.parent.mkdir(parents=True, exist_ok=True)
    with tarfile.open(
        bundle_path.as_posix(), "w:gz", compresslevel=9, format=tarfile.GNU_FORMAT
    ) as tar:
        # embed staged files
        for f in sorted(staging_dir.rglob("*")):
            if f == staging_dir:
                continue
            arc = f.relative_to(staging_dir).as_posix()
            if f.is_symlink():
                _tar_add_symlink(tar, arc, os.readlink(f))
            elif f.is_file():
                b = f.read_bytes()
                mode = f.stat().st_mode & 0o777
                _tar_add_bytes(tar, arc, b, mode)
        # embed locked manifest as well
        locked = Path("dist/release.manifest.lock.json")
        _tar_add_bytes(tar, "release.manifest.lock.json", locked.read_bytes(), 0o644)
    _evidence_append_release(
        "PACK",
        {"release_id": m.release_id, "version": m.version, "bundle": bundle_path.as_posix()},
    )
    try:
        dal = ArchiveDAL.from_env()
        meta = dal.create_release_meta(
            release_id=m.release_id,
            version=m.version,
            created_at=m.created_at,
            actor=m.actor,
            metadata={
                "target": m.target,
                "checks": manifest_dict.get("checks", {}),
            },
        )
        release_meta_id = meta.get("id", "")
        for component in m.components:
            item_id: str | None
            try:
                item_row, _ = dal.fetch_by_tombstone(component.tombstone)
                item_id = item_row.id
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                item_id = None
            dal.add_release_component(
                release_meta_id=release_meta_id,
                item_id=item_id,
                tombstone=component.tombstone,
                dest_path=component.dest_path,
                mode=component.mode,
                template_vars=component.template_vars or {},
            )
        _evidence_append_release(
            "RELEASE_PERSIST", {"release_id": m.release_id, "meta_id": release_meta_id}
        )
    except Exception as exc:  # pragma: no cover - best effort logging
        _evidence_append_release(
            "RELEASE_PERSIST_FAIL",
            {"release_id": m.release_id, "error": str(None)},
        )
    return bundle_path, manifest_dict

x_pack_release__mutmut_mutants : ClassVar[MutantDict] = {
'x_pack_release__mutmut_1': x_pack_release__mutmut_1, 
    'x_pack_release__mutmut_2': x_pack_release__mutmut_2, 
    'x_pack_release__mutmut_3': x_pack_release__mutmut_3, 
    'x_pack_release__mutmut_4': x_pack_release__mutmut_4, 
    'x_pack_release__mutmut_5': x_pack_release__mutmut_5, 
    'x_pack_release__mutmut_6': x_pack_release__mutmut_6, 
    'x_pack_release__mutmut_7': x_pack_release__mutmut_7, 
    'x_pack_release__mutmut_8': x_pack_release__mutmut_8, 
    'x_pack_release__mutmut_9': x_pack_release__mutmut_9, 
    'x_pack_release__mutmut_10': x_pack_release__mutmut_10, 
    'x_pack_release__mutmut_11': x_pack_release__mutmut_11, 
    'x_pack_release__mutmut_12': x_pack_release__mutmut_12, 
    'x_pack_release__mutmut_13': x_pack_release__mutmut_13, 
    'x_pack_release__mutmut_14': x_pack_release__mutmut_14, 
    'x_pack_release__mutmut_15': x_pack_release__mutmut_15, 
    'x_pack_release__mutmut_16': x_pack_release__mutmut_16, 
    'x_pack_release__mutmut_17': x_pack_release__mutmut_17, 
    'x_pack_release__mutmut_18': x_pack_release__mutmut_18, 
    'x_pack_release__mutmut_19': x_pack_release__mutmut_19, 
    'x_pack_release__mutmut_20': x_pack_release__mutmut_20, 
    'x_pack_release__mutmut_21': x_pack_release__mutmut_21, 
    'x_pack_release__mutmut_22': x_pack_release__mutmut_22, 
    'x_pack_release__mutmut_23': x_pack_release__mutmut_23, 
    'x_pack_release__mutmut_24': x_pack_release__mutmut_24, 
    'x_pack_release__mutmut_25': x_pack_release__mutmut_25, 
    'x_pack_release__mutmut_26': x_pack_release__mutmut_26, 
    'x_pack_release__mutmut_27': x_pack_release__mutmut_27, 
    'x_pack_release__mutmut_28': x_pack_release__mutmut_28, 
    'x_pack_release__mutmut_29': x_pack_release__mutmut_29, 
    'x_pack_release__mutmut_30': x_pack_release__mutmut_30, 
    'x_pack_release__mutmut_31': x_pack_release__mutmut_31, 
    'x_pack_release__mutmut_32': x_pack_release__mutmut_32, 
    'x_pack_release__mutmut_33': x_pack_release__mutmut_33, 
    'x_pack_release__mutmut_34': x_pack_release__mutmut_34, 
    'x_pack_release__mutmut_35': x_pack_release__mutmut_35, 
    'x_pack_release__mutmut_36': x_pack_release__mutmut_36, 
    'x_pack_release__mutmut_37': x_pack_release__mutmut_37, 
    'x_pack_release__mutmut_38': x_pack_release__mutmut_38, 
    'x_pack_release__mutmut_39': x_pack_release__mutmut_39, 
    'x_pack_release__mutmut_40': x_pack_release__mutmut_40, 
    'x_pack_release__mutmut_41': x_pack_release__mutmut_41, 
    'x_pack_release__mutmut_42': x_pack_release__mutmut_42, 
    'x_pack_release__mutmut_43': x_pack_release__mutmut_43, 
    'x_pack_release__mutmut_44': x_pack_release__mutmut_44, 
    'x_pack_release__mutmut_45': x_pack_release__mutmut_45, 
    'x_pack_release__mutmut_46': x_pack_release__mutmut_46, 
    'x_pack_release__mutmut_47': x_pack_release__mutmut_47, 
    'x_pack_release__mutmut_48': x_pack_release__mutmut_48, 
    'x_pack_release__mutmut_49': x_pack_release__mutmut_49, 
    'x_pack_release__mutmut_50': x_pack_release__mutmut_50, 
    'x_pack_release__mutmut_51': x_pack_release__mutmut_51, 
    'x_pack_release__mutmut_52': x_pack_release__mutmut_52, 
    'x_pack_release__mutmut_53': x_pack_release__mutmut_53, 
    'x_pack_release__mutmut_54': x_pack_release__mutmut_54, 
    'x_pack_release__mutmut_55': x_pack_release__mutmut_55, 
    'x_pack_release__mutmut_56': x_pack_release__mutmut_56, 
    'x_pack_release__mutmut_57': x_pack_release__mutmut_57, 
    'x_pack_release__mutmut_58': x_pack_release__mutmut_58, 
    'x_pack_release__mutmut_59': x_pack_release__mutmut_59, 
    'x_pack_release__mutmut_60': x_pack_release__mutmut_60, 
    'x_pack_release__mutmut_61': x_pack_release__mutmut_61, 
    'x_pack_release__mutmut_62': x_pack_release__mutmut_62, 
    'x_pack_release__mutmut_63': x_pack_release__mutmut_63, 
    'x_pack_release__mutmut_64': x_pack_release__mutmut_64, 
    'x_pack_release__mutmut_65': x_pack_release__mutmut_65, 
    'x_pack_release__mutmut_66': x_pack_release__mutmut_66, 
    'x_pack_release__mutmut_67': x_pack_release__mutmut_67, 
    'x_pack_release__mutmut_68': x_pack_release__mutmut_68, 
    'x_pack_release__mutmut_69': x_pack_release__mutmut_69, 
    'x_pack_release__mutmut_70': x_pack_release__mutmut_70, 
    'x_pack_release__mutmut_71': x_pack_release__mutmut_71, 
    'x_pack_release__mutmut_72': x_pack_release__mutmut_72, 
    'x_pack_release__mutmut_73': x_pack_release__mutmut_73, 
    'x_pack_release__mutmut_74': x_pack_release__mutmut_74, 
    'x_pack_release__mutmut_75': x_pack_release__mutmut_75, 
    'x_pack_release__mutmut_76': x_pack_release__mutmut_76, 
    'x_pack_release__mutmut_77': x_pack_release__mutmut_77, 
    'x_pack_release__mutmut_78': x_pack_release__mutmut_78, 
    'x_pack_release__mutmut_79': x_pack_release__mutmut_79, 
    'x_pack_release__mutmut_80': x_pack_release__mutmut_80, 
    'x_pack_release__mutmut_81': x_pack_release__mutmut_81, 
    'x_pack_release__mutmut_82': x_pack_release__mutmut_82, 
    'x_pack_release__mutmut_83': x_pack_release__mutmut_83, 
    'x_pack_release__mutmut_84': x_pack_release__mutmut_84, 
    'x_pack_release__mutmut_85': x_pack_release__mutmut_85, 
    'x_pack_release__mutmut_86': x_pack_release__mutmut_86, 
    'x_pack_release__mutmut_87': x_pack_release__mutmut_87, 
    'x_pack_release__mutmut_88': x_pack_release__mutmut_88, 
    'x_pack_release__mutmut_89': x_pack_release__mutmut_89, 
    'x_pack_release__mutmut_90': x_pack_release__mutmut_90, 
    'x_pack_release__mutmut_91': x_pack_release__mutmut_91, 
    'x_pack_release__mutmut_92': x_pack_release__mutmut_92, 
    'x_pack_release__mutmut_93': x_pack_release__mutmut_93, 
    'x_pack_release__mutmut_94': x_pack_release__mutmut_94, 
    'x_pack_release__mutmut_95': x_pack_release__mutmut_95, 
    'x_pack_release__mutmut_96': x_pack_release__mutmut_96, 
    'x_pack_release__mutmut_97': x_pack_release__mutmut_97, 
    'x_pack_release__mutmut_98': x_pack_release__mutmut_98, 
    'x_pack_release__mutmut_99': x_pack_release__mutmut_99, 
    'x_pack_release__mutmut_100': x_pack_release__mutmut_100, 
    'x_pack_release__mutmut_101': x_pack_release__mutmut_101, 
    'x_pack_release__mutmut_102': x_pack_release__mutmut_102, 
    'x_pack_release__mutmut_103': x_pack_release__mutmut_103, 
    'x_pack_release__mutmut_104': x_pack_release__mutmut_104, 
    'x_pack_release__mutmut_105': x_pack_release__mutmut_105, 
    'x_pack_release__mutmut_106': x_pack_release__mutmut_106, 
    'x_pack_release__mutmut_107': x_pack_release__mutmut_107, 
    'x_pack_release__mutmut_108': x_pack_release__mutmut_108, 
    'x_pack_release__mutmut_109': x_pack_release__mutmut_109, 
    'x_pack_release__mutmut_110': x_pack_release__mutmut_110, 
    'x_pack_release__mutmut_111': x_pack_release__mutmut_111, 
    'x_pack_release__mutmut_112': x_pack_release__mutmut_112, 
    'x_pack_release__mutmut_113': x_pack_release__mutmut_113, 
    'x_pack_release__mutmut_114': x_pack_release__mutmut_114, 
    'x_pack_release__mutmut_115': x_pack_release__mutmut_115, 
    'x_pack_release__mutmut_116': x_pack_release__mutmut_116, 
    'x_pack_release__mutmut_117': x_pack_release__mutmut_117, 
    'x_pack_release__mutmut_118': x_pack_release__mutmut_118, 
    'x_pack_release__mutmut_119': x_pack_release__mutmut_119, 
    'x_pack_release__mutmut_120': x_pack_release__mutmut_120, 
    'x_pack_release__mutmut_121': x_pack_release__mutmut_121, 
    'x_pack_release__mutmut_122': x_pack_release__mutmut_122, 
    'x_pack_release__mutmut_123': x_pack_release__mutmut_123, 
    'x_pack_release__mutmut_124': x_pack_release__mutmut_124, 
    'x_pack_release__mutmut_125': x_pack_release__mutmut_125, 
    'x_pack_release__mutmut_126': x_pack_release__mutmut_126, 
    'x_pack_release__mutmut_127': x_pack_release__mutmut_127, 
    'x_pack_release__mutmut_128': x_pack_release__mutmut_128, 
    'x_pack_release__mutmut_129': x_pack_release__mutmut_129, 
    'x_pack_release__mutmut_130': x_pack_release__mutmut_130, 
    'x_pack_release__mutmut_131': x_pack_release__mutmut_131, 
    'x_pack_release__mutmut_132': x_pack_release__mutmut_132, 
    'x_pack_release__mutmut_133': x_pack_release__mutmut_133, 
    'x_pack_release__mutmut_134': x_pack_release__mutmut_134, 
    'x_pack_release__mutmut_135': x_pack_release__mutmut_135, 
    'x_pack_release__mutmut_136': x_pack_release__mutmut_136, 
    'x_pack_release__mutmut_137': x_pack_release__mutmut_137, 
    'x_pack_release__mutmut_138': x_pack_release__mutmut_138, 
    'x_pack_release__mutmut_139': x_pack_release__mutmut_139, 
    'x_pack_release__mutmut_140': x_pack_release__mutmut_140, 
    'x_pack_release__mutmut_141': x_pack_release__mutmut_141, 
    'x_pack_release__mutmut_142': x_pack_release__mutmut_142, 
    'x_pack_release__mutmut_143': x_pack_release__mutmut_143, 
    'x_pack_release__mutmut_144': x_pack_release__mutmut_144, 
    'x_pack_release__mutmut_145': x_pack_release__mutmut_145, 
    'x_pack_release__mutmut_146': x_pack_release__mutmut_146, 
    'x_pack_release__mutmut_147': x_pack_release__mutmut_147, 
    'x_pack_release__mutmut_148': x_pack_release__mutmut_148, 
    'x_pack_release__mutmut_149': x_pack_release__mutmut_149, 
    'x_pack_release__mutmut_150': x_pack_release__mutmut_150, 
    'x_pack_release__mutmut_151': x_pack_release__mutmut_151, 
    'x_pack_release__mutmut_152': x_pack_release__mutmut_152, 
    'x_pack_release__mutmut_153': x_pack_release__mutmut_153, 
    'x_pack_release__mutmut_154': x_pack_release__mutmut_154, 
    'x_pack_release__mutmut_155': x_pack_release__mutmut_155, 
    'x_pack_release__mutmut_156': x_pack_release__mutmut_156, 
    'x_pack_release__mutmut_157': x_pack_release__mutmut_157, 
    'x_pack_release__mutmut_158': x_pack_release__mutmut_158, 
    'x_pack_release__mutmut_159': x_pack_release__mutmut_159, 
    'x_pack_release__mutmut_160': x_pack_release__mutmut_160, 
    'x_pack_release__mutmut_161': x_pack_release__mutmut_161, 
    'x_pack_release__mutmut_162': x_pack_release__mutmut_162, 
    'x_pack_release__mutmut_163': x_pack_release__mutmut_163, 
    'x_pack_release__mutmut_164': x_pack_release__mutmut_164, 
    'x_pack_release__mutmut_165': x_pack_release__mutmut_165, 
    'x_pack_release__mutmut_166': x_pack_release__mutmut_166, 
    'x_pack_release__mutmut_167': x_pack_release__mutmut_167, 
    'x_pack_release__mutmut_168': x_pack_release__mutmut_168, 
    'x_pack_release__mutmut_169': x_pack_release__mutmut_169, 
    'x_pack_release__mutmut_170': x_pack_release__mutmut_170, 
    'x_pack_release__mutmut_171': x_pack_release__mutmut_171, 
    'x_pack_release__mutmut_172': x_pack_release__mutmut_172, 
    'x_pack_release__mutmut_173': x_pack_release__mutmut_173, 
    'x_pack_release__mutmut_174': x_pack_release__mutmut_174, 
    'x_pack_release__mutmut_175': x_pack_release__mutmut_175, 
    'x_pack_release__mutmut_176': x_pack_release__mutmut_176, 
    'x_pack_release__mutmut_177': x_pack_release__mutmut_177, 
    'x_pack_release__mutmut_178': x_pack_release__mutmut_178, 
    'x_pack_release__mutmut_179': x_pack_release__mutmut_179, 
    'x_pack_release__mutmut_180': x_pack_release__mutmut_180, 
    'x_pack_release__mutmut_181': x_pack_release__mutmut_181, 
    'x_pack_release__mutmut_182': x_pack_release__mutmut_182, 
    'x_pack_release__mutmut_183': x_pack_release__mutmut_183, 
    'x_pack_release__mutmut_184': x_pack_release__mutmut_184, 
    'x_pack_release__mutmut_185': x_pack_release__mutmut_185, 
    'x_pack_release__mutmut_186': x_pack_release__mutmut_186, 
    'x_pack_release__mutmut_187': x_pack_release__mutmut_187, 
    'x_pack_release__mutmut_188': x_pack_release__mutmut_188, 
    'x_pack_release__mutmut_189': x_pack_release__mutmut_189, 
    'x_pack_release__mutmut_190': x_pack_release__mutmut_190, 
    'x_pack_release__mutmut_191': x_pack_release__mutmut_191, 
    'x_pack_release__mutmut_192': x_pack_release__mutmut_192, 
    'x_pack_release__mutmut_193': x_pack_release__mutmut_193, 
    'x_pack_release__mutmut_194': x_pack_release__mutmut_194, 
    'x_pack_release__mutmut_195': x_pack_release__mutmut_195, 
    'x_pack_release__mutmut_196': x_pack_release__mutmut_196, 
    'x_pack_release__mutmut_197': x_pack_release__mutmut_197, 
    'x_pack_release__mutmut_198': x_pack_release__mutmut_198, 
    'x_pack_release__mutmut_199': x_pack_release__mutmut_199, 
    'x_pack_release__mutmut_200': x_pack_release__mutmut_200, 
    'x_pack_release__mutmut_201': x_pack_release__mutmut_201, 
    'x_pack_release__mutmut_202': x_pack_release__mutmut_202, 
    'x_pack_release__mutmut_203': x_pack_release__mutmut_203, 
    'x_pack_release__mutmut_204': x_pack_release__mutmut_204, 
    'x_pack_release__mutmut_205': x_pack_release__mutmut_205, 
    'x_pack_release__mutmut_206': x_pack_release__mutmut_206, 
    'x_pack_release__mutmut_207': x_pack_release__mutmut_207, 
    'x_pack_release__mutmut_208': x_pack_release__mutmut_208, 
    'x_pack_release__mutmut_209': x_pack_release__mutmut_209, 
    'x_pack_release__mutmut_210': x_pack_release__mutmut_210, 
    'x_pack_release__mutmut_211': x_pack_release__mutmut_211, 
    'x_pack_release__mutmut_212': x_pack_release__mutmut_212, 
    'x_pack_release__mutmut_213': x_pack_release__mutmut_213, 
    'x_pack_release__mutmut_214': x_pack_release__mutmut_214, 
    'x_pack_release__mutmut_215': x_pack_release__mutmut_215, 
    'x_pack_release__mutmut_216': x_pack_release__mutmut_216, 
    'x_pack_release__mutmut_217': x_pack_release__mutmut_217, 
    'x_pack_release__mutmut_218': x_pack_release__mutmut_218, 
    'x_pack_release__mutmut_219': x_pack_release__mutmut_219, 
    'x_pack_release__mutmut_220': x_pack_release__mutmut_220, 
    'x_pack_release__mutmut_221': x_pack_release__mutmut_221, 
    'x_pack_release__mutmut_222': x_pack_release__mutmut_222, 
    'x_pack_release__mutmut_223': x_pack_release__mutmut_223, 
    'x_pack_release__mutmut_224': x_pack_release__mutmut_224, 
    'x_pack_release__mutmut_225': x_pack_release__mutmut_225, 
    'x_pack_release__mutmut_226': x_pack_release__mutmut_226, 
    'x_pack_release__mutmut_227': x_pack_release__mutmut_227, 
    'x_pack_release__mutmut_228': x_pack_release__mutmut_228, 
    'x_pack_release__mutmut_229': x_pack_release__mutmut_229, 
    'x_pack_release__mutmut_230': x_pack_release__mutmut_230, 
    'x_pack_release__mutmut_231': x_pack_release__mutmut_231, 
    'x_pack_release__mutmut_232': x_pack_release__mutmut_232, 
    'x_pack_release__mutmut_233': x_pack_release__mutmut_233, 
    'x_pack_release__mutmut_234': x_pack_release__mutmut_234, 
    'x_pack_release__mutmut_235': x_pack_release__mutmut_235, 
    'x_pack_release__mutmut_236': x_pack_release__mutmut_236, 
    'x_pack_release__mutmut_237': x_pack_release__mutmut_237, 
    'x_pack_release__mutmut_238': x_pack_release__mutmut_238, 
    'x_pack_release__mutmut_239': x_pack_release__mutmut_239, 
    'x_pack_release__mutmut_240': x_pack_release__mutmut_240, 
    'x_pack_release__mutmut_241': x_pack_release__mutmut_241, 
    'x_pack_release__mutmut_242': x_pack_release__mutmut_242, 
    'x_pack_release__mutmut_243': x_pack_release__mutmut_243, 
    'x_pack_release__mutmut_244': x_pack_release__mutmut_244, 
    'x_pack_release__mutmut_245': x_pack_release__mutmut_245, 
    'x_pack_release__mutmut_246': x_pack_release__mutmut_246, 
    'x_pack_release__mutmut_247': x_pack_release__mutmut_247, 
    'x_pack_release__mutmut_248': x_pack_release__mutmut_248, 
    'x_pack_release__mutmut_249': x_pack_release__mutmut_249, 
    'x_pack_release__mutmut_250': x_pack_release__mutmut_250, 
    'x_pack_release__mutmut_251': x_pack_release__mutmut_251, 
    'x_pack_release__mutmut_252': x_pack_release__mutmut_252, 
    'x_pack_release__mutmut_253': x_pack_release__mutmut_253, 
    'x_pack_release__mutmut_254': x_pack_release__mutmut_254, 
    'x_pack_release__mutmut_255': x_pack_release__mutmut_255, 
    'x_pack_release__mutmut_256': x_pack_release__mutmut_256, 
    'x_pack_release__mutmut_257': x_pack_release__mutmut_257, 
    'x_pack_release__mutmut_258': x_pack_release__mutmut_258, 
    'x_pack_release__mutmut_259': x_pack_release__mutmut_259, 
    'x_pack_release__mutmut_260': x_pack_release__mutmut_260, 
    'x_pack_release__mutmut_261': x_pack_release__mutmut_261, 
    'x_pack_release__mutmut_262': x_pack_release__mutmut_262
}

def pack_release(*args, **kwargs):
    result = _mutmut_trampoline(x_pack_release__mutmut_orig, x_pack_release__mutmut_mutants, args, kwargs)
    return result 

pack_release.__signature__ = _mutmut_signature(x_pack_release__mutmut_orig)
x_pack_release__mutmut_orig.__name__ = 'x_pack_release'


def x_verify_bundle__mutmut_orig(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_1(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(None, "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_2(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), None) as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_3(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open("r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_4(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), ) as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_5(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "XXr:gzXX") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_6(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "R:GZ") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_7(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = None
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_8(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile(None)
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_9(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("XXrelease.manifest.lock.jsonXX")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_10(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("RELEASE.MANIFEST.LOCK.JSON")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_11(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is not None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_12(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError(None)
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_13(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("XXrelease.manifest.lock.json missing from bundleXX")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_14(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("RELEASE.MANIFEST.LOCK.JSON MISSING FROM BUNDLE")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_15(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = None
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_16(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = None
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_17(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(None)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_18(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = None
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_19(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get(None)
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_20(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get(None, {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_21(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", None).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_22(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get({}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_23(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", ).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_24(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("XXchecksXX", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_25(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("CHECKS", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_26(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("XXsha256_manifestXX")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_27(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("SHA256_MANIFEST")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_28(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = None
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_29(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(None)
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_30(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(None))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_31(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = None
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_32(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get(None, {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_33(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", None)
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_34(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get({})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_35(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", )
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_36(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("XXchecksXX", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_37(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("CHECKS", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_38(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = None
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_39(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(None)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_40(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop(None, None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_41(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop(None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_42(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", )
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_43(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("XXsha256_manifestXX", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_44(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("SHA256_MANIFEST", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_45(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = None
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_46(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["XXchecksXX"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_47(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["CHECKS"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_48(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop(None, None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_49(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop(None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_50(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", )
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_51(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("XXchecksXX", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_52(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("CHECKS", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_53(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = None
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_54(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            None, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_55(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=None, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_56(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=None
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_57(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_58(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_59(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_60(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=False, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_61(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=("XX,XX", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_62(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", "XX:XX")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_63(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = None
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_64(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(None).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_65(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = None
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_66(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored != recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_67(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release(None, {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_68(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", None)
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_69(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release({"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_70(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", )
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_71(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("XXVERIFYXX", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_72(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("verify", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_73(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"XXbundleXX": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_74(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"BUNDLE": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_75(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "XXokXX": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_76(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "OK": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_77(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_78(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError(None)
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_79(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("XXmanifest sha mismatchXX")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_80(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("MANIFEST SHA MISMATCH")
    return {"ok": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_81(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"XXokXX": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_82(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"OK": ok, "sha256_manifest": stored}


def x_verify_bundle__mutmut_83(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "XXsha256_manifestXX": stored}


def x_verify_bundle__mutmut_84(bundle_path: Path) -> dict:
    import json
    import tarfile

    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        extracted = tar.extractfile("release.manifest.lock.json")
        if extracted is None:
            raise FileNotFoundError("release.manifest.lock.json missing from bundle")
        manifest_bytes = extracted.read()
        locked_manifest = json.loads(manifest_bytes)
        stored = locked_manifest.get("checks", {}).get("sha256_manifest")
        manifest_for_hash = json.loads(json.dumps(locked_manifest))
        checks = manifest_for_hash.get("checks", {})
        if isinstance(checks, dict):
            checks = dict(checks)
            checks.pop("sha256_manifest", None)
            if checks:
                manifest_for_hash["checks"] = checks
            else:
                manifest_for_hash.pop("checks", None)
        canonical_bytes = json.dumps(
            manifest_for_hash, sort_keys=True, separators=(",", ":")
        ).encode()
        recomputed = hashlib.sha256(canonical_bytes).hexdigest()
        ok = stored == recomputed
    _evidence_append_release("VERIFY", {"bundle": bundle_path.as_posix(), "ok": ok})
    if not ok:
        raise RuntimeError("manifest sha mismatch")
    return {"ok": ok, "SHA256_MANIFEST": stored}

x_verify_bundle__mutmut_mutants : ClassVar[MutantDict] = {
'x_verify_bundle__mutmut_1': x_verify_bundle__mutmut_1, 
    'x_verify_bundle__mutmut_2': x_verify_bundle__mutmut_2, 
    'x_verify_bundle__mutmut_3': x_verify_bundle__mutmut_3, 
    'x_verify_bundle__mutmut_4': x_verify_bundle__mutmut_4, 
    'x_verify_bundle__mutmut_5': x_verify_bundle__mutmut_5, 
    'x_verify_bundle__mutmut_6': x_verify_bundle__mutmut_6, 
    'x_verify_bundle__mutmut_7': x_verify_bundle__mutmut_7, 
    'x_verify_bundle__mutmut_8': x_verify_bundle__mutmut_8, 
    'x_verify_bundle__mutmut_9': x_verify_bundle__mutmut_9, 
    'x_verify_bundle__mutmut_10': x_verify_bundle__mutmut_10, 
    'x_verify_bundle__mutmut_11': x_verify_bundle__mutmut_11, 
    'x_verify_bundle__mutmut_12': x_verify_bundle__mutmut_12, 
    'x_verify_bundle__mutmut_13': x_verify_bundle__mutmut_13, 
    'x_verify_bundle__mutmut_14': x_verify_bundle__mutmut_14, 
    'x_verify_bundle__mutmut_15': x_verify_bundle__mutmut_15, 
    'x_verify_bundle__mutmut_16': x_verify_bundle__mutmut_16, 
    'x_verify_bundle__mutmut_17': x_verify_bundle__mutmut_17, 
    'x_verify_bundle__mutmut_18': x_verify_bundle__mutmut_18, 
    'x_verify_bundle__mutmut_19': x_verify_bundle__mutmut_19, 
    'x_verify_bundle__mutmut_20': x_verify_bundle__mutmut_20, 
    'x_verify_bundle__mutmut_21': x_verify_bundle__mutmut_21, 
    'x_verify_bundle__mutmut_22': x_verify_bundle__mutmut_22, 
    'x_verify_bundle__mutmut_23': x_verify_bundle__mutmut_23, 
    'x_verify_bundle__mutmut_24': x_verify_bundle__mutmut_24, 
    'x_verify_bundle__mutmut_25': x_verify_bundle__mutmut_25, 
    'x_verify_bundle__mutmut_26': x_verify_bundle__mutmut_26, 
    'x_verify_bundle__mutmut_27': x_verify_bundle__mutmut_27, 
    'x_verify_bundle__mutmut_28': x_verify_bundle__mutmut_28, 
    'x_verify_bundle__mutmut_29': x_verify_bundle__mutmut_29, 
    'x_verify_bundle__mutmut_30': x_verify_bundle__mutmut_30, 
    'x_verify_bundle__mutmut_31': x_verify_bundle__mutmut_31, 
    'x_verify_bundle__mutmut_32': x_verify_bundle__mutmut_32, 
    'x_verify_bundle__mutmut_33': x_verify_bundle__mutmut_33, 
    'x_verify_bundle__mutmut_34': x_verify_bundle__mutmut_34, 
    'x_verify_bundle__mutmut_35': x_verify_bundle__mutmut_35, 
    'x_verify_bundle__mutmut_36': x_verify_bundle__mutmut_36, 
    'x_verify_bundle__mutmut_37': x_verify_bundle__mutmut_37, 
    'x_verify_bundle__mutmut_38': x_verify_bundle__mutmut_38, 
    'x_verify_bundle__mutmut_39': x_verify_bundle__mutmut_39, 
    'x_verify_bundle__mutmut_40': x_verify_bundle__mutmut_40, 
    'x_verify_bundle__mutmut_41': x_verify_bundle__mutmut_41, 
    'x_verify_bundle__mutmut_42': x_verify_bundle__mutmut_42, 
    'x_verify_bundle__mutmut_43': x_verify_bundle__mutmut_43, 
    'x_verify_bundle__mutmut_44': x_verify_bundle__mutmut_44, 
    'x_verify_bundle__mutmut_45': x_verify_bundle__mutmut_45, 
    'x_verify_bundle__mutmut_46': x_verify_bundle__mutmut_46, 
    'x_verify_bundle__mutmut_47': x_verify_bundle__mutmut_47, 
    'x_verify_bundle__mutmut_48': x_verify_bundle__mutmut_48, 
    'x_verify_bundle__mutmut_49': x_verify_bundle__mutmut_49, 
    'x_verify_bundle__mutmut_50': x_verify_bundle__mutmut_50, 
    'x_verify_bundle__mutmut_51': x_verify_bundle__mutmut_51, 
    'x_verify_bundle__mutmut_52': x_verify_bundle__mutmut_52, 
    'x_verify_bundle__mutmut_53': x_verify_bundle__mutmut_53, 
    'x_verify_bundle__mutmut_54': x_verify_bundle__mutmut_54, 
    'x_verify_bundle__mutmut_55': x_verify_bundle__mutmut_55, 
    'x_verify_bundle__mutmut_56': x_verify_bundle__mutmut_56, 
    'x_verify_bundle__mutmut_57': x_verify_bundle__mutmut_57, 
    'x_verify_bundle__mutmut_58': x_verify_bundle__mutmut_58, 
    'x_verify_bundle__mutmut_59': x_verify_bundle__mutmut_59, 
    'x_verify_bundle__mutmut_60': x_verify_bundle__mutmut_60, 
    'x_verify_bundle__mutmut_61': x_verify_bundle__mutmut_61, 
    'x_verify_bundle__mutmut_62': x_verify_bundle__mutmut_62, 
    'x_verify_bundle__mutmut_63': x_verify_bundle__mutmut_63, 
    'x_verify_bundle__mutmut_64': x_verify_bundle__mutmut_64, 
    'x_verify_bundle__mutmut_65': x_verify_bundle__mutmut_65, 
    'x_verify_bundle__mutmut_66': x_verify_bundle__mutmut_66, 
    'x_verify_bundle__mutmut_67': x_verify_bundle__mutmut_67, 
    'x_verify_bundle__mutmut_68': x_verify_bundle__mutmut_68, 
    'x_verify_bundle__mutmut_69': x_verify_bundle__mutmut_69, 
    'x_verify_bundle__mutmut_70': x_verify_bundle__mutmut_70, 
    'x_verify_bundle__mutmut_71': x_verify_bundle__mutmut_71, 
    'x_verify_bundle__mutmut_72': x_verify_bundle__mutmut_72, 
    'x_verify_bundle__mutmut_73': x_verify_bundle__mutmut_73, 
    'x_verify_bundle__mutmut_74': x_verify_bundle__mutmut_74, 
    'x_verify_bundle__mutmut_75': x_verify_bundle__mutmut_75, 
    'x_verify_bundle__mutmut_76': x_verify_bundle__mutmut_76, 
    'x_verify_bundle__mutmut_77': x_verify_bundle__mutmut_77, 
    'x_verify_bundle__mutmut_78': x_verify_bundle__mutmut_78, 
    'x_verify_bundle__mutmut_79': x_verify_bundle__mutmut_79, 
    'x_verify_bundle__mutmut_80': x_verify_bundle__mutmut_80, 
    'x_verify_bundle__mutmut_81': x_verify_bundle__mutmut_81, 
    'x_verify_bundle__mutmut_82': x_verify_bundle__mutmut_82, 
    'x_verify_bundle__mutmut_83': x_verify_bundle__mutmut_83, 
    'x_verify_bundle__mutmut_84': x_verify_bundle__mutmut_84
}

def verify_bundle(*args, **kwargs):
    result = _mutmut_trampoline(x_verify_bundle__mutmut_orig, x_verify_bundle__mutmut_mutants, args, kwargs)
    return result 

verify_bundle.__signature__ = _mutmut_signature(x_verify_bundle__mutmut_orig)
x_verify_bundle__mutmut_orig.__name__ = 'x_verify_bundle'


def x_unpack_bundle__mutmut_orig(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_1(bundle_path: Path, dest_dir: Path, allow_scripts: bool = True) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_2(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=None, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_3(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=None)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_4(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_5(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, )
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_6(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=False, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_7(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=False)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_8(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(None, "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_9(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), None) as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_10(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open("r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_11(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), ) as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_12(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "XXr:gzXX") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_13(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "R:GZ") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_14(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(None, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_15(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, None)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_16(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_17(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, )
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_18(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_19(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        None, {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_20(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", None
    )
    return dest_dir


def x_unpack_bundle__mutmut_21(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_22(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", )
    return dest_dir


def x_unpack_bundle__mutmut_23(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "XXUNPACKXX", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_24(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "unpack", {"bundle": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_25(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"XXbundleXX": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_26(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"BUNDLE": bundle_path.as_posix(), "dest": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_27(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "XXdestXX": dest_dir.as_posix()}
    )
    return dest_dir


def x_unpack_bundle__mutmut_28(bundle_path: Path, dest_dir: Path, allow_scripts: bool = False) -> Path:
    import tarfile

    dest_dir.mkdir(parents=True, exist_ok=True)
    with tarfile.open(bundle_path.as_posix(), "r:gz") as tar:
        _safe_extract(tar, dest_dir)
        # Do not execute any scripts by default; a future flag can explicitly allow it.
        if not allow_scripts:
            # noop
            pass
    _evidence_append_release(
        "UNPACK", {"bundle": bundle_path.as_posix(), "DEST": dest_dir.as_posix()}
    )
    return dest_dir

x_unpack_bundle__mutmut_mutants : ClassVar[MutantDict] = {
'x_unpack_bundle__mutmut_1': x_unpack_bundle__mutmut_1, 
    'x_unpack_bundle__mutmut_2': x_unpack_bundle__mutmut_2, 
    'x_unpack_bundle__mutmut_3': x_unpack_bundle__mutmut_3, 
    'x_unpack_bundle__mutmut_4': x_unpack_bundle__mutmut_4, 
    'x_unpack_bundle__mutmut_5': x_unpack_bundle__mutmut_5, 
    'x_unpack_bundle__mutmut_6': x_unpack_bundle__mutmut_6, 
    'x_unpack_bundle__mutmut_7': x_unpack_bundle__mutmut_7, 
    'x_unpack_bundle__mutmut_8': x_unpack_bundle__mutmut_8, 
    'x_unpack_bundle__mutmut_9': x_unpack_bundle__mutmut_9, 
    'x_unpack_bundle__mutmut_10': x_unpack_bundle__mutmut_10, 
    'x_unpack_bundle__mutmut_11': x_unpack_bundle__mutmut_11, 
    'x_unpack_bundle__mutmut_12': x_unpack_bundle__mutmut_12, 
    'x_unpack_bundle__mutmut_13': x_unpack_bundle__mutmut_13, 
    'x_unpack_bundle__mutmut_14': x_unpack_bundle__mutmut_14, 
    'x_unpack_bundle__mutmut_15': x_unpack_bundle__mutmut_15, 
    'x_unpack_bundle__mutmut_16': x_unpack_bundle__mutmut_16, 
    'x_unpack_bundle__mutmut_17': x_unpack_bundle__mutmut_17, 
    'x_unpack_bundle__mutmut_18': x_unpack_bundle__mutmut_18, 
    'x_unpack_bundle__mutmut_19': x_unpack_bundle__mutmut_19, 
    'x_unpack_bundle__mutmut_20': x_unpack_bundle__mutmut_20, 
    'x_unpack_bundle__mutmut_21': x_unpack_bundle__mutmut_21, 
    'x_unpack_bundle__mutmut_22': x_unpack_bundle__mutmut_22, 
    'x_unpack_bundle__mutmut_23': x_unpack_bundle__mutmut_23, 
    'x_unpack_bundle__mutmut_24': x_unpack_bundle__mutmut_24, 
    'x_unpack_bundle__mutmut_25': x_unpack_bundle__mutmut_25, 
    'x_unpack_bundle__mutmut_26': x_unpack_bundle__mutmut_26, 
    'x_unpack_bundle__mutmut_27': x_unpack_bundle__mutmut_27, 
    'x_unpack_bundle__mutmut_28': x_unpack_bundle__mutmut_28
}

def unpack_bundle(*args, **kwargs):
    result = _mutmut_trampoline(x_unpack_bundle__mutmut_orig, x_unpack_bundle__mutmut_mutants, args, kwargs)
    return result 

unpack_bundle.__signature__ = _mutmut_signature(x_unpack_bundle__mutmut_orig)
x_unpack_bundle__mutmut_orig.__name__ = 'x_unpack_bundle'
