"""Typer application for Zendesk configuration workflows."""

from __future__ import annotations
import logging
logger = logging.getLogger(__name__)

import importlib
import json
import os
import subprocess
import sys
from collections.abc import Callable, Iterable, Mapping, Sequence
from pathlib import Path
from typing import Any

from pydantic import BaseModel, ValidationError

import typer
from codex.versioning import update_artifact_version
from codex.zendesk import apply as apply_module
from codex.zendesk.model import (
    App,
    Group,
    GuideThemeRef,
    Macro,
    RoutingRule,
    SLAPolicy,
    TemplatePatch,
    TicketField,
    TicketForm,
    Trigger,
    View,
    Webhook,
)
from codex.zendesk.monitoring import register_zendesk_metrics
from codex.zendesk.plan.diff_engine import (
    diff_apps,
    diff_fields,
    diff_forms,
    diff_groups,
    diff_guide,
    diff_macros,
    diff_routing,
    diff_slas,
    diff_triggers,
    diff_views,
    diff_webhooks,
)
from codex.zendesk.plan.validators import validate_plan

app = typer.Typer(help="Manage Zendesk admin resources with Codex.")


ResourceConfig = tuple[
    type[BaseModel], Callable[[Iterable[object], Iterable[object]], list[dict[str, Any]]]
]
_RESOURCE_CONFIG: dict[str, ResourceConfig] = {
    "triggers": (Trigger, diff_triggers),
    "fields": (TicketField, diff_fields),
    "forms": (TicketForm, diff_forms),
    "groups": (Group, diff_groups),
    "macros": (Macro, diff_macros),
    "views": (View, diff_views),
    "webhooks": (Webhook, diff_webhooks),
    "apps": (App, diff_apps),
    "routing": (RoutingRule, diff_routing),
    "slas": (SLAPolicy, diff_slas),
}
RESOURCE_TYPES = (
    "apps",
    "fields",
    "forms",
    "groups",
    "guide",
    "macros",
    "routing",
    "slas",
    "talk",
    "triggers",
    "views",
    "webhooks",
    "widgets",
)
APPLY_RESOURCE_HELP = "Resource type of the plan (" + ", ".join(RESOURCE_TYPES) + ")"
SUPPORTED_RESOURCES = tuple(sorted((*_RESOURCE_CONFIG.keys(), "guide")))

_APPLY_HANDLERS: dict[str, Callable[[Any, str, bool], None]] = {
    "apps": apply_module.apply_apps,
    "fields": apply_module.apply_fields,
    "forms": apply_module.apply_forms,
    "groups": apply_module.apply_groups,
    "guide": apply_module.apply_guide,
    "macros": apply_module.apply_macros,
    "routing": apply_module.apply_routing,
    "slas": apply_module.apply_slas,
    "talk": apply_module.apply_talk,
    "triggers": apply_module.apply_triggers,
    "views": apply_module.apply_views,
    "webhooks": apply_module.apply_webhooks,
    "widgets": apply_module.apply_widgets,
}


RESOURCE_ARGUMENT = typer.Argument(
    ...,
    help=f"Resource type ({', '.join(SUPPORTED_RESOURCES)})",
)
DESIRED_FILE_OPTION = typer.Option(
    ..., exists=True, readable=True, help="Desired state file (JSON or TOML)."
)
CURRENT_FILE_OPTION = typer.Option(
    ..., exists=True, readable=True, help="Observed state file (JSON or TOML)."
)
DIFF_OUTPUT_OPTION = typer.Option(None, help="Optional file path to store the diff JSON.")
PLAN_DIFF_ARGUMENT = typer.Argument(
    ..., exists=True, readable=True, help="Diff JSON produced by the diff command."
)
PLAN_OUTPUT_OPTION = typer.Option(None, help="Optional file path to store the plan JSON.")
PLAN_FILE_ARGUMENT = typer.Argument(..., exists=True, readable=True, help="Plan JSON to apply.")
ENVIRONMENT_OPTION = typer.Option(..., help="Zendesk environment identifier.")
SNAPSHOT_OUTPUT_OPTION = typer.Option(
    None, "--output", "-o", help="Output file path for snapshot JSON"
)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@app.command("env-check")
def env_check(env: str = ENVIRONMENT_OPTION) -> None:
    """Validate Zendesk credentials and required libraries for the environment."""

    prefix = f"ZENDESK_{env.upper()}_"
    missing = [key for key in ("SUBDOMAIN", "EMAIL", "TOKEN") if not os.getenv(f"{prefix}{key}")]
    if missing:
        typer.echo(
            f"Missing Zendesk credentials: {', '.join(missing)} for env '{env}'",
            err=True,
        )
        raise SystemExit(2)

    try:
        zenpy_spec = importlib.util.find_spec("zenpy")
    except Exception:  # pragma: no cover - defensive guard
        zenpy_spec = None
    if zenpy_spec is None:
        typer.echo("Zenpy is not installed. `pip install zenpy`", err=True)
        raise SystemExit(3)

    typer.echo("ok")


@app.command("deps-check")
def deps_check() -> None:
    """Report availability of optional dependencies (zenpy, torch)."""

    modules = []
    for name in ("zenpy", "torch"):
        available = importlib.util.find_spec(name) is not None
        modules.append({"module": name, "available": available})
    typer.echo(json.dumps(modules, indent=2))


@app.command("docs-sync")
def docs_sync(
    dry_run: bool = typer.Option(False, help="List URLs only, do not download"),
) -> None:
    """Fetch and snapshot Zendesk developer docs under docs/vendors/zendesk/YYYY-MM-DD/..."""

    script = Path(__file__).resolve().parents[2] / "scripts" / "zendesk_docs_fetch.py"
    cmd = [sys.executable, script.as_posix()]
    if dry_run:
        cmd.append("--dry-run")
    result = subprocess.run(
        cmd,
        check=False,
        capture_output=True,
        text=True,
    )
    if result.stdout:
        typer.echo(result.stdout.rstrip())
    if result.stderr:
        typer.echo(result.stderr.rstrip(), err=True)
    raise SystemExit(result.returncode)


@app.command("docs-catalog")
def docs_catalog() -> None:
    """Regenerate Markdown catalog index from the docs manifest."""

    script = Path(__file__).resolve().parents[2] / "scripts" / "zendesk_docs_catalog.py"
    result = subprocess.run(
        [sys.executable, script.as_posix()],
        check=False,
        capture_output=True,
        text=True,
    )
    if result.stdout:
        typer.echo(result.stdout.rstrip())
    if result.stderr:
        typer.echo(result.stderr.rstrip(), err=True)
    raise SystemExit(result.returncode)


@app.command()
def snapshot(
    env: str = ENVIRONMENT_OPTION,
    output: Path | None = SNAPSHOT_OUTPUT_OPTION,
) -> None:
    """Export the active Zendesk configuration for the given environment."""

    try:
        data = _export_zendesk_config(env)
    except Exception as exc:  # pragma: no cover - depends on Zenpy availability
        data = {
            "error": f"{exc}",
            "triggers": [],
            "fields": [],
            "forms": [],
            "groups": [],
            "macros": [],
            "views": [],
            "webhooks": [],
        }

    snapshot_json = json.dumps(data, indent=2)
    if output:
        output.write_text(snapshot_json, encoding="utf-8")
        typer.echo(f"Snapshot written to {output}")
    else:
        typer.echo(snapshot_json)


@app.command()
def diff(
    resource: str = RESOURCE_ARGUMENT,
    desired_file: Path = DESIRED_FILE_OPTION,
    current_file: Path = CURRENT_FILE_OPTION,
    output: Path | None = DIFF_OUTPUT_OPTION,
) -> None:
    """Compute differences between desired and observed Zendesk resources."""

    if resource == "guide":
        diffs = _diff_guide_resources(desired_file, current_file)
    else:
        model_cls, diff_fn = _resolve_resource(resource)
        desired_models = _load_models(desired_file, resource, model_cls)
        current_models = _load_models(current_file, resource, model_cls)
        diffs = diff_fn(desired_models, current_models)

    result = {resource: diffs}
    diff_text = json.dumps(result, indent=2, sort_keys=True)
    if output is not None:
        output.write_text(diff_text, encoding="utf-8")
        typer.echo(f"Diff written to {output}")
    else:
        adds = sum(1 for d in diffs if isinstance(d, Mapping) and d.get("op") == "add")
        updates = sum(
            1 for d in diffs if isinstance(d, Mapping) and d.get("op") in {"patch", "update"}
        )
        removes = sum(1 for d in diffs if isinstance(d, Mapping) and d.get("op") == "remove")
        typer.echo(
            f"{resource.capitalize()} diff: {adds} additions, {updates} changes, {removes} removals"
        )
        typer.echo(diff_text)


@app.command()
def plan(
    diff_file: Path = PLAN_DIFF_ARGUMENT,
    output: Path | None = PLAN_OUTPUT_OPTION,
) -> None:
    """Emit a plan from a previously generated diff."""

    diff_data = _read_structured_file(diff_file)

    plan_ops: list[dict[str, Any]] = []
    if isinstance(diff_data, Mapping):
        for resource_name, changes in diff_data.items():
            if not isinstance(changes, Sequence):
                continue
            for entry in changes:
                if not isinstance(entry, Mapping):
                    continue
                plan_ops.extend(_plan_operations_from_entry(resource_name, entry))
    elif isinstance(diff_data, Sequence):
        inferred_resource = _infer_resource_from_entries(diff_data)
        for entry in diff_data:
            if not isinstance(entry, Mapping):
                continue
            plan_ops.extend(_plan_operations_from_entry(inferred_resource, entry))

    plan_payload = {"operations": plan_ops}
    plan_json = json.dumps(plan_payload, indent=2)
    if output is not None:
        output.write_text(plan_json, encoding="utf-8")
        typer.echo(f"Plan written to {output}")
    else:
        typer.echo(plan_json)


@app.command()
def apply(
    resource: str = typer.Argument(..., help=APPLY_RESOURCE_HELP),
    plan_file: Path = PLAN_FILE_ARGUMENT,
    env: str = ENVIRONMENT_OPTION,
    dry_run: bool = typer.Option(False, "--dry-run", help="Simulate apply without making changes"),
) -> None:
    """Apply a previously generated plan for a specific Zendesk resource."""

    try:
        plan_payload = json.loads(plan_file.read_text(encoding="utf-8"))
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Plan file '{plan_file}' is not valid JSON: {exc}") from exc

    candidate: Any = plan_payload
    if isinstance(plan_payload, Mapping):
        operations = plan_payload.get("operations")
        if operations is None:
            operations = plan_payload.get(resource, plan_payload)
        candidate = operations

    if isinstance(candidate, (str | bytes | bytearray)):
        raise typer.BadParameter(
            "Plan payload must be a sequence of operations, not a scalar value."
        )
    if not isinstance(candidate, Sequence):
        raise typer.BadParameter("Plan payload must be a sequence of operations.")
    operations_list = list(candidate)
    try:
        validate_plan({"resource": resource, "operations": operations_list})
    except ValidationError as exc:
        logger.debug(f"ValidationError: {exc}")
        raise typer.BadParameter(f"Invalid plan for resource '{resource}': {exc}") from exc

    handlers = _APPLY_HANDLERS
    if resource not in handlers:
        valid = ", ".join(RESOURCE_TYPES)
        message = f"Unsupported resource '{resource}'. Valid options: {valid}."
        raise typer.BadParameter(message)

    try:
        handlers[resource](plan_payload, env, dry_run=dry_run)
        if not dry_run:
            update_artifact_version(f"zendesk/{resource}", operations_list)
    except ValueError as exc:
        logger.debug(f"ValueError: {exc}")
        raise typer.BadParameter(str(exc)) from exc
    except ImportError as exc:
        logger.debug(f"ImportError: {exc}")
        if dry_run:
            typer.echo(f"SDK missing but continuing due to --dry-run: {exc}", err=True)
        else:
            raise

    typer.echo("ok")


def x__plan_operations_from_entry__mutmut_orig(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_1(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = None
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_2(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") and entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_3(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get(None) or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_4(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("XXopXX") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_5(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("OP") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_6(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get(None)
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_7(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("XXactionXX")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_8(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("ACTION")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_9(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = None
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_10(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(None)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_11(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = None
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_12(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type not in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_13(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"XXaddXX", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_14(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"ADD", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_15(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "XXcreateXX"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_16(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "CREATE"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_17(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = None
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_18(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get(None) if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_19(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("XXvalueXX") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_20(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("VALUE") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_21(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "XXvalueXX" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_22(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "VALUE" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_23(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" not in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_24(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get(None, {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_25(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", None)
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_26(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get({})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_27(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", )
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_28(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("XXdataXX", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_29(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("DATA", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_30(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is not None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_31(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = None
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_32(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get(None, {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_33(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", None)
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_34(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get({})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_35(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", )
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_36(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("XXdataXX", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_37(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("DATA", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_38(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            None
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_39(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "XXactionXX": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_40(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "ACTION": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_41(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "XXcreateXX",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_42(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "CREATE",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_43(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "XXresourceXX": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_44(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "RESOURCE": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_45(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "XXnameXX": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_46(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "NAME": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_47(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "XXdataXX": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_48(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "DATA": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_49(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type not in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_50(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"XXremoveXX", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_51(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"REMOVE", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_52(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "XXdeleteXX"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_53(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "DELETE"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_54(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            None
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_55(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "XXactionXX": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_56(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "ACTION": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_57(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "XXdeleteXX",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_58(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "DELETE",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_59(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "XXresourceXX": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_60(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "RESOURCE": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_61(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "XXnameXX": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_62(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "NAME": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_63(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type not in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_64(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"XXpatchXX", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_65(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"PATCH", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_66(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "XXupdateXX"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_67(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "UPDATE"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_68(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = None
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_69(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get(None) if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_70(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("XXpatchesXX") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_71(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("PATCHES") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_72(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "XXpatchesXX" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_73(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "PATCHES" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_74(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" not in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_75(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get(None, [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_76(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", None)
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_77(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get([])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_78(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", )
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_79(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("XXchangesXX", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_80(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("CHANGES", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_81(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            None
        )
    return operations


def x__plan_operations_from_entry__mutmut_82(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "XXactionXX": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_83(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "ACTION": "update",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_84(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "XXupdateXX",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_85(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "UPDATE",
                "resource": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_86(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "XXresourceXX": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_87(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "RESOURCE": resource,
                "name": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_88(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "XXnameXX": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_89(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "NAME": name,
                "changes": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_90(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "XXchangesXX": changes,
            }
        )
    return operations


def x__plan_operations_from_entry__mutmut_91(resource: str, entry: Mapping[str, Any]) -> list[dict[str, Any]]:
    op_type = entry.get("op") or entry.get("action")
    name = _extract_name(entry)
    operations: list[dict[str, Any]] = []
    if op_type in {"add", "create"}:
        value = entry.get("value") if "value" in entry else entry.get("data", {})
        if value is None:
            value = entry.get("data", {})
        operations.append(
            {
                "action": "create",
                "resource": resource,
                "name": name,
                "data": value,
            }
        )
    elif op_type in {"remove", "delete"}:
        operations.append(
            {
                "action": "delete",
                "resource": resource,
                "name": name,
            }
        )
    elif op_type in {"patch", "update"}:
        changes = entry.get("patches") if "patches" in entry else entry.get("changes", [])
        operations.append(
            {
                "action": "update",
                "resource": resource,
                "name": name,
                "CHANGES": changes,
            }
        )
    return operations

x__plan_operations_from_entry__mutmut_mutants : ClassVar[MutantDict] = {
'x__plan_operations_from_entry__mutmut_1': x__plan_operations_from_entry__mutmut_1, 
    'x__plan_operations_from_entry__mutmut_2': x__plan_operations_from_entry__mutmut_2, 
    'x__plan_operations_from_entry__mutmut_3': x__plan_operations_from_entry__mutmut_3, 
    'x__plan_operations_from_entry__mutmut_4': x__plan_operations_from_entry__mutmut_4, 
    'x__plan_operations_from_entry__mutmut_5': x__plan_operations_from_entry__mutmut_5, 
    'x__plan_operations_from_entry__mutmut_6': x__plan_operations_from_entry__mutmut_6, 
    'x__plan_operations_from_entry__mutmut_7': x__plan_operations_from_entry__mutmut_7, 
    'x__plan_operations_from_entry__mutmut_8': x__plan_operations_from_entry__mutmut_8, 
    'x__plan_operations_from_entry__mutmut_9': x__plan_operations_from_entry__mutmut_9, 
    'x__plan_operations_from_entry__mutmut_10': x__plan_operations_from_entry__mutmut_10, 
    'x__plan_operations_from_entry__mutmut_11': x__plan_operations_from_entry__mutmut_11, 
    'x__plan_operations_from_entry__mutmut_12': x__plan_operations_from_entry__mutmut_12, 
    'x__plan_operations_from_entry__mutmut_13': x__plan_operations_from_entry__mutmut_13, 
    'x__plan_operations_from_entry__mutmut_14': x__plan_operations_from_entry__mutmut_14, 
    'x__plan_operations_from_entry__mutmut_15': x__plan_operations_from_entry__mutmut_15, 
    'x__plan_operations_from_entry__mutmut_16': x__plan_operations_from_entry__mutmut_16, 
    'x__plan_operations_from_entry__mutmut_17': x__plan_operations_from_entry__mutmut_17, 
    'x__plan_operations_from_entry__mutmut_18': x__plan_operations_from_entry__mutmut_18, 
    'x__plan_operations_from_entry__mutmut_19': x__plan_operations_from_entry__mutmut_19, 
    'x__plan_operations_from_entry__mutmut_20': x__plan_operations_from_entry__mutmut_20, 
    'x__plan_operations_from_entry__mutmut_21': x__plan_operations_from_entry__mutmut_21, 
    'x__plan_operations_from_entry__mutmut_22': x__plan_operations_from_entry__mutmut_22, 
    'x__plan_operations_from_entry__mutmut_23': x__plan_operations_from_entry__mutmut_23, 
    'x__plan_operations_from_entry__mutmut_24': x__plan_operations_from_entry__mutmut_24, 
    'x__plan_operations_from_entry__mutmut_25': x__plan_operations_from_entry__mutmut_25, 
    'x__plan_operations_from_entry__mutmut_26': x__plan_operations_from_entry__mutmut_26, 
    'x__plan_operations_from_entry__mutmut_27': x__plan_operations_from_entry__mutmut_27, 
    'x__plan_operations_from_entry__mutmut_28': x__plan_operations_from_entry__mutmut_28, 
    'x__plan_operations_from_entry__mutmut_29': x__plan_operations_from_entry__mutmut_29, 
    'x__plan_operations_from_entry__mutmut_30': x__plan_operations_from_entry__mutmut_30, 
    'x__plan_operations_from_entry__mutmut_31': x__plan_operations_from_entry__mutmut_31, 
    'x__plan_operations_from_entry__mutmut_32': x__plan_operations_from_entry__mutmut_32, 
    'x__plan_operations_from_entry__mutmut_33': x__plan_operations_from_entry__mutmut_33, 
    'x__plan_operations_from_entry__mutmut_34': x__plan_operations_from_entry__mutmut_34, 
    'x__plan_operations_from_entry__mutmut_35': x__plan_operations_from_entry__mutmut_35, 
    'x__plan_operations_from_entry__mutmut_36': x__plan_operations_from_entry__mutmut_36, 
    'x__plan_operations_from_entry__mutmut_37': x__plan_operations_from_entry__mutmut_37, 
    'x__plan_operations_from_entry__mutmut_38': x__plan_operations_from_entry__mutmut_38, 
    'x__plan_operations_from_entry__mutmut_39': x__plan_operations_from_entry__mutmut_39, 
    'x__plan_operations_from_entry__mutmut_40': x__plan_operations_from_entry__mutmut_40, 
    'x__plan_operations_from_entry__mutmut_41': x__plan_operations_from_entry__mutmut_41, 
    'x__plan_operations_from_entry__mutmut_42': x__plan_operations_from_entry__mutmut_42, 
    'x__plan_operations_from_entry__mutmut_43': x__plan_operations_from_entry__mutmut_43, 
    'x__plan_operations_from_entry__mutmut_44': x__plan_operations_from_entry__mutmut_44, 
    'x__plan_operations_from_entry__mutmut_45': x__plan_operations_from_entry__mutmut_45, 
    'x__plan_operations_from_entry__mutmut_46': x__plan_operations_from_entry__mutmut_46, 
    'x__plan_operations_from_entry__mutmut_47': x__plan_operations_from_entry__mutmut_47, 
    'x__plan_operations_from_entry__mutmut_48': x__plan_operations_from_entry__mutmut_48, 
    'x__plan_operations_from_entry__mutmut_49': x__plan_operations_from_entry__mutmut_49, 
    'x__plan_operations_from_entry__mutmut_50': x__plan_operations_from_entry__mutmut_50, 
    'x__plan_operations_from_entry__mutmut_51': x__plan_operations_from_entry__mutmut_51, 
    'x__plan_operations_from_entry__mutmut_52': x__plan_operations_from_entry__mutmut_52, 
    'x__plan_operations_from_entry__mutmut_53': x__plan_operations_from_entry__mutmut_53, 
    'x__plan_operations_from_entry__mutmut_54': x__plan_operations_from_entry__mutmut_54, 
    'x__plan_operations_from_entry__mutmut_55': x__plan_operations_from_entry__mutmut_55, 
    'x__plan_operations_from_entry__mutmut_56': x__plan_operations_from_entry__mutmut_56, 
    'x__plan_operations_from_entry__mutmut_57': x__plan_operations_from_entry__mutmut_57, 
    'x__plan_operations_from_entry__mutmut_58': x__plan_operations_from_entry__mutmut_58, 
    'x__plan_operations_from_entry__mutmut_59': x__plan_operations_from_entry__mutmut_59, 
    'x__plan_operations_from_entry__mutmut_60': x__plan_operations_from_entry__mutmut_60, 
    'x__plan_operations_from_entry__mutmut_61': x__plan_operations_from_entry__mutmut_61, 
    'x__plan_operations_from_entry__mutmut_62': x__plan_operations_from_entry__mutmut_62, 
    'x__plan_operations_from_entry__mutmut_63': x__plan_operations_from_entry__mutmut_63, 
    'x__plan_operations_from_entry__mutmut_64': x__plan_operations_from_entry__mutmut_64, 
    'x__plan_operations_from_entry__mutmut_65': x__plan_operations_from_entry__mutmut_65, 
    'x__plan_operations_from_entry__mutmut_66': x__plan_operations_from_entry__mutmut_66, 
    'x__plan_operations_from_entry__mutmut_67': x__plan_operations_from_entry__mutmut_67, 
    'x__plan_operations_from_entry__mutmut_68': x__plan_operations_from_entry__mutmut_68, 
    'x__plan_operations_from_entry__mutmut_69': x__plan_operations_from_entry__mutmut_69, 
    'x__plan_operations_from_entry__mutmut_70': x__plan_operations_from_entry__mutmut_70, 
    'x__plan_operations_from_entry__mutmut_71': x__plan_operations_from_entry__mutmut_71, 
    'x__plan_operations_from_entry__mutmut_72': x__plan_operations_from_entry__mutmut_72, 
    'x__plan_operations_from_entry__mutmut_73': x__plan_operations_from_entry__mutmut_73, 
    'x__plan_operations_from_entry__mutmut_74': x__plan_operations_from_entry__mutmut_74, 
    'x__plan_operations_from_entry__mutmut_75': x__plan_operations_from_entry__mutmut_75, 
    'x__plan_operations_from_entry__mutmut_76': x__plan_operations_from_entry__mutmut_76, 
    'x__plan_operations_from_entry__mutmut_77': x__plan_operations_from_entry__mutmut_77, 
    'x__plan_operations_from_entry__mutmut_78': x__plan_operations_from_entry__mutmut_78, 
    'x__plan_operations_from_entry__mutmut_79': x__plan_operations_from_entry__mutmut_79, 
    'x__plan_operations_from_entry__mutmut_80': x__plan_operations_from_entry__mutmut_80, 
    'x__plan_operations_from_entry__mutmut_81': x__plan_operations_from_entry__mutmut_81, 
    'x__plan_operations_from_entry__mutmut_82': x__plan_operations_from_entry__mutmut_82, 
    'x__plan_operations_from_entry__mutmut_83': x__plan_operations_from_entry__mutmut_83, 
    'x__plan_operations_from_entry__mutmut_84': x__plan_operations_from_entry__mutmut_84, 
    'x__plan_operations_from_entry__mutmut_85': x__plan_operations_from_entry__mutmut_85, 
    'x__plan_operations_from_entry__mutmut_86': x__plan_operations_from_entry__mutmut_86, 
    'x__plan_operations_from_entry__mutmut_87': x__plan_operations_from_entry__mutmut_87, 
    'x__plan_operations_from_entry__mutmut_88': x__plan_operations_from_entry__mutmut_88, 
    'x__plan_operations_from_entry__mutmut_89': x__plan_operations_from_entry__mutmut_89, 
    'x__plan_operations_from_entry__mutmut_90': x__plan_operations_from_entry__mutmut_90, 
    'x__plan_operations_from_entry__mutmut_91': x__plan_operations_from_entry__mutmut_91
}

def _plan_operations_from_entry(*args, **kwargs):
    result = _mutmut_trampoline(x__plan_operations_from_entry__mutmut_orig, x__plan_operations_from_entry__mutmut_mutants, args, kwargs)
    return result 

_plan_operations_from_entry.__signature__ = _mutmut_signature(x__plan_operations_from_entry__mutmut_orig)
x__plan_operations_from_entry__mutmut_orig.__name__ = 'x__plan_operations_from_entry'


def x__extract_name__mutmut_orig(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_1(entry: Mapping[str, Any]) -> str:
    path = None
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_2(entry: Mapping[str, Any]) -> str:
    path = entry.get(None, "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_3(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", None)
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_4(entry: Mapping[str, Any]) -> str:
    path = entry.get("")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_5(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", )
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_6(entry: Mapping[str, Any]) -> str:
    path = entry.get("XXpathXX", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_7(entry: Mapping[str, Any]) -> str:
    path = entry.get("PATH", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_8(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "XXXX")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_9(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) or path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_10(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = None
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_11(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split(None)[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_12(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip(None).split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_13(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.lstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_14(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("XX/XX").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_15(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("XX/XX")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_16(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[+1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_17(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-2]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_18(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = None
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_19(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get(None)
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_20(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("XXnameXX")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_21(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("NAME")
    if isinstance(name, str):
        return name
    return ""


def x__extract_name__mutmut_22(entry: Mapping[str, Any]) -> str:
    path = entry.get("path", "")
    if isinstance(path, str) and path:
        segment = path.rstrip("/").split("/")[-1]
        if segment:
            return segment
    name = entry.get("name")
    if isinstance(name, str):
        return name
    return "XXXX"

x__extract_name__mutmut_mutants : ClassVar[MutantDict] = {
'x__extract_name__mutmut_1': x__extract_name__mutmut_1, 
    'x__extract_name__mutmut_2': x__extract_name__mutmut_2, 
    'x__extract_name__mutmut_3': x__extract_name__mutmut_3, 
    'x__extract_name__mutmut_4': x__extract_name__mutmut_4, 
    'x__extract_name__mutmut_5': x__extract_name__mutmut_5, 
    'x__extract_name__mutmut_6': x__extract_name__mutmut_6, 
    'x__extract_name__mutmut_7': x__extract_name__mutmut_7, 
    'x__extract_name__mutmut_8': x__extract_name__mutmut_8, 
    'x__extract_name__mutmut_9': x__extract_name__mutmut_9, 
    'x__extract_name__mutmut_10': x__extract_name__mutmut_10, 
    'x__extract_name__mutmut_11': x__extract_name__mutmut_11, 
    'x__extract_name__mutmut_12': x__extract_name__mutmut_12, 
    'x__extract_name__mutmut_13': x__extract_name__mutmut_13, 
    'x__extract_name__mutmut_14': x__extract_name__mutmut_14, 
    'x__extract_name__mutmut_15': x__extract_name__mutmut_15, 
    'x__extract_name__mutmut_16': x__extract_name__mutmut_16, 
    'x__extract_name__mutmut_17': x__extract_name__mutmut_17, 
    'x__extract_name__mutmut_18': x__extract_name__mutmut_18, 
    'x__extract_name__mutmut_19': x__extract_name__mutmut_19, 
    'x__extract_name__mutmut_20': x__extract_name__mutmut_20, 
    'x__extract_name__mutmut_21': x__extract_name__mutmut_21, 
    'x__extract_name__mutmut_22': x__extract_name__mutmut_22
}

def _extract_name(*args, **kwargs):
    result = _mutmut_trampoline(x__extract_name__mutmut_orig, x__extract_name__mutmut_mutants, args, kwargs)
    return result 

_extract_name.__signature__ = _mutmut_signature(x__extract_name__mutmut_orig)
x__extract_name__mutmut_orig.__name__ = 'x__extract_name'


def x__infer_resource_from_entries__mutmut_orig(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_1(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = None
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_2(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get(None)
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_3(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("XXresourceXX")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_4(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("RESOURCE")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_5(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) or resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_6(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = None
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_7(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get(None)
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_8(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("XXpathXX")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_9(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("PATH")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_10(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) or path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_11(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = None
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_12(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip(None)
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_13(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.rstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_14(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("XX/XX")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_15(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split(None, 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_16(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", None)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_17(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split(1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_18(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", )[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_19(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.rsplit("/", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_20(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("XX/XX", 1)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_21(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 2)[0]
    return "resource"


def x__infer_resource_from_entries__mutmut_22(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[1]
    return "resource"


def x__infer_resource_from_entries__mutmut_23(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "XXresourceXX"


def x__infer_resource_from_entries__mutmut_24(entries: Sequence[object]) -> str:
    for entry in entries:
        if isinstance(entry, Mapping):
            resource = entry.get("resource")
            if isinstance(resource, str) and resource:
                return resource
            path = entry.get("path")
            if isinstance(path, str) and path:
                stripped = path.lstrip("/")
                if stripped:
                    return stripped.split("/", 1)[0]
    return "RESOURCE"

x__infer_resource_from_entries__mutmut_mutants : ClassVar[MutantDict] = {
'x__infer_resource_from_entries__mutmut_1': x__infer_resource_from_entries__mutmut_1, 
    'x__infer_resource_from_entries__mutmut_2': x__infer_resource_from_entries__mutmut_2, 
    'x__infer_resource_from_entries__mutmut_3': x__infer_resource_from_entries__mutmut_3, 
    'x__infer_resource_from_entries__mutmut_4': x__infer_resource_from_entries__mutmut_4, 
    'x__infer_resource_from_entries__mutmut_5': x__infer_resource_from_entries__mutmut_5, 
    'x__infer_resource_from_entries__mutmut_6': x__infer_resource_from_entries__mutmut_6, 
    'x__infer_resource_from_entries__mutmut_7': x__infer_resource_from_entries__mutmut_7, 
    'x__infer_resource_from_entries__mutmut_8': x__infer_resource_from_entries__mutmut_8, 
    'x__infer_resource_from_entries__mutmut_9': x__infer_resource_from_entries__mutmut_9, 
    'x__infer_resource_from_entries__mutmut_10': x__infer_resource_from_entries__mutmut_10, 
    'x__infer_resource_from_entries__mutmut_11': x__infer_resource_from_entries__mutmut_11, 
    'x__infer_resource_from_entries__mutmut_12': x__infer_resource_from_entries__mutmut_12, 
    'x__infer_resource_from_entries__mutmut_13': x__infer_resource_from_entries__mutmut_13, 
    'x__infer_resource_from_entries__mutmut_14': x__infer_resource_from_entries__mutmut_14, 
    'x__infer_resource_from_entries__mutmut_15': x__infer_resource_from_entries__mutmut_15, 
    'x__infer_resource_from_entries__mutmut_16': x__infer_resource_from_entries__mutmut_16, 
    'x__infer_resource_from_entries__mutmut_17': x__infer_resource_from_entries__mutmut_17, 
    'x__infer_resource_from_entries__mutmut_18': x__infer_resource_from_entries__mutmut_18, 
    'x__infer_resource_from_entries__mutmut_19': x__infer_resource_from_entries__mutmut_19, 
    'x__infer_resource_from_entries__mutmut_20': x__infer_resource_from_entries__mutmut_20, 
    'x__infer_resource_from_entries__mutmut_21': x__infer_resource_from_entries__mutmut_21, 
    'x__infer_resource_from_entries__mutmut_22': x__infer_resource_from_entries__mutmut_22, 
    'x__infer_resource_from_entries__mutmut_23': x__infer_resource_from_entries__mutmut_23, 
    'x__infer_resource_from_entries__mutmut_24': x__infer_resource_from_entries__mutmut_24
}

def _infer_resource_from_entries(*args, **kwargs):
    result = _mutmut_trampoline(x__infer_resource_from_entries__mutmut_orig, x__infer_resource_from_entries__mutmut_mutants, args, kwargs)
    return result 

_infer_resource_from_entries.__signature__ = _mutmut_signature(x__infer_resource_from_entries__mutmut_orig)
x__infer_resource_from_entries__mutmut_orig.__name__ = 'x__infer_resource_from_entries'


@app.command()
def metrics() -> None:
    """Register Zendesk metrics and list their identifiers."""

    registered = register_zendesk_metrics()
    typer.echo("Registered metrics:\n" + "\n".join(f"- {name}" for name in registered))


def x__resolve_resource__mutmut_orig(resource: str) -> ResourceConfig:
    try:
        return _RESOURCE_CONFIG[resource]
    except KeyError as exc:  # pragma: no cover - CLI validation
        valid = ", ".join(sorted(SUPPORTED_RESOURCES))
        raise typer.BadParameter(
            f"Unsupported resource '{resource}'. Valid options: {valid}."
        ) from exc


def x__resolve_resource__mutmut_1(resource: str) -> ResourceConfig:
    try:
        return _RESOURCE_CONFIG[resource]
    except KeyError as exc:  # pragma: no cover - CLI validation
        valid = None
        raise typer.BadParameter(
            f"Unsupported resource '{resource}'. Valid options: {valid}."
        ) from exc


def x__resolve_resource__mutmut_2(resource: str) -> ResourceConfig:
    try:
        return _RESOURCE_CONFIG[resource]
    except KeyError as exc:  # pragma: no cover - CLI validation
        valid = ", ".join(None)
        raise typer.BadParameter(
            f"Unsupported resource '{resource}'. Valid options: {valid}."
        ) from exc


def x__resolve_resource__mutmut_3(resource: str) -> ResourceConfig:
    try:
        return _RESOURCE_CONFIG[resource]
    except KeyError as exc:  # pragma: no cover - CLI validation
        valid = "XX, XX".join(sorted(SUPPORTED_RESOURCES))
        raise typer.BadParameter(
            f"Unsupported resource '{resource}'. Valid options: {valid}."
        ) from exc


def x__resolve_resource__mutmut_4(resource: str) -> ResourceConfig:
    try:
        return _RESOURCE_CONFIG[resource]
    except KeyError as exc:  # pragma: no cover - CLI validation
        valid = ", ".join(sorted(None))
        raise typer.BadParameter(
            f"Unsupported resource '{resource}'. Valid options: {valid}."
        ) from exc


def x__resolve_resource__mutmut_5(resource: str) -> ResourceConfig:
    try:
        return _RESOURCE_CONFIG[resource]
    except KeyError as exc:  # pragma: no cover - CLI validation
        valid = ", ".join(sorted(SUPPORTED_RESOURCES))
        raise typer.BadParameter(
            None
        ) from exc

x__resolve_resource__mutmut_mutants : ClassVar[MutantDict] = {
'x__resolve_resource__mutmut_1': x__resolve_resource__mutmut_1, 
    'x__resolve_resource__mutmut_2': x__resolve_resource__mutmut_2, 
    'x__resolve_resource__mutmut_3': x__resolve_resource__mutmut_3, 
    'x__resolve_resource__mutmut_4': x__resolve_resource__mutmut_4, 
    'x__resolve_resource__mutmut_5': x__resolve_resource__mutmut_5
}

def _resolve_resource(*args, **kwargs):
    result = _mutmut_trampoline(x__resolve_resource__mutmut_orig, x__resolve_resource__mutmut_mutants, args, kwargs)
    return result 

_resolve_resource.__signature__ = _mutmut_signature(x__resolve_resource__mutmut_orig)
x__resolve_resource__mutmut_orig.__name__ = 'x__resolve_resource'


def x__diff_guide_resources__mutmut_orig(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_1(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = None
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_2(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(None)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_3(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = None
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_4(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(None)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_5(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) and not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_6(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_7(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_8(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            None
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_9(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "XXGuide files must contain a mapping with 'themes' and 'templates' lists.XX"
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_10(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_11(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "GUIDE FILES MUST CONTAIN A MAPPING WITH 'THEMES' AND 'TEMPLATES' LISTS."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_12(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = None
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_13(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        None,
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_14(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        None,
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_15(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        None,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_16(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        None,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_17(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_18(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_19(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_20(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_21(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get(None),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_22(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("XXthemesXX"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_23(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("THEMES"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_24(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "XXguide themesXX",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_25(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "GUIDE THEMES",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_26(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = None
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_27(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        None,
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_28(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        None,
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_29(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        None,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_30(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        None,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_31(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_32(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_33(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_34(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_35(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get(None),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_36(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("XXthemesXX"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_37(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("THEMES"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_38(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "XXguide themesXX",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_39(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "GUIDE THEMES",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_40(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = None
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_41(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        None,
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_42(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        None,
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_43(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        None,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_44(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        None,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_45(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_46(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_47(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_48(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_49(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get(None),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_50(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("XXtemplatesXX"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_51(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("TEMPLATES"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_52(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "XXguide templatesXX",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_53(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "GUIDE TEMPLATES",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_54(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = None
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_55(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        None,
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_56(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        None,
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_57(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        None,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_58(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        None,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_59(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_60(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_61(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_62(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_63(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get(None),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_64(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("XXtemplatesXX"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_65(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("TEMPLATES"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_66(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "XXguide templatesXX",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_67(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "GUIDE TEMPLATES",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_68(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(None, current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_69(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, None, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_70(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, None, current_templates)


def x__diff_guide_resources__mutmut_71(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, None)


def x__diff_guide_resources__mutmut_72(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(current_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_73(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, desired_templates, current_templates)


def x__diff_guide_resources__mutmut_74(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, current_templates)


def x__diff_guide_resources__mutmut_75(desired_file: Path, current_file: Path) -> list[dict[str, Any]]:
    desired_payload = _read_structured_file(desired_file)
    current_payload = _read_structured_file(current_file)
    if not isinstance(desired_payload, Mapping) or not isinstance(current_payload, Mapping):
        raise typer.BadParameter(
            "Guide files must contain a mapping with 'themes' and 'templates' lists."
        )

    desired_themes = _coerce_model_sequence(
        desired_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        desired_file,
    )
    current_themes = _coerce_model_sequence(
        current_payload.get("themes"),
        "guide themes",
        GuideThemeRef,
        current_file,
    )
    desired_templates = _coerce_model_sequence(
        desired_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        desired_file,
    )
    current_templates = _coerce_model_sequence(
        current_payload.get("templates"),
        "guide templates",
        TemplatePatch,
        current_file,
    )
    return diff_guide(desired_themes, current_themes, desired_templates, )

x__diff_guide_resources__mutmut_mutants : ClassVar[MutantDict] = {
'x__diff_guide_resources__mutmut_1': x__diff_guide_resources__mutmut_1, 
    'x__diff_guide_resources__mutmut_2': x__diff_guide_resources__mutmut_2, 
    'x__diff_guide_resources__mutmut_3': x__diff_guide_resources__mutmut_3, 
    'x__diff_guide_resources__mutmut_4': x__diff_guide_resources__mutmut_4, 
    'x__diff_guide_resources__mutmut_5': x__diff_guide_resources__mutmut_5, 
    'x__diff_guide_resources__mutmut_6': x__diff_guide_resources__mutmut_6, 
    'x__diff_guide_resources__mutmut_7': x__diff_guide_resources__mutmut_7, 
    'x__diff_guide_resources__mutmut_8': x__diff_guide_resources__mutmut_8, 
    'x__diff_guide_resources__mutmut_9': x__diff_guide_resources__mutmut_9, 
    'x__diff_guide_resources__mutmut_10': x__diff_guide_resources__mutmut_10, 
    'x__diff_guide_resources__mutmut_11': x__diff_guide_resources__mutmut_11, 
    'x__diff_guide_resources__mutmut_12': x__diff_guide_resources__mutmut_12, 
    'x__diff_guide_resources__mutmut_13': x__diff_guide_resources__mutmut_13, 
    'x__diff_guide_resources__mutmut_14': x__diff_guide_resources__mutmut_14, 
    'x__diff_guide_resources__mutmut_15': x__diff_guide_resources__mutmut_15, 
    'x__diff_guide_resources__mutmut_16': x__diff_guide_resources__mutmut_16, 
    'x__diff_guide_resources__mutmut_17': x__diff_guide_resources__mutmut_17, 
    'x__diff_guide_resources__mutmut_18': x__diff_guide_resources__mutmut_18, 
    'x__diff_guide_resources__mutmut_19': x__diff_guide_resources__mutmut_19, 
    'x__diff_guide_resources__mutmut_20': x__diff_guide_resources__mutmut_20, 
    'x__diff_guide_resources__mutmut_21': x__diff_guide_resources__mutmut_21, 
    'x__diff_guide_resources__mutmut_22': x__diff_guide_resources__mutmut_22, 
    'x__diff_guide_resources__mutmut_23': x__diff_guide_resources__mutmut_23, 
    'x__diff_guide_resources__mutmut_24': x__diff_guide_resources__mutmut_24, 
    'x__diff_guide_resources__mutmut_25': x__diff_guide_resources__mutmut_25, 
    'x__diff_guide_resources__mutmut_26': x__diff_guide_resources__mutmut_26, 
    'x__diff_guide_resources__mutmut_27': x__diff_guide_resources__mutmut_27, 
    'x__diff_guide_resources__mutmut_28': x__diff_guide_resources__mutmut_28, 
    'x__diff_guide_resources__mutmut_29': x__diff_guide_resources__mutmut_29, 
    'x__diff_guide_resources__mutmut_30': x__diff_guide_resources__mutmut_30, 
    'x__diff_guide_resources__mutmut_31': x__diff_guide_resources__mutmut_31, 
    'x__diff_guide_resources__mutmut_32': x__diff_guide_resources__mutmut_32, 
    'x__diff_guide_resources__mutmut_33': x__diff_guide_resources__mutmut_33, 
    'x__diff_guide_resources__mutmut_34': x__diff_guide_resources__mutmut_34, 
    'x__diff_guide_resources__mutmut_35': x__diff_guide_resources__mutmut_35, 
    'x__diff_guide_resources__mutmut_36': x__diff_guide_resources__mutmut_36, 
    'x__diff_guide_resources__mutmut_37': x__diff_guide_resources__mutmut_37, 
    'x__diff_guide_resources__mutmut_38': x__diff_guide_resources__mutmut_38, 
    'x__diff_guide_resources__mutmut_39': x__diff_guide_resources__mutmut_39, 
    'x__diff_guide_resources__mutmut_40': x__diff_guide_resources__mutmut_40, 
    'x__diff_guide_resources__mutmut_41': x__diff_guide_resources__mutmut_41, 
    'x__diff_guide_resources__mutmut_42': x__diff_guide_resources__mutmut_42, 
    'x__diff_guide_resources__mutmut_43': x__diff_guide_resources__mutmut_43, 
    'x__diff_guide_resources__mutmut_44': x__diff_guide_resources__mutmut_44, 
    'x__diff_guide_resources__mutmut_45': x__diff_guide_resources__mutmut_45, 
    'x__diff_guide_resources__mutmut_46': x__diff_guide_resources__mutmut_46, 
    'x__diff_guide_resources__mutmut_47': x__diff_guide_resources__mutmut_47, 
    'x__diff_guide_resources__mutmut_48': x__diff_guide_resources__mutmut_48, 
    'x__diff_guide_resources__mutmut_49': x__diff_guide_resources__mutmut_49, 
    'x__diff_guide_resources__mutmut_50': x__diff_guide_resources__mutmut_50, 
    'x__diff_guide_resources__mutmut_51': x__diff_guide_resources__mutmut_51, 
    'x__diff_guide_resources__mutmut_52': x__diff_guide_resources__mutmut_52, 
    'x__diff_guide_resources__mutmut_53': x__diff_guide_resources__mutmut_53, 
    'x__diff_guide_resources__mutmut_54': x__diff_guide_resources__mutmut_54, 
    'x__diff_guide_resources__mutmut_55': x__diff_guide_resources__mutmut_55, 
    'x__diff_guide_resources__mutmut_56': x__diff_guide_resources__mutmut_56, 
    'x__diff_guide_resources__mutmut_57': x__diff_guide_resources__mutmut_57, 
    'x__diff_guide_resources__mutmut_58': x__diff_guide_resources__mutmut_58, 
    'x__diff_guide_resources__mutmut_59': x__diff_guide_resources__mutmut_59, 
    'x__diff_guide_resources__mutmut_60': x__diff_guide_resources__mutmut_60, 
    'x__diff_guide_resources__mutmut_61': x__diff_guide_resources__mutmut_61, 
    'x__diff_guide_resources__mutmut_62': x__diff_guide_resources__mutmut_62, 
    'x__diff_guide_resources__mutmut_63': x__diff_guide_resources__mutmut_63, 
    'x__diff_guide_resources__mutmut_64': x__diff_guide_resources__mutmut_64, 
    'x__diff_guide_resources__mutmut_65': x__diff_guide_resources__mutmut_65, 
    'x__diff_guide_resources__mutmut_66': x__diff_guide_resources__mutmut_66, 
    'x__diff_guide_resources__mutmut_67': x__diff_guide_resources__mutmut_67, 
    'x__diff_guide_resources__mutmut_68': x__diff_guide_resources__mutmut_68, 
    'x__diff_guide_resources__mutmut_69': x__diff_guide_resources__mutmut_69, 
    'x__diff_guide_resources__mutmut_70': x__diff_guide_resources__mutmut_70, 
    'x__diff_guide_resources__mutmut_71': x__diff_guide_resources__mutmut_71, 
    'x__diff_guide_resources__mutmut_72': x__diff_guide_resources__mutmut_72, 
    'x__diff_guide_resources__mutmut_73': x__diff_guide_resources__mutmut_73, 
    'x__diff_guide_resources__mutmut_74': x__diff_guide_resources__mutmut_74, 
    'x__diff_guide_resources__mutmut_75': x__diff_guide_resources__mutmut_75
}

def _diff_guide_resources(*args, **kwargs):
    result = _mutmut_trampoline(x__diff_guide_resources__mutmut_orig, x__diff_guide_resources__mutmut_mutants, args, kwargs)
    return result 

_diff_guide_resources.__signature__ = _mutmut_signature(x__diff_guide_resources__mutmut_orig)
x__diff_guide_resources__mutmut_orig.__name__ = 'x__diff_guide_resources'


def x__load_models__mutmut_orig(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, path)


def x__load_models__mutmut_1(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = None
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, path)


def x__load_models__mutmut_2(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(None)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, path)


def x__load_models__mutmut_3(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) or resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, path)


def x__load_models__mutmut_4(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource not in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, path)


def x__load_models__mutmut_5(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = None
    return _coerce_model_sequence(payload, resource, model_cls, path)


def x__load_models__mutmut_6(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(None, resource, model_cls, path)


def x__load_models__mutmut_7(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, None, model_cls, path)


def x__load_models__mutmut_8(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, None, path)


def x__load_models__mutmut_9(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, None)


def x__load_models__mutmut_10(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(resource, model_cls, path)


def x__load_models__mutmut_11(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, model_cls, path)


def x__load_models__mutmut_12(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, path)


def x__load_models__mutmut_13(
    path: Path,
    resource: str,
    model_cls: type[BaseModel],
) -> list[BaseModel]:
    payload = _read_structured_file(path)
    if isinstance(payload, Mapping) and resource in payload:
        payload = payload[resource]
    return _coerce_model_sequence(payload, resource, model_cls, )

x__load_models__mutmut_mutants : ClassVar[MutantDict] = {
'x__load_models__mutmut_1': x__load_models__mutmut_1, 
    'x__load_models__mutmut_2': x__load_models__mutmut_2, 
    'x__load_models__mutmut_3': x__load_models__mutmut_3, 
    'x__load_models__mutmut_4': x__load_models__mutmut_4, 
    'x__load_models__mutmut_5': x__load_models__mutmut_5, 
    'x__load_models__mutmut_6': x__load_models__mutmut_6, 
    'x__load_models__mutmut_7': x__load_models__mutmut_7, 
    'x__load_models__mutmut_8': x__load_models__mutmut_8, 
    'x__load_models__mutmut_9': x__load_models__mutmut_9, 
    'x__load_models__mutmut_10': x__load_models__mutmut_10, 
    'x__load_models__mutmut_11': x__load_models__mutmut_11, 
    'x__load_models__mutmut_12': x__load_models__mutmut_12, 
    'x__load_models__mutmut_13': x__load_models__mutmut_13
}

def _load_models(*args, **kwargs):
    result = _mutmut_trampoline(x__load_models__mutmut_orig, x__load_models__mutmut_mutants, args, kwargs)
    return result 

_load_models.__signature__ = _mutmut_signature(x__load_models__mutmut_orig)
x__load_models__mutmut_orig.__name__ = 'x__load_models'


def x__coerce_model_sequence__mutmut_orig(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_1(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is not None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_2(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_3(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            None,
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_4(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            None,
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_5(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = None
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_6(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(None)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_7(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(None)
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_8(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(None))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_9(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(None)
            raise typer.BadParameter(f"Invalid {resource} entry in {source}: {exc}") from exc
    return models


def x__coerce_model_sequence__mutmut_10(
    payload: object,
    resource: str,
    model_cls: type[BaseModel],
    source: Path,
) -> list[BaseModel]:
    if payload is None:
        return []
    if not isinstance(payload, Sequence):
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    if isinstance(payload, (str, bytes, bytearray)):  # noqa: UP038
        raise typer.BadParameter(
            f"Expected a list of {resource} definitions in {source}.",
        )
    models: list[BaseModel] = []
    for item in payload:
        try:
            if isinstance(item, model_cls):
                models.append(item)
            else:
                models.append(model_cls.model_validate(item))
        except ValidationError as exc:
            logger.debug(f"ValidationError: {exc}")
            raise typer.BadParameter(None) from exc
    return models

x__coerce_model_sequence__mutmut_mutants : ClassVar[MutantDict] = {
'x__coerce_model_sequence__mutmut_1': x__coerce_model_sequence__mutmut_1, 
    'x__coerce_model_sequence__mutmut_2': x__coerce_model_sequence__mutmut_2, 
    'x__coerce_model_sequence__mutmut_3': x__coerce_model_sequence__mutmut_3, 
    'x__coerce_model_sequence__mutmut_4': x__coerce_model_sequence__mutmut_4, 
    'x__coerce_model_sequence__mutmut_5': x__coerce_model_sequence__mutmut_5, 
    'x__coerce_model_sequence__mutmut_6': x__coerce_model_sequence__mutmut_6, 
    'x__coerce_model_sequence__mutmut_7': x__coerce_model_sequence__mutmut_7, 
    'x__coerce_model_sequence__mutmut_8': x__coerce_model_sequence__mutmut_8, 
    'x__coerce_model_sequence__mutmut_9': x__coerce_model_sequence__mutmut_9, 
    'x__coerce_model_sequence__mutmut_10': x__coerce_model_sequence__mutmut_10
}

def _coerce_model_sequence(*args, **kwargs):
    result = _mutmut_trampoline(x__coerce_model_sequence__mutmut_orig, x__coerce_model_sequence__mutmut_mutants, args, kwargs)
    return result 

_coerce_model_sequence.__signature__ = _mutmut_signature(x__coerce_model_sequence__mutmut_orig)
x__coerce_model_sequence__mutmut_orig.__name__ = 'x__coerce_model_sequence'


def x__read_structured_file__mutmut_orig(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_1(path: Path) -> object:
    text = None
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_2(path: Path) -> object:
    text = path.read_text(encoding=None)
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_3(path: Path) -> object:
    text = path.read_text(encoding="XXutf-8XX")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_4(path: Path) -> object:
    text = path.read_text(encoding="UTF-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_5(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.upper() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_6(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() not in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_7(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {"XX.tomlXX", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_8(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".TOML", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_9(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", "XX.tmlXX"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_10(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".TML"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_11(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter(None) from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_12(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("XXTOML support requires Python 3.11 or newer.XX") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_13(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("toml support requires python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_14(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML SUPPORT REQUIRES PYTHON 3.11 OR NEWER.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_15(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(None)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_16(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(None)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(f"Failed to parse {path}: {exc}") from exc


def x__read_structured_file__mutmut_17(path: Path) -> object:
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() in {".toml", ".tml"}:
        try:
            import tomllib
        except ModuleNotFoundError as exc:  # pragma: no cover - Python <3.11 guard
            raise typer.BadParameter("TOML support requires Python 3.11 or newer.") from exc
        return tomllib.loads(text)
    try:
        return json.loads(text)
    except json.JSONDecodeError as exc:
        raise typer.BadParameter(None) from exc

x__read_structured_file__mutmut_mutants : ClassVar[MutantDict] = {
'x__read_structured_file__mutmut_1': x__read_structured_file__mutmut_1, 
    'x__read_structured_file__mutmut_2': x__read_structured_file__mutmut_2, 
    'x__read_structured_file__mutmut_3': x__read_structured_file__mutmut_3, 
    'x__read_structured_file__mutmut_4': x__read_structured_file__mutmut_4, 
    'x__read_structured_file__mutmut_5': x__read_structured_file__mutmut_5, 
    'x__read_structured_file__mutmut_6': x__read_structured_file__mutmut_6, 
    'x__read_structured_file__mutmut_7': x__read_structured_file__mutmut_7, 
    'x__read_structured_file__mutmut_8': x__read_structured_file__mutmut_8, 
    'x__read_structured_file__mutmut_9': x__read_structured_file__mutmut_9, 
    'x__read_structured_file__mutmut_10': x__read_structured_file__mutmut_10, 
    'x__read_structured_file__mutmut_11': x__read_structured_file__mutmut_11, 
    'x__read_structured_file__mutmut_12': x__read_structured_file__mutmut_12, 
    'x__read_structured_file__mutmut_13': x__read_structured_file__mutmut_13, 
    'x__read_structured_file__mutmut_14': x__read_structured_file__mutmut_14, 
    'x__read_structured_file__mutmut_15': x__read_structured_file__mutmut_15, 
    'x__read_structured_file__mutmut_16': x__read_structured_file__mutmut_16, 
    'x__read_structured_file__mutmut_17': x__read_structured_file__mutmut_17
}

def _read_structured_file(*args, **kwargs):
    result = _mutmut_trampoline(x__read_structured_file__mutmut_orig, x__read_structured_file__mutmut_mutants, args, kwargs)
    return result 

_read_structured_file.__signature__ = _mutmut_signature(x__read_structured_file__mutmut_orig)
x__read_structured_file__mutmut_orig.__name__ = 'x__read_structured_file'


def x__get_zendesk_client__mutmut_orig(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_1(env: str):
    module_spec = None
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_2(env: str):
    module_spec = importlib.util.find_spec(None)
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_3(env: str):
    module_spec = importlib.util.find_spec("XXzenpyXX")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_4(env: str):
    module_spec = importlib.util.find_spec("ZENPY")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_5(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is not None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_6(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError(None)
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_7(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("XXZenpy is required to connect to Zendesk.XX")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_8(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("zenpy is required to connect to zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_9(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("ZENPY IS REQUIRED TO CONNECT TO ZENDESK.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_10(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = None
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_11(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module(None)
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_12(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("XXzenpyXX")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_13(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("ZENPY")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_14(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = None
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_15(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(None, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_16(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, None, None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_17(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr("Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_18(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_19(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", )
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_20(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "XXZenpyXX", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_21(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_22(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "ZENPY", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_23(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is not None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_24(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError(None)

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_25(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("XXZenpy client is unavailable.XX")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_26(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_27(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("ZENPY CLIENT IS UNAVAILABLE.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_28(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = None
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_29(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.lower()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_30(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = None
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_31(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(None)
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_32(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = None
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_33(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(None)
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_34(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = None
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_35(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(None)
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_36(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email and not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_37(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain and not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_38(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_39(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_40(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_41(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(None)

    return zenpy_client(subdomain=subdomain, email=email, token=token)


def x__get_zendesk_client__mutmut_42(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=None, email=email, token=token)


def x__get_zendesk_client__mutmut_43(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=None, token=token)


def x__get_zendesk_client__mutmut_44(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, token=None)


def x__get_zendesk_client__mutmut_45(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(email=email, token=token)


def x__get_zendesk_client__mutmut_46(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, token=token)


def x__get_zendesk_client__mutmut_47(env: str):
    module_spec = importlib.util.find_spec("zenpy")
    if module_spec is None:
        raise RuntimeError("Zenpy is required to connect to Zendesk.")
    zenpy_module = importlib.import_module("zenpy")
    zenpy_client = getattr(zenpy_module, "Zenpy", None)
    if zenpy_client is None:
        raise RuntimeError("Zenpy client is unavailable.")

    prefix = f"ZENDESK_{env.upper()}_"
    subdomain = os.getenv(f"{prefix}SUBDOMAIN")
    email = os.getenv(f"{prefix}EMAIL")
    token = os.getenv(f"{prefix}TOKEN")
    if not subdomain or not email or not token:
        raise RuntimeError(f"Missing Zendesk credentials for environment '{env}'.")

    return zenpy_client(subdomain=subdomain, email=email, )

x__get_zendesk_client__mutmut_mutants : ClassVar[MutantDict] = {
'x__get_zendesk_client__mutmut_1': x__get_zendesk_client__mutmut_1, 
    'x__get_zendesk_client__mutmut_2': x__get_zendesk_client__mutmut_2, 
    'x__get_zendesk_client__mutmut_3': x__get_zendesk_client__mutmut_3, 
    'x__get_zendesk_client__mutmut_4': x__get_zendesk_client__mutmut_4, 
    'x__get_zendesk_client__mutmut_5': x__get_zendesk_client__mutmut_5, 
    'x__get_zendesk_client__mutmut_6': x__get_zendesk_client__mutmut_6, 
    'x__get_zendesk_client__mutmut_7': x__get_zendesk_client__mutmut_7, 
    'x__get_zendesk_client__mutmut_8': x__get_zendesk_client__mutmut_8, 
    'x__get_zendesk_client__mutmut_9': x__get_zendesk_client__mutmut_9, 
    'x__get_zendesk_client__mutmut_10': x__get_zendesk_client__mutmut_10, 
    'x__get_zendesk_client__mutmut_11': x__get_zendesk_client__mutmut_11, 
    'x__get_zendesk_client__mutmut_12': x__get_zendesk_client__mutmut_12, 
    'x__get_zendesk_client__mutmut_13': x__get_zendesk_client__mutmut_13, 
    'x__get_zendesk_client__mutmut_14': x__get_zendesk_client__mutmut_14, 
    'x__get_zendesk_client__mutmut_15': x__get_zendesk_client__mutmut_15, 
    'x__get_zendesk_client__mutmut_16': x__get_zendesk_client__mutmut_16, 
    'x__get_zendesk_client__mutmut_17': x__get_zendesk_client__mutmut_17, 
    'x__get_zendesk_client__mutmut_18': x__get_zendesk_client__mutmut_18, 
    'x__get_zendesk_client__mutmut_19': x__get_zendesk_client__mutmut_19, 
    'x__get_zendesk_client__mutmut_20': x__get_zendesk_client__mutmut_20, 
    'x__get_zendesk_client__mutmut_21': x__get_zendesk_client__mutmut_21, 
    'x__get_zendesk_client__mutmut_22': x__get_zendesk_client__mutmut_22, 
    'x__get_zendesk_client__mutmut_23': x__get_zendesk_client__mutmut_23, 
    'x__get_zendesk_client__mutmut_24': x__get_zendesk_client__mutmut_24, 
    'x__get_zendesk_client__mutmut_25': x__get_zendesk_client__mutmut_25, 
    'x__get_zendesk_client__mutmut_26': x__get_zendesk_client__mutmut_26, 
    'x__get_zendesk_client__mutmut_27': x__get_zendesk_client__mutmut_27, 
    'x__get_zendesk_client__mutmut_28': x__get_zendesk_client__mutmut_28, 
    'x__get_zendesk_client__mutmut_29': x__get_zendesk_client__mutmut_29, 
    'x__get_zendesk_client__mutmut_30': x__get_zendesk_client__mutmut_30, 
    'x__get_zendesk_client__mutmut_31': x__get_zendesk_client__mutmut_31, 
    'x__get_zendesk_client__mutmut_32': x__get_zendesk_client__mutmut_32, 
    'x__get_zendesk_client__mutmut_33': x__get_zendesk_client__mutmut_33, 
    'x__get_zendesk_client__mutmut_34': x__get_zendesk_client__mutmut_34, 
    'x__get_zendesk_client__mutmut_35': x__get_zendesk_client__mutmut_35, 
    'x__get_zendesk_client__mutmut_36': x__get_zendesk_client__mutmut_36, 
    'x__get_zendesk_client__mutmut_37': x__get_zendesk_client__mutmut_37, 
    'x__get_zendesk_client__mutmut_38': x__get_zendesk_client__mutmut_38, 
    'x__get_zendesk_client__mutmut_39': x__get_zendesk_client__mutmut_39, 
    'x__get_zendesk_client__mutmut_40': x__get_zendesk_client__mutmut_40, 
    'x__get_zendesk_client__mutmut_41': x__get_zendesk_client__mutmut_41, 
    'x__get_zendesk_client__mutmut_42': x__get_zendesk_client__mutmut_42, 
    'x__get_zendesk_client__mutmut_43': x__get_zendesk_client__mutmut_43, 
    'x__get_zendesk_client__mutmut_44': x__get_zendesk_client__mutmut_44, 
    'x__get_zendesk_client__mutmut_45': x__get_zendesk_client__mutmut_45, 
    'x__get_zendesk_client__mutmut_46': x__get_zendesk_client__mutmut_46, 
    'x__get_zendesk_client__mutmut_47': x__get_zendesk_client__mutmut_47
}

def _get_zendesk_client(*args, **kwargs):
    result = _mutmut_trampoline(x__get_zendesk_client__mutmut_orig, x__get_zendesk_client__mutmut_mutants, args, kwargs)
    return result 

_get_zendesk_client.__signature__ = _mutmut_signature(x__get_zendesk_client__mutmut_orig)
x__get_zendesk_client__mutmut_orig.__name__ = 'x__get_zendesk_client'


def x__collect_objects__mutmut_orig(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_1(client: object, attribute: str) -> list[Any]:
    collection = None
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_2(client: object, attribute: str) -> list[Any]:
    collection = getattr(None, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_3(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, None, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_4(client: object, attribute: str) -> list[Any]:
    collection = getattr(attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_5(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_6(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, )
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_7(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is not None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_8(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = None
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_9(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(None) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_10(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is not None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_11(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(None)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_12(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(None)
        logger.warning(f"TypeError: {e}", exc_info=True)
        return []


def x__collect_objects__mutmut_13(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(None, exc_info=True)
        return []


def x__collect_objects__mutmut_14(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=None)
        return []


def x__collect_objects__mutmut_15(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(exc_info=True)
        return []


def x__collect_objects__mutmut_16(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", )
        return []


def x__collect_objects__mutmut_17(client: object, attribute: str) -> list[Any]:
    collection = getattr(client, attribute, None)
    if collection is None:
        return []
    result = collection() if callable(collection) else collection
    if result is None:
        return []
    if isinstance(result, list):
        return result
    try:
        return list(result)
    except TypeError as e:
        logger.debug(f"TypeError: {e}")
        logger.warning(f"TypeError: {e}", exc_info=False)
        return []

x__collect_objects__mutmut_mutants : ClassVar[MutantDict] = {
'x__collect_objects__mutmut_1': x__collect_objects__mutmut_1, 
    'x__collect_objects__mutmut_2': x__collect_objects__mutmut_2, 
    'x__collect_objects__mutmut_3': x__collect_objects__mutmut_3, 
    'x__collect_objects__mutmut_4': x__collect_objects__mutmut_4, 
    'x__collect_objects__mutmut_5': x__collect_objects__mutmut_5, 
    'x__collect_objects__mutmut_6': x__collect_objects__mutmut_6, 
    'x__collect_objects__mutmut_7': x__collect_objects__mutmut_7, 
    'x__collect_objects__mutmut_8': x__collect_objects__mutmut_8, 
    'x__collect_objects__mutmut_9': x__collect_objects__mutmut_9, 
    'x__collect_objects__mutmut_10': x__collect_objects__mutmut_10, 
    'x__collect_objects__mutmut_11': x__collect_objects__mutmut_11, 
    'x__collect_objects__mutmut_12': x__collect_objects__mutmut_12, 
    'x__collect_objects__mutmut_13': x__collect_objects__mutmut_13, 
    'x__collect_objects__mutmut_14': x__collect_objects__mutmut_14, 
    'x__collect_objects__mutmut_15': x__collect_objects__mutmut_15, 
    'x__collect_objects__mutmut_16': x__collect_objects__mutmut_16, 
    'x__collect_objects__mutmut_17': x__collect_objects__mutmut_17
}

def _collect_objects(*args, **kwargs):
    result = _mutmut_trampoline(x__collect_objects__mutmut_orig, x__collect_objects__mutmut_mutants, args, kwargs)
    return result 

_collect_objects.__signature__ = _mutmut_signature(x__collect_objects__mutmut_orig)
x__collect_objects__mutmut_orig.__name__ = 'x__collect_objects'


def x__collect_first_non_empty__mutmut_orig(client: object, *attributes: str) -> list[Any]:
    for attribute in attributes:
        items = _collect_objects(client, attribute)
        if items:
            return items
    return []


def x__collect_first_non_empty__mutmut_1(client: object, *attributes: str) -> list[Any]:
    for attribute in attributes:
        items = None
        if items:
            return items
    return []


def x__collect_first_non_empty__mutmut_2(client: object, *attributes: str) -> list[Any]:
    for attribute in attributes:
        items = _collect_objects(None, attribute)
        if items:
            return items
    return []


def x__collect_first_non_empty__mutmut_3(client: object, *attributes: str) -> list[Any]:
    for attribute in attributes:
        items = _collect_objects(client, None)
        if items:
            return items
    return []


def x__collect_first_non_empty__mutmut_4(client: object, *attributes: str) -> list[Any]:
    for attribute in attributes:
        items = _collect_objects(attribute)
        if items:
            return items
    return []


def x__collect_first_non_empty__mutmut_5(client: object, *attributes: str) -> list[Any]:
    for attribute in attributes:
        items = _collect_objects(client, )
        if items:
            return items
    return []

x__collect_first_non_empty__mutmut_mutants : ClassVar[MutantDict] = {
'x__collect_first_non_empty__mutmut_1': x__collect_first_non_empty__mutmut_1, 
    'x__collect_first_non_empty__mutmut_2': x__collect_first_non_empty__mutmut_2, 
    'x__collect_first_non_empty__mutmut_3': x__collect_first_non_empty__mutmut_3, 
    'x__collect_first_non_empty__mutmut_4': x__collect_first_non_empty__mutmut_4, 
    'x__collect_first_non_empty__mutmut_5': x__collect_first_non_empty__mutmut_5
}

def _collect_first_non_empty(*args, **kwargs):
    result = _mutmut_trampoline(x__collect_first_non_empty__mutmut_orig, x__collect_first_non_empty__mutmut_mutants, args, kwargs)
    return result 

_collect_first_non_empty.__signature__ = _mutmut_signature(x__collect_first_non_empty__mutmut_orig)
x__collect_first_non_empty__mutmut_orig.__name__ = 'x__collect_first_non_empty'


def x__object_to_mapping__mutmut_orig(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_1(obj: Any) -> dict[str, Any]:
    if obj is not None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_2(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(None)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_3(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = None
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_4(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(None, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_5(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, None, None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_6(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr("to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_7(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_8(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", )
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_9(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "XXto_dictXX", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_10(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "TO_DICT", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_11(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(None):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_12(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = None
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_13(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(None)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_14(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(None, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_15(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, None):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_16(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr("__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_17(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, ):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_18(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "XX__dict__XX"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_19(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__DICT__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_20(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(None).items() if not k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_21(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if k.startswith("_")}
    return {}


def x__object_to_mapping__mutmut_22(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith(None)}
    return {}


def x__object_to_mapping__mutmut_23(obj: Any) -> dict[str, Any]:
    if obj is None:
        return {}
    if isinstance(obj, Mapping):
        return dict(obj)
    to_dict = getattr(obj, "to_dict", None)
    if callable(to_dict):
        data = to_dict()
        if isinstance(data, Mapping):
            return dict(data)
    if hasattr(obj, "__dict__"):
        return {k: v for k, v in vars(obj).items() if not k.startswith("XX_XX")}
    return {}

x__object_to_mapping__mutmut_mutants : ClassVar[MutantDict] = {
'x__object_to_mapping__mutmut_1': x__object_to_mapping__mutmut_1, 
    'x__object_to_mapping__mutmut_2': x__object_to_mapping__mutmut_2, 
    'x__object_to_mapping__mutmut_3': x__object_to_mapping__mutmut_3, 
    'x__object_to_mapping__mutmut_4': x__object_to_mapping__mutmut_4, 
    'x__object_to_mapping__mutmut_5': x__object_to_mapping__mutmut_5, 
    'x__object_to_mapping__mutmut_6': x__object_to_mapping__mutmut_6, 
    'x__object_to_mapping__mutmut_7': x__object_to_mapping__mutmut_7, 
    'x__object_to_mapping__mutmut_8': x__object_to_mapping__mutmut_8, 
    'x__object_to_mapping__mutmut_9': x__object_to_mapping__mutmut_9, 
    'x__object_to_mapping__mutmut_10': x__object_to_mapping__mutmut_10, 
    'x__object_to_mapping__mutmut_11': x__object_to_mapping__mutmut_11, 
    'x__object_to_mapping__mutmut_12': x__object_to_mapping__mutmut_12, 
    'x__object_to_mapping__mutmut_13': x__object_to_mapping__mutmut_13, 
    'x__object_to_mapping__mutmut_14': x__object_to_mapping__mutmut_14, 
    'x__object_to_mapping__mutmut_15': x__object_to_mapping__mutmut_15, 
    'x__object_to_mapping__mutmut_16': x__object_to_mapping__mutmut_16, 
    'x__object_to_mapping__mutmut_17': x__object_to_mapping__mutmut_17, 
    'x__object_to_mapping__mutmut_18': x__object_to_mapping__mutmut_18, 
    'x__object_to_mapping__mutmut_19': x__object_to_mapping__mutmut_19, 
    'x__object_to_mapping__mutmut_20': x__object_to_mapping__mutmut_20, 
    'x__object_to_mapping__mutmut_21': x__object_to_mapping__mutmut_21, 
    'x__object_to_mapping__mutmut_22': x__object_to_mapping__mutmut_22, 
    'x__object_to_mapping__mutmut_23': x__object_to_mapping__mutmut_23
}

def _object_to_mapping(*args, **kwargs):
    result = _mutmut_trampoline(x__object_to_mapping__mutmut_orig, x__object_to_mapping__mutmut_mutants, args, kwargs)
    return result 

_object_to_mapping.__signature__ = _mutmut_signature(x__object_to_mapping__mutmut_orig)
x__object_to_mapping__mutmut_orig.__name__ = 'x__object_to_mapping'


def x__normalize_routing_rule__mutmut_orig(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_1(rule: Any) -> dict[str, Any] | None:
    data = None
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_2(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(None)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_3(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = None
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_4(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") and data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_5(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") and data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_6(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get(None) or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_7(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("XXnameXX") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_8(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("NAME") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_9(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get(None) or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_10(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("XXtitleXX") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_11(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("TITLE") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_12(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get(None)
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_13(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("XXidXX")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_14(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("ID")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_15(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_16(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = None
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_17(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") and {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_18(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") and data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_19(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") and data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_20(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get(None) or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_21(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("XXconditionsXX") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_22(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("CONDITIONS") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_23(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get(None) or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_24(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("XXmatching_criteriaXX") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_25(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("MATCHING_CRITERIA") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_26(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get(None) or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_27(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("XXmatchXX") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_28(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("MATCH") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_29(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = None
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_30(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") and data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_31(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") and data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_32(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") and data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_33(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get(None) or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_34(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("XXdestinationXX") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_35(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("DESTINATION") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_36(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get(None) or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_37(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("XXtargetXX") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_38(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("TARGET") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_39(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get(None) or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_40(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("XXqueueXX") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_41(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("QUEUE") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_42(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get(None)
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_43(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("XXqueue_idXX")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_44(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("QUEUE_ID")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_45(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = None
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_46(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "XXnameXX": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_47(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "NAME": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_48(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "XXconditionsXX": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_49(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "CONDITIONS": conditions,
    }
    if destination is not None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_50(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is None:
        payload["destination"] = destination
    return payload


def x__normalize_routing_rule__mutmut_51(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["destination"] = None
    return payload


def x__normalize_routing_rule__mutmut_52(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["XXdestinationXX"] = destination
    return payload


def x__normalize_routing_rule__mutmut_53(rule: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(rule)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    conditions = data.get("conditions") or data.get("matching_criteria") or data.get("match") or {}
    destination = (
        data.get("destination") or data.get("target") or data.get("queue") or data.get("queue_id")
    )
    payload: dict[str, Any] = {
        "name": name,
        "conditions": conditions,
    }
    if destination is not None:
        payload["DESTINATION"] = destination
    return payload

x__normalize_routing_rule__mutmut_mutants : ClassVar[MutantDict] = {
'x__normalize_routing_rule__mutmut_1': x__normalize_routing_rule__mutmut_1, 
    'x__normalize_routing_rule__mutmut_2': x__normalize_routing_rule__mutmut_2, 
    'x__normalize_routing_rule__mutmut_3': x__normalize_routing_rule__mutmut_3, 
    'x__normalize_routing_rule__mutmut_4': x__normalize_routing_rule__mutmut_4, 
    'x__normalize_routing_rule__mutmut_5': x__normalize_routing_rule__mutmut_5, 
    'x__normalize_routing_rule__mutmut_6': x__normalize_routing_rule__mutmut_6, 
    'x__normalize_routing_rule__mutmut_7': x__normalize_routing_rule__mutmut_7, 
    'x__normalize_routing_rule__mutmut_8': x__normalize_routing_rule__mutmut_8, 
    'x__normalize_routing_rule__mutmut_9': x__normalize_routing_rule__mutmut_9, 
    'x__normalize_routing_rule__mutmut_10': x__normalize_routing_rule__mutmut_10, 
    'x__normalize_routing_rule__mutmut_11': x__normalize_routing_rule__mutmut_11, 
    'x__normalize_routing_rule__mutmut_12': x__normalize_routing_rule__mutmut_12, 
    'x__normalize_routing_rule__mutmut_13': x__normalize_routing_rule__mutmut_13, 
    'x__normalize_routing_rule__mutmut_14': x__normalize_routing_rule__mutmut_14, 
    'x__normalize_routing_rule__mutmut_15': x__normalize_routing_rule__mutmut_15, 
    'x__normalize_routing_rule__mutmut_16': x__normalize_routing_rule__mutmut_16, 
    'x__normalize_routing_rule__mutmut_17': x__normalize_routing_rule__mutmut_17, 
    'x__normalize_routing_rule__mutmut_18': x__normalize_routing_rule__mutmut_18, 
    'x__normalize_routing_rule__mutmut_19': x__normalize_routing_rule__mutmut_19, 
    'x__normalize_routing_rule__mutmut_20': x__normalize_routing_rule__mutmut_20, 
    'x__normalize_routing_rule__mutmut_21': x__normalize_routing_rule__mutmut_21, 
    'x__normalize_routing_rule__mutmut_22': x__normalize_routing_rule__mutmut_22, 
    'x__normalize_routing_rule__mutmut_23': x__normalize_routing_rule__mutmut_23, 
    'x__normalize_routing_rule__mutmut_24': x__normalize_routing_rule__mutmut_24, 
    'x__normalize_routing_rule__mutmut_25': x__normalize_routing_rule__mutmut_25, 
    'x__normalize_routing_rule__mutmut_26': x__normalize_routing_rule__mutmut_26, 
    'x__normalize_routing_rule__mutmut_27': x__normalize_routing_rule__mutmut_27, 
    'x__normalize_routing_rule__mutmut_28': x__normalize_routing_rule__mutmut_28, 
    'x__normalize_routing_rule__mutmut_29': x__normalize_routing_rule__mutmut_29, 
    'x__normalize_routing_rule__mutmut_30': x__normalize_routing_rule__mutmut_30, 
    'x__normalize_routing_rule__mutmut_31': x__normalize_routing_rule__mutmut_31, 
    'x__normalize_routing_rule__mutmut_32': x__normalize_routing_rule__mutmut_32, 
    'x__normalize_routing_rule__mutmut_33': x__normalize_routing_rule__mutmut_33, 
    'x__normalize_routing_rule__mutmut_34': x__normalize_routing_rule__mutmut_34, 
    'x__normalize_routing_rule__mutmut_35': x__normalize_routing_rule__mutmut_35, 
    'x__normalize_routing_rule__mutmut_36': x__normalize_routing_rule__mutmut_36, 
    'x__normalize_routing_rule__mutmut_37': x__normalize_routing_rule__mutmut_37, 
    'x__normalize_routing_rule__mutmut_38': x__normalize_routing_rule__mutmut_38, 
    'x__normalize_routing_rule__mutmut_39': x__normalize_routing_rule__mutmut_39, 
    'x__normalize_routing_rule__mutmut_40': x__normalize_routing_rule__mutmut_40, 
    'x__normalize_routing_rule__mutmut_41': x__normalize_routing_rule__mutmut_41, 
    'x__normalize_routing_rule__mutmut_42': x__normalize_routing_rule__mutmut_42, 
    'x__normalize_routing_rule__mutmut_43': x__normalize_routing_rule__mutmut_43, 
    'x__normalize_routing_rule__mutmut_44': x__normalize_routing_rule__mutmut_44, 
    'x__normalize_routing_rule__mutmut_45': x__normalize_routing_rule__mutmut_45, 
    'x__normalize_routing_rule__mutmut_46': x__normalize_routing_rule__mutmut_46, 
    'x__normalize_routing_rule__mutmut_47': x__normalize_routing_rule__mutmut_47, 
    'x__normalize_routing_rule__mutmut_48': x__normalize_routing_rule__mutmut_48, 
    'x__normalize_routing_rule__mutmut_49': x__normalize_routing_rule__mutmut_49, 
    'x__normalize_routing_rule__mutmut_50': x__normalize_routing_rule__mutmut_50, 
    'x__normalize_routing_rule__mutmut_51': x__normalize_routing_rule__mutmut_51, 
    'x__normalize_routing_rule__mutmut_52': x__normalize_routing_rule__mutmut_52, 
    'x__normalize_routing_rule__mutmut_53': x__normalize_routing_rule__mutmut_53
}

def _normalize_routing_rule(*args, **kwargs):
    result = _mutmut_trampoline(x__normalize_routing_rule__mutmut_orig, x__normalize_routing_rule__mutmut_mutants, args, kwargs)
    return result 

_normalize_routing_rule.__signature__ = _mutmut_signature(x__normalize_routing_rule__mutmut_orig)
x__normalize_routing_rule__mutmut_orig.__name__ = 'x__normalize_routing_rule'


def x__normalize_talk_menu__mutmut_orig(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_1(menu: Any) -> dict[str, Any] | None:
    data = None
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_2(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(None)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_3(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = None
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_4(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") and data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_5(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") and data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_6(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get(None) or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_7(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("XXnameXX") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_8(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("NAME") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_9(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get(None) or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_10(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("XXtitleXX") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_11(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("TITLE") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_12(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get(None)
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_13(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("XXidXX")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_14(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("ID")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_15(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_16(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = None
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_17(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") and []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_18(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") and data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_19(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") and data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_20(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get(None) or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_21(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("XXpromptsXX") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_22(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("PROMPTS") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_23(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get(None) or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_24(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("XXmessagesXX") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_25(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("MESSAGES") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_26(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get(None) or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_27(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("XXgreetingsXX") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_28(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("GREETINGS") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_29(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = None
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_30(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = None
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_31(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = None
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_32(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") and []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_33(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") and data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_34(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") and data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_35(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get(None) or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_36(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("XXentriesXX") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_37(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("ENTRIES") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_38(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get(None) or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_39(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("XXoptionsXX") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_40(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("OPTIONS") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_41(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get(None) or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_42(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("XXroutesXX") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_43(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("ROUTES") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_44(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries and []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_45(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = None
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_46(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(None)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_47(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = None
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_48(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit") and entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_49(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress") and entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_50(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf") and entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_51(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get(None)
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_52(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("XXdtmfXX")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_53(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("DTMF")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_54(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get(None)
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_55(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("XXkeypressXX")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_56(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("KEYPRESS")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_57(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get(None)
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_58(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("XXdigitXX")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_59(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("DIGIT")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_60(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get(None)
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_61(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("XXkeyXX")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_62(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("KEY")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_63(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = None
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_64(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") and entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_65(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get(None) or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_66(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("XXactionXX") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_67(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("ACTION") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_68(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get(None)
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_69(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("XXtypeXX")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_70(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("TYPE")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_71(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = None
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_72(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target") and entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_73(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref") and entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_74(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get(None)
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_75(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("XXtarget_refXX")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_76(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("TARGET_REF")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_77(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get(None)
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_78(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("XXtargetXX")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_79(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("TARGET")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_80(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get(None)
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_81(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("XXdestinationXX")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_82(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("DESTINATION")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_83(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = None
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_84(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") and entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_85(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get(None) or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_86(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("XXschedule_refXX") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_87(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("SCHEDULE_REF") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_88(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get(None)
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_89(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("XXscheduleXX")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_90(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("SCHEDULE")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_91(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None or action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_92(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None or target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_93(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is not None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_94(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is not None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_95(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is not None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_96(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            break
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_97(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = None
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_98(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "XXdtmfXX": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_99(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "DTMF": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_100(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "XXactionXX": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_101(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "ACTION": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_102(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "XXtarget_refXX": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_103(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "TARGET_REF": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_104(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_105(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = None
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_106(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["XXschedule_refXX"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_107(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["SCHEDULE_REF"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_108(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append(None)
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_109(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_110(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "XXnameXX": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_111(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "NAME": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_112(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "XXpromptsXX": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_113(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "PROMPTS": prompts if isinstance(prompts, list) else list(prompts),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_114(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(None),
        "entries": entries,
    }


def x__normalize_talk_menu__mutmut_115(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "XXentriesXX": entries,
    }


def x__normalize_talk_menu__mutmut_116(menu: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(menu)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    prompts = data.get("prompts") or data.get("messages") or data.get("greetings") or []
    if isinstance(prompts, (str, bytes)):
        prompts = [prompts]
    entries = []
    raw_entries = data.get("entries") or data.get("options") or data.get("routes") or []
    for entry in raw_entries or []:
        entry_data = _object_to_mapping(entry)
        dtmf = (
            entry_data.get("dtmf")
            or entry_data.get("keypress")
            or entry_data.get("digit")
            or entry_data.get("key")
        )
        action = entry_data.get("action") or entry_data.get("type")
        target = (
            entry_data.get("target_ref")
            or entry_data.get("target")
            or entry_data.get("destination")
        )
        schedule = entry_data.get("schedule_ref") or entry_data.get("schedule")
        if dtmf is None and target is None and action is None:
            continue
        entry_payload: dict[str, Any] = {
            "dtmf": dtmf,
            "action": action,
            "target_ref": target,
        }
        if schedule is not None:
            entry_payload["schedule_ref"] = schedule
        entries.append({k: v for k, v in entry_payload.items() if v is not None})
    return {
        "name": name,
        "prompts": prompts if isinstance(prompts, list) else list(prompts),
        "ENTRIES": entries,
    }

x__normalize_talk_menu__mutmut_mutants : ClassVar[MutantDict] = {
'x__normalize_talk_menu__mutmut_1': x__normalize_talk_menu__mutmut_1, 
    'x__normalize_talk_menu__mutmut_2': x__normalize_talk_menu__mutmut_2, 
    'x__normalize_talk_menu__mutmut_3': x__normalize_talk_menu__mutmut_3, 
    'x__normalize_talk_menu__mutmut_4': x__normalize_talk_menu__mutmut_4, 
    'x__normalize_talk_menu__mutmut_5': x__normalize_talk_menu__mutmut_5, 
    'x__normalize_talk_menu__mutmut_6': x__normalize_talk_menu__mutmut_6, 
    'x__normalize_talk_menu__mutmut_7': x__normalize_talk_menu__mutmut_7, 
    'x__normalize_talk_menu__mutmut_8': x__normalize_talk_menu__mutmut_8, 
    'x__normalize_talk_menu__mutmut_9': x__normalize_talk_menu__mutmut_9, 
    'x__normalize_talk_menu__mutmut_10': x__normalize_talk_menu__mutmut_10, 
    'x__normalize_talk_menu__mutmut_11': x__normalize_talk_menu__mutmut_11, 
    'x__normalize_talk_menu__mutmut_12': x__normalize_talk_menu__mutmut_12, 
    'x__normalize_talk_menu__mutmut_13': x__normalize_talk_menu__mutmut_13, 
    'x__normalize_talk_menu__mutmut_14': x__normalize_talk_menu__mutmut_14, 
    'x__normalize_talk_menu__mutmut_15': x__normalize_talk_menu__mutmut_15, 
    'x__normalize_talk_menu__mutmut_16': x__normalize_talk_menu__mutmut_16, 
    'x__normalize_talk_menu__mutmut_17': x__normalize_talk_menu__mutmut_17, 
    'x__normalize_talk_menu__mutmut_18': x__normalize_talk_menu__mutmut_18, 
    'x__normalize_talk_menu__mutmut_19': x__normalize_talk_menu__mutmut_19, 
    'x__normalize_talk_menu__mutmut_20': x__normalize_talk_menu__mutmut_20, 
    'x__normalize_talk_menu__mutmut_21': x__normalize_talk_menu__mutmut_21, 
    'x__normalize_talk_menu__mutmut_22': x__normalize_talk_menu__mutmut_22, 
    'x__normalize_talk_menu__mutmut_23': x__normalize_talk_menu__mutmut_23, 
    'x__normalize_talk_menu__mutmut_24': x__normalize_talk_menu__mutmut_24, 
    'x__normalize_talk_menu__mutmut_25': x__normalize_talk_menu__mutmut_25, 
    'x__normalize_talk_menu__mutmut_26': x__normalize_talk_menu__mutmut_26, 
    'x__normalize_talk_menu__mutmut_27': x__normalize_talk_menu__mutmut_27, 
    'x__normalize_talk_menu__mutmut_28': x__normalize_talk_menu__mutmut_28, 
    'x__normalize_talk_menu__mutmut_29': x__normalize_talk_menu__mutmut_29, 
    'x__normalize_talk_menu__mutmut_30': x__normalize_talk_menu__mutmut_30, 
    'x__normalize_talk_menu__mutmut_31': x__normalize_talk_menu__mutmut_31, 
    'x__normalize_talk_menu__mutmut_32': x__normalize_talk_menu__mutmut_32, 
    'x__normalize_talk_menu__mutmut_33': x__normalize_talk_menu__mutmut_33, 
    'x__normalize_talk_menu__mutmut_34': x__normalize_talk_menu__mutmut_34, 
    'x__normalize_talk_menu__mutmut_35': x__normalize_talk_menu__mutmut_35, 
    'x__normalize_talk_menu__mutmut_36': x__normalize_talk_menu__mutmut_36, 
    'x__normalize_talk_menu__mutmut_37': x__normalize_talk_menu__mutmut_37, 
    'x__normalize_talk_menu__mutmut_38': x__normalize_talk_menu__mutmut_38, 
    'x__normalize_talk_menu__mutmut_39': x__normalize_talk_menu__mutmut_39, 
    'x__normalize_talk_menu__mutmut_40': x__normalize_talk_menu__mutmut_40, 
    'x__normalize_talk_menu__mutmut_41': x__normalize_talk_menu__mutmut_41, 
    'x__normalize_talk_menu__mutmut_42': x__normalize_talk_menu__mutmut_42, 
    'x__normalize_talk_menu__mutmut_43': x__normalize_talk_menu__mutmut_43, 
    'x__normalize_talk_menu__mutmut_44': x__normalize_talk_menu__mutmut_44, 
    'x__normalize_talk_menu__mutmut_45': x__normalize_talk_menu__mutmut_45, 
    'x__normalize_talk_menu__mutmut_46': x__normalize_talk_menu__mutmut_46, 
    'x__normalize_talk_menu__mutmut_47': x__normalize_talk_menu__mutmut_47, 
    'x__normalize_talk_menu__mutmut_48': x__normalize_talk_menu__mutmut_48, 
    'x__normalize_talk_menu__mutmut_49': x__normalize_talk_menu__mutmut_49, 
    'x__normalize_talk_menu__mutmut_50': x__normalize_talk_menu__mutmut_50, 
    'x__normalize_talk_menu__mutmut_51': x__normalize_talk_menu__mutmut_51, 
    'x__normalize_talk_menu__mutmut_52': x__normalize_talk_menu__mutmut_52, 
    'x__normalize_talk_menu__mutmut_53': x__normalize_talk_menu__mutmut_53, 
    'x__normalize_talk_menu__mutmut_54': x__normalize_talk_menu__mutmut_54, 
    'x__normalize_talk_menu__mutmut_55': x__normalize_talk_menu__mutmut_55, 
    'x__normalize_talk_menu__mutmut_56': x__normalize_talk_menu__mutmut_56, 
    'x__normalize_talk_menu__mutmut_57': x__normalize_talk_menu__mutmut_57, 
    'x__normalize_talk_menu__mutmut_58': x__normalize_talk_menu__mutmut_58, 
    'x__normalize_talk_menu__mutmut_59': x__normalize_talk_menu__mutmut_59, 
    'x__normalize_talk_menu__mutmut_60': x__normalize_talk_menu__mutmut_60, 
    'x__normalize_talk_menu__mutmut_61': x__normalize_talk_menu__mutmut_61, 
    'x__normalize_talk_menu__mutmut_62': x__normalize_talk_menu__mutmut_62, 
    'x__normalize_talk_menu__mutmut_63': x__normalize_talk_menu__mutmut_63, 
    'x__normalize_talk_menu__mutmut_64': x__normalize_talk_menu__mutmut_64, 
    'x__normalize_talk_menu__mutmut_65': x__normalize_talk_menu__mutmut_65, 
    'x__normalize_talk_menu__mutmut_66': x__normalize_talk_menu__mutmut_66, 
    'x__normalize_talk_menu__mutmut_67': x__normalize_talk_menu__mutmut_67, 
    'x__normalize_talk_menu__mutmut_68': x__normalize_talk_menu__mutmut_68, 
    'x__normalize_talk_menu__mutmut_69': x__normalize_talk_menu__mutmut_69, 
    'x__normalize_talk_menu__mutmut_70': x__normalize_talk_menu__mutmut_70, 
    'x__normalize_talk_menu__mutmut_71': x__normalize_talk_menu__mutmut_71, 
    'x__normalize_talk_menu__mutmut_72': x__normalize_talk_menu__mutmut_72, 
    'x__normalize_talk_menu__mutmut_73': x__normalize_talk_menu__mutmut_73, 
    'x__normalize_talk_menu__mutmut_74': x__normalize_talk_menu__mutmut_74, 
    'x__normalize_talk_menu__mutmut_75': x__normalize_talk_menu__mutmut_75, 
    'x__normalize_talk_menu__mutmut_76': x__normalize_talk_menu__mutmut_76, 
    'x__normalize_talk_menu__mutmut_77': x__normalize_talk_menu__mutmut_77, 
    'x__normalize_talk_menu__mutmut_78': x__normalize_talk_menu__mutmut_78, 
    'x__normalize_talk_menu__mutmut_79': x__normalize_talk_menu__mutmut_79, 
    'x__normalize_talk_menu__mutmut_80': x__normalize_talk_menu__mutmut_80, 
    'x__normalize_talk_menu__mutmut_81': x__normalize_talk_menu__mutmut_81, 
    'x__normalize_talk_menu__mutmut_82': x__normalize_talk_menu__mutmut_82, 
    'x__normalize_talk_menu__mutmut_83': x__normalize_talk_menu__mutmut_83, 
    'x__normalize_talk_menu__mutmut_84': x__normalize_talk_menu__mutmut_84, 
    'x__normalize_talk_menu__mutmut_85': x__normalize_talk_menu__mutmut_85, 
    'x__normalize_talk_menu__mutmut_86': x__normalize_talk_menu__mutmut_86, 
    'x__normalize_talk_menu__mutmut_87': x__normalize_talk_menu__mutmut_87, 
    'x__normalize_talk_menu__mutmut_88': x__normalize_talk_menu__mutmut_88, 
    'x__normalize_talk_menu__mutmut_89': x__normalize_talk_menu__mutmut_89, 
    'x__normalize_talk_menu__mutmut_90': x__normalize_talk_menu__mutmut_90, 
    'x__normalize_talk_menu__mutmut_91': x__normalize_talk_menu__mutmut_91, 
    'x__normalize_talk_menu__mutmut_92': x__normalize_talk_menu__mutmut_92, 
    'x__normalize_talk_menu__mutmut_93': x__normalize_talk_menu__mutmut_93, 
    'x__normalize_talk_menu__mutmut_94': x__normalize_talk_menu__mutmut_94, 
    'x__normalize_talk_menu__mutmut_95': x__normalize_talk_menu__mutmut_95, 
    'x__normalize_talk_menu__mutmut_96': x__normalize_talk_menu__mutmut_96, 
    'x__normalize_talk_menu__mutmut_97': x__normalize_talk_menu__mutmut_97, 
    'x__normalize_talk_menu__mutmut_98': x__normalize_talk_menu__mutmut_98, 
    'x__normalize_talk_menu__mutmut_99': x__normalize_talk_menu__mutmut_99, 
    'x__normalize_talk_menu__mutmut_100': x__normalize_talk_menu__mutmut_100, 
    'x__normalize_talk_menu__mutmut_101': x__normalize_talk_menu__mutmut_101, 
    'x__normalize_talk_menu__mutmut_102': x__normalize_talk_menu__mutmut_102, 
    'x__normalize_talk_menu__mutmut_103': x__normalize_talk_menu__mutmut_103, 
    'x__normalize_talk_menu__mutmut_104': x__normalize_talk_menu__mutmut_104, 
    'x__normalize_talk_menu__mutmut_105': x__normalize_talk_menu__mutmut_105, 
    'x__normalize_talk_menu__mutmut_106': x__normalize_talk_menu__mutmut_106, 
    'x__normalize_talk_menu__mutmut_107': x__normalize_talk_menu__mutmut_107, 
    'x__normalize_talk_menu__mutmut_108': x__normalize_talk_menu__mutmut_108, 
    'x__normalize_talk_menu__mutmut_109': x__normalize_talk_menu__mutmut_109, 
    'x__normalize_talk_menu__mutmut_110': x__normalize_talk_menu__mutmut_110, 
    'x__normalize_talk_menu__mutmut_111': x__normalize_talk_menu__mutmut_111, 
    'x__normalize_talk_menu__mutmut_112': x__normalize_talk_menu__mutmut_112, 
    'x__normalize_talk_menu__mutmut_113': x__normalize_talk_menu__mutmut_113, 
    'x__normalize_talk_menu__mutmut_114': x__normalize_talk_menu__mutmut_114, 
    'x__normalize_talk_menu__mutmut_115': x__normalize_talk_menu__mutmut_115, 
    'x__normalize_talk_menu__mutmut_116': x__normalize_talk_menu__mutmut_116
}

def _normalize_talk_menu(*args, **kwargs):
    result = _mutmut_trampoline(x__normalize_talk_menu__mutmut_orig, x__normalize_talk_menu__mutmut_mutants, args, kwargs)
    return result 

_normalize_talk_menu.__signature__ = _mutmut_signature(x__normalize_talk_menu__mutmut_orig)
x__normalize_talk_menu__mutmut_orig.__name__ = 'x__normalize_talk_menu'


def x__normalize_talk_route__mutmut_orig(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_1(route: Any) -> dict[str, Any] | None:
    data = None
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_2(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(None)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_3(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = None
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_4(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") and data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_5(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") and data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_6(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get(None) or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_7(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("XXnameXX") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_8(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("NAME") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_9(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get(None) or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_10(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("XXtitleXX") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_11(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("TITLE") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_12(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get(None)
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_13(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("XXidXX")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_14(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("ID")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_15(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = None
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_16(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") and data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_17(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get(None) or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_18(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("XXactionXX") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_19(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("ACTION") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_20(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get(None)
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_21(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("XXtypeXX")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_22(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("TYPE")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_23(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = None
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_24(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") and data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_25(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") and data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_26(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get(None) or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_27(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("XXtarget_refXX") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_28(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("TARGET_REF") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_29(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get(None) or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_30(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("XXtargetXX") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_31(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("TARGET") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_32(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get(None)
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_33(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("XXdestinationXX")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_34(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("DESTINATION")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_35(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_36(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any(None):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_37(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = None
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_38(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_39(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = None
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_40(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["XXnameXX"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_41(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["NAME"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_42(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_43(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = None
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_44(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["XXactionXX"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_45(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["ACTION"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_46(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_47(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = None
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_48(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["XXtarget_refXX"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_49(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["TARGET_REF"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_50(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = None
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_51(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") and data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_52(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") and data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_53(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get(None) or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_54(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("XXschedule_refXX") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_55(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("SCHEDULE_REF") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_56(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get(None) or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_57(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("XXscheduleXX") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_58(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("SCHEDULE") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_59(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get(None)
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_60(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("XXschedule_idXX")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_61(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("SCHEDULE_ID")
    if schedule is not None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_62(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is None:
        payload["schedule_ref"] = schedule
    return payload


def x__normalize_talk_route__mutmut_63(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["schedule_ref"] = None
    return payload


def x__normalize_talk_route__mutmut_64(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["XXschedule_refXX"] = schedule
    return payload


def x__normalize_talk_route__mutmut_65(route: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(route)
    name = data.get("name") or data.get("title") or data.get("id")
    action = data.get("action") or data.get("type")
    target = data.get("target_ref") or data.get("target") or data.get("destination")
    if not any((name, action, target)):
        return None
    payload: dict[str, Any] = {}
    if name is not None:
        payload["name"] = name
    if action is not None:
        payload["action"] = action
    if target is not None:
        payload["target_ref"] = target
    schedule = data.get("schedule_ref") or data.get("schedule") or data.get("schedule_id")
    if schedule is not None:
        payload["SCHEDULE_REF"] = schedule
    return payload

x__normalize_talk_route__mutmut_mutants : ClassVar[MutantDict] = {
'x__normalize_talk_route__mutmut_1': x__normalize_talk_route__mutmut_1, 
    'x__normalize_talk_route__mutmut_2': x__normalize_talk_route__mutmut_2, 
    'x__normalize_talk_route__mutmut_3': x__normalize_talk_route__mutmut_3, 
    'x__normalize_talk_route__mutmut_4': x__normalize_talk_route__mutmut_4, 
    'x__normalize_talk_route__mutmut_5': x__normalize_talk_route__mutmut_5, 
    'x__normalize_talk_route__mutmut_6': x__normalize_talk_route__mutmut_6, 
    'x__normalize_talk_route__mutmut_7': x__normalize_talk_route__mutmut_7, 
    'x__normalize_talk_route__mutmut_8': x__normalize_talk_route__mutmut_8, 
    'x__normalize_talk_route__mutmut_9': x__normalize_talk_route__mutmut_9, 
    'x__normalize_talk_route__mutmut_10': x__normalize_talk_route__mutmut_10, 
    'x__normalize_talk_route__mutmut_11': x__normalize_talk_route__mutmut_11, 
    'x__normalize_talk_route__mutmut_12': x__normalize_talk_route__mutmut_12, 
    'x__normalize_talk_route__mutmut_13': x__normalize_talk_route__mutmut_13, 
    'x__normalize_talk_route__mutmut_14': x__normalize_talk_route__mutmut_14, 
    'x__normalize_talk_route__mutmut_15': x__normalize_talk_route__mutmut_15, 
    'x__normalize_talk_route__mutmut_16': x__normalize_talk_route__mutmut_16, 
    'x__normalize_talk_route__mutmut_17': x__normalize_talk_route__mutmut_17, 
    'x__normalize_talk_route__mutmut_18': x__normalize_talk_route__mutmut_18, 
    'x__normalize_talk_route__mutmut_19': x__normalize_talk_route__mutmut_19, 
    'x__normalize_talk_route__mutmut_20': x__normalize_talk_route__mutmut_20, 
    'x__normalize_talk_route__mutmut_21': x__normalize_talk_route__mutmut_21, 
    'x__normalize_talk_route__mutmut_22': x__normalize_talk_route__mutmut_22, 
    'x__normalize_talk_route__mutmut_23': x__normalize_talk_route__mutmut_23, 
    'x__normalize_talk_route__mutmut_24': x__normalize_talk_route__mutmut_24, 
    'x__normalize_talk_route__mutmut_25': x__normalize_talk_route__mutmut_25, 
    'x__normalize_talk_route__mutmut_26': x__normalize_talk_route__mutmut_26, 
    'x__normalize_talk_route__mutmut_27': x__normalize_talk_route__mutmut_27, 
    'x__normalize_talk_route__mutmut_28': x__normalize_talk_route__mutmut_28, 
    'x__normalize_talk_route__mutmut_29': x__normalize_talk_route__mutmut_29, 
    'x__normalize_talk_route__mutmut_30': x__normalize_talk_route__mutmut_30, 
    'x__normalize_talk_route__mutmut_31': x__normalize_talk_route__mutmut_31, 
    'x__normalize_talk_route__mutmut_32': x__normalize_talk_route__mutmut_32, 
    'x__normalize_talk_route__mutmut_33': x__normalize_talk_route__mutmut_33, 
    'x__normalize_talk_route__mutmut_34': x__normalize_talk_route__mutmut_34, 
    'x__normalize_talk_route__mutmut_35': x__normalize_talk_route__mutmut_35, 
    'x__normalize_talk_route__mutmut_36': x__normalize_talk_route__mutmut_36, 
    'x__normalize_talk_route__mutmut_37': x__normalize_talk_route__mutmut_37, 
    'x__normalize_talk_route__mutmut_38': x__normalize_talk_route__mutmut_38, 
    'x__normalize_talk_route__mutmut_39': x__normalize_talk_route__mutmut_39, 
    'x__normalize_talk_route__mutmut_40': x__normalize_talk_route__mutmut_40, 
    'x__normalize_talk_route__mutmut_41': x__normalize_talk_route__mutmut_41, 
    'x__normalize_talk_route__mutmut_42': x__normalize_talk_route__mutmut_42, 
    'x__normalize_talk_route__mutmut_43': x__normalize_talk_route__mutmut_43, 
    'x__normalize_talk_route__mutmut_44': x__normalize_talk_route__mutmut_44, 
    'x__normalize_talk_route__mutmut_45': x__normalize_talk_route__mutmut_45, 
    'x__normalize_talk_route__mutmut_46': x__normalize_talk_route__mutmut_46, 
    'x__normalize_talk_route__mutmut_47': x__normalize_talk_route__mutmut_47, 
    'x__normalize_talk_route__mutmut_48': x__normalize_talk_route__mutmut_48, 
    'x__normalize_talk_route__mutmut_49': x__normalize_talk_route__mutmut_49, 
    'x__normalize_talk_route__mutmut_50': x__normalize_talk_route__mutmut_50, 
    'x__normalize_talk_route__mutmut_51': x__normalize_talk_route__mutmut_51, 
    'x__normalize_talk_route__mutmut_52': x__normalize_talk_route__mutmut_52, 
    'x__normalize_talk_route__mutmut_53': x__normalize_talk_route__mutmut_53, 
    'x__normalize_talk_route__mutmut_54': x__normalize_talk_route__mutmut_54, 
    'x__normalize_talk_route__mutmut_55': x__normalize_talk_route__mutmut_55, 
    'x__normalize_talk_route__mutmut_56': x__normalize_talk_route__mutmut_56, 
    'x__normalize_talk_route__mutmut_57': x__normalize_talk_route__mutmut_57, 
    'x__normalize_talk_route__mutmut_58': x__normalize_talk_route__mutmut_58, 
    'x__normalize_talk_route__mutmut_59': x__normalize_talk_route__mutmut_59, 
    'x__normalize_talk_route__mutmut_60': x__normalize_talk_route__mutmut_60, 
    'x__normalize_talk_route__mutmut_61': x__normalize_talk_route__mutmut_61, 
    'x__normalize_talk_route__mutmut_62': x__normalize_talk_route__mutmut_62, 
    'x__normalize_talk_route__mutmut_63': x__normalize_talk_route__mutmut_63, 
    'x__normalize_talk_route__mutmut_64': x__normalize_talk_route__mutmut_64, 
    'x__normalize_talk_route__mutmut_65': x__normalize_talk_route__mutmut_65
}

def _normalize_talk_route(*args, **kwargs):
    result = _mutmut_trampoline(x__normalize_talk_route__mutmut_orig, x__normalize_talk_route__mutmut_mutants, args, kwargs)
    return result 

_normalize_talk_route.__signature__ = _mutmut_signature(x__normalize_talk_route__mutmut_orig)
x__normalize_talk_route__mutmut_orig.__name__ = 'x__normalize_talk_route'


def x__normalize_talk_ivr__mutmut_orig(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_1(ivr: Any) -> dict[str, Any] | None:
    data = None
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_2(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(None)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_3(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = None
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_4(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") and data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_5(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") and data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_6(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get(None) or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_7(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("XXnameXX") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_8(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("NAME") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_9(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get(None) or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_10(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("XXtitleXX") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_11(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("TITLE") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_12(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get(None)
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_13(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("XXidXX")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_14(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("ID")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_15(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_16(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = None
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_17(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = None
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_18(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") and []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_19(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") and data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_20(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get(None) or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_21(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("XXmenusXX") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_22(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("MENUS") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_23(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get(None) or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_24(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("XXivr_menusXX") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_25(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("IVR_MENUS") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_26(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus and []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_27(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = None
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_28(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(None)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_29(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(None)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_30(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = None
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_31(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = None
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_32(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") and []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_33(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") and data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_34(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get(None) or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_35(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("XXroutesXX") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_36(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("ROUTES") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_37(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get(None) or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_38(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("XXivr_routesXX") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_39(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("IVR_ROUTES") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_40(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes and []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_41(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = None
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_42(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(None)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_43(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(None)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_44(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = None
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_45(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "XXnameXX": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_46(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "NAME": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_47(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "XXactiveXX": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_48(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "ACTIVE": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_49(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get(None),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_50(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("XXactiveXX"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_51(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("ACTIVE"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_52(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "XXmenusXX": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_53(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "MENUS": menus,
    }
    if routes:
        payload["routes"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_54(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["routes"] = None
    return payload


def x__normalize_talk_ivr__mutmut_55(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["XXroutesXX"] = routes
    return payload


def x__normalize_talk_ivr__mutmut_56(ivr: Any) -> dict[str, Any] | None:
    data = _object_to_mapping(ivr)
    name = data.get("name") or data.get("title") or data.get("id")
    if not name:
        return None
    menus: list[dict[str, Any]] = []
    raw_menus = data.get("menus") or data.get("ivr_menus") or []
    for menu in raw_menus or []:
        normalized = _normalize_talk_menu(menu)
        if normalized:
            menus.append(normalized)
    routes: list[dict[str, Any]] = []
    raw_routes = data.get("routes") or data.get("ivr_routes") or []
    for route in raw_routes or []:
        normalized_route = _normalize_talk_route(route)
        if normalized_route:
            routes.append(normalized_route)
    payload: dict[str, Any] = {
        "name": name,
        "active": data.get("active"),
        "menus": menus,
    }
    if routes:
        payload["ROUTES"] = routes
    return payload

x__normalize_talk_ivr__mutmut_mutants : ClassVar[MutantDict] = {
'x__normalize_talk_ivr__mutmut_1': x__normalize_talk_ivr__mutmut_1, 
    'x__normalize_talk_ivr__mutmut_2': x__normalize_talk_ivr__mutmut_2, 
    'x__normalize_talk_ivr__mutmut_3': x__normalize_talk_ivr__mutmut_3, 
    'x__normalize_talk_ivr__mutmut_4': x__normalize_talk_ivr__mutmut_4, 
    'x__normalize_talk_ivr__mutmut_5': x__normalize_talk_ivr__mutmut_5, 
    'x__normalize_talk_ivr__mutmut_6': x__normalize_talk_ivr__mutmut_6, 
    'x__normalize_talk_ivr__mutmut_7': x__normalize_talk_ivr__mutmut_7, 
    'x__normalize_talk_ivr__mutmut_8': x__normalize_talk_ivr__mutmut_8, 
    'x__normalize_talk_ivr__mutmut_9': x__normalize_talk_ivr__mutmut_9, 
    'x__normalize_talk_ivr__mutmut_10': x__normalize_talk_ivr__mutmut_10, 
    'x__normalize_talk_ivr__mutmut_11': x__normalize_talk_ivr__mutmut_11, 
    'x__normalize_talk_ivr__mutmut_12': x__normalize_talk_ivr__mutmut_12, 
    'x__normalize_talk_ivr__mutmut_13': x__normalize_talk_ivr__mutmut_13, 
    'x__normalize_talk_ivr__mutmut_14': x__normalize_talk_ivr__mutmut_14, 
    'x__normalize_talk_ivr__mutmut_15': x__normalize_talk_ivr__mutmut_15, 
    'x__normalize_talk_ivr__mutmut_16': x__normalize_talk_ivr__mutmut_16, 
    'x__normalize_talk_ivr__mutmut_17': x__normalize_talk_ivr__mutmut_17, 
    'x__normalize_talk_ivr__mutmut_18': x__normalize_talk_ivr__mutmut_18, 
    'x__normalize_talk_ivr__mutmut_19': x__normalize_talk_ivr__mutmut_19, 
    'x__normalize_talk_ivr__mutmut_20': x__normalize_talk_ivr__mutmut_20, 
    'x__normalize_talk_ivr__mutmut_21': x__normalize_talk_ivr__mutmut_21, 
    'x__normalize_talk_ivr__mutmut_22': x__normalize_talk_ivr__mutmut_22, 
    'x__normalize_talk_ivr__mutmut_23': x__normalize_talk_ivr__mutmut_23, 
    'x__normalize_talk_ivr__mutmut_24': x__normalize_talk_ivr__mutmut_24, 
    'x__normalize_talk_ivr__mutmut_25': x__normalize_talk_ivr__mutmut_25, 
    'x__normalize_talk_ivr__mutmut_26': x__normalize_talk_ivr__mutmut_26, 
    'x__normalize_talk_ivr__mutmut_27': x__normalize_talk_ivr__mutmut_27, 
    'x__normalize_talk_ivr__mutmut_28': x__normalize_talk_ivr__mutmut_28, 
    'x__normalize_talk_ivr__mutmut_29': x__normalize_talk_ivr__mutmut_29, 
    'x__normalize_talk_ivr__mutmut_30': x__normalize_talk_ivr__mutmut_30, 
    'x__normalize_talk_ivr__mutmut_31': x__normalize_talk_ivr__mutmut_31, 
    'x__normalize_talk_ivr__mutmut_32': x__normalize_talk_ivr__mutmut_32, 
    'x__normalize_talk_ivr__mutmut_33': x__normalize_talk_ivr__mutmut_33, 
    'x__normalize_talk_ivr__mutmut_34': x__normalize_talk_ivr__mutmut_34, 
    'x__normalize_talk_ivr__mutmut_35': x__normalize_talk_ivr__mutmut_35, 
    'x__normalize_talk_ivr__mutmut_36': x__normalize_talk_ivr__mutmut_36, 
    'x__normalize_talk_ivr__mutmut_37': x__normalize_talk_ivr__mutmut_37, 
    'x__normalize_talk_ivr__mutmut_38': x__normalize_talk_ivr__mutmut_38, 
    'x__normalize_talk_ivr__mutmut_39': x__normalize_talk_ivr__mutmut_39, 
    'x__normalize_talk_ivr__mutmut_40': x__normalize_talk_ivr__mutmut_40, 
    'x__normalize_talk_ivr__mutmut_41': x__normalize_talk_ivr__mutmut_41, 
    'x__normalize_talk_ivr__mutmut_42': x__normalize_talk_ivr__mutmut_42, 
    'x__normalize_talk_ivr__mutmut_43': x__normalize_talk_ivr__mutmut_43, 
    'x__normalize_talk_ivr__mutmut_44': x__normalize_talk_ivr__mutmut_44, 
    'x__normalize_talk_ivr__mutmut_45': x__normalize_talk_ivr__mutmut_45, 
    'x__normalize_talk_ivr__mutmut_46': x__normalize_talk_ivr__mutmut_46, 
    'x__normalize_talk_ivr__mutmut_47': x__normalize_talk_ivr__mutmut_47, 
    'x__normalize_talk_ivr__mutmut_48': x__normalize_talk_ivr__mutmut_48, 
    'x__normalize_talk_ivr__mutmut_49': x__normalize_talk_ivr__mutmut_49, 
    'x__normalize_talk_ivr__mutmut_50': x__normalize_talk_ivr__mutmut_50, 
    'x__normalize_talk_ivr__mutmut_51': x__normalize_talk_ivr__mutmut_51, 
    'x__normalize_talk_ivr__mutmut_52': x__normalize_talk_ivr__mutmut_52, 
    'x__normalize_talk_ivr__mutmut_53': x__normalize_talk_ivr__mutmut_53, 
    'x__normalize_talk_ivr__mutmut_54': x__normalize_talk_ivr__mutmut_54, 
    'x__normalize_talk_ivr__mutmut_55': x__normalize_talk_ivr__mutmut_55, 
    'x__normalize_talk_ivr__mutmut_56': x__normalize_talk_ivr__mutmut_56
}

def _normalize_talk_ivr(*args, **kwargs):
    result = _mutmut_trampoline(x__normalize_talk_ivr__mutmut_orig, x__normalize_talk_ivr__mutmut_mutants, args, kwargs)
    return result 

_normalize_talk_ivr.__signature__ = _mutmut_signature(x__normalize_talk_ivr__mutmut_orig)
x__normalize_talk_ivr__mutmut_orig.__name__ = 'x__normalize_talk_ivr'


def x__export_zendesk_config__mutmut_orig(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_1(env: str) -> dict[str, Any]:
    client = None
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_2(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(None)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_3(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = None

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_4(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = None

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_5(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["XXtriggersXX"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_6(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["TRIGGERS"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_7(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(None, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_8(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, None)
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_9(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr("to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_10(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, )
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_11(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "XXto_dictXX")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_12(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "TO_DICT")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_13(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "XXnameXX": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_14(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "NAME": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_15(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None) and getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_16(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None) and getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_17(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(None, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_18(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, None, None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_19(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr("title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_20(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_21(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", )
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_22(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "XXtitleXX", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_23(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "TITLE", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_24(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(None, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_25(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, None, None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_26(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr("name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_27(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_28(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", )
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_29(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "XXnameXX", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_30(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "NAME", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_31(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(None, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_32(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, None, None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_33(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr("id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_34(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_35(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", ),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_36(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "XXidXX", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_37(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "ID", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_38(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "XXactiveXX": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_39(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "ACTIVE": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_40(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(None, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_41(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, None, None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_42(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr("active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_43(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_44(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", ),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_45(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "XXactiveXX", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_46(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "ACTIVE", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_47(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "XXcategoryXX": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_48(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "CATEGORY": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_49(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(None, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_50(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, None, None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_51(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr("category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_52(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_53(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", ),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_54(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "XXcategoryXX", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_55(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "CATEGORY", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_56(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "XXconditionsXX": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_57(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "CONDITIONS": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_58(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(None, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_59(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, None, None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_60(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr("conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_61(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_62(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", ),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_63(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "XXconditionsXX", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_64(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "CONDITIONS", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_65(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "XXactionsXX": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_66(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "ACTIONS": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_67(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(None, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_68(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, None, None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_69(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr("actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_70(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_71(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", ),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_72(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "XXactionsXX", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_73(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "ACTIONS", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_74(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "XXpositionXX": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_75(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "POSITION": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_76(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(None, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_77(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, None, None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_78(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr("position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_79(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_80(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", ),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_81(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "XXpositionXX", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_82(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "POSITION", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_83(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(None, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_84(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, None)
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_85(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects("triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_86(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, )
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_87(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "XXtriggersXX")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_88(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "TRIGGERS")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_89(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = None

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_90(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["XXfieldsXX"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_91(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["FIELDS"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_92(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "XXnameXX": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_93(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "NAME": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_94(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) and getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_95(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(None, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_96(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, None, None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_97(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr("title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_98(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_99(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", ) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_100(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "XXtitleXX", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_101(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "TITLE", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_102(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(None, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_103(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, None, None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_104(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr("id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_105(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_106(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", ),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_107(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "XXidXX", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_108(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "ID", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_109(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "XXtypeXX": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_110(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "TYPE": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_111(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(None, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_112(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, None, None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_113(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr("type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_114(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_115(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", ),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_116(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "XXtypeXX", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_117(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "TYPE", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_118(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "XXoptionsXX": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_119(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "OPTIONS": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_120(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(None, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_121(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, None, opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_122(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", None)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_123(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr("value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_124(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_125(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", )
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_126(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "XXvalueXX", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_127(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "VALUE", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_128(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) and []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_129(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(None, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_130(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, None, []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_131(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", None) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_132(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr("custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_133(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_134(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", ) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_135(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "XXcustom_field_optionsXX", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_136(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "CUSTOM_FIELD_OPTIONS", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_137(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "XXactiveXX": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_138(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "ACTIVE": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_139(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(None, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_140(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, None, None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_141(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr("active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_142(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_143(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", ),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_144(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "XXactiveXX", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_145(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "ACTIVE", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_146(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "XXdescriptionXX": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_147(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "DESCRIPTION": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_148(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(None, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_149(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, None, None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_150(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr("description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_151(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_152(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", ),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_153(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "XXdescriptionXX", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_154(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "DESCRIPTION", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_155(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(None, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_156(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, None)
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_157(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects("ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_158(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, )
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_159(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "XXticket_fieldsXX")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_160(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "TICKET_FIELDS")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_161(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = None

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_162(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["XXformsXX"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_163(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["FORMS"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_164(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "XXnameXX": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_165(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "NAME": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_166(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) and getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_167(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(None, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_168(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, None, None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_169(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr("name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_170(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_171(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", ) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_172(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "XXnameXX", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_173(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "NAME", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_174(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(None, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_175(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, None, None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_176(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr("id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_177(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_178(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", ),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_179(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "XXidXX", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_180(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "ID", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_181(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "XXfieldsXX": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_182(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "FIELDS": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_183(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(None),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_184(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) and []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_185(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(None, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_186(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, None, []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_187(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", None) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_188(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr("ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_189(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_190(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", ) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_191(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "XXticket_field_idsXX", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_192(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "TICKET_FIELD_IDS", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_193(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "XXactiveXX": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_194(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "ACTIVE": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_195(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(None, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_196(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, None, None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_197(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr("active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_198(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_199(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", ),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_200(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "XXactiveXX", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_201(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "ACTIVE", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_202(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(None, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_203(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, None)
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_204(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects("ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_205(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, )
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_206(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "XXticket_formsXX")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_207(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "TICKET_FORMS")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_208(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = None

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_209(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["XXgroupsXX"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_210(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["GROUPS"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_211(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "XXnameXX": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_212(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "NAME": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_213(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) and getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_214(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(None, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_215(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, None, None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_216(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr("name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_217(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_218(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", ) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_219(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "XXnameXX", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_220(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "NAME", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_221(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(None, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_222(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, None, None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_223(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr("id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_224(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_225(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", ),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_226(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "XXidXX", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_227(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "ID", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_228(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "XXdescriptionXX": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_229(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "DESCRIPTION": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_230(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(None, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_231(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, None, None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_232(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr("description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_233(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_234(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", ),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_235(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "XXdescriptionXX", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_236(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "DESCRIPTION", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_237(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "XXmembershipsXX": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_238(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "MEMBERSHIPS": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_239(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(None, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_240(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, None)
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_241(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects("groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_242(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, )
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_243(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "XXgroupsXX")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_244(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "GROUPS")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_245(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = None

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_246(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["XXmacrosXX"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_247(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["MACROS"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_248(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "XXnameXX": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_249(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "NAME": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_250(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None) and getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_251(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None) and getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_252(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(None, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_253(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, None, None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_254(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr("title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_255(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_256(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", )
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_257(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "XXtitleXX", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_258(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "TITLE", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_259(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(None, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_260(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, None, None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_261(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr("name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_262(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_263(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", )
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_264(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "XXnameXX", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_265(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "NAME", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_266(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(None, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_267(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, None, None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_268(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr("id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_269(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_270(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", ),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_271(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "XXidXX", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_272(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "ID", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_273(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "XXactionsXX": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_274(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "ACTIONS": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_275(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"XXfieldXX": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_276(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"FIELD": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_277(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(None, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_278(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, None, None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_279(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr("field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_280(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_281(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", ), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_282(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "XXfieldXX", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_283(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "FIELD", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_284(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "XXvalueXX": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_285(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "VALUE": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_286(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(None, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_287(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, None, None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_288(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr("value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_289(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_290(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", )}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_291(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "XXvalueXX", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_292(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "VALUE", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_293(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) and []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_294(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(None, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_295(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, None, []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_296(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", None) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_297(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr("actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_298(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_299(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", ) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_300(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "XXactionsXX", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_301(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "ACTIONS", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_302(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "XXactiveXX": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_303(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "ACTIVE": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_304(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(None, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_305(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, None, None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_306(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr("active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_307(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_308(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", ),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_309(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "XXactiveXX", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_310(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "ACTIVE", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_311(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(None, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_312(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, None)
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_313(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects("macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_314(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, )
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_315(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "XXmacrosXX")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_316(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "MACROS")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_317(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = None

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_318(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["XXviewsXX"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_319(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["VIEWS"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_320(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "XXnameXX": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_321(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "NAME": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_322(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None) and getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_323(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None) and getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_324(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(None, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_325(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, None, None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_326(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr("title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_327(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_328(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", )
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_329(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "XXtitleXX", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_330(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "TITLE", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_331(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(None, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_332(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, None, None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_333(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr("name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_334(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_335(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", )
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_336(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "XXnameXX", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_337(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "NAME", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_338(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(None, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_339(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, None, None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_340(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr("id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_341(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_342(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", ),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_343(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "XXidXX", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_344(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "ID", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_345(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "XXfiltersXX": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_346(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "FILTERS": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_347(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) and {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_348(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(None, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_349(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, None, None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_350(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr("conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_351(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_352(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", ) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_353(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "XXconditionsXX", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_354(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "CONDITIONS", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_355(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "XXcolumnsXX": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_356(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "COLUMNS": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_357(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(None),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_358(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) and []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_359(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(None, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_360(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, None, []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_361(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", None) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_362(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr("columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_363(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_364(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", ) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_365(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "XXcolumnsXX", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_366(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "COLUMNS", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_367(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "XXsortXX": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_368(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "SORT": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_369(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) and {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_370(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(None, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_371(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, None, None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_372(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr("execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_373(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_374(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", ) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_375(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "XXexecutionXX", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_376(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "EXECUTION", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_377(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(None, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_378(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, None)
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_379(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects("views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_380(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, )
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_381(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "XXviewsXX")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_382(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "VIEWS")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_383(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = None

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_384(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["XXwebhooksXX"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_385(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["WEBHOOKS"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_386(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "XXnameXX": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_387(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "NAME": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_388(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) and getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_389(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(None, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_390(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, None, None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_391(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr("name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_392(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_393(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", ) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_394(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "XXnameXX", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_395(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "NAME", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_396(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(None, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_397(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, None, None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_398(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr("id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_399(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_400(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", ),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_401(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "XXidXX", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_402(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "ID", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_403(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "XXendpointXX": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_404(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "ENDPOINT": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_405(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(None, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_406(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, None, None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_407(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr("endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_408(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_409(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", ),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_410(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "XXendpointXX", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_411(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "ENDPOINT", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_412(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "XXstatusXX": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_413(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "STATUS": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_414(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(None, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_415(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, None, None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_416(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr("status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_417(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_418(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", ),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_419(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "XXstatusXX", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_420(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "STATUS", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_421(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(None, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_422(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, None)
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_423(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects("webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_424(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, )
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_425(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "XXwebhooksXX")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_426(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "WEBHOOKS")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_427(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = None
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_428(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        None,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_429(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        None,
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_430(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        None,
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_431(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        None,
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_432(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_433(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_434(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_435(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_436(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "XXrouting_rulesXX",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_437(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "ROUTING_RULES",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_438(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "XXskills_based_routing_rulesXX",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_439(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "SKILLS_BASED_ROUTING_RULES",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_440(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "XXskills_based_routingXX",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_441(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "SKILLS_BASED_ROUTING",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_442(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = None

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_443(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["XXroutingXX"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_444(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["ROUTING"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_445(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(None) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_446(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_447(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = None
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_448(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        None,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_449(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        None,
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_450(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        None,
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_451(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        None,
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_452(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_453(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_454(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_455(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_456(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "XXtalk_ivrsXX",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_457(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "TALK_IVRS",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_458(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "XXivr_menusXX",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_459(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "IVR_MENUS",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_460(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "XXtalk_ivr_menusXX",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_461(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "TALK_IVR_MENUS",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_462(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = None

    return snapshot


def x__export_zendesk_config__mutmut_463(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["XXtalkXX"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_464(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["TALK"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_465(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(None) for ivr in talk_ivrs)
        if normalized is not None
    ]

    return snapshot


def x__export_zendesk_config__mutmut_466(env: str) -> dict[str, Any]:
    client = _get_zendesk_client(env)
    snapshot: dict[str, Any] = {}

    snapshot["triggers"] = [
        (
            trigger.to_dict()
            if hasattr(trigger, "to_dict")
            else {
                "name": getattr(trigger, "title", None)
                or getattr(trigger, "name", None)
                or getattr(trigger, "id", None),
                "active": getattr(trigger, "active", None),
                "category": getattr(trigger, "category", None),
                "conditions": getattr(trigger, "conditions", None),
                "actions": getattr(trigger, "actions", None),
                "position": getattr(trigger, "position", None),
            }
        )
        for trigger in _collect_objects(client, "triggers")
    ]

    snapshot["fields"] = [
        {
            "name": getattr(field, "title", None) or getattr(field, "id", None),
            "type": getattr(field, "type", None),
            "options": [
                getattr(opt, "value", opt)
                for opt in getattr(field, "custom_field_options", []) or []
            ],
            "active": getattr(field, "active", None),
            "description": getattr(field, "description", None),
        }
        for field in _collect_objects(client, "ticket_fields")
    ]

    snapshot["forms"] = [
        {
            "name": getattr(form, "name", None) or getattr(form, "id", None),
            "fields": list(getattr(form, "ticket_field_ids", []) or []),
            "active": getattr(form, "active", None),
        }
        for form in _collect_objects(client, "ticket_forms")
    ]

    snapshot["groups"] = [
        {
            "name": getattr(group, "name", None) or getattr(group, "id", None),
            "description": getattr(group, "description", None),
            "memberships": [],
        }
        for group in _collect_objects(client, "groups")
    ]

    snapshot["macros"] = [
        {
            "name": getattr(macro, "title", None)
            or getattr(macro, "name", None)
            or getattr(macro, "id", None),
            "actions": [
                {"field": getattr(action, "field", None), "value": getattr(action, "value", None)}
                for action in getattr(macro, "actions", []) or []
            ],
            "active": getattr(macro, "active", None),
        }
        for macro in _collect_objects(client, "macros")
    ]

    snapshot["views"] = [
        {
            "name": getattr(view, "title", None)
            or getattr(view, "name", None)
            or getattr(view, "id", None),
            "filters": getattr(view, "conditions", None) or {},
            "columns": list(getattr(view, "columns", []) or []),
            "sort": getattr(view, "execution", None) or {},
        }
        for view in _collect_objects(client, "views")
    ]

    snapshot["webhooks"] = [
        {
            "name": getattr(webhook, "name", None) or getattr(webhook, "id", None),
            "endpoint": getattr(webhook, "endpoint", None),
            "status": getattr(webhook, "status", None),
        }
        for webhook in _collect_objects(client, "webhooks")
    ]

    routing_rules = _collect_first_non_empty(
        client,
        "routing_rules",
        "skills_based_routing_rules",
        "skills_based_routing",
    )
    snapshot["routing"] = [
        normalized
        for normalized in (_normalize_routing_rule(rule) for rule in routing_rules)
        if normalized is not None
    ]

    talk_ivrs = _collect_first_non_empty(
        client,
        "talk_ivrs",
        "ivr_menus",
        "talk_ivr_menus",
    )
    snapshot["talk"] = [
        normalized
        for normalized in (_normalize_talk_ivr(ivr) for ivr in talk_ivrs)
        if normalized is None
    ]

    return snapshot

x__export_zendesk_config__mutmut_mutants : ClassVar[MutantDict] = {
'x__export_zendesk_config__mutmut_1': x__export_zendesk_config__mutmut_1, 
    'x__export_zendesk_config__mutmut_2': x__export_zendesk_config__mutmut_2, 
    'x__export_zendesk_config__mutmut_3': x__export_zendesk_config__mutmut_3, 
    'x__export_zendesk_config__mutmut_4': x__export_zendesk_config__mutmut_4, 
    'x__export_zendesk_config__mutmut_5': x__export_zendesk_config__mutmut_5, 
    'x__export_zendesk_config__mutmut_6': x__export_zendesk_config__mutmut_6, 
    'x__export_zendesk_config__mutmut_7': x__export_zendesk_config__mutmut_7, 
    'x__export_zendesk_config__mutmut_8': x__export_zendesk_config__mutmut_8, 
    'x__export_zendesk_config__mutmut_9': x__export_zendesk_config__mutmut_9, 
    'x__export_zendesk_config__mutmut_10': x__export_zendesk_config__mutmut_10, 
    'x__export_zendesk_config__mutmut_11': x__export_zendesk_config__mutmut_11, 
    'x__export_zendesk_config__mutmut_12': x__export_zendesk_config__mutmut_12, 
    'x__export_zendesk_config__mutmut_13': x__export_zendesk_config__mutmut_13, 
    'x__export_zendesk_config__mutmut_14': x__export_zendesk_config__mutmut_14, 
    'x__export_zendesk_config__mutmut_15': x__export_zendesk_config__mutmut_15, 
    'x__export_zendesk_config__mutmut_16': x__export_zendesk_config__mutmut_16, 
    'x__export_zendesk_config__mutmut_17': x__export_zendesk_config__mutmut_17, 
    'x__export_zendesk_config__mutmut_18': x__export_zendesk_config__mutmut_18, 
    'x__export_zendesk_config__mutmut_19': x__export_zendesk_config__mutmut_19, 
    'x__export_zendesk_config__mutmut_20': x__export_zendesk_config__mutmut_20, 
    'x__export_zendesk_config__mutmut_21': x__export_zendesk_config__mutmut_21, 
    'x__export_zendesk_config__mutmut_22': x__export_zendesk_config__mutmut_22, 
    'x__export_zendesk_config__mutmut_23': x__export_zendesk_config__mutmut_23, 
    'x__export_zendesk_config__mutmut_24': x__export_zendesk_config__mutmut_24, 
    'x__export_zendesk_config__mutmut_25': x__export_zendesk_config__mutmut_25, 
    'x__export_zendesk_config__mutmut_26': x__export_zendesk_config__mutmut_26, 
    'x__export_zendesk_config__mutmut_27': x__export_zendesk_config__mutmut_27, 
    'x__export_zendesk_config__mutmut_28': x__export_zendesk_config__mutmut_28, 
    'x__export_zendesk_config__mutmut_29': x__export_zendesk_config__mutmut_29, 
    'x__export_zendesk_config__mutmut_30': x__export_zendesk_config__mutmut_30, 
    'x__export_zendesk_config__mutmut_31': x__export_zendesk_config__mutmut_31, 
    'x__export_zendesk_config__mutmut_32': x__export_zendesk_config__mutmut_32, 
    'x__export_zendesk_config__mutmut_33': x__export_zendesk_config__mutmut_33, 
    'x__export_zendesk_config__mutmut_34': x__export_zendesk_config__mutmut_34, 
    'x__export_zendesk_config__mutmut_35': x__export_zendesk_config__mutmut_35, 
    'x__export_zendesk_config__mutmut_36': x__export_zendesk_config__mutmut_36, 
    'x__export_zendesk_config__mutmut_37': x__export_zendesk_config__mutmut_37, 
    'x__export_zendesk_config__mutmut_38': x__export_zendesk_config__mutmut_38, 
    'x__export_zendesk_config__mutmut_39': x__export_zendesk_config__mutmut_39, 
    'x__export_zendesk_config__mutmut_40': x__export_zendesk_config__mutmut_40, 
    'x__export_zendesk_config__mutmut_41': x__export_zendesk_config__mutmut_41, 
    'x__export_zendesk_config__mutmut_42': x__export_zendesk_config__mutmut_42, 
    'x__export_zendesk_config__mutmut_43': x__export_zendesk_config__mutmut_43, 
    'x__export_zendesk_config__mutmut_44': x__export_zendesk_config__mutmut_44, 
    'x__export_zendesk_config__mutmut_45': x__export_zendesk_config__mutmut_45, 
    'x__export_zendesk_config__mutmut_46': x__export_zendesk_config__mutmut_46, 
    'x__export_zendesk_config__mutmut_47': x__export_zendesk_config__mutmut_47, 
    'x__export_zendesk_config__mutmut_48': x__export_zendesk_config__mutmut_48, 
    'x__export_zendesk_config__mutmut_49': x__export_zendesk_config__mutmut_49, 
    'x__export_zendesk_config__mutmut_50': x__export_zendesk_config__mutmut_50, 
    'x__export_zendesk_config__mutmut_51': x__export_zendesk_config__mutmut_51, 
    'x__export_zendesk_config__mutmut_52': x__export_zendesk_config__mutmut_52, 
    'x__export_zendesk_config__mutmut_53': x__export_zendesk_config__mutmut_53, 
    'x__export_zendesk_config__mutmut_54': x__export_zendesk_config__mutmut_54, 
    'x__export_zendesk_config__mutmut_55': x__export_zendesk_config__mutmut_55, 
    'x__export_zendesk_config__mutmut_56': x__export_zendesk_config__mutmut_56, 
    'x__export_zendesk_config__mutmut_57': x__export_zendesk_config__mutmut_57, 
    'x__export_zendesk_config__mutmut_58': x__export_zendesk_config__mutmut_58, 
    'x__export_zendesk_config__mutmut_59': x__export_zendesk_config__mutmut_59, 
    'x__export_zendesk_config__mutmut_60': x__export_zendesk_config__mutmut_60, 
    'x__export_zendesk_config__mutmut_61': x__export_zendesk_config__mutmut_61, 
    'x__export_zendesk_config__mutmut_62': x__export_zendesk_config__mutmut_62, 
    'x__export_zendesk_config__mutmut_63': x__export_zendesk_config__mutmut_63, 
    'x__export_zendesk_config__mutmut_64': x__export_zendesk_config__mutmut_64, 
    'x__export_zendesk_config__mutmut_65': x__export_zendesk_config__mutmut_65, 
    'x__export_zendesk_config__mutmut_66': x__export_zendesk_config__mutmut_66, 
    'x__export_zendesk_config__mutmut_67': x__export_zendesk_config__mutmut_67, 
    'x__export_zendesk_config__mutmut_68': x__export_zendesk_config__mutmut_68, 
    'x__export_zendesk_config__mutmut_69': x__export_zendesk_config__mutmut_69, 
    'x__export_zendesk_config__mutmut_70': x__export_zendesk_config__mutmut_70, 
    'x__export_zendesk_config__mutmut_71': x__export_zendesk_config__mutmut_71, 
    'x__export_zendesk_config__mutmut_72': x__export_zendesk_config__mutmut_72, 
    'x__export_zendesk_config__mutmut_73': x__export_zendesk_config__mutmut_73, 
    'x__export_zendesk_config__mutmut_74': x__export_zendesk_config__mutmut_74, 
    'x__export_zendesk_config__mutmut_75': x__export_zendesk_config__mutmut_75, 
    'x__export_zendesk_config__mutmut_76': x__export_zendesk_config__mutmut_76, 
    'x__export_zendesk_config__mutmut_77': x__export_zendesk_config__mutmut_77, 
    'x__export_zendesk_config__mutmut_78': x__export_zendesk_config__mutmut_78, 
    'x__export_zendesk_config__mutmut_79': x__export_zendesk_config__mutmut_79, 
    'x__export_zendesk_config__mutmut_80': x__export_zendesk_config__mutmut_80, 
    'x__export_zendesk_config__mutmut_81': x__export_zendesk_config__mutmut_81, 
    'x__export_zendesk_config__mutmut_82': x__export_zendesk_config__mutmut_82, 
    'x__export_zendesk_config__mutmut_83': x__export_zendesk_config__mutmut_83, 
    'x__export_zendesk_config__mutmut_84': x__export_zendesk_config__mutmut_84, 
    'x__export_zendesk_config__mutmut_85': x__export_zendesk_config__mutmut_85, 
    'x__export_zendesk_config__mutmut_86': x__export_zendesk_config__mutmut_86, 
    'x__export_zendesk_config__mutmut_87': x__export_zendesk_config__mutmut_87, 
    'x__export_zendesk_config__mutmut_88': x__export_zendesk_config__mutmut_88, 
    'x__export_zendesk_config__mutmut_89': x__export_zendesk_config__mutmut_89, 
    'x__export_zendesk_config__mutmut_90': x__export_zendesk_config__mutmut_90, 
    'x__export_zendesk_config__mutmut_91': x__export_zendesk_config__mutmut_91, 
    'x__export_zendesk_config__mutmut_92': x__export_zendesk_config__mutmut_92, 
    'x__export_zendesk_config__mutmut_93': x__export_zendesk_config__mutmut_93, 
    'x__export_zendesk_config__mutmut_94': x__export_zendesk_config__mutmut_94, 
    'x__export_zendesk_config__mutmut_95': x__export_zendesk_config__mutmut_95, 
    'x__export_zendesk_config__mutmut_96': x__export_zendesk_config__mutmut_96, 
    'x__export_zendesk_config__mutmut_97': x__export_zendesk_config__mutmut_97, 
    'x__export_zendesk_config__mutmut_98': x__export_zendesk_config__mutmut_98, 
    'x__export_zendesk_config__mutmut_99': x__export_zendesk_config__mutmut_99, 
    'x__export_zendesk_config__mutmut_100': x__export_zendesk_config__mutmut_100, 
    'x__export_zendesk_config__mutmut_101': x__export_zendesk_config__mutmut_101, 
    'x__export_zendesk_config__mutmut_102': x__export_zendesk_config__mutmut_102, 
    'x__export_zendesk_config__mutmut_103': x__export_zendesk_config__mutmut_103, 
    'x__export_zendesk_config__mutmut_104': x__export_zendesk_config__mutmut_104, 
    'x__export_zendesk_config__mutmut_105': x__export_zendesk_config__mutmut_105, 
    'x__export_zendesk_config__mutmut_106': x__export_zendesk_config__mutmut_106, 
    'x__export_zendesk_config__mutmut_107': x__export_zendesk_config__mutmut_107, 
    'x__export_zendesk_config__mutmut_108': x__export_zendesk_config__mutmut_108, 
    'x__export_zendesk_config__mutmut_109': x__export_zendesk_config__mutmut_109, 
    'x__export_zendesk_config__mutmut_110': x__export_zendesk_config__mutmut_110, 
    'x__export_zendesk_config__mutmut_111': x__export_zendesk_config__mutmut_111, 
    'x__export_zendesk_config__mutmut_112': x__export_zendesk_config__mutmut_112, 
    'x__export_zendesk_config__mutmut_113': x__export_zendesk_config__mutmut_113, 
    'x__export_zendesk_config__mutmut_114': x__export_zendesk_config__mutmut_114, 
    'x__export_zendesk_config__mutmut_115': x__export_zendesk_config__mutmut_115, 
    'x__export_zendesk_config__mutmut_116': x__export_zendesk_config__mutmut_116, 
    'x__export_zendesk_config__mutmut_117': x__export_zendesk_config__mutmut_117, 
    'x__export_zendesk_config__mutmut_118': x__export_zendesk_config__mutmut_118, 
    'x__export_zendesk_config__mutmut_119': x__export_zendesk_config__mutmut_119, 
    'x__export_zendesk_config__mutmut_120': x__export_zendesk_config__mutmut_120, 
    'x__export_zendesk_config__mutmut_121': x__export_zendesk_config__mutmut_121, 
    'x__export_zendesk_config__mutmut_122': x__export_zendesk_config__mutmut_122, 
    'x__export_zendesk_config__mutmut_123': x__export_zendesk_config__mutmut_123, 
    'x__export_zendesk_config__mutmut_124': x__export_zendesk_config__mutmut_124, 
    'x__export_zendesk_config__mutmut_125': x__export_zendesk_config__mutmut_125, 
    'x__export_zendesk_config__mutmut_126': x__export_zendesk_config__mutmut_126, 
    'x__export_zendesk_config__mutmut_127': x__export_zendesk_config__mutmut_127, 
    'x__export_zendesk_config__mutmut_128': x__export_zendesk_config__mutmut_128, 
    'x__export_zendesk_config__mutmut_129': x__export_zendesk_config__mutmut_129, 
    'x__export_zendesk_config__mutmut_130': x__export_zendesk_config__mutmut_130, 
    'x__export_zendesk_config__mutmut_131': x__export_zendesk_config__mutmut_131, 
    'x__export_zendesk_config__mutmut_132': x__export_zendesk_config__mutmut_132, 
    'x__export_zendesk_config__mutmut_133': x__export_zendesk_config__mutmut_133, 
    'x__export_zendesk_config__mutmut_134': x__export_zendesk_config__mutmut_134, 
    'x__export_zendesk_config__mutmut_135': x__export_zendesk_config__mutmut_135, 
    'x__export_zendesk_config__mutmut_136': x__export_zendesk_config__mutmut_136, 
    'x__export_zendesk_config__mutmut_137': x__export_zendesk_config__mutmut_137, 
    'x__export_zendesk_config__mutmut_138': x__export_zendesk_config__mutmut_138, 
    'x__export_zendesk_config__mutmut_139': x__export_zendesk_config__mutmut_139, 
    'x__export_zendesk_config__mutmut_140': x__export_zendesk_config__mutmut_140, 
    'x__export_zendesk_config__mutmut_141': x__export_zendesk_config__mutmut_141, 
    'x__export_zendesk_config__mutmut_142': x__export_zendesk_config__mutmut_142, 
    'x__export_zendesk_config__mutmut_143': x__export_zendesk_config__mutmut_143, 
    'x__export_zendesk_config__mutmut_144': x__export_zendesk_config__mutmut_144, 
    'x__export_zendesk_config__mutmut_145': x__export_zendesk_config__mutmut_145, 
    'x__export_zendesk_config__mutmut_146': x__export_zendesk_config__mutmut_146, 
    'x__export_zendesk_config__mutmut_147': x__export_zendesk_config__mutmut_147, 
    'x__export_zendesk_config__mutmut_148': x__export_zendesk_config__mutmut_148, 
    'x__export_zendesk_config__mutmut_149': x__export_zendesk_config__mutmut_149, 
    'x__export_zendesk_config__mutmut_150': x__export_zendesk_config__mutmut_150, 
    'x__export_zendesk_config__mutmut_151': x__export_zendesk_config__mutmut_151, 
    'x__export_zendesk_config__mutmut_152': x__export_zendesk_config__mutmut_152, 
    'x__export_zendesk_config__mutmut_153': x__export_zendesk_config__mutmut_153, 
    'x__export_zendesk_config__mutmut_154': x__export_zendesk_config__mutmut_154, 
    'x__export_zendesk_config__mutmut_155': x__export_zendesk_config__mutmut_155, 
    'x__export_zendesk_config__mutmut_156': x__export_zendesk_config__mutmut_156, 
    'x__export_zendesk_config__mutmut_157': x__export_zendesk_config__mutmut_157, 
    'x__export_zendesk_config__mutmut_158': x__export_zendesk_config__mutmut_158, 
    'x__export_zendesk_config__mutmut_159': x__export_zendesk_config__mutmut_159, 
    'x__export_zendesk_config__mutmut_160': x__export_zendesk_config__mutmut_160, 
    'x__export_zendesk_config__mutmut_161': x__export_zendesk_config__mutmut_161, 
    'x__export_zendesk_config__mutmut_162': x__export_zendesk_config__mutmut_162, 
    'x__export_zendesk_config__mutmut_163': x__export_zendesk_config__mutmut_163, 
    'x__export_zendesk_config__mutmut_164': x__export_zendesk_config__mutmut_164, 
    'x__export_zendesk_config__mutmut_165': x__export_zendesk_config__mutmut_165, 
    'x__export_zendesk_config__mutmut_166': x__export_zendesk_config__mutmut_166, 
    'x__export_zendesk_config__mutmut_167': x__export_zendesk_config__mutmut_167, 
    'x__export_zendesk_config__mutmut_168': x__export_zendesk_config__mutmut_168, 
    'x__export_zendesk_config__mutmut_169': x__export_zendesk_config__mutmut_169, 
    'x__export_zendesk_config__mutmut_170': x__export_zendesk_config__mutmut_170, 
    'x__export_zendesk_config__mutmut_171': x__export_zendesk_config__mutmut_171, 
    'x__export_zendesk_config__mutmut_172': x__export_zendesk_config__mutmut_172, 
    'x__export_zendesk_config__mutmut_173': x__export_zendesk_config__mutmut_173, 
    'x__export_zendesk_config__mutmut_174': x__export_zendesk_config__mutmut_174, 
    'x__export_zendesk_config__mutmut_175': x__export_zendesk_config__mutmut_175, 
    'x__export_zendesk_config__mutmut_176': x__export_zendesk_config__mutmut_176, 
    'x__export_zendesk_config__mutmut_177': x__export_zendesk_config__mutmut_177, 
    'x__export_zendesk_config__mutmut_178': x__export_zendesk_config__mutmut_178, 
    'x__export_zendesk_config__mutmut_179': x__export_zendesk_config__mutmut_179, 
    'x__export_zendesk_config__mutmut_180': x__export_zendesk_config__mutmut_180, 
    'x__export_zendesk_config__mutmut_181': x__export_zendesk_config__mutmut_181, 
    'x__export_zendesk_config__mutmut_182': x__export_zendesk_config__mutmut_182, 
    'x__export_zendesk_config__mutmut_183': x__export_zendesk_config__mutmut_183, 
    'x__export_zendesk_config__mutmut_184': x__export_zendesk_config__mutmut_184, 
    'x__export_zendesk_config__mutmut_185': x__export_zendesk_config__mutmut_185, 
    'x__export_zendesk_config__mutmut_186': x__export_zendesk_config__mutmut_186, 
    'x__export_zendesk_config__mutmut_187': x__export_zendesk_config__mutmut_187, 
    'x__export_zendesk_config__mutmut_188': x__export_zendesk_config__mutmut_188, 
    'x__export_zendesk_config__mutmut_189': x__export_zendesk_config__mutmut_189, 
    'x__export_zendesk_config__mutmut_190': x__export_zendesk_config__mutmut_190, 
    'x__export_zendesk_config__mutmut_191': x__export_zendesk_config__mutmut_191, 
    'x__export_zendesk_config__mutmut_192': x__export_zendesk_config__mutmut_192, 
    'x__export_zendesk_config__mutmut_193': x__export_zendesk_config__mutmut_193, 
    'x__export_zendesk_config__mutmut_194': x__export_zendesk_config__mutmut_194, 
    'x__export_zendesk_config__mutmut_195': x__export_zendesk_config__mutmut_195, 
    'x__export_zendesk_config__mutmut_196': x__export_zendesk_config__mutmut_196, 
    'x__export_zendesk_config__mutmut_197': x__export_zendesk_config__mutmut_197, 
    'x__export_zendesk_config__mutmut_198': x__export_zendesk_config__mutmut_198, 
    'x__export_zendesk_config__mutmut_199': x__export_zendesk_config__mutmut_199, 
    'x__export_zendesk_config__mutmut_200': x__export_zendesk_config__mutmut_200, 
    'x__export_zendesk_config__mutmut_201': x__export_zendesk_config__mutmut_201, 
    'x__export_zendesk_config__mutmut_202': x__export_zendesk_config__mutmut_202, 
    'x__export_zendesk_config__mutmut_203': x__export_zendesk_config__mutmut_203, 
    'x__export_zendesk_config__mutmut_204': x__export_zendesk_config__mutmut_204, 
    'x__export_zendesk_config__mutmut_205': x__export_zendesk_config__mutmut_205, 
    'x__export_zendesk_config__mutmut_206': x__export_zendesk_config__mutmut_206, 
    'x__export_zendesk_config__mutmut_207': x__export_zendesk_config__mutmut_207, 
    'x__export_zendesk_config__mutmut_208': x__export_zendesk_config__mutmut_208, 
    'x__export_zendesk_config__mutmut_209': x__export_zendesk_config__mutmut_209, 
    'x__export_zendesk_config__mutmut_210': x__export_zendesk_config__mutmut_210, 
    'x__export_zendesk_config__mutmut_211': x__export_zendesk_config__mutmut_211, 
    'x__export_zendesk_config__mutmut_212': x__export_zendesk_config__mutmut_212, 
    'x__export_zendesk_config__mutmut_213': x__export_zendesk_config__mutmut_213, 
    'x__export_zendesk_config__mutmut_214': x__export_zendesk_config__mutmut_214, 
    'x__export_zendesk_config__mutmut_215': x__export_zendesk_config__mutmut_215, 
    'x__export_zendesk_config__mutmut_216': x__export_zendesk_config__mutmut_216, 
    'x__export_zendesk_config__mutmut_217': x__export_zendesk_config__mutmut_217, 
    'x__export_zendesk_config__mutmut_218': x__export_zendesk_config__mutmut_218, 
    'x__export_zendesk_config__mutmut_219': x__export_zendesk_config__mutmut_219, 
    'x__export_zendesk_config__mutmut_220': x__export_zendesk_config__mutmut_220, 
    'x__export_zendesk_config__mutmut_221': x__export_zendesk_config__mutmut_221, 
    'x__export_zendesk_config__mutmut_222': x__export_zendesk_config__mutmut_222, 
    'x__export_zendesk_config__mutmut_223': x__export_zendesk_config__mutmut_223, 
    'x__export_zendesk_config__mutmut_224': x__export_zendesk_config__mutmut_224, 
    'x__export_zendesk_config__mutmut_225': x__export_zendesk_config__mutmut_225, 
    'x__export_zendesk_config__mutmut_226': x__export_zendesk_config__mutmut_226, 
    'x__export_zendesk_config__mutmut_227': x__export_zendesk_config__mutmut_227, 
    'x__export_zendesk_config__mutmut_228': x__export_zendesk_config__mutmut_228, 
    'x__export_zendesk_config__mutmut_229': x__export_zendesk_config__mutmut_229, 
    'x__export_zendesk_config__mutmut_230': x__export_zendesk_config__mutmut_230, 
    'x__export_zendesk_config__mutmut_231': x__export_zendesk_config__mutmut_231, 
    'x__export_zendesk_config__mutmut_232': x__export_zendesk_config__mutmut_232, 
    'x__export_zendesk_config__mutmut_233': x__export_zendesk_config__mutmut_233, 
    'x__export_zendesk_config__mutmut_234': x__export_zendesk_config__mutmut_234, 
    'x__export_zendesk_config__mutmut_235': x__export_zendesk_config__mutmut_235, 
    'x__export_zendesk_config__mutmut_236': x__export_zendesk_config__mutmut_236, 
    'x__export_zendesk_config__mutmut_237': x__export_zendesk_config__mutmut_237, 
    'x__export_zendesk_config__mutmut_238': x__export_zendesk_config__mutmut_238, 
    'x__export_zendesk_config__mutmut_239': x__export_zendesk_config__mutmut_239, 
    'x__export_zendesk_config__mutmut_240': x__export_zendesk_config__mutmut_240, 
    'x__export_zendesk_config__mutmut_241': x__export_zendesk_config__mutmut_241, 
    'x__export_zendesk_config__mutmut_242': x__export_zendesk_config__mutmut_242, 
    'x__export_zendesk_config__mutmut_243': x__export_zendesk_config__mutmut_243, 
    'x__export_zendesk_config__mutmut_244': x__export_zendesk_config__mutmut_244, 
    'x__export_zendesk_config__mutmut_245': x__export_zendesk_config__mutmut_245, 
    'x__export_zendesk_config__mutmut_246': x__export_zendesk_config__mutmut_246, 
    'x__export_zendesk_config__mutmut_247': x__export_zendesk_config__mutmut_247, 
    'x__export_zendesk_config__mutmut_248': x__export_zendesk_config__mutmut_248, 
    'x__export_zendesk_config__mutmut_249': x__export_zendesk_config__mutmut_249, 
    'x__export_zendesk_config__mutmut_250': x__export_zendesk_config__mutmut_250, 
    'x__export_zendesk_config__mutmut_251': x__export_zendesk_config__mutmut_251, 
    'x__export_zendesk_config__mutmut_252': x__export_zendesk_config__mutmut_252, 
    'x__export_zendesk_config__mutmut_253': x__export_zendesk_config__mutmut_253, 
    'x__export_zendesk_config__mutmut_254': x__export_zendesk_config__mutmut_254, 
    'x__export_zendesk_config__mutmut_255': x__export_zendesk_config__mutmut_255, 
    'x__export_zendesk_config__mutmut_256': x__export_zendesk_config__mutmut_256, 
    'x__export_zendesk_config__mutmut_257': x__export_zendesk_config__mutmut_257, 
    'x__export_zendesk_config__mutmut_258': x__export_zendesk_config__mutmut_258, 
    'x__export_zendesk_config__mutmut_259': x__export_zendesk_config__mutmut_259, 
    'x__export_zendesk_config__mutmut_260': x__export_zendesk_config__mutmut_260, 
    'x__export_zendesk_config__mutmut_261': x__export_zendesk_config__mutmut_261, 
    'x__export_zendesk_config__mutmut_262': x__export_zendesk_config__mutmut_262, 
    'x__export_zendesk_config__mutmut_263': x__export_zendesk_config__mutmut_263, 
    'x__export_zendesk_config__mutmut_264': x__export_zendesk_config__mutmut_264, 
    'x__export_zendesk_config__mutmut_265': x__export_zendesk_config__mutmut_265, 
    'x__export_zendesk_config__mutmut_266': x__export_zendesk_config__mutmut_266, 
    'x__export_zendesk_config__mutmut_267': x__export_zendesk_config__mutmut_267, 
    'x__export_zendesk_config__mutmut_268': x__export_zendesk_config__mutmut_268, 
    'x__export_zendesk_config__mutmut_269': x__export_zendesk_config__mutmut_269, 
    'x__export_zendesk_config__mutmut_270': x__export_zendesk_config__mutmut_270, 
    'x__export_zendesk_config__mutmut_271': x__export_zendesk_config__mutmut_271, 
    'x__export_zendesk_config__mutmut_272': x__export_zendesk_config__mutmut_272, 
    'x__export_zendesk_config__mutmut_273': x__export_zendesk_config__mutmut_273, 
    'x__export_zendesk_config__mutmut_274': x__export_zendesk_config__mutmut_274, 
    'x__export_zendesk_config__mutmut_275': x__export_zendesk_config__mutmut_275, 
    'x__export_zendesk_config__mutmut_276': x__export_zendesk_config__mutmut_276, 
    'x__export_zendesk_config__mutmut_277': x__export_zendesk_config__mutmut_277, 
    'x__export_zendesk_config__mutmut_278': x__export_zendesk_config__mutmut_278, 
    'x__export_zendesk_config__mutmut_279': x__export_zendesk_config__mutmut_279, 
    'x__export_zendesk_config__mutmut_280': x__export_zendesk_config__mutmut_280, 
    'x__export_zendesk_config__mutmut_281': x__export_zendesk_config__mutmut_281, 
    'x__export_zendesk_config__mutmut_282': x__export_zendesk_config__mutmut_282, 
    'x__export_zendesk_config__mutmut_283': x__export_zendesk_config__mutmut_283, 
    'x__export_zendesk_config__mutmut_284': x__export_zendesk_config__mutmut_284, 
    'x__export_zendesk_config__mutmut_285': x__export_zendesk_config__mutmut_285, 
    'x__export_zendesk_config__mutmut_286': x__export_zendesk_config__mutmut_286, 
    'x__export_zendesk_config__mutmut_287': x__export_zendesk_config__mutmut_287, 
    'x__export_zendesk_config__mutmut_288': x__export_zendesk_config__mutmut_288, 
    'x__export_zendesk_config__mutmut_289': x__export_zendesk_config__mutmut_289, 
    'x__export_zendesk_config__mutmut_290': x__export_zendesk_config__mutmut_290, 
    'x__export_zendesk_config__mutmut_291': x__export_zendesk_config__mutmut_291, 
    'x__export_zendesk_config__mutmut_292': x__export_zendesk_config__mutmut_292, 
    'x__export_zendesk_config__mutmut_293': x__export_zendesk_config__mutmut_293, 
    'x__export_zendesk_config__mutmut_294': x__export_zendesk_config__mutmut_294, 
    'x__export_zendesk_config__mutmut_295': x__export_zendesk_config__mutmut_295, 
    'x__export_zendesk_config__mutmut_296': x__export_zendesk_config__mutmut_296, 
    'x__export_zendesk_config__mutmut_297': x__export_zendesk_config__mutmut_297, 
    'x__export_zendesk_config__mutmut_298': x__export_zendesk_config__mutmut_298, 
    'x__export_zendesk_config__mutmut_299': x__export_zendesk_config__mutmut_299, 
    'x__export_zendesk_config__mutmut_300': x__export_zendesk_config__mutmut_300, 
    'x__export_zendesk_config__mutmut_301': x__export_zendesk_config__mutmut_301, 
    'x__export_zendesk_config__mutmut_302': x__export_zendesk_config__mutmut_302, 
    'x__export_zendesk_config__mutmut_303': x__export_zendesk_config__mutmut_303, 
    'x__export_zendesk_config__mutmut_304': x__export_zendesk_config__mutmut_304, 
    'x__export_zendesk_config__mutmut_305': x__export_zendesk_config__mutmut_305, 
    'x__export_zendesk_config__mutmut_306': x__export_zendesk_config__mutmut_306, 
    'x__export_zendesk_config__mutmut_307': x__export_zendesk_config__mutmut_307, 
    'x__export_zendesk_config__mutmut_308': x__export_zendesk_config__mutmut_308, 
    'x__export_zendesk_config__mutmut_309': x__export_zendesk_config__mutmut_309, 
    'x__export_zendesk_config__mutmut_310': x__export_zendesk_config__mutmut_310, 
    'x__export_zendesk_config__mutmut_311': x__export_zendesk_config__mutmut_311, 
    'x__export_zendesk_config__mutmut_312': x__export_zendesk_config__mutmut_312, 
    'x__export_zendesk_config__mutmut_313': x__export_zendesk_config__mutmut_313, 
    'x__export_zendesk_config__mutmut_314': x__export_zendesk_config__mutmut_314, 
    'x__export_zendesk_config__mutmut_315': x__export_zendesk_config__mutmut_315, 
    'x__export_zendesk_config__mutmut_316': x__export_zendesk_config__mutmut_316, 
    'x__export_zendesk_config__mutmut_317': x__export_zendesk_config__mutmut_317, 
    'x__export_zendesk_config__mutmut_318': x__export_zendesk_config__mutmut_318, 
    'x__export_zendesk_config__mutmut_319': x__export_zendesk_config__mutmut_319, 
    'x__export_zendesk_config__mutmut_320': x__export_zendesk_config__mutmut_320, 
    'x__export_zendesk_config__mutmut_321': x__export_zendesk_config__mutmut_321, 
    'x__export_zendesk_config__mutmut_322': x__export_zendesk_config__mutmut_322, 
    'x__export_zendesk_config__mutmut_323': x__export_zendesk_config__mutmut_323, 
    'x__export_zendesk_config__mutmut_324': x__export_zendesk_config__mutmut_324, 
    'x__export_zendesk_config__mutmut_325': x__export_zendesk_config__mutmut_325, 
    'x__export_zendesk_config__mutmut_326': x__export_zendesk_config__mutmut_326, 
    'x__export_zendesk_config__mutmut_327': x__export_zendesk_config__mutmut_327, 
    'x__export_zendesk_config__mutmut_328': x__export_zendesk_config__mutmut_328, 
    'x__export_zendesk_config__mutmut_329': x__export_zendesk_config__mutmut_329, 
    'x__export_zendesk_config__mutmut_330': x__export_zendesk_config__mutmut_330, 
    'x__export_zendesk_config__mutmut_331': x__export_zendesk_config__mutmut_331, 
    'x__export_zendesk_config__mutmut_332': x__export_zendesk_config__mutmut_332, 
    'x__export_zendesk_config__mutmut_333': x__export_zendesk_config__mutmut_333, 
    'x__export_zendesk_config__mutmut_334': x__export_zendesk_config__mutmut_334, 
    'x__export_zendesk_config__mutmut_335': x__export_zendesk_config__mutmut_335, 
    'x__export_zendesk_config__mutmut_336': x__export_zendesk_config__mutmut_336, 
    'x__export_zendesk_config__mutmut_337': x__export_zendesk_config__mutmut_337, 
    'x__export_zendesk_config__mutmut_338': x__export_zendesk_config__mutmut_338, 
    'x__export_zendesk_config__mutmut_339': x__export_zendesk_config__mutmut_339, 
    'x__export_zendesk_config__mutmut_340': x__export_zendesk_config__mutmut_340, 
    'x__export_zendesk_config__mutmut_341': x__export_zendesk_config__mutmut_341, 
    'x__export_zendesk_config__mutmut_342': x__export_zendesk_config__mutmut_342, 
    'x__export_zendesk_config__mutmut_343': x__export_zendesk_config__mutmut_343, 
    'x__export_zendesk_config__mutmut_344': x__export_zendesk_config__mutmut_344, 
    'x__export_zendesk_config__mutmut_345': x__export_zendesk_config__mutmut_345, 
    'x__export_zendesk_config__mutmut_346': x__export_zendesk_config__mutmut_346, 
    'x__export_zendesk_config__mutmut_347': x__export_zendesk_config__mutmut_347, 
    'x__export_zendesk_config__mutmut_348': x__export_zendesk_config__mutmut_348, 
    'x__export_zendesk_config__mutmut_349': x__export_zendesk_config__mutmut_349, 
    'x__export_zendesk_config__mutmut_350': x__export_zendesk_config__mutmut_350, 
    'x__export_zendesk_config__mutmut_351': x__export_zendesk_config__mutmut_351, 
    'x__export_zendesk_config__mutmut_352': x__export_zendesk_config__mutmut_352, 
    'x__export_zendesk_config__mutmut_353': x__export_zendesk_config__mutmut_353, 
    'x__export_zendesk_config__mutmut_354': x__export_zendesk_config__mutmut_354, 
    'x__export_zendesk_config__mutmut_355': x__export_zendesk_config__mutmut_355, 
    'x__export_zendesk_config__mutmut_356': x__export_zendesk_config__mutmut_356, 
    'x__export_zendesk_config__mutmut_357': x__export_zendesk_config__mutmut_357, 
    'x__export_zendesk_config__mutmut_358': x__export_zendesk_config__mutmut_358, 
    'x__export_zendesk_config__mutmut_359': x__export_zendesk_config__mutmut_359, 
    'x__export_zendesk_config__mutmut_360': x__export_zendesk_config__mutmut_360, 
    'x__export_zendesk_config__mutmut_361': x__export_zendesk_config__mutmut_361, 
    'x__export_zendesk_config__mutmut_362': x__export_zendesk_config__mutmut_362, 
    'x__export_zendesk_config__mutmut_363': x__export_zendesk_config__mutmut_363, 
    'x__export_zendesk_config__mutmut_364': x__export_zendesk_config__mutmut_364, 
    'x__export_zendesk_config__mutmut_365': x__export_zendesk_config__mutmut_365, 
    'x__export_zendesk_config__mutmut_366': x__export_zendesk_config__mutmut_366, 
    'x__export_zendesk_config__mutmut_367': x__export_zendesk_config__mutmut_367, 
    'x__export_zendesk_config__mutmut_368': x__export_zendesk_config__mutmut_368, 
    'x__export_zendesk_config__mutmut_369': x__export_zendesk_config__mutmut_369, 
    'x__export_zendesk_config__mutmut_370': x__export_zendesk_config__mutmut_370, 
    'x__export_zendesk_config__mutmut_371': x__export_zendesk_config__mutmut_371, 
    'x__export_zendesk_config__mutmut_372': x__export_zendesk_config__mutmut_372, 
    'x__export_zendesk_config__mutmut_373': x__export_zendesk_config__mutmut_373, 
    'x__export_zendesk_config__mutmut_374': x__export_zendesk_config__mutmut_374, 
    'x__export_zendesk_config__mutmut_375': x__export_zendesk_config__mutmut_375, 
    'x__export_zendesk_config__mutmut_376': x__export_zendesk_config__mutmut_376, 
    'x__export_zendesk_config__mutmut_377': x__export_zendesk_config__mutmut_377, 
    'x__export_zendesk_config__mutmut_378': x__export_zendesk_config__mutmut_378, 
    'x__export_zendesk_config__mutmut_379': x__export_zendesk_config__mutmut_379, 
    'x__export_zendesk_config__mutmut_380': x__export_zendesk_config__mutmut_380, 
    'x__export_zendesk_config__mutmut_381': x__export_zendesk_config__mutmut_381, 
    'x__export_zendesk_config__mutmut_382': x__export_zendesk_config__mutmut_382, 
    'x__export_zendesk_config__mutmut_383': x__export_zendesk_config__mutmut_383, 
    'x__export_zendesk_config__mutmut_384': x__export_zendesk_config__mutmut_384, 
    'x__export_zendesk_config__mutmut_385': x__export_zendesk_config__mutmut_385, 
    'x__export_zendesk_config__mutmut_386': x__export_zendesk_config__mutmut_386, 
    'x__export_zendesk_config__mutmut_387': x__export_zendesk_config__mutmut_387, 
    'x__export_zendesk_config__mutmut_388': x__export_zendesk_config__mutmut_388, 
    'x__export_zendesk_config__mutmut_389': x__export_zendesk_config__mutmut_389, 
    'x__export_zendesk_config__mutmut_390': x__export_zendesk_config__mutmut_390, 
    'x__export_zendesk_config__mutmut_391': x__export_zendesk_config__mutmut_391, 
    'x__export_zendesk_config__mutmut_392': x__export_zendesk_config__mutmut_392, 
    'x__export_zendesk_config__mutmut_393': x__export_zendesk_config__mutmut_393, 
    'x__export_zendesk_config__mutmut_394': x__export_zendesk_config__mutmut_394, 
    'x__export_zendesk_config__mutmut_395': x__export_zendesk_config__mutmut_395, 
    'x__export_zendesk_config__mutmut_396': x__export_zendesk_config__mutmut_396, 
    'x__export_zendesk_config__mutmut_397': x__export_zendesk_config__mutmut_397, 
    'x__export_zendesk_config__mutmut_398': x__export_zendesk_config__mutmut_398, 
    'x__export_zendesk_config__mutmut_399': x__export_zendesk_config__mutmut_399, 
    'x__export_zendesk_config__mutmut_400': x__export_zendesk_config__mutmut_400, 
    'x__export_zendesk_config__mutmut_401': x__export_zendesk_config__mutmut_401, 
    'x__export_zendesk_config__mutmut_402': x__export_zendesk_config__mutmut_402, 
    'x__export_zendesk_config__mutmut_403': x__export_zendesk_config__mutmut_403, 
    'x__export_zendesk_config__mutmut_404': x__export_zendesk_config__mutmut_404, 
    'x__export_zendesk_config__mutmut_405': x__export_zendesk_config__mutmut_405, 
    'x__export_zendesk_config__mutmut_406': x__export_zendesk_config__mutmut_406, 
    'x__export_zendesk_config__mutmut_407': x__export_zendesk_config__mutmut_407, 
    'x__export_zendesk_config__mutmut_408': x__export_zendesk_config__mutmut_408, 
    'x__export_zendesk_config__mutmut_409': x__export_zendesk_config__mutmut_409, 
    'x__export_zendesk_config__mutmut_410': x__export_zendesk_config__mutmut_410, 
    'x__export_zendesk_config__mutmut_411': x__export_zendesk_config__mutmut_411, 
    'x__export_zendesk_config__mutmut_412': x__export_zendesk_config__mutmut_412, 
    'x__export_zendesk_config__mutmut_413': x__export_zendesk_config__mutmut_413, 
    'x__export_zendesk_config__mutmut_414': x__export_zendesk_config__mutmut_414, 
    'x__export_zendesk_config__mutmut_415': x__export_zendesk_config__mutmut_415, 
    'x__export_zendesk_config__mutmut_416': x__export_zendesk_config__mutmut_416, 
    'x__export_zendesk_config__mutmut_417': x__export_zendesk_config__mutmut_417, 
    'x__export_zendesk_config__mutmut_418': x__export_zendesk_config__mutmut_418, 
    'x__export_zendesk_config__mutmut_419': x__export_zendesk_config__mutmut_419, 
    'x__export_zendesk_config__mutmut_420': x__export_zendesk_config__mutmut_420, 
    'x__export_zendesk_config__mutmut_421': x__export_zendesk_config__mutmut_421, 
    'x__export_zendesk_config__mutmut_422': x__export_zendesk_config__mutmut_422, 
    'x__export_zendesk_config__mutmut_423': x__export_zendesk_config__mutmut_423, 
    'x__export_zendesk_config__mutmut_424': x__export_zendesk_config__mutmut_424, 
    'x__export_zendesk_config__mutmut_425': x__export_zendesk_config__mutmut_425, 
    'x__export_zendesk_config__mutmut_426': x__export_zendesk_config__mutmut_426, 
    'x__export_zendesk_config__mutmut_427': x__export_zendesk_config__mutmut_427, 
    'x__export_zendesk_config__mutmut_428': x__export_zendesk_config__mutmut_428, 
    'x__export_zendesk_config__mutmut_429': x__export_zendesk_config__mutmut_429, 
    'x__export_zendesk_config__mutmut_430': x__export_zendesk_config__mutmut_430, 
    'x__export_zendesk_config__mutmut_431': x__export_zendesk_config__mutmut_431, 
    'x__export_zendesk_config__mutmut_432': x__export_zendesk_config__mutmut_432, 
    'x__export_zendesk_config__mutmut_433': x__export_zendesk_config__mutmut_433, 
    'x__export_zendesk_config__mutmut_434': x__export_zendesk_config__mutmut_434, 
    'x__export_zendesk_config__mutmut_435': x__export_zendesk_config__mutmut_435, 
    'x__export_zendesk_config__mutmut_436': x__export_zendesk_config__mutmut_436, 
    'x__export_zendesk_config__mutmut_437': x__export_zendesk_config__mutmut_437, 
    'x__export_zendesk_config__mutmut_438': x__export_zendesk_config__mutmut_438, 
    'x__export_zendesk_config__mutmut_439': x__export_zendesk_config__mutmut_439, 
    'x__export_zendesk_config__mutmut_440': x__export_zendesk_config__mutmut_440, 
    'x__export_zendesk_config__mutmut_441': x__export_zendesk_config__mutmut_441, 
    'x__export_zendesk_config__mutmut_442': x__export_zendesk_config__mutmut_442, 
    'x__export_zendesk_config__mutmut_443': x__export_zendesk_config__mutmut_443, 
    'x__export_zendesk_config__mutmut_444': x__export_zendesk_config__mutmut_444, 
    'x__export_zendesk_config__mutmut_445': x__export_zendesk_config__mutmut_445, 
    'x__export_zendesk_config__mutmut_446': x__export_zendesk_config__mutmut_446, 
    'x__export_zendesk_config__mutmut_447': x__export_zendesk_config__mutmut_447, 
    'x__export_zendesk_config__mutmut_448': x__export_zendesk_config__mutmut_448, 
    'x__export_zendesk_config__mutmut_449': x__export_zendesk_config__mutmut_449, 
    'x__export_zendesk_config__mutmut_450': x__export_zendesk_config__mutmut_450, 
    'x__export_zendesk_config__mutmut_451': x__export_zendesk_config__mutmut_451, 
    'x__export_zendesk_config__mutmut_452': x__export_zendesk_config__mutmut_452, 
    'x__export_zendesk_config__mutmut_453': x__export_zendesk_config__mutmut_453, 
    'x__export_zendesk_config__mutmut_454': x__export_zendesk_config__mutmut_454, 
    'x__export_zendesk_config__mutmut_455': x__export_zendesk_config__mutmut_455, 
    'x__export_zendesk_config__mutmut_456': x__export_zendesk_config__mutmut_456, 
    'x__export_zendesk_config__mutmut_457': x__export_zendesk_config__mutmut_457, 
    'x__export_zendesk_config__mutmut_458': x__export_zendesk_config__mutmut_458, 
    'x__export_zendesk_config__mutmut_459': x__export_zendesk_config__mutmut_459, 
    'x__export_zendesk_config__mutmut_460': x__export_zendesk_config__mutmut_460, 
    'x__export_zendesk_config__mutmut_461': x__export_zendesk_config__mutmut_461, 
    'x__export_zendesk_config__mutmut_462': x__export_zendesk_config__mutmut_462, 
    'x__export_zendesk_config__mutmut_463': x__export_zendesk_config__mutmut_463, 
    'x__export_zendesk_config__mutmut_464': x__export_zendesk_config__mutmut_464, 
    'x__export_zendesk_config__mutmut_465': x__export_zendesk_config__mutmut_465, 
    'x__export_zendesk_config__mutmut_466': x__export_zendesk_config__mutmut_466
}

def _export_zendesk_config(*args, **kwargs):
    result = _mutmut_trampoline(x__export_zendesk_config__mutmut_orig, x__export_zendesk_config__mutmut_mutants, args, kwargs)
    return result 

_export_zendesk_config.__signature__ = _mutmut_signature(x__export_zendesk_config__mutmut_orig)
x__export_zendesk_config__mutmut_orig.__name__ = 'x__export_zendesk_config'


__all__ = ["app"]


if __name__ == "__main__":  # pragma: no cover - CLI entrypoint
    app()
