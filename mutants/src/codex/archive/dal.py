"""
Dal Module

This module provides functionality for dal.

Usage:
    from archive.dal import ...

Classes:
    [To be documented]

Functions:
    [To be documented]

Author: Codex Team
"""

from __future__ import annotations
import logging
logger = logging.getLogger(__name__)

import abc
import contextlib
import json
import os
import sqlite3
import uuid
from collections.abc import Iterable, Iterator
from dataclasses import dataclass
from pathlib import Path
from typing import Any, cast
from urllib.parse import parse_qsl, urlparse
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@dataclass
class ArtifactRow:
    id: str
    content_sha256: str
    size_bytes: int
    compression: str
    mime_type: str
    storage_driver: str
    blob_bytes: bytes | None
    object_url: str | None


@dataclass
class ItemRow:
    id: str
    repo: str
    path: str
    commit_sha: str
    language: str
    kind: str
    reason: str
    artifact_id: str
    metadata: dict[str, Any]
    tombstone_id: str


def x__cursor_row_to_dict__mutmut_orig(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_1(cursor: Any, row: Any) -> dict[str, Any]:
    description = None
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_2(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) and []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_3(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(None, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_4(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, None, None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_5(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr("description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_6(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_7(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", ) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_8(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "XXdescriptionXX", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_9(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "DESCRIPTION", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_10(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = None
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_11(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = None
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_12(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(None, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_13(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, None, None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_14(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr("name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_15(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_16(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", )
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_17(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "XXnameXX", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_18(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "NAME", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_19(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is not None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_20(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = None
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_21(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[1]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_22(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = None
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_23(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(None)
        columns.append(name)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_24(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(None)
    return dict(zip(columns, row, strict=False))


def x__cursor_row_to_dict__mutmut_25(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(None)


def x__cursor_row_to_dict__mutmut_26(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(None, row, strict=False))


def x__cursor_row_to_dict__mutmut_27(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, None, strict=False))


def x__cursor_row_to_dict__mutmut_28(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=None))


def x__cursor_row_to_dict__mutmut_29(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(row, strict=False))


def x__cursor_row_to_dict__mutmut_30(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, strict=False))


def x__cursor_row_to_dict__mutmut_31(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, ))


def x__cursor_row_to_dict__mutmut_32(cursor: Any, row: Any) -> dict[str, Any]:
    description = getattr(cursor, "description", None) or []
    columns: list[str] = []
    for desc in description:
        name = getattr(desc, "name", None)
        if name is None:
            try:
                name = desc[0]
            except Exception:  # pragma: no cover - defensive
                name = str(desc)
        columns.append(name)
    return dict(zip(columns, row, strict=True))

x__cursor_row_to_dict__mutmut_mutants : ClassVar[MutantDict] = {
'x__cursor_row_to_dict__mutmut_1': x__cursor_row_to_dict__mutmut_1, 
    'x__cursor_row_to_dict__mutmut_2': x__cursor_row_to_dict__mutmut_2, 
    'x__cursor_row_to_dict__mutmut_3': x__cursor_row_to_dict__mutmut_3, 
    'x__cursor_row_to_dict__mutmut_4': x__cursor_row_to_dict__mutmut_4, 
    'x__cursor_row_to_dict__mutmut_5': x__cursor_row_to_dict__mutmut_5, 
    'x__cursor_row_to_dict__mutmut_6': x__cursor_row_to_dict__mutmut_6, 
    'x__cursor_row_to_dict__mutmut_7': x__cursor_row_to_dict__mutmut_7, 
    'x__cursor_row_to_dict__mutmut_8': x__cursor_row_to_dict__mutmut_8, 
    'x__cursor_row_to_dict__mutmut_9': x__cursor_row_to_dict__mutmut_9, 
    'x__cursor_row_to_dict__mutmut_10': x__cursor_row_to_dict__mutmut_10, 
    'x__cursor_row_to_dict__mutmut_11': x__cursor_row_to_dict__mutmut_11, 
    'x__cursor_row_to_dict__mutmut_12': x__cursor_row_to_dict__mutmut_12, 
    'x__cursor_row_to_dict__mutmut_13': x__cursor_row_to_dict__mutmut_13, 
    'x__cursor_row_to_dict__mutmut_14': x__cursor_row_to_dict__mutmut_14, 
    'x__cursor_row_to_dict__mutmut_15': x__cursor_row_to_dict__mutmut_15, 
    'x__cursor_row_to_dict__mutmut_16': x__cursor_row_to_dict__mutmut_16, 
    'x__cursor_row_to_dict__mutmut_17': x__cursor_row_to_dict__mutmut_17, 
    'x__cursor_row_to_dict__mutmut_18': x__cursor_row_to_dict__mutmut_18, 
    'x__cursor_row_to_dict__mutmut_19': x__cursor_row_to_dict__mutmut_19, 
    'x__cursor_row_to_dict__mutmut_20': x__cursor_row_to_dict__mutmut_20, 
    'x__cursor_row_to_dict__mutmut_21': x__cursor_row_to_dict__mutmut_21, 
    'x__cursor_row_to_dict__mutmut_22': x__cursor_row_to_dict__mutmut_22, 
    'x__cursor_row_to_dict__mutmut_23': x__cursor_row_to_dict__mutmut_23, 
    'x__cursor_row_to_dict__mutmut_24': x__cursor_row_to_dict__mutmut_24, 
    'x__cursor_row_to_dict__mutmut_25': x__cursor_row_to_dict__mutmut_25, 
    'x__cursor_row_to_dict__mutmut_26': x__cursor_row_to_dict__mutmut_26, 
    'x__cursor_row_to_dict__mutmut_27': x__cursor_row_to_dict__mutmut_27, 
    'x__cursor_row_to_dict__mutmut_28': x__cursor_row_to_dict__mutmut_28, 
    'x__cursor_row_to_dict__mutmut_29': x__cursor_row_to_dict__mutmut_29, 
    'x__cursor_row_to_dict__mutmut_30': x__cursor_row_to_dict__mutmut_30, 
    'x__cursor_row_to_dict__mutmut_31': x__cursor_row_to_dict__mutmut_31, 
    'x__cursor_row_to_dict__mutmut_32': x__cursor_row_to_dict__mutmut_32
}

def _cursor_row_to_dict(*args, **kwargs):
    result = _mutmut_trampoline(x__cursor_row_to_dict__mutmut_orig, x__cursor_row_to_dict__mutmut_mutants, args, kwargs)
    return result 

_cursor_row_to_dict.__signature__ = _mutmut_signature(x__cursor_row_to_dict__mutmut_orig)
x__cursor_row_to_dict__mutmut_orig.__name__ = 'x__cursor_row_to_dict'


def x__decode_json_field__mutmut_orig(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("utf-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_1(raw: Any) -> dict[str, Any]:
    if raw is not None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("utf-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_2(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = None
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("utf-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_3(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = None
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_4(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode(None)
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_5(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("XXutf-8XX")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_6(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("UTF-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_7(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("utf-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(None):
            parsed = json.loads(raw)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_8(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("utf-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = None
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}


def x__decode_json_field__mutmut_9(raw: Any) -> dict[str, Any]:
    if raw is None:
        return {}
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        raw = raw.tobytes()
    if isinstance(raw, bytes | bytearray):
        try:
            raw = raw.decode("utf-8")
        except UnicodeDecodeError:  # pragma: no cover - defensive
            return {}
    if isinstance(raw, str):
        with contextlib.suppress(json.JSONDecodeError):
            parsed = json.loads(None)
            if isinstance(parsed, dict):
                return parsed
        return {}
    if isinstance(raw, dict):
        return raw
    return {}

x__decode_json_field__mutmut_mutants : ClassVar[MutantDict] = {
'x__decode_json_field__mutmut_1': x__decode_json_field__mutmut_1, 
    'x__decode_json_field__mutmut_2': x__decode_json_field__mutmut_2, 
    'x__decode_json_field__mutmut_3': x__decode_json_field__mutmut_3, 
    'x__decode_json_field__mutmut_4': x__decode_json_field__mutmut_4, 
    'x__decode_json_field__mutmut_5': x__decode_json_field__mutmut_5, 
    'x__decode_json_field__mutmut_6': x__decode_json_field__mutmut_6, 
    'x__decode_json_field__mutmut_7': x__decode_json_field__mutmut_7, 
    'x__decode_json_field__mutmut_8': x__decode_json_field__mutmut_8, 
    'x__decode_json_field__mutmut_9': x__decode_json_field__mutmut_9
}

def _decode_json_field(*args, **kwargs):
    result = _mutmut_trampoline(x__decode_json_field__mutmut_orig, x__decode_json_field__mutmut_mutants, args, kwargs)
    return result 

_decode_json_field.__signature__ = _mutmut_signature(x__decode_json_field__mutmut_orig)
x__decode_json_field__mutmut_orig.__name__ = 'x__decode_json_field'


def x__maybe_bytes__mutmut_orig(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(bytes | None, raw)


def x__maybe_bytes__mutmut_1(raw: Any) -> bytes | None:
    if raw is not None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(bytes | None, raw)


def x__maybe_bytes__mutmut_2(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(None)
    return cast(bytes | None, raw)


def x__maybe_bytes__mutmut_3(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(None, raw)


def x__maybe_bytes__mutmut_4(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(bytes | None, None)


def x__maybe_bytes__mutmut_5(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(raw)


def x__maybe_bytes__mutmut_6(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(bytes | None, )


def x__maybe_bytes__mutmut_7(raw: Any) -> bytes | None:
    if raw is None:
        return None
    if isinstance(raw, memoryview):  # pragma: no cover - driver specific
        return raw.tobytes()
    if isinstance(raw, bytearray):
        return bytes(raw)
    return cast(bytes & None, raw)

x__maybe_bytes__mutmut_mutants : ClassVar[MutantDict] = {
'x__maybe_bytes__mutmut_1': x__maybe_bytes__mutmut_1, 
    'x__maybe_bytes__mutmut_2': x__maybe_bytes__mutmut_2, 
    'x__maybe_bytes__mutmut_3': x__maybe_bytes__mutmut_3, 
    'x__maybe_bytes__mutmut_4': x__maybe_bytes__mutmut_4, 
    'x__maybe_bytes__mutmut_5': x__maybe_bytes__mutmut_5, 
    'x__maybe_bytes__mutmut_6': x__maybe_bytes__mutmut_6, 
    'x__maybe_bytes__mutmut_7': x__maybe_bytes__mutmut_7
}

def _maybe_bytes(*args, **kwargs):
    result = _mutmut_trampoline(x__maybe_bytes__mutmut_orig, x__maybe_bytes__mutmut_mutants, args, kwargs)
    return result 

_maybe_bytes.__signature__ = _mutmut_signature(x__maybe_bytes__mutmut_orig)
x__maybe_bytes__mutmut_orig.__name__ = 'x__maybe_bytes'


class ArchiveDAL:
    """Factory for backend-specific DALs."""

    @staticmethod
    def from_env() -> BaseDAL:
        backend = os.getenv("CODEX_ARCHIVE_BACKEND", "sqlite").strip().lower()
        url = os.getenv("CODEX_ARCHIVE_URL", "sqlite:///./.codex/archive.sqlite")
        if backend == "sqlite":
            return SqliteDAL.from_url(url)
        if backend == "postgres":
            return PostgresDAL.from_url(url)
        if backend == "mariadb":
            return MariaDbDAL.from_url(url)
        raise ValueError(f"Unsupported CODEX_ARCHIVE_BACKEND: {backend}")


class BaseDAL(abc.ABC):
    @abc.abstractmethod
    def txn(self) -> contextlib.AbstractContextManager[None]:
        """Create a transaction context. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def ensure_schema(self) -> None:
        """Ensure database schema exists. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def insert_referent(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        """Insert referent record. Subclasses must implement."""
        pass

    @staticmethod
    def validate_identifier(name: str, allowed: Iterable[str]) -> str:
        allowed_set = set(allowed)
        if name not in allowed_set:
            raise ValueError(f"identifier not allowed: {name}")
        return name

    @abc.abstractmethod
    def recent_items(self, limit: int) -> list[dict[str, Any]]:
        """Get recent items. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def summary(self) -> dict[str, int]:
        """Get summary statistics. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def ensure_artifact(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        """Ensure artifact exists. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def insert_item(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        """Insert item record. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def insert_event(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        """Insert event record. Subclasses must implement."""
        pass

    @abc.abstractmethod
    def fetch_by_tombstone(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        """Fetch item and artifact by tombstone ID. Subclasses must implement."""
        pass

    # -------- Release persistence (optional) --------
    @abc.abstractmethod
    def create_release_meta(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        """Insert a row into release_meta and return the persisted row."""
        pass

    @abc.abstractmethod
    def add_release_component(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        """Insert a row into release_component for this release."""
        pass

    @abc.abstractmethod
    def get_release_meta_by_release_id(self, *, release_id: str) -> dict[str, Any] | None:
        """Lookup release_meta by its human release_id."""
        pass


class SqliteDAL(BaseDAL):
    _ALLOWED_TABLES = frozenset(
        {
            "artifact",
            "item",
            "event",
            "tag",
            "referent",
            "release_meta",
            "release_component",
        }
    )

    def xǁSqliteDALǁ__init____mutmut_orig(self, conn: sqlite3.Connection, db_root: Path):
        self.conn = conn
        self.db_root = db_root
        self.conn.row_factory = sqlite3.Row

    def xǁSqliteDALǁ__init____mutmut_1(self, conn: sqlite3.Connection, db_root: Path):
        self.conn = None
        self.db_root = db_root
        self.conn.row_factory = sqlite3.Row

    def xǁSqliteDALǁ__init____mutmut_2(self, conn: sqlite3.Connection, db_root: Path):
        self.conn = conn
        self.db_root = None
        self.conn.row_factory = sqlite3.Row

    def xǁSqliteDALǁ__init____mutmut_3(self, conn: sqlite3.Connection, db_root: Path):
        self.conn = conn
        self.db_root = db_root
        self.conn.row_factory = None
    
    xǁSqliteDALǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁ__init____mutmut_1': xǁSqliteDALǁ__init____mutmut_1, 
        'xǁSqliteDALǁ__init____mutmut_2': xǁSqliteDALǁ__init____mutmut_2, 
        'xǁSqliteDALǁ__init____mutmut_3': xǁSqliteDALǁ__init____mutmut_3
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁSqliteDALǁ__init____mutmut_orig)
    xǁSqliteDALǁ__init____mutmut_orig.__name__ = 'xǁSqliteDALǁ__init__'

    @staticmethod
    def from_url(url: str) -> SqliteDAL:
        if not url.startswith("sqlite:///"):
            raise ValueError("SQLite URL must start with sqlite:///")
        path = url[len("sqlite:///") :]
        p = Path(path)
        p.parent.mkdir(parents=True, exist_ok=True)
        conn = sqlite3.connect(p.as_posix())
        dal = SqliteDAL(conn, p.parent)
        dal.ensure_schema()
        return dal

    def txn(self) -> contextlib.AbstractContextManager[None]:
        @contextlib.contextmanager
        def _ctx() -> Iterator[None]:
            try:
                yield
                self.conn.commit()
            except Exception:  # pragma: no cover - passthrough
                self.conn.rollback()
                raise

        return _ctx()

    def xǁSqliteDALǁ_load_migration_sql__mutmut_orig(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_1(self) -> str:
        here = None
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_2(self) -> str:
        here = Path(None).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_3(self) -> str:
        here = Path(__file__).resolve()
        root = None
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_4(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[4]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_5(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = None
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_6(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" * "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_7(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" * "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_8(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" * "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_9(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root * "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_10(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "XXdbXX" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_11(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "DB" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_12(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "XXmigrationsXX" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_13(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "MIGRATIONS" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_14(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "XXsqliteXX" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_15(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "SQLITE" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_16(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "XX001_init_archive.sqlXX"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_17(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_INIT_ARCHIVE.SQL"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_18(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding=None)
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_19(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="XXutf-8XX")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_20(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="UTF-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_21(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = None
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_22(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" * "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_23(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" * "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_24(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" * "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_25(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root * "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_26(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "XXdbXX" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_27(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "DB" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_28(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "XXmigrationsXX" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_29(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "MIGRATIONS" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_30(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "XXsqliteXX" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_31(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "SQLITE" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_32(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "XX001_init.sqlXX"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_33(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_INIT.SQL"
        if mig.exists():
            return mig.read_text(encoding="utf-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_34(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding=None)
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_35(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="XXutf-8XX")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """

    def xǁSqliteDALǁ_load_migration_sql__mutmut_36(self) -> str:
        here = Path(__file__).resolve()
        root = here.parents[3]
        preferred = root / "db" / "migrations" / "sqlite" / "001_init_archive.sql"
        if preferred.exists():
            return preferred.read_text(encoding="utf-8")
        mig = root / "db" / "migrations" / "sqlite" / "001_init.sql"
        if mig.exists():
            return mig.read_text(encoding="UTF-8")
        return """
        PRAGMA journal_mode=WAL;
        CREATE TABLE IF NOT EXISTS artifact (
          id             TEXT PRIMARY KEY,
          content_sha256 TEXT NOT NULL UNIQUE,
          size_bytes     INTEGER NOT NULL,
          compression    TEXT NOT NULL DEFAULT 'zlib',
          mime_type      TEXT NOT NULL,
          storage_driver TEXT NOT NULL DEFAULT 'db',
          blob_bytes     BLOB,
          object_url     TEXT,
          created_at     TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS item (
          id             TEXT PRIMARY KEY,
          repo           TEXT NOT NULL,
          path           TEXT NOT NULL,
          commit_sha     TEXT NOT NULL,
          language       TEXT,
          kind           TEXT NOT NULL,
          reason         TEXT NOT NULL,
          artifact_id    TEXT NOT NULL REFERENCES artifact(id),
          metadata       TEXT NOT NULL DEFAULT '{}',
          archived_by    TEXT NOT NULL,
          archived_at    TEXT NOT NULL DEFAULT (datetime('now')),
          tombstone_id   TEXT NOT NULL UNIQUE,
          legal_hold     INTEGER NOT NULL DEFAULT 0,
          delete_after   TEXT,
          restored_at    TEXT
        );
        CREATE TABLE IF NOT EXISTS event (
          id         TEXT PRIMARY KEY,
          item_id    TEXT NOT NULL REFERENCES item(id),
          action     TEXT NOT NULL,
          actor      TEXT NOT NULL,
          context    TEXT NOT NULL DEFAULT '{}',
          created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS tag (
          item_id TEXT NOT NULL,
          tag     TEXT NOT NULL,
          PRIMARY KEY (item_id, tag)
        );
        CREATE TABLE IF NOT EXISTS referent (
          item_id   TEXT NOT NULL,
          ref_type  TEXT NOT NULL,
          ref_value TEXT NOT NULL,
          PRIMARY KEY (item_id, ref_type, ref_value)
        );
        """
    
    xǁSqliteDALǁ_load_migration_sql__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁ_load_migration_sql__mutmut_1': xǁSqliteDALǁ_load_migration_sql__mutmut_1, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_2': xǁSqliteDALǁ_load_migration_sql__mutmut_2, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_3': xǁSqliteDALǁ_load_migration_sql__mutmut_3, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_4': xǁSqliteDALǁ_load_migration_sql__mutmut_4, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_5': xǁSqliteDALǁ_load_migration_sql__mutmut_5, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_6': xǁSqliteDALǁ_load_migration_sql__mutmut_6, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_7': xǁSqliteDALǁ_load_migration_sql__mutmut_7, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_8': xǁSqliteDALǁ_load_migration_sql__mutmut_8, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_9': xǁSqliteDALǁ_load_migration_sql__mutmut_9, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_10': xǁSqliteDALǁ_load_migration_sql__mutmut_10, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_11': xǁSqliteDALǁ_load_migration_sql__mutmut_11, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_12': xǁSqliteDALǁ_load_migration_sql__mutmut_12, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_13': xǁSqliteDALǁ_load_migration_sql__mutmut_13, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_14': xǁSqliteDALǁ_load_migration_sql__mutmut_14, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_15': xǁSqliteDALǁ_load_migration_sql__mutmut_15, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_16': xǁSqliteDALǁ_load_migration_sql__mutmut_16, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_17': xǁSqliteDALǁ_load_migration_sql__mutmut_17, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_18': xǁSqliteDALǁ_load_migration_sql__mutmut_18, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_19': xǁSqliteDALǁ_load_migration_sql__mutmut_19, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_20': xǁSqliteDALǁ_load_migration_sql__mutmut_20, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_21': xǁSqliteDALǁ_load_migration_sql__mutmut_21, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_22': xǁSqliteDALǁ_load_migration_sql__mutmut_22, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_23': xǁSqliteDALǁ_load_migration_sql__mutmut_23, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_24': xǁSqliteDALǁ_load_migration_sql__mutmut_24, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_25': xǁSqliteDALǁ_load_migration_sql__mutmut_25, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_26': xǁSqliteDALǁ_load_migration_sql__mutmut_26, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_27': xǁSqliteDALǁ_load_migration_sql__mutmut_27, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_28': xǁSqliteDALǁ_load_migration_sql__mutmut_28, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_29': xǁSqliteDALǁ_load_migration_sql__mutmut_29, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_30': xǁSqliteDALǁ_load_migration_sql__mutmut_30, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_31': xǁSqliteDALǁ_load_migration_sql__mutmut_31, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_32': xǁSqliteDALǁ_load_migration_sql__mutmut_32, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_33': xǁSqliteDALǁ_load_migration_sql__mutmut_33, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_34': xǁSqliteDALǁ_load_migration_sql__mutmut_34, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_35': xǁSqliteDALǁ_load_migration_sql__mutmut_35, 
        'xǁSqliteDALǁ_load_migration_sql__mutmut_36': xǁSqliteDALǁ_load_migration_sql__mutmut_36
    }
    
    def _load_migration_sql(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁ_load_migration_sql__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁ_load_migration_sql__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _load_migration_sql.__signature__ = _mutmut_signature(xǁSqliteDALǁ_load_migration_sql__mutmut_orig)
    xǁSqliteDALǁ_load_migration_sql__mutmut_orig.__name__ = 'xǁSqliteDALǁ_load_migration_sql'

    def xǁSqliteDALǁensure_schema__mutmut_orig(self) -> None:
        sql = self._load_migration_sql()
        with self.txn():
            self.conn.executescript(sql)
        self._ensure_release_tables()

    def xǁSqliteDALǁensure_schema__mutmut_1(self) -> None:
        sql = None
        with self.txn():
            self.conn.executescript(sql)
        self._ensure_release_tables()

    def xǁSqliteDALǁensure_schema__mutmut_2(self) -> None:
        sql = self._load_migration_sql()
        with self.txn():
            self.conn.executescript(None)
        self._ensure_release_tables()
    
    xǁSqliteDALǁensure_schema__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁensure_schema__mutmut_1': xǁSqliteDALǁensure_schema__mutmut_1, 
        'xǁSqliteDALǁensure_schema__mutmut_2': xǁSqliteDALǁensure_schema__mutmut_2
    }
    
    def ensure_schema(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁensure_schema__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁensure_schema__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_schema.__signature__ = _mutmut_signature(xǁSqliteDALǁensure_schema__mutmut_orig)
    xǁSqliteDALǁensure_schema__mutmut_orig.__name__ = 'xǁSqliteDALǁensure_schema'

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_orig(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "CREATE INDEX IF NOT EXISTS "
                "idx_release_component_release_id ON release_component(release_id)"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_1(self) -> None:
        with self.txn():
            self.conn.execute(
                None
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "CREATE INDEX IF NOT EXISTS "
                "idx_release_component_release_id ON release_component(release_id)"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_2(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                None
            )
            self.conn.execute(
                "CREATE INDEX IF NOT EXISTS "
                "idx_release_component_release_id ON release_component(release_id)"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_3(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                None
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_4(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "XXCREATE INDEX IF NOT EXISTS XX"
                "idx_release_component_release_id ON release_component(release_id)"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_5(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "create index if not exists "
                "idx_release_component_release_id ON release_component(release_id)"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_6(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "CREATE INDEX IF NOT EXISTS "
                "XXidx_release_component_release_id ON release_component(release_id)XX"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_7(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "CREATE INDEX IF NOT EXISTS "
                "idx_release_component_release_id on release_component(release_id)"
            )

    def xǁSqliteDALǁ_ensure_release_tables__mutmut_8(self) -> None:
        with self.txn():
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_meta (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL UNIQUE,
                  version TEXT NOT NULL,
                  created_at TEXT NOT NULL,
                  actor TEXT NOT NULL,
                  metadata TEXT NOT NULL DEFAULT '{}'
                )
                """
            )
            self.conn.execute(
                """
                CREATE TABLE IF NOT EXISTS release_component (
                  id TEXT PRIMARY KEY,
                  release_id TEXT NOT NULL,
                  item_id TEXT,
                  tombstone TEXT NOT NULL,
                  dest_path TEXT NOT NULL,
                  mode TEXT NOT NULL DEFAULT '0644',
                  template_vars TEXT NOT NULL DEFAULT '{}',
                  FOREIGN KEY(release_id) REFERENCES release_meta(id)
                )
                """
            )
            self.conn.execute(
                "CREATE INDEX IF NOT EXISTS "
                "IDX_RELEASE_COMPONENT_RELEASE_ID ON RELEASE_COMPONENT(RELEASE_ID)"
            )
    
    xǁSqliteDALǁ_ensure_release_tables__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁ_ensure_release_tables__mutmut_1': xǁSqliteDALǁ_ensure_release_tables__mutmut_1, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_2': xǁSqliteDALǁ_ensure_release_tables__mutmut_2, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_3': xǁSqliteDALǁ_ensure_release_tables__mutmut_3, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_4': xǁSqliteDALǁ_ensure_release_tables__mutmut_4, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_5': xǁSqliteDALǁ_ensure_release_tables__mutmut_5, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_6': xǁSqliteDALǁ_ensure_release_tables__mutmut_6, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_7': xǁSqliteDALǁ_ensure_release_tables__mutmut_7, 
        'xǁSqliteDALǁ_ensure_release_tables__mutmut_8': xǁSqliteDALǁ_ensure_release_tables__mutmut_8
    }
    
    def _ensure_release_tables(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁ_ensure_release_tables__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁ_ensure_release_tables__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _ensure_release_tables.__signature__ = _mutmut_signature(xǁSqliteDALǁ_ensure_release_tables__mutmut_orig)
    xǁSqliteDALǁ_ensure_release_tables__mutmut_orig.__name__ = 'xǁSqliteDALǁ_ensure_release_tables'

    def xǁSqliteDALǁinsert_referent__mutmut_orig(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_1(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = None
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_2(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                None,
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_3(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                None,
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_4(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_5(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_6(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "XXSELECT id FROM item WHERE id = ?XX",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_7(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "select id from item where id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_8(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT ID FROM ITEM WHERE ID = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_9(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = None
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_10(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is not None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_11(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(None)
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_12(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = None
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_13(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["XXidXX"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_14(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["ID"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_15(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                None,
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_16(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                None,
            )

    def xǁSqliteDALǁinsert_referent__mutmut_17(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_18(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)",
                )

    def xǁSqliteDALǁinsert_referent__mutmut_19(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "XXINSERT OR IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (?,?,?)XX",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_20(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "insert or ignore into referent (item_id, ref_type, ref_value) values (?,?,?)",
                (item_id, ref_type, ref_value),
            )

    def xǁSqliteDALǁinsert_referent__mutmut_21(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.execute(
                "SELECT id FROM item WHERE id = ?",
                (item_id,),
            )
            row = cur.fetchone()
            if row is None:
                raise KeyError(f"Item not found: {item_id}")
            item_id = row["id"]
            self.conn.execute(
                "INSERT OR IGNORE INTO REFERENT (ITEM_ID, REF_TYPE, REF_VALUE) VALUES (?,?,?)",
                (item_id, ref_type, ref_value),
            )
    
    xǁSqliteDALǁinsert_referent__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁinsert_referent__mutmut_1': xǁSqliteDALǁinsert_referent__mutmut_1, 
        'xǁSqliteDALǁinsert_referent__mutmut_2': xǁSqliteDALǁinsert_referent__mutmut_2, 
        'xǁSqliteDALǁinsert_referent__mutmut_3': xǁSqliteDALǁinsert_referent__mutmut_3, 
        'xǁSqliteDALǁinsert_referent__mutmut_4': xǁSqliteDALǁinsert_referent__mutmut_4, 
        'xǁSqliteDALǁinsert_referent__mutmut_5': xǁSqliteDALǁinsert_referent__mutmut_5, 
        'xǁSqliteDALǁinsert_referent__mutmut_6': xǁSqliteDALǁinsert_referent__mutmut_6, 
        'xǁSqliteDALǁinsert_referent__mutmut_7': xǁSqliteDALǁinsert_referent__mutmut_7, 
        'xǁSqliteDALǁinsert_referent__mutmut_8': xǁSqliteDALǁinsert_referent__mutmut_8, 
        'xǁSqliteDALǁinsert_referent__mutmut_9': xǁSqliteDALǁinsert_referent__mutmut_9, 
        'xǁSqliteDALǁinsert_referent__mutmut_10': xǁSqliteDALǁinsert_referent__mutmut_10, 
        'xǁSqliteDALǁinsert_referent__mutmut_11': xǁSqliteDALǁinsert_referent__mutmut_11, 
        'xǁSqliteDALǁinsert_referent__mutmut_12': xǁSqliteDALǁinsert_referent__mutmut_12, 
        'xǁSqliteDALǁinsert_referent__mutmut_13': xǁSqliteDALǁinsert_referent__mutmut_13, 
        'xǁSqliteDALǁinsert_referent__mutmut_14': xǁSqliteDALǁinsert_referent__mutmut_14, 
        'xǁSqliteDALǁinsert_referent__mutmut_15': xǁSqliteDALǁinsert_referent__mutmut_15, 
        'xǁSqliteDALǁinsert_referent__mutmut_16': xǁSqliteDALǁinsert_referent__mutmut_16, 
        'xǁSqliteDALǁinsert_referent__mutmut_17': xǁSqliteDALǁinsert_referent__mutmut_17, 
        'xǁSqliteDALǁinsert_referent__mutmut_18': xǁSqliteDALǁinsert_referent__mutmut_18, 
        'xǁSqliteDALǁinsert_referent__mutmut_19': xǁSqliteDALǁinsert_referent__mutmut_19, 
        'xǁSqliteDALǁinsert_referent__mutmut_20': xǁSqliteDALǁinsert_referent__mutmut_20, 
        'xǁSqliteDALǁinsert_referent__mutmut_21': xǁSqliteDALǁinsert_referent__mutmut_21
    }
    
    def insert_referent(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁinsert_referent__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁinsert_referent__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_referent.__signature__ = _mutmut_signature(xǁSqliteDALǁinsert_referent__mutmut_orig)
    xǁSqliteDALǁinsert_referent__mutmut_orig.__name__ = 'xǁSqliteDALǁinsert_referent'

    def xǁSqliteDALǁrecent_items__mutmut_orig(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_1(self, limit: int) -> list[dict[str, Any]]:
        cur = None
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_2(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            None,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_3(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            None,
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_4(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_5(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_6(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = None
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_7(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                None
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_8(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "XXtombstoneXX": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_9(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "TOMBSTONE": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_10(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["XXtombstone_idXX"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_11(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["TOMBSTONE_ID"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_12(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "XXrepoXX": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_13(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "REPO": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_14(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["XXrepoXX"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_15(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["REPO"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_16(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "XXpathXX": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_17(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "PATH": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_18(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["XXpathXX"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_19(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["PATH"],
                    "archived_at": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_20(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "XXarchived_atXX": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_21(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "ARCHIVED_AT": row["archived_at"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_22(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["XXarchived_atXX"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_23(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["ARCHIVED_AT"],
                    "sha256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_24(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "XXsha256XX": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_25(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "SHA256": row["content_sha256"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_26(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["XXcontent_sha256XX"],
                }
            )
        return rows

    def xǁSqliteDALǁrecent_items__mutmut_27(self, limit: int) -> list[dict[str, Any]]:
        cur = self.conn.execute(
            """
            SELECT item.tombstone_id,
                   item.repo,
                   item.path,
                   item.archived_at,
                   artifact.content_sha256
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            ORDER BY item.archived_at DESC
            LIMIT ?
            """,
            (limit,),
        )
        rows: list[dict[str, Any]] = []
        for row in cur.fetchall():
            rows.append(
                {
                    "tombstone": row["tombstone_id"],
                    "repo": row["repo"],
                    "path": row["path"],
                    "archived_at": row["archived_at"],
                    "sha256": row["CONTENT_SHA256"],
                }
            )
        return rows
    
    xǁSqliteDALǁrecent_items__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁrecent_items__mutmut_1': xǁSqliteDALǁrecent_items__mutmut_1, 
        'xǁSqliteDALǁrecent_items__mutmut_2': xǁSqliteDALǁrecent_items__mutmut_2, 
        'xǁSqliteDALǁrecent_items__mutmut_3': xǁSqliteDALǁrecent_items__mutmut_3, 
        'xǁSqliteDALǁrecent_items__mutmut_4': xǁSqliteDALǁrecent_items__mutmut_4, 
        'xǁSqliteDALǁrecent_items__mutmut_5': xǁSqliteDALǁrecent_items__mutmut_5, 
        'xǁSqliteDALǁrecent_items__mutmut_6': xǁSqliteDALǁrecent_items__mutmut_6, 
        'xǁSqliteDALǁrecent_items__mutmut_7': xǁSqliteDALǁrecent_items__mutmut_7, 
        'xǁSqliteDALǁrecent_items__mutmut_8': xǁSqliteDALǁrecent_items__mutmut_8, 
        'xǁSqliteDALǁrecent_items__mutmut_9': xǁSqliteDALǁrecent_items__mutmut_9, 
        'xǁSqliteDALǁrecent_items__mutmut_10': xǁSqliteDALǁrecent_items__mutmut_10, 
        'xǁSqliteDALǁrecent_items__mutmut_11': xǁSqliteDALǁrecent_items__mutmut_11, 
        'xǁSqliteDALǁrecent_items__mutmut_12': xǁSqliteDALǁrecent_items__mutmut_12, 
        'xǁSqliteDALǁrecent_items__mutmut_13': xǁSqliteDALǁrecent_items__mutmut_13, 
        'xǁSqliteDALǁrecent_items__mutmut_14': xǁSqliteDALǁrecent_items__mutmut_14, 
        'xǁSqliteDALǁrecent_items__mutmut_15': xǁSqliteDALǁrecent_items__mutmut_15, 
        'xǁSqliteDALǁrecent_items__mutmut_16': xǁSqliteDALǁrecent_items__mutmut_16, 
        'xǁSqliteDALǁrecent_items__mutmut_17': xǁSqliteDALǁrecent_items__mutmut_17, 
        'xǁSqliteDALǁrecent_items__mutmut_18': xǁSqliteDALǁrecent_items__mutmut_18, 
        'xǁSqliteDALǁrecent_items__mutmut_19': xǁSqliteDALǁrecent_items__mutmut_19, 
        'xǁSqliteDALǁrecent_items__mutmut_20': xǁSqliteDALǁrecent_items__mutmut_20, 
        'xǁSqliteDALǁrecent_items__mutmut_21': xǁSqliteDALǁrecent_items__mutmut_21, 
        'xǁSqliteDALǁrecent_items__mutmut_22': xǁSqliteDALǁrecent_items__mutmut_22, 
        'xǁSqliteDALǁrecent_items__mutmut_23': xǁSqliteDALǁrecent_items__mutmut_23, 
        'xǁSqliteDALǁrecent_items__mutmut_24': xǁSqliteDALǁrecent_items__mutmut_24, 
        'xǁSqliteDALǁrecent_items__mutmut_25': xǁSqliteDALǁrecent_items__mutmut_25, 
        'xǁSqliteDALǁrecent_items__mutmut_26': xǁSqliteDALǁrecent_items__mutmut_26, 
        'xǁSqliteDALǁrecent_items__mutmut_27': xǁSqliteDALǁrecent_items__mutmut_27
    }
    
    def recent_items(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁrecent_items__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁrecent_items__mutmut_mutants"), args, kwargs, self)
        return result 
    
    recent_items.__signature__ = _mutmut_signature(xǁSqliteDALǁrecent_items__mutmut_orig)
    xǁSqliteDALǁrecent_items__mutmut_orig.__name__ = 'xǁSqliteDALǁrecent_items'

    def xǁSqliteDALǁsummary__mutmut_orig(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_1(self) -> dict[str, int]:
        cur = None
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_2(self) -> dict[str, int]:
        cur = self.conn.execute(
            None
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_3(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = None
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_4(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is not None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_5(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"XXcountXX": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_6(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"COUNT": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_7(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 1, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_8(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "XXtotal_bytesXX": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_9(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "TOTAL_BYTES": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_10(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 1}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_11(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "XXcountXX": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_12(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "COUNT": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_13(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(None),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_14(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] and 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_15(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["XXcntXX"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_16(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["CNT"] or 0),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_17(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 1),
            "total_bytes": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_18(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "XXtotal_bytesXX": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_19(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "TOTAL_BYTES": int(row["total_bytes"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_20(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(None),
        }

    def xǁSqliteDALǁsummary__mutmut_21(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] and 0),
        }

    def xǁSqliteDALǁsummary__mutmut_22(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["XXtotal_bytesXX"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_23(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["TOTAL_BYTES"] or 0),
        }

    def xǁSqliteDALǁsummary__mutmut_24(self) -> dict[str, int]:
        cur = self.conn.execute(
            """
            SELECT COUNT(*) AS cnt, COALESCE(SUM(artifact.size_bytes), 0) AS total_bytes
            FROM item
            JOIN artifact ON item.artifact_id = artifact.id
            """
        )
        row = cur.fetchone()
        if row is None:
            return {"count": 0, "total_bytes": 0}
        return {
            "count": int(row["cnt"] or 0),
            "total_bytes": int(row["total_bytes"] or 1),
        }
    
    xǁSqliteDALǁsummary__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁsummary__mutmut_1': xǁSqliteDALǁsummary__mutmut_1, 
        'xǁSqliteDALǁsummary__mutmut_2': xǁSqliteDALǁsummary__mutmut_2, 
        'xǁSqliteDALǁsummary__mutmut_3': xǁSqliteDALǁsummary__mutmut_3, 
        'xǁSqliteDALǁsummary__mutmut_4': xǁSqliteDALǁsummary__mutmut_4, 
        'xǁSqliteDALǁsummary__mutmut_5': xǁSqliteDALǁsummary__mutmut_5, 
        'xǁSqliteDALǁsummary__mutmut_6': xǁSqliteDALǁsummary__mutmut_6, 
        'xǁSqliteDALǁsummary__mutmut_7': xǁSqliteDALǁsummary__mutmut_7, 
        'xǁSqliteDALǁsummary__mutmut_8': xǁSqliteDALǁsummary__mutmut_8, 
        'xǁSqliteDALǁsummary__mutmut_9': xǁSqliteDALǁsummary__mutmut_9, 
        'xǁSqliteDALǁsummary__mutmut_10': xǁSqliteDALǁsummary__mutmut_10, 
        'xǁSqliteDALǁsummary__mutmut_11': xǁSqliteDALǁsummary__mutmut_11, 
        'xǁSqliteDALǁsummary__mutmut_12': xǁSqliteDALǁsummary__mutmut_12, 
        'xǁSqliteDALǁsummary__mutmut_13': xǁSqliteDALǁsummary__mutmut_13, 
        'xǁSqliteDALǁsummary__mutmut_14': xǁSqliteDALǁsummary__mutmut_14, 
        'xǁSqliteDALǁsummary__mutmut_15': xǁSqliteDALǁsummary__mutmut_15, 
        'xǁSqliteDALǁsummary__mutmut_16': xǁSqliteDALǁsummary__mutmut_16, 
        'xǁSqliteDALǁsummary__mutmut_17': xǁSqliteDALǁsummary__mutmut_17, 
        'xǁSqliteDALǁsummary__mutmut_18': xǁSqliteDALǁsummary__mutmut_18, 
        'xǁSqliteDALǁsummary__mutmut_19': xǁSqliteDALǁsummary__mutmut_19, 
        'xǁSqliteDALǁsummary__mutmut_20': xǁSqliteDALǁsummary__mutmut_20, 
        'xǁSqliteDALǁsummary__mutmut_21': xǁSqliteDALǁsummary__mutmut_21, 
        'xǁSqliteDALǁsummary__mutmut_22': xǁSqliteDALǁsummary__mutmut_22, 
        'xǁSqliteDALǁsummary__mutmut_23': xǁSqliteDALǁsummary__mutmut_23, 
        'xǁSqliteDALǁsummary__mutmut_24': xǁSqliteDALǁsummary__mutmut_24
    }
    
    def summary(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁsummary__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁsummary__mutmut_mutants"), args, kwargs, self)
        return result 
    
    summary.__signature__ = _mutmut_signature(xǁSqliteDALǁsummary__mutmut_orig)
    xǁSqliteDALǁsummary__mutmut_orig.__name__ = 'xǁSqliteDALǁsummary'

    def xǁSqliteDALǁensure_artifact__mutmut_orig(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_1(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "XXzlibXX",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_2(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "ZLIB",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_3(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "XXdbXX",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_4(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "DB",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_5(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = None
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_6(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                None,
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_7(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                None,
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_8(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_9(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_10(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "XXSELECT id, content_sha256, size_bytes, compression, mime_type, XX"
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_11(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "select id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_12(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT ID, CONTENT_SHA256, SIZE_BYTES, COMPRESSION, MIME_TYPE, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_13(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "XXstorage_driver, blob_bytes, object_url FROM artifact XX"
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_14(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url from artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_15(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "STORAGE_DRIVER, BLOB_BYTES, OBJECT_URL FROM ARTIFACT "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_16(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "XXWHERE content_sha256 = ?XX"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_17(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "where content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_18(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE CONTENT_SHA256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_19(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = None
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_20(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(None)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_21(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = None
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_22(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(None)
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_23(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                None,
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_24(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                None,
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_25(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_26(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_27(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "XXINSERT INTO artifact (id, content_sha256, size_bytes, compression, XX"
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_28(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "insert into artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_29(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO ARTIFACT (ID, CONTENT_SHA256, SIZE_BYTES, COMPRESSION, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_30(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "XXmime_type, storage_driver, blob_bytes, object_url) XX"
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_31(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "MIME_TYPE, STORAGE_DRIVER, BLOB_BYTES, OBJECT_URL) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_32(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "XXVALUES (?,?,?,?,?,?,?,?)XX"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_33(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "values (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_34(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver != "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_35(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "XXdbXX" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_36(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "DB" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_37(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "XXidXX": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_38(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "ID": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_39(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "XXcontent_sha256XX": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_40(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "CONTENT_SHA256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_41(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "XXsize_bytesXX": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_42(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "SIZE_BYTES": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_43(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "XXcompressionXX": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_44(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "COMPRESSION": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_45(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "XXmime_typeXX": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_46(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "MIME_TYPE": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_47(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "XXstorage_driverXX": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_48(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "STORAGE_DRIVER": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_49(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "XXblob_bytesXX": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_50(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "BLOB_BYTES": blob if storage_driver == "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_51(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver != "db" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_52(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "XXdbXX" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_53(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "DB" else None,
                "object_url": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_54(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "XXobject_urlXX": object_url,
            }

    def xǁSqliteDALǁensure_artifact__mutmut_55(
        self,
        *,
        sha: str,
        size: int,
        mime: str,
        blob: bytes,
        compression: str = "zlib",
        storage_driver: str = "db",
        object_url: str | None = None,
    ) -> dict[str, Any]:
        with self.txn():
            cur = self.conn.execute(
                (
                    "SELECT id, content_sha256, size_bytes, compression, mime_type, "
                    "storage_driver, blob_bytes, object_url FROM artifact "
                    "WHERE content_sha256 = ?"
                ),
                (sha,),
            )
            row = cur.fetchone()
            if row:
                return dict(row)
            aid = str(uuid.uuid4())
            self.conn.execute(
                (
                    "INSERT INTO artifact (id, content_sha256, size_bytes, compression, "
                    "mime_type, storage_driver, blob_bytes, object_url) "
                    "VALUES (?,?,?,?,?,?,?,?)"
                ),
                (
                    aid,
                    sha,
                    size,
                    compression,
                    mime,
                    storage_driver,
                    blob if storage_driver == "db" else None,
                    object_url,
                ),
            )
            return {
                "id": aid,
                "content_sha256": sha,
                "size_bytes": size,
                "compression": compression,
                "mime_type": mime,
                "storage_driver": storage_driver,
                "blob_bytes": blob if storage_driver == "db" else None,
                "OBJECT_URL": object_url,
            }
    
    xǁSqliteDALǁensure_artifact__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁensure_artifact__mutmut_1': xǁSqliteDALǁensure_artifact__mutmut_1, 
        'xǁSqliteDALǁensure_artifact__mutmut_2': xǁSqliteDALǁensure_artifact__mutmut_2, 
        'xǁSqliteDALǁensure_artifact__mutmut_3': xǁSqliteDALǁensure_artifact__mutmut_3, 
        'xǁSqliteDALǁensure_artifact__mutmut_4': xǁSqliteDALǁensure_artifact__mutmut_4, 
        'xǁSqliteDALǁensure_artifact__mutmut_5': xǁSqliteDALǁensure_artifact__mutmut_5, 
        'xǁSqliteDALǁensure_artifact__mutmut_6': xǁSqliteDALǁensure_artifact__mutmut_6, 
        'xǁSqliteDALǁensure_artifact__mutmut_7': xǁSqliteDALǁensure_artifact__mutmut_7, 
        'xǁSqliteDALǁensure_artifact__mutmut_8': xǁSqliteDALǁensure_artifact__mutmut_8, 
        'xǁSqliteDALǁensure_artifact__mutmut_9': xǁSqliteDALǁensure_artifact__mutmut_9, 
        'xǁSqliteDALǁensure_artifact__mutmut_10': xǁSqliteDALǁensure_artifact__mutmut_10, 
        'xǁSqliteDALǁensure_artifact__mutmut_11': xǁSqliteDALǁensure_artifact__mutmut_11, 
        'xǁSqliteDALǁensure_artifact__mutmut_12': xǁSqliteDALǁensure_artifact__mutmut_12, 
        'xǁSqliteDALǁensure_artifact__mutmut_13': xǁSqliteDALǁensure_artifact__mutmut_13, 
        'xǁSqliteDALǁensure_artifact__mutmut_14': xǁSqliteDALǁensure_artifact__mutmut_14, 
        'xǁSqliteDALǁensure_artifact__mutmut_15': xǁSqliteDALǁensure_artifact__mutmut_15, 
        'xǁSqliteDALǁensure_artifact__mutmut_16': xǁSqliteDALǁensure_artifact__mutmut_16, 
        'xǁSqliteDALǁensure_artifact__mutmut_17': xǁSqliteDALǁensure_artifact__mutmut_17, 
        'xǁSqliteDALǁensure_artifact__mutmut_18': xǁSqliteDALǁensure_artifact__mutmut_18, 
        'xǁSqliteDALǁensure_artifact__mutmut_19': xǁSqliteDALǁensure_artifact__mutmut_19, 
        'xǁSqliteDALǁensure_artifact__mutmut_20': xǁSqliteDALǁensure_artifact__mutmut_20, 
        'xǁSqliteDALǁensure_artifact__mutmut_21': xǁSqliteDALǁensure_artifact__mutmut_21, 
        'xǁSqliteDALǁensure_artifact__mutmut_22': xǁSqliteDALǁensure_artifact__mutmut_22, 
        'xǁSqliteDALǁensure_artifact__mutmut_23': xǁSqliteDALǁensure_artifact__mutmut_23, 
        'xǁSqliteDALǁensure_artifact__mutmut_24': xǁSqliteDALǁensure_artifact__mutmut_24, 
        'xǁSqliteDALǁensure_artifact__mutmut_25': xǁSqliteDALǁensure_artifact__mutmut_25, 
        'xǁSqliteDALǁensure_artifact__mutmut_26': xǁSqliteDALǁensure_artifact__mutmut_26, 
        'xǁSqliteDALǁensure_artifact__mutmut_27': xǁSqliteDALǁensure_artifact__mutmut_27, 
        'xǁSqliteDALǁensure_artifact__mutmut_28': xǁSqliteDALǁensure_artifact__mutmut_28, 
        'xǁSqliteDALǁensure_artifact__mutmut_29': xǁSqliteDALǁensure_artifact__mutmut_29, 
        'xǁSqliteDALǁensure_artifact__mutmut_30': xǁSqliteDALǁensure_artifact__mutmut_30, 
        'xǁSqliteDALǁensure_artifact__mutmut_31': xǁSqliteDALǁensure_artifact__mutmut_31, 
        'xǁSqliteDALǁensure_artifact__mutmut_32': xǁSqliteDALǁensure_artifact__mutmut_32, 
        'xǁSqliteDALǁensure_artifact__mutmut_33': xǁSqliteDALǁensure_artifact__mutmut_33, 
        'xǁSqliteDALǁensure_artifact__mutmut_34': xǁSqliteDALǁensure_artifact__mutmut_34, 
        'xǁSqliteDALǁensure_artifact__mutmut_35': xǁSqliteDALǁensure_artifact__mutmut_35, 
        'xǁSqliteDALǁensure_artifact__mutmut_36': xǁSqliteDALǁensure_artifact__mutmut_36, 
        'xǁSqliteDALǁensure_artifact__mutmut_37': xǁSqliteDALǁensure_artifact__mutmut_37, 
        'xǁSqliteDALǁensure_artifact__mutmut_38': xǁSqliteDALǁensure_artifact__mutmut_38, 
        'xǁSqliteDALǁensure_artifact__mutmut_39': xǁSqliteDALǁensure_artifact__mutmut_39, 
        'xǁSqliteDALǁensure_artifact__mutmut_40': xǁSqliteDALǁensure_artifact__mutmut_40, 
        'xǁSqliteDALǁensure_artifact__mutmut_41': xǁSqliteDALǁensure_artifact__mutmut_41, 
        'xǁSqliteDALǁensure_artifact__mutmut_42': xǁSqliteDALǁensure_artifact__mutmut_42, 
        'xǁSqliteDALǁensure_artifact__mutmut_43': xǁSqliteDALǁensure_artifact__mutmut_43, 
        'xǁSqliteDALǁensure_artifact__mutmut_44': xǁSqliteDALǁensure_artifact__mutmut_44, 
        'xǁSqliteDALǁensure_artifact__mutmut_45': xǁSqliteDALǁensure_artifact__mutmut_45, 
        'xǁSqliteDALǁensure_artifact__mutmut_46': xǁSqliteDALǁensure_artifact__mutmut_46, 
        'xǁSqliteDALǁensure_artifact__mutmut_47': xǁSqliteDALǁensure_artifact__mutmut_47, 
        'xǁSqliteDALǁensure_artifact__mutmut_48': xǁSqliteDALǁensure_artifact__mutmut_48, 
        'xǁSqliteDALǁensure_artifact__mutmut_49': xǁSqliteDALǁensure_artifact__mutmut_49, 
        'xǁSqliteDALǁensure_artifact__mutmut_50': xǁSqliteDALǁensure_artifact__mutmut_50, 
        'xǁSqliteDALǁensure_artifact__mutmut_51': xǁSqliteDALǁensure_artifact__mutmut_51, 
        'xǁSqliteDALǁensure_artifact__mutmut_52': xǁSqliteDALǁensure_artifact__mutmut_52, 
        'xǁSqliteDALǁensure_artifact__mutmut_53': xǁSqliteDALǁensure_artifact__mutmut_53, 
        'xǁSqliteDALǁensure_artifact__mutmut_54': xǁSqliteDALǁensure_artifact__mutmut_54, 
        'xǁSqliteDALǁensure_artifact__mutmut_55': xǁSqliteDALǁensure_artifact__mutmut_55
    }
    
    def ensure_artifact(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁensure_artifact__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁensure_artifact__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_artifact.__signature__ = _mutmut_signature(xǁSqliteDALǁensure_artifact__mutmut_orig)
    xǁSqliteDALǁensure_artifact__mutmut_orig.__name__ = 'xǁSqliteDALǁensure_artifact'

    def xǁSqliteDALǁinsert_item__mutmut_orig(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_1(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "XXcodeXX",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_2(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "CODE",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_3(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "XXcodexXX",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_4(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "CODEX",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_5(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = None
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_6(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(None)
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_7(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = None
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_8(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(None, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_9(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=None)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_10(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_11(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, )
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_12(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata and {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_13(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=False)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_14(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                None,
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_15(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                None,
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_16(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_17(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_18(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "XXINSERT INTO item (id, repo, path, commit_sha, language, kind, XX"
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_19(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "insert into item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_20(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO ITEM (ID, REPO, PATH, COMMIT_SHA, LANGUAGE, KIND, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_21(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "XXreason, artifact_id, metadata, archived_by, tombstone_id) XX"
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_22(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "REASON, ARTIFACT_ID, METADATA, ARCHIVED_BY, TOMBSTONE_ID) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_23(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "XXVALUES (?,?,?,?,?,?,?,?,?,?,?)XX"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_24(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "values (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_25(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"XXidXX": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_26(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"ID": iid, "tombstone_id": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_27(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "XXtombstone_idXX": tombstone_id}

    def xǁSqliteDALǁinsert_item__mutmut_28(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str,
        reason: str,
        artifact_id: str,
        tombstone_id: str,
        kind: str = "code",
        metadata: dict[str, Any] | None = None,
        archived_by: str = "codex",
    ) -> dict[str, Any]:
        iid = str(uuid.uuid4())
        meta_s = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO item (id, repo, path, commit_sha, language, kind, "
                    "reason, artifact_id, metadata, archived_by, tombstone_id) "
                    "VALUES (?,?,?,?,?,?,?,?,?,?,?)"
                ),
                (
                    iid,
                    repo,
                    path,
                    commit_sha,
                    language,
                    kind,
                    reason,
                    artifact_id,
                    meta_s,
                    archived_by,
                    tombstone_id,
                ),
            )
        return {"id": iid, "TOMBSTONE_ID": tombstone_id}
    
    xǁSqliteDALǁinsert_item__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁinsert_item__mutmut_1': xǁSqliteDALǁinsert_item__mutmut_1, 
        'xǁSqliteDALǁinsert_item__mutmut_2': xǁSqliteDALǁinsert_item__mutmut_2, 
        'xǁSqliteDALǁinsert_item__mutmut_3': xǁSqliteDALǁinsert_item__mutmut_3, 
        'xǁSqliteDALǁinsert_item__mutmut_4': xǁSqliteDALǁinsert_item__mutmut_4, 
        'xǁSqliteDALǁinsert_item__mutmut_5': xǁSqliteDALǁinsert_item__mutmut_5, 
        'xǁSqliteDALǁinsert_item__mutmut_6': xǁSqliteDALǁinsert_item__mutmut_6, 
        'xǁSqliteDALǁinsert_item__mutmut_7': xǁSqliteDALǁinsert_item__mutmut_7, 
        'xǁSqliteDALǁinsert_item__mutmut_8': xǁSqliteDALǁinsert_item__mutmut_8, 
        'xǁSqliteDALǁinsert_item__mutmut_9': xǁSqliteDALǁinsert_item__mutmut_9, 
        'xǁSqliteDALǁinsert_item__mutmut_10': xǁSqliteDALǁinsert_item__mutmut_10, 
        'xǁSqliteDALǁinsert_item__mutmut_11': xǁSqliteDALǁinsert_item__mutmut_11, 
        'xǁSqliteDALǁinsert_item__mutmut_12': xǁSqliteDALǁinsert_item__mutmut_12, 
        'xǁSqliteDALǁinsert_item__mutmut_13': xǁSqliteDALǁinsert_item__mutmut_13, 
        'xǁSqliteDALǁinsert_item__mutmut_14': xǁSqliteDALǁinsert_item__mutmut_14, 
        'xǁSqliteDALǁinsert_item__mutmut_15': xǁSqliteDALǁinsert_item__mutmut_15, 
        'xǁSqliteDALǁinsert_item__mutmut_16': xǁSqliteDALǁinsert_item__mutmut_16, 
        'xǁSqliteDALǁinsert_item__mutmut_17': xǁSqliteDALǁinsert_item__mutmut_17, 
        'xǁSqliteDALǁinsert_item__mutmut_18': xǁSqliteDALǁinsert_item__mutmut_18, 
        'xǁSqliteDALǁinsert_item__mutmut_19': xǁSqliteDALǁinsert_item__mutmut_19, 
        'xǁSqliteDALǁinsert_item__mutmut_20': xǁSqliteDALǁinsert_item__mutmut_20, 
        'xǁSqliteDALǁinsert_item__mutmut_21': xǁSqliteDALǁinsert_item__mutmut_21, 
        'xǁSqliteDALǁinsert_item__mutmut_22': xǁSqliteDALǁinsert_item__mutmut_22, 
        'xǁSqliteDALǁinsert_item__mutmut_23': xǁSqliteDALǁinsert_item__mutmut_23, 
        'xǁSqliteDALǁinsert_item__mutmut_24': xǁSqliteDALǁinsert_item__mutmut_24, 
        'xǁSqliteDALǁinsert_item__mutmut_25': xǁSqliteDALǁinsert_item__mutmut_25, 
        'xǁSqliteDALǁinsert_item__mutmut_26': xǁSqliteDALǁinsert_item__mutmut_26, 
        'xǁSqliteDALǁinsert_item__mutmut_27': xǁSqliteDALǁinsert_item__mutmut_27, 
        'xǁSqliteDALǁinsert_item__mutmut_28': xǁSqliteDALǁinsert_item__mutmut_28
    }
    
    def insert_item(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁinsert_item__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁinsert_item__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_item.__signature__ = _mutmut_signature(xǁSqliteDALǁinsert_item__mutmut_orig)
    xǁSqliteDALǁinsert_item__mutmut_orig.__name__ = 'xǁSqliteDALǁinsert_item'

    def xǁSqliteDALǁinsert_event__mutmut_orig(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_1(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = None
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_2(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(None)
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_3(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = None
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_4(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(None, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_5(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=None)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_6(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_7(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, )
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_8(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context and {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_9(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=False)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_10(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                None,
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_11(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                None,
            )

    def xǁSqliteDALǁinsert_event__mutmut_12(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_13(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                )

    def xǁSqliteDALǁinsert_event__mutmut_14(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("XXINSERT INTO event (id, item_id, action, actor, context) XX" "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_15(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("insert into event (id, item_id, action, actor, context) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_16(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO EVENT (ID, ITEM_ID, ACTION, ACTOR, CONTEXT) " "VALUES (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_17(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "XXVALUES (?,?,?,?,?)XX"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )

    def xǁSqliteDALǁinsert_event__mutmut_18(
        self,
        *,
        item_id: str,
        action: str,
        actor: str,
        context: dict[str, Any] | None = None,
    ) -> None:
        eid = str(uuid.uuid4())
        ctx = json.dumps(context or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                ("INSERT INTO event (id, item_id, action, actor, context) " "values (?,?,?,?,?)"),
                (
                    eid,
                    item_id,
                    action,
                    actor,
                    ctx,
                ),
            )
    
    xǁSqliteDALǁinsert_event__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁinsert_event__mutmut_1': xǁSqliteDALǁinsert_event__mutmut_1, 
        'xǁSqliteDALǁinsert_event__mutmut_2': xǁSqliteDALǁinsert_event__mutmut_2, 
        'xǁSqliteDALǁinsert_event__mutmut_3': xǁSqliteDALǁinsert_event__mutmut_3, 
        'xǁSqliteDALǁinsert_event__mutmut_4': xǁSqliteDALǁinsert_event__mutmut_4, 
        'xǁSqliteDALǁinsert_event__mutmut_5': xǁSqliteDALǁinsert_event__mutmut_5, 
        'xǁSqliteDALǁinsert_event__mutmut_6': xǁSqliteDALǁinsert_event__mutmut_6, 
        'xǁSqliteDALǁinsert_event__mutmut_7': xǁSqliteDALǁinsert_event__mutmut_7, 
        'xǁSqliteDALǁinsert_event__mutmut_8': xǁSqliteDALǁinsert_event__mutmut_8, 
        'xǁSqliteDALǁinsert_event__mutmut_9': xǁSqliteDALǁinsert_event__mutmut_9, 
        'xǁSqliteDALǁinsert_event__mutmut_10': xǁSqliteDALǁinsert_event__mutmut_10, 
        'xǁSqliteDALǁinsert_event__mutmut_11': xǁSqliteDALǁinsert_event__mutmut_11, 
        'xǁSqliteDALǁinsert_event__mutmut_12': xǁSqliteDALǁinsert_event__mutmut_12, 
        'xǁSqliteDALǁinsert_event__mutmut_13': xǁSqliteDALǁinsert_event__mutmut_13, 
        'xǁSqliteDALǁinsert_event__mutmut_14': xǁSqliteDALǁinsert_event__mutmut_14, 
        'xǁSqliteDALǁinsert_event__mutmut_15': xǁSqliteDALǁinsert_event__mutmut_15, 
        'xǁSqliteDALǁinsert_event__mutmut_16': xǁSqliteDALǁinsert_event__mutmut_16, 
        'xǁSqliteDALǁinsert_event__mutmut_17': xǁSqliteDALǁinsert_event__mutmut_17, 
        'xǁSqliteDALǁinsert_event__mutmut_18': xǁSqliteDALǁinsert_event__mutmut_18
    }
    
    def insert_event(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁinsert_event__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁinsert_event__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_event.__signature__ = _mutmut_signature(xǁSqliteDALǁinsert_event__mutmut_orig)
    xǁSqliteDALǁinsert_event__mutmut_orig.__name__ = 'xǁSqliteDALǁinsert_event'

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_orig(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_1(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = None
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_2(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            None,
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_3(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            None,
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_4(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_5(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_6(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "XXSELECT * FROM item WHERE tombstone_id = ?XX",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_7(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "select * from item where tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_8(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM ITEM WHERE TOMBSTONE_ID = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_9(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = None
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_10(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_11(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(None)
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_12(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = None
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_13(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            None,
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_14(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            None,
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_15(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_16(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_17(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "XXSELECT * FROM artifact WHERE id = ?XX",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_18(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "select * from artifact where id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_19(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM ARTIFACT WHERE ID = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_20(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["XXartifact_idXX"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_21(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["ARTIFACT_ID"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_22(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = None
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_23(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_24(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(None)
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_25(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['XXidXX']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_26(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['ID']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_27(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = None
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_28(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=None,
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_29(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=None,
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_30(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=None,
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_31(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=None,
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_32(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=None,
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_33(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=None,
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_34(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=None,
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_35(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=None,
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_36(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=None,
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_37(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=None,
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_38(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_39(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_40(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_41(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_42(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_43(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_44(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_45(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_46(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_47(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_48(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["XXidXX"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_49(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["ID"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_50(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["XXrepoXX"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_51(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["REPO"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_52(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["XXpathXX"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_53(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["PATH"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_54(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["XXcommit_shaXX"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_55(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["COMMIT_SHA"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_56(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] and "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_57(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["XXlanguageXX"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_58(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["LANGUAGE"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_59(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "XXXX",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_60(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["XXkindXX"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_61(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["KIND"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_62(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["XXreasonXX"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_63(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["REASON"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_64(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["XXartifact_idXX"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_65(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["ARTIFACT_ID"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_66(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(None),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_67(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] and "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_68(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["XXmetadataXX"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_69(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["METADATA"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_70(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "XX{}XX"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_71(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["XXtombstone_idXX"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_72(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["TOMBSTONE_ID"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_73(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = None
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_74(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=None,
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_75(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=None,
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_76(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=None,
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_77(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=None,
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_78(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=None,
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_79(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=None,
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_80(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=None,
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_81(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=None,
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_82(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_83(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_84(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_85(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_86(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_87(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_88(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_89(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_90(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["XXidXX"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_91(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["ID"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_92(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["XXcontent_sha256XX"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_93(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["CONTENT_SHA256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_94(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["XXsize_bytesXX"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_95(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["SIZE_BYTES"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_96(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["XXcompressionXX"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_97(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["COMPRESSION"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_98(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["XXmime_typeXX"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_99(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["MIME_TYPE"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_100(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["XXstorage_driverXX"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_101(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["STORAGE_DRIVER"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_102(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["XXblob_bytesXX"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_103(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["BLOB_BYTES"],
            object_url=ar["object_url"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_104(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["XXobject_urlXX"],
        )
        return item, art

    def xǁSqliteDALǁfetch_by_tombstone__mutmut_105(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.execute(
            "SELECT * FROM item WHERE tombstone_id = ?",
            (tombstone_id,),
        )
        it = cur.fetchone()
        if not it:
            raise KeyError(f"Tombstone not found: {tombstone_id}")
        cur = self.conn.execute(
            "SELECT * FROM artifact WHERE id = ?",
            (it["artifact_id"],),
        )
        ar = cur.fetchone()
        if not ar:
            raise KeyError(f"Artifact missing for item {it['id']}")
        item = ItemRow(
            id=it["id"],
            repo=it["repo"],
            path=it["path"],
            commit_sha=it["commit_sha"],
            language=it["language"] or "",
            kind=it["kind"],
            reason=it["reason"],
            artifact_id=it["artifact_id"],
            metadata=json.loads(it["metadata"] or "{}"),
            tombstone_id=it["tombstone_id"],
        )
        art = ArtifactRow(
            id=ar["id"],
            content_sha256=ar["content_sha256"],
            size_bytes=ar["size_bytes"],
            compression=ar["compression"],
            mime_type=ar["mime_type"],
            storage_driver=ar["storage_driver"],
            blob_bytes=ar["blob_bytes"],
            object_url=ar["OBJECT_URL"],
        )
        return item, art
    
    xǁSqliteDALǁfetch_by_tombstone__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁfetch_by_tombstone__mutmut_1': xǁSqliteDALǁfetch_by_tombstone__mutmut_1, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_2': xǁSqliteDALǁfetch_by_tombstone__mutmut_2, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_3': xǁSqliteDALǁfetch_by_tombstone__mutmut_3, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_4': xǁSqliteDALǁfetch_by_tombstone__mutmut_4, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_5': xǁSqliteDALǁfetch_by_tombstone__mutmut_5, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_6': xǁSqliteDALǁfetch_by_tombstone__mutmut_6, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_7': xǁSqliteDALǁfetch_by_tombstone__mutmut_7, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_8': xǁSqliteDALǁfetch_by_tombstone__mutmut_8, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_9': xǁSqliteDALǁfetch_by_tombstone__mutmut_9, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_10': xǁSqliteDALǁfetch_by_tombstone__mutmut_10, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_11': xǁSqliteDALǁfetch_by_tombstone__mutmut_11, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_12': xǁSqliteDALǁfetch_by_tombstone__mutmut_12, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_13': xǁSqliteDALǁfetch_by_tombstone__mutmut_13, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_14': xǁSqliteDALǁfetch_by_tombstone__mutmut_14, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_15': xǁSqliteDALǁfetch_by_tombstone__mutmut_15, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_16': xǁSqliteDALǁfetch_by_tombstone__mutmut_16, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_17': xǁSqliteDALǁfetch_by_tombstone__mutmut_17, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_18': xǁSqliteDALǁfetch_by_tombstone__mutmut_18, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_19': xǁSqliteDALǁfetch_by_tombstone__mutmut_19, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_20': xǁSqliteDALǁfetch_by_tombstone__mutmut_20, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_21': xǁSqliteDALǁfetch_by_tombstone__mutmut_21, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_22': xǁSqliteDALǁfetch_by_tombstone__mutmut_22, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_23': xǁSqliteDALǁfetch_by_tombstone__mutmut_23, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_24': xǁSqliteDALǁfetch_by_tombstone__mutmut_24, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_25': xǁSqliteDALǁfetch_by_tombstone__mutmut_25, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_26': xǁSqliteDALǁfetch_by_tombstone__mutmut_26, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_27': xǁSqliteDALǁfetch_by_tombstone__mutmut_27, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_28': xǁSqliteDALǁfetch_by_tombstone__mutmut_28, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_29': xǁSqliteDALǁfetch_by_tombstone__mutmut_29, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_30': xǁSqliteDALǁfetch_by_tombstone__mutmut_30, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_31': xǁSqliteDALǁfetch_by_tombstone__mutmut_31, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_32': xǁSqliteDALǁfetch_by_tombstone__mutmut_32, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_33': xǁSqliteDALǁfetch_by_tombstone__mutmut_33, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_34': xǁSqliteDALǁfetch_by_tombstone__mutmut_34, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_35': xǁSqliteDALǁfetch_by_tombstone__mutmut_35, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_36': xǁSqliteDALǁfetch_by_tombstone__mutmut_36, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_37': xǁSqliteDALǁfetch_by_tombstone__mutmut_37, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_38': xǁSqliteDALǁfetch_by_tombstone__mutmut_38, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_39': xǁSqliteDALǁfetch_by_tombstone__mutmut_39, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_40': xǁSqliteDALǁfetch_by_tombstone__mutmut_40, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_41': xǁSqliteDALǁfetch_by_tombstone__mutmut_41, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_42': xǁSqliteDALǁfetch_by_tombstone__mutmut_42, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_43': xǁSqliteDALǁfetch_by_tombstone__mutmut_43, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_44': xǁSqliteDALǁfetch_by_tombstone__mutmut_44, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_45': xǁSqliteDALǁfetch_by_tombstone__mutmut_45, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_46': xǁSqliteDALǁfetch_by_tombstone__mutmut_46, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_47': xǁSqliteDALǁfetch_by_tombstone__mutmut_47, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_48': xǁSqliteDALǁfetch_by_tombstone__mutmut_48, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_49': xǁSqliteDALǁfetch_by_tombstone__mutmut_49, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_50': xǁSqliteDALǁfetch_by_tombstone__mutmut_50, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_51': xǁSqliteDALǁfetch_by_tombstone__mutmut_51, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_52': xǁSqliteDALǁfetch_by_tombstone__mutmut_52, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_53': xǁSqliteDALǁfetch_by_tombstone__mutmut_53, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_54': xǁSqliteDALǁfetch_by_tombstone__mutmut_54, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_55': xǁSqliteDALǁfetch_by_tombstone__mutmut_55, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_56': xǁSqliteDALǁfetch_by_tombstone__mutmut_56, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_57': xǁSqliteDALǁfetch_by_tombstone__mutmut_57, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_58': xǁSqliteDALǁfetch_by_tombstone__mutmut_58, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_59': xǁSqliteDALǁfetch_by_tombstone__mutmut_59, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_60': xǁSqliteDALǁfetch_by_tombstone__mutmut_60, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_61': xǁSqliteDALǁfetch_by_tombstone__mutmut_61, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_62': xǁSqliteDALǁfetch_by_tombstone__mutmut_62, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_63': xǁSqliteDALǁfetch_by_tombstone__mutmut_63, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_64': xǁSqliteDALǁfetch_by_tombstone__mutmut_64, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_65': xǁSqliteDALǁfetch_by_tombstone__mutmut_65, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_66': xǁSqliteDALǁfetch_by_tombstone__mutmut_66, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_67': xǁSqliteDALǁfetch_by_tombstone__mutmut_67, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_68': xǁSqliteDALǁfetch_by_tombstone__mutmut_68, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_69': xǁSqliteDALǁfetch_by_tombstone__mutmut_69, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_70': xǁSqliteDALǁfetch_by_tombstone__mutmut_70, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_71': xǁSqliteDALǁfetch_by_tombstone__mutmut_71, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_72': xǁSqliteDALǁfetch_by_tombstone__mutmut_72, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_73': xǁSqliteDALǁfetch_by_tombstone__mutmut_73, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_74': xǁSqliteDALǁfetch_by_tombstone__mutmut_74, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_75': xǁSqliteDALǁfetch_by_tombstone__mutmut_75, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_76': xǁSqliteDALǁfetch_by_tombstone__mutmut_76, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_77': xǁSqliteDALǁfetch_by_tombstone__mutmut_77, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_78': xǁSqliteDALǁfetch_by_tombstone__mutmut_78, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_79': xǁSqliteDALǁfetch_by_tombstone__mutmut_79, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_80': xǁSqliteDALǁfetch_by_tombstone__mutmut_80, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_81': xǁSqliteDALǁfetch_by_tombstone__mutmut_81, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_82': xǁSqliteDALǁfetch_by_tombstone__mutmut_82, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_83': xǁSqliteDALǁfetch_by_tombstone__mutmut_83, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_84': xǁSqliteDALǁfetch_by_tombstone__mutmut_84, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_85': xǁSqliteDALǁfetch_by_tombstone__mutmut_85, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_86': xǁSqliteDALǁfetch_by_tombstone__mutmut_86, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_87': xǁSqliteDALǁfetch_by_tombstone__mutmut_87, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_88': xǁSqliteDALǁfetch_by_tombstone__mutmut_88, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_89': xǁSqliteDALǁfetch_by_tombstone__mutmut_89, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_90': xǁSqliteDALǁfetch_by_tombstone__mutmut_90, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_91': xǁSqliteDALǁfetch_by_tombstone__mutmut_91, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_92': xǁSqliteDALǁfetch_by_tombstone__mutmut_92, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_93': xǁSqliteDALǁfetch_by_tombstone__mutmut_93, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_94': xǁSqliteDALǁfetch_by_tombstone__mutmut_94, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_95': xǁSqliteDALǁfetch_by_tombstone__mutmut_95, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_96': xǁSqliteDALǁfetch_by_tombstone__mutmut_96, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_97': xǁSqliteDALǁfetch_by_tombstone__mutmut_97, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_98': xǁSqliteDALǁfetch_by_tombstone__mutmut_98, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_99': xǁSqliteDALǁfetch_by_tombstone__mutmut_99, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_100': xǁSqliteDALǁfetch_by_tombstone__mutmut_100, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_101': xǁSqliteDALǁfetch_by_tombstone__mutmut_101, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_102': xǁSqliteDALǁfetch_by_tombstone__mutmut_102, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_103': xǁSqliteDALǁfetch_by_tombstone__mutmut_103, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_104': xǁSqliteDALǁfetch_by_tombstone__mutmut_104, 
        'xǁSqliteDALǁfetch_by_tombstone__mutmut_105': xǁSqliteDALǁfetch_by_tombstone__mutmut_105
    }
    
    def fetch_by_tombstone(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁfetch_by_tombstone__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁfetch_by_tombstone__mutmut_mutants"), args, kwargs, self)
        return result 
    
    fetch_by_tombstone.__signature__ = _mutmut_signature(xǁSqliteDALǁfetch_by_tombstone__mutmut_orig)
    xǁSqliteDALǁfetch_by_tombstone__mutmut_orig.__name__ = 'xǁSqliteDALǁfetch_by_tombstone'

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_orig(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_1(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = None
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_2(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(None)
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_3(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = None
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_4(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(None, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_5(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=None, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_6(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=None)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_7(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_8(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_9(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, )
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_10(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata and {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_11(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=True, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_12(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=False)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_13(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                None,
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_14(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                None,
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_15(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_16(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_17(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "XXINSERT INTO release_meta (id, release_id, version, created_at, actor, XX"
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_18(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "insert into release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_19(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO RELEASE_META (ID, RELEASE_ID, VERSION, CREATED_AT, ACTOR, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_20(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "XXmetadata) VALUES (?,?,?,?,?,?)XX"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_21(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) values (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_22(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "METADATA) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_23(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"XXidXX": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_24(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"ID": rid, "release_id": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_25(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "XXrelease_idXX": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_26(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "RELEASE_ID": release_id, "version": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_27(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "XXversionXX": version}

    # -------- Release persistence (sqlite) --------
    def xǁSqliteDALǁcreate_release_meta__mutmut_28(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_meta (id, release_id, version, created_at, actor, "
                    "metadata) VALUES (?,?,?,?,?,?)"
                ),
                (rid, release_id, version, created_at, actor, payload),
            )
        return {"id": rid, "release_id": release_id, "VERSION": version}
    
    xǁSqliteDALǁcreate_release_meta__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁcreate_release_meta__mutmut_1': xǁSqliteDALǁcreate_release_meta__mutmut_1, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_2': xǁSqliteDALǁcreate_release_meta__mutmut_2, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_3': xǁSqliteDALǁcreate_release_meta__mutmut_3, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_4': xǁSqliteDALǁcreate_release_meta__mutmut_4, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_5': xǁSqliteDALǁcreate_release_meta__mutmut_5, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_6': xǁSqliteDALǁcreate_release_meta__mutmut_6, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_7': xǁSqliteDALǁcreate_release_meta__mutmut_7, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_8': xǁSqliteDALǁcreate_release_meta__mutmut_8, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_9': xǁSqliteDALǁcreate_release_meta__mutmut_9, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_10': xǁSqliteDALǁcreate_release_meta__mutmut_10, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_11': xǁSqliteDALǁcreate_release_meta__mutmut_11, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_12': xǁSqliteDALǁcreate_release_meta__mutmut_12, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_13': xǁSqliteDALǁcreate_release_meta__mutmut_13, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_14': xǁSqliteDALǁcreate_release_meta__mutmut_14, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_15': xǁSqliteDALǁcreate_release_meta__mutmut_15, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_16': xǁSqliteDALǁcreate_release_meta__mutmut_16, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_17': xǁSqliteDALǁcreate_release_meta__mutmut_17, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_18': xǁSqliteDALǁcreate_release_meta__mutmut_18, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_19': xǁSqliteDALǁcreate_release_meta__mutmut_19, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_20': xǁSqliteDALǁcreate_release_meta__mutmut_20, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_21': xǁSqliteDALǁcreate_release_meta__mutmut_21, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_22': xǁSqliteDALǁcreate_release_meta__mutmut_22, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_23': xǁSqliteDALǁcreate_release_meta__mutmut_23, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_24': xǁSqliteDALǁcreate_release_meta__mutmut_24, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_25': xǁSqliteDALǁcreate_release_meta__mutmut_25, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_26': xǁSqliteDALǁcreate_release_meta__mutmut_26, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_27': xǁSqliteDALǁcreate_release_meta__mutmut_27, 
        'xǁSqliteDALǁcreate_release_meta__mutmut_28': xǁSqliteDALǁcreate_release_meta__mutmut_28
    }
    
    def create_release_meta(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁcreate_release_meta__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁcreate_release_meta__mutmut_mutants"), args, kwargs, self)
        return result 
    
    create_release_meta.__signature__ = _mutmut_signature(xǁSqliteDALǁcreate_release_meta__mutmut_orig)
    xǁSqliteDALǁcreate_release_meta__mutmut_orig.__name__ = 'xǁSqliteDALǁcreate_release_meta'

    def xǁSqliteDALǁadd_release_component__mutmut_orig(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_1(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = None
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_2(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(None)
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_3(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = None
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_4(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(None, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_5(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=None, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_6(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=None)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_7(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_8(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_9(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, )
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_10(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars and {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_11(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=True, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_12(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=False)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_13(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                None,
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_14(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                None,
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_15(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_16(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_17(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "XXINSERT INTO release_component (id, release_id, item_id, tombstone, XX"
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_18(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "insert into release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_19(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO RELEASE_COMPONENT (ID, RELEASE_ID, ITEM_ID, TOMBSTONE, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_20(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "XXdest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)XX"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_21(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) values (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_22(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "DEST_PATH, MODE, TEMPLATE_VARS) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_23(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"XXidXX": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_24(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"ID": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_25(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "XXrelease_idXX": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_26(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "RELEASE_ID": release_meta_id, "tombstone": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_27(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "XXtombstoneXX": tombstone}

    def xǁSqliteDALǁadd_release_component__mutmut_28(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        self._ensure_release_tables()
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            self.conn.execute(
                (
                    "INSERT INTO release_component (id, release_id, item_id, tombstone, "
                    "dest_path, mode, template_vars) VALUES (?,?,?,?,?,?,?)"
                ),
                (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
            )
        return {"id": cid, "release_id": release_meta_id, "TOMBSTONE": tombstone}
    
    xǁSqliteDALǁadd_release_component__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁadd_release_component__mutmut_1': xǁSqliteDALǁadd_release_component__mutmut_1, 
        'xǁSqliteDALǁadd_release_component__mutmut_2': xǁSqliteDALǁadd_release_component__mutmut_2, 
        'xǁSqliteDALǁadd_release_component__mutmut_3': xǁSqliteDALǁadd_release_component__mutmut_3, 
        'xǁSqliteDALǁadd_release_component__mutmut_4': xǁSqliteDALǁadd_release_component__mutmut_4, 
        'xǁSqliteDALǁadd_release_component__mutmut_5': xǁSqliteDALǁadd_release_component__mutmut_5, 
        'xǁSqliteDALǁadd_release_component__mutmut_6': xǁSqliteDALǁadd_release_component__mutmut_6, 
        'xǁSqliteDALǁadd_release_component__mutmut_7': xǁSqliteDALǁadd_release_component__mutmut_7, 
        'xǁSqliteDALǁadd_release_component__mutmut_8': xǁSqliteDALǁadd_release_component__mutmut_8, 
        'xǁSqliteDALǁadd_release_component__mutmut_9': xǁSqliteDALǁadd_release_component__mutmut_9, 
        'xǁSqliteDALǁadd_release_component__mutmut_10': xǁSqliteDALǁadd_release_component__mutmut_10, 
        'xǁSqliteDALǁadd_release_component__mutmut_11': xǁSqliteDALǁadd_release_component__mutmut_11, 
        'xǁSqliteDALǁadd_release_component__mutmut_12': xǁSqliteDALǁadd_release_component__mutmut_12, 
        'xǁSqliteDALǁadd_release_component__mutmut_13': xǁSqliteDALǁadd_release_component__mutmut_13, 
        'xǁSqliteDALǁadd_release_component__mutmut_14': xǁSqliteDALǁadd_release_component__mutmut_14, 
        'xǁSqliteDALǁadd_release_component__mutmut_15': xǁSqliteDALǁadd_release_component__mutmut_15, 
        'xǁSqliteDALǁadd_release_component__mutmut_16': xǁSqliteDALǁadd_release_component__mutmut_16, 
        'xǁSqliteDALǁadd_release_component__mutmut_17': xǁSqliteDALǁadd_release_component__mutmut_17, 
        'xǁSqliteDALǁadd_release_component__mutmut_18': xǁSqliteDALǁadd_release_component__mutmut_18, 
        'xǁSqliteDALǁadd_release_component__mutmut_19': xǁSqliteDALǁadd_release_component__mutmut_19, 
        'xǁSqliteDALǁadd_release_component__mutmut_20': xǁSqliteDALǁadd_release_component__mutmut_20, 
        'xǁSqliteDALǁadd_release_component__mutmut_21': xǁSqliteDALǁadd_release_component__mutmut_21, 
        'xǁSqliteDALǁadd_release_component__mutmut_22': xǁSqliteDALǁadd_release_component__mutmut_22, 
        'xǁSqliteDALǁadd_release_component__mutmut_23': xǁSqliteDALǁadd_release_component__mutmut_23, 
        'xǁSqliteDALǁadd_release_component__mutmut_24': xǁSqliteDALǁadd_release_component__mutmut_24, 
        'xǁSqliteDALǁadd_release_component__mutmut_25': xǁSqliteDALǁadd_release_component__mutmut_25, 
        'xǁSqliteDALǁadd_release_component__mutmut_26': xǁSqliteDALǁadd_release_component__mutmut_26, 
        'xǁSqliteDALǁadd_release_component__mutmut_27': xǁSqliteDALǁadd_release_component__mutmut_27, 
        'xǁSqliteDALǁadd_release_component__mutmut_28': xǁSqliteDALǁadd_release_component__mutmut_28
    }
    
    def add_release_component(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁadd_release_component__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁadd_release_component__mutmut_mutants"), args, kwargs, self)
        return result 
    
    add_release_component.__signature__ = _mutmut_signature(xǁSqliteDALǁadd_release_component__mutmut_orig)
    xǁSqliteDALǁadd_release_component__mutmut_orig.__name__ = 'xǁSqliteDALǁadd_release_component'

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_orig(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_1(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = None
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_2(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            None,
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_3(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            None,
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_4(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_5(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_6(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "XXSELECT * FROM release_meta WHERE release_id = ?XX",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_7(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "select * from release_meta where release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_8(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM RELEASE_META WHERE RELEASE_ID = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_9(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = None
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_10(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_11(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = None
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_12(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(None)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_13(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = None
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_14(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get(None)
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_15(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("XXmetadataXX")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_16(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("METADATA")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_17(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(None):
                data["metadata"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_18(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = None
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_19(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["XXmetadataXX"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_20(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["METADATA"] = json.loads(meta_raw)
        return data

    def xǁSqliteDALǁget_release_meta_by_release_id__mutmut_21(self, *, release_id: str) -> dict[str, Any] | None:
        self._ensure_release_tables()
        cur = self.conn.execute(
            "SELECT * FROM release_meta WHERE release_id = ?",
            (release_id,),
        )
        row = cur.fetchone()
        if not row:
            return None
        data = dict(row)
        meta_raw = data.get("metadata")
        if isinstance(meta_raw, str):
            with contextlib.suppress(json.JSONDecodeError):
                data["metadata"] = json.loads(None)
        return data
    
    xǁSqliteDALǁget_release_meta_by_release_id__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_1': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_1, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_2': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_2, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_3': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_3, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_4': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_4, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_5': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_5, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_6': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_6, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_7': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_7, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_8': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_8, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_9': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_9, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_10': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_10, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_11': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_11, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_12': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_12, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_13': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_13, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_14': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_14, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_15': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_15, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_16': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_16, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_17': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_17, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_18': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_18, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_19': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_19, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_20': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_20, 
        'xǁSqliteDALǁget_release_meta_by_release_id__mutmut_21': xǁSqliteDALǁget_release_meta_by_release_id__mutmut_21
    }
    
    def get_release_meta_by_release_id(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁSqliteDALǁget_release_meta_by_release_id__mutmut_orig"), object.__getattribute__(self, "xǁSqliteDALǁget_release_meta_by_release_id__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_release_meta_by_release_id.__signature__ = _mutmut_signature(xǁSqliteDALǁget_release_meta_by_release_id__mutmut_orig)
    xǁSqliteDALǁget_release_meta_by_release_id__mutmut_orig.__name__ = 'xǁSqliteDALǁget_release_meta_by_release_id'


class PostgresDAL(BaseDAL):
    _ALLOWED_TABLES = frozenset(
        {
            "artifact",
            "item",
            "event",
            "referent",
            "release_meta",
            "release_component",
        }
    )

    def xǁPostgresDALǁ__init____mutmut_orig(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("psycopg (v3) is required for postgres backend") from e
        self.pg = psycopg
        self.conn = self.pg.connect(self.dsn)
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_1(self, dsn: str):
        self.dsn = None
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("psycopg (v3) is required for postgres backend") from e
        self.pg = psycopg
        self.conn = self.pg.connect(self.dsn)
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_2(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError(None) from e
        self.pg = psycopg
        self.conn = self.pg.connect(self.dsn)
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_3(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("XXpsycopg (v3) is required for postgres backendXX") from e
        self.pg = psycopg
        self.conn = self.pg.connect(self.dsn)
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_4(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("PSYCOPG (V3) IS REQUIRED FOR POSTGRES BACKEND") from e
        self.pg = psycopg
        self.conn = self.pg.connect(self.dsn)
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_5(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("psycopg (v3) is required for postgres backend") from e
        self.pg = None
        self.conn = self.pg.connect(self.dsn)
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_6(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("psycopg (v3) is required for postgres backend") from e
        self.pg = psycopg
        self.conn = None
        self.ensure_schema()

    def xǁPostgresDALǁ__init____mutmut_7(self, dsn: str):
        self.dsn = dsn
        try:
            import psycopg
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("psycopg (v3) is required for postgres backend") from e
        self.pg = psycopg
        self.conn = self.pg.connect(None)
        self.ensure_schema()
    
    xǁPostgresDALǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁ__init____mutmut_1': xǁPostgresDALǁ__init____mutmut_1, 
        'xǁPostgresDALǁ__init____mutmut_2': xǁPostgresDALǁ__init____mutmut_2, 
        'xǁPostgresDALǁ__init____mutmut_3': xǁPostgresDALǁ__init____mutmut_3, 
        'xǁPostgresDALǁ__init____mutmut_4': xǁPostgresDALǁ__init____mutmut_4, 
        'xǁPostgresDALǁ__init____mutmut_5': xǁPostgresDALǁ__init____mutmut_5, 
        'xǁPostgresDALǁ__init____mutmut_6': xǁPostgresDALǁ__init____mutmut_6, 
        'xǁPostgresDALǁ__init____mutmut_7': xǁPostgresDALǁ__init____mutmut_7
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁPostgresDALǁ__init____mutmut_orig)
    xǁPostgresDALǁ__init____mutmut_orig.__name__ = 'xǁPostgresDALǁ__init__'

    def xǁPostgresDALǁ_sql_ident__mutmut_orig(self, name: str):
        from psycopg import sql  # type: ignore

        self.validate_identifier(name, self._ALLOWED_TABLES)
        return sql.Identifier(name)

    def xǁPostgresDALǁ_sql_ident__mutmut_1(self, name: str):
        from psycopg import sql  # type: ignore

        self.validate_identifier(None, self._ALLOWED_TABLES)
        return sql.Identifier(name)

    def xǁPostgresDALǁ_sql_ident__mutmut_2(self, name: str):
        from psycopg import sql  # type: ignore

        self.validate_identifier(name, None)
        return sql.Identifier(name)

    def xǁPostgresDALǁ_sql_ident__mutmut_3(self, name: str):
        from psycopg import sql  # type: ignore

        self.validate_identifier(self._ALLOWED_TABLES)
        return sql.Identifier(name)

    def xǁPostgresDALǁ_sql_ident__mutmut_4(self, name: str):
        from psycopg import sql  # type: ignore

        self.validate_identifier(name, )
        return sql.Identifier(name)

    def xǁPostgresDALǁ_sql_ident__mutmut_5(self, name: str):
        from psycopg import sql  # type: ignore

        self.validate_identifier(name, self._ALLOWED_TABLES)
        return sql.Identifier(None)
    
    xǁPostgresDALǁ_sql_ident__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁ_sql_ident__mutmut_1': xǁPostgresDALǁ_sql_ident__mutmut_1, 
        'xǁPostgresDALǁ_sql_ident__mutmut_2': xǁPostgresDALǁ_sql_ident__mutmut_2, 
        'xǁPostgresDALǁ_sql_ident__mutmut_3': xǁPostgresDALǁ_sql_ident__mutmut_3, 
        'xǁPostgresDALǁ_sql_ident__mutmut_4': xǁPostgresDALǁ_sql_ident__mutmut_4, 
        'xǁPostgresDALǁ_sql_ident__mutmut_5': xǁPostgresDALǁ_sql_ident__mutmut_5
    }
    
    def _sql_ident(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁ_sql_ident__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁ_sql_ident__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _sql_ident.__signature__ = _mutmut_signature(xǁPostgresDALǁ_sql_ident__mutmut_orig)
    xǁPostgresDALǁ_sql_ident__mutmut_orig.__name__ = 'xǁPostgresDALǁ_sql_ident'

    @staticmethod
    def from_url(url: str) -> PostgresDAL:
        return PostgresDAL(url)

    def xǁPostgresDALǁtxn__mutmut_orig(self) -> contextlib.AbstractContextManager[None]:
        return cast(contextlib.AbstractContextManager[None], self.conn.transaction())

    def xǁPostgresDALǁtxn__mutmut_1(self) -> contextlib.AbstractContextManager[None]:
        return cast(None, self.conn.transaction())

    def xǁPostgresDALǁtxn__mutmut_2(self) -> contextlib.AbstractContextManager[None]:
        return cast(contextlib.AbstractContextManager[None], None)

    def xǁPostgresDALǁtxn__mutmut_3(self) -> contextlib.AbstractContextManager[None]:
        return cast(self.conn.transaction())

    def xǁPostgresDALǁtxn__mutmut_4(self) -> contextlib.AbstractContextManager[None]:
        return cast(contextlib.AbstractContextManager[None], )
    
    xǁPostgresDALǁtxn__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁtxn__mutmut_1': xǁPostgresDALǁtxn__mutmut_1, 
        'xǁPostgresDALǁtxn__mutmut_2': xǁPostgresDALǁtxn__mutmut_2, 
        'xǁPostgresDALǁtxn__mutmut_3': xǁPostgresDALǁtxn__mutmut_3, 
        'xǁPostgresDALǁtxn__mutmut_4': xǁPostgresDALǁtxn__mutmut_4
    }
    
    def txn(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁtxn__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁtxn__mutmut_mutants"), args, kwargs, self)
        return result 
    
    txn.__signature__ = _mutmut_signature(xǁPostgresDALǁtxn__mutmut_orig)
    xǁPostgresDALǁtxn__mutmut_orig.__name__ = 'xǁPostgresDALǁtxn'

    def xǁPostgresDALǁensure_schema__mutmut_orig(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_1(self) -> None:
        here = None
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_2(self) -> None:
        here = Path(None).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_3(self) -> None:
        here = Path(__file__).resolve()
        root = None
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_4(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[4]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_5(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = None
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_6(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" * "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_7(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" * "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_8(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root * "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_9(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "XXdbXX" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_10(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "DB" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_11(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "XXmigrationsXX" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_12(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "MIGRATIONS" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_13(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "XXpostgresXX"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_14(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "POSTGRES"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_15(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = None
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_16(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(None)
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_17(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob(None))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_18(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("XX*.sqlXX"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_19(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.SQL"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_20(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_21(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = None
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_22(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = None
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_23(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding=None)
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_24(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="XXutf-8XX")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_25(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="UTF-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_26(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(None):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_27(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split("XX;XX"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁPostgresDALǁensure_schema__mutmut_28(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "postgres"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(None)
            finally:
                cur.close()
    
    xǁPostgresDALǁensure_schema__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁensure_schema__mutmut_1': xǁPostgresDALǁensure_schema__mutmut_1, 
        'xǁPostgresDALǁensure_schema__mutmut_2': xǁPostgresDALǁensure_schema__mutmut_2, 
        'xǁPostgresDALǁensure_schema__mutmut_3': xǁPostgresDALǁensure_schema__mutmut_3, 
        'xǁPostgresDALǁensure_schema__mutmut_4': xǁPostgresDALǁensure_schema__mutmut_4, 
        'xǁPostgresDALǁensure_schema__mutmut_5': xǁPostgresDALǁensure_schema__mutmut_5, 
        'xǁPostgresDALǁensure_schema__mutmut_6': xǁPostgresDALǁensure_schema__mutmut_6, 
        'xǁPostgresDALǁensure_schema__mutmut_7': xǁPostgresDALǁensure_schema__mutmut_7, 
        'xǁPostgresDALǁensure_schema__mutmut_8': xǁPostgresDALǁensure_schema__mutmut_8, 
        'xǁPostgresDALǁensure_schema__mutmut_9': xǁPostgresDALǁensure_schema__mutmut_9, 
        'xǁPostgresDALǁensure_schema__mutmut_10': xǁPostgresDALǁensure_schema__mutmut_10, 
        'xǁPostgresDALǁensure_schema__mutmut_11': xǁPostgresDALǁensure_schema__mutmut_11, 
        'xǁPostgresDALǁensure_schema__mutmut_12': xǁPostgresDALǁensure_schema__mutmut_12, 
        'xǁPostgresDALǁensure_schema__mutmut_13': xǁPostgresDALǁensure_schema__mutmut_13, 
        'xǁPostgresDALǁensure_schema__mutmut_14': xǁPostgresDALǁensure_schema__mutmut_14, 
        'xǁPostgresDALǁensure_schema__mutmut_15': xǁPostgresDALǁensure_schema__mutmut_15, 
        'xǁPostgresDALǁensure_schema__mutmut_16': xǁPostgresDALǁensure_schema__mutmut_16, 
        'xǁPostgresDALǁensure_schema__mutmut_17': xǁPostgresDALǁensure_schema__mutmut_17, 
        'xǁPostgresDALǁensure_schema__mutmut_18': xǁPostgresDALǁensure_schema__mutmut_18, 
        'xǁPostgresDALǁensure_schema__mutmut_19': xǁPostgresDALǁensure_schema__mutmut_19, 
        'xǁPostgresDALǁensure_schema__mutmut_20': xǁPostgresDALǁensure_schema__mutmut_20, 
        'xǁPostgresDALǁensure_schema__mutmut_21': xǁPostgresDALǁensure_schema__mutmut_21, 
        'xǁPostgresDALǁensure_schema__mutmut_22': xǁPostgresDALǁensure_schema__mutmut_22, 
        'xǁPostgresDALǁensure_schema__mutmut_23': xǁPostgresDALǁensure_schema__mutmut_23, 
        'xǁPostgresDALǁensure_schema__mutmut_24': xǁPostgresDALǁensure_schema__mutmut_24, 
        'xǁPostgresDALǁensure_schema__mutmut_25': xǁPostgresDALǁensure_schema__mutmut_25, 
        'xǁPostgresDALǁensure_schema__mutmut_26': xǁPostgresDALǁensure_schema__mutmut_26, 
        'xǁPostgresDALǁensure_schema__mutmut_27': xǁPostgresDALǁensure_schema__mutmut_27, 
        'xǁPostgresDALǁensure_schema__mutmut_28': xǁPostgresDALǁensure_schema__mutmut_28
    }
    
    def ensure_schema(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁensure_schema__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁensure_schema__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_schema.__signature__ = _mutmut_signature(xǁPostgresDALǁensure_schema__mutmut_orig)
    xǁPostgresDALǁensure_schema__mutmut_orig.__name__ = 'xǁPostgresDALǁensure_schema'

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_orig(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_1(self) -> None:  # pragma: no cover - runtime guard
        cur = None
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_2(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute(None)
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_3(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("XXSELECT 1 FROM pg_extension WHERE extname='pgcrypto'XX")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_4(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("select 1 from pg_extension where extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_5(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM PG_EXTENSION WHERE EXTNAME='PGCRYPTO'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_6(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_7(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    None
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_8(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "XXPostgres missing extension 'pgcrypto'. Run: XX"
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_9(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "postgres missing extension 'pgcrypto'. run: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_10(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "POSTGRES MISSING EXTENSION 'PGCRYPTO'. RUN: "
                    "CREATE EXTENSION IF NOT EXISTS pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_11(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "XXCREATE EXTENSION IF NOT EXISTS pgcrypto;XX"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_12(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "create extension if not exists pgcrypto;"
                )
        finally:
            cur.close()

    def xǁPostgresDALǁ_ensure_pgcrypto__mutmut_13(self) -> None:  # pragma: no cover - runtime guard
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT 1 FROM pg_extension WHERE extname='pgcrypto'")
            if not cur.fetchone():
                raise RuntimeError(
                    "Postgres missing extension 'pgcrypto'. Run: "
                    "CREATE EXTENSION IF NOT EXISTS PGCRYPTO;"
                )
        finally:
            cur.close()
    
    xǁPostgresDALǁ_ensure_pgcrypto__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_1': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_1, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_2': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_2, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_3': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_3, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_4': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_4, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_5': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_5, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_6': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_6, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_7': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_7, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_8': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_8, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_9': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_9, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_10': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_10, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_11': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_11, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_12': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_12, 
        'xǁPostgresDALǁ_ensure_pgcrypto__mutmut_13': xǁPostgresDALǁ_ensure_pgcrypto__mutmut_13
    }
    
    def _ensure_pgcrypto(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁ_ensure_pgcrypto__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁ_ensure_pgcrypto__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _ensure_pgcrypto.__signature__ = _mutmut_signature(xǁPostgresDALǁ_ensure_pgcrypto__mutmut_orig)
    xǁPostgresDALǁ_ensure_pgcrypto__mutmut_orig.__name__ = 'xǁPostgresDALǁ_ensure_pgcrypto'

    def xǁPostgresDALǁensure_artifact__mutmut_orig(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL artifact operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁensure_artifact__mutmut_1(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁPostgresDALǁensure_artifact__mutmut_2(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXPostgreSQL artifact operations not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁensure_artifact__mutmut_3(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "postgresql artifact operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁensure_artifact__mutmut_4(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "POSTGRESQL ARTIFACT OPERATIONS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁensure_artifact__mutmut_5(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL artifact operations not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁPostgresDALǁensure_artifact__mutmut_6(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL artifact operations not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁensure_artifact__mutmut_7(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL artifact operations not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁPostgresDALǁensure_artifact__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁensure_artifact__mutmut_1': xǁPostgresDALǁensure_artifact__mutmut_1, 
        'xǁPostgresDALǁensure_artifact__mutmut_2': xǁPostgresDALǁensure_artifact__mutmut_2, 
        'xǁPostgresDALǁensure_artifact__mutmut_3': xǁPostgresDALǁensure_artifact__mutmut_3, 
        'xǁPostgresDALǁensure_artifact__mutmut_4': xǁPostgresDALǁensure_artifact__mutmut_4, 
        'xǁPostgresDALǁensure_artifact__mutmut_5': xǁPostgresDALǁensure_artifact__mutmut_5, 
        'xǁPostgresDALǁensure_artifact__mutmut_6': xǁPostgresDALǁensure_artifact__mutmut_6, 
        'xǁPostgresDALǁensure_artifact__mutmut_7': xǁPostgresDALǁensure_artifact__mutmut_7
    }
    
    def ensure_artifact(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁensure_artifact__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁensure_artifact__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_artifact.__signature__ = _mutmut_signature(xǁPostgresDALǁensure_artifact__mutmut_orig)
    xǁPostgresDALǁensure_artifact__mutmut_orig.__name__ = 'xǁPostgresDALǁensure_artifact'

    def xǁPostgresDALǁinsert_item__mutmut_orig(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL item operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_item__mutmut_1(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁPostgresDALǁinsert_item__mutmut_2(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXPostgreSQL item operations not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_item__mutmut_3(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "postgresql item operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_item__mutmut_4(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "POSTGRESQL ITEM OPERATIONS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_item__mutmut_5(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL item operations not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁPostgresDALǁinsert_item__mutmut_6(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL item operations not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_item__mutmut_7(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL item operations not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁPostgresDALǁinsert_item__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁinsert_item__mutmut_1': xǁPostgresDALǁinsert_item__mutmut_1, 
        'xǁPostgresDALǁinsert_item__mutmut_2': xǁPostgresDALǁinsert_item__mutmut_2, 
        'xǁPostgresDALǁinsert_item__mutmut_3': xǁPostgresDALǁinsert_item__mutmut_3, 
        'xǁPostgresDALǁinsert_item__mutmut_4': xǁPostgresDALǁinsert_item__mutmut_4, 
        'xǁPostgresDALǁinsert_item__mutmut_5': xǁPostgresDALǁinsert_item__mutmut_5, 
        'xǁPostgresDALǁinsert_item__mutmut_6': xǁPostgresDALǁinsert_item__mutmut_6, 
        'xǁPostgresDALǁinsert_item__mutmut_7': xǁPostgresDALǁinsert_item__mutmut_7
    }
    
    def insert_item(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁinsert_item__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁinsert_item__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_item.__signature__ = _mutmut_signature(xǁPostgresDALǁinsert_item__mutmut_orig)
    xǁPostgresDALǁinsert_item__mutmut_orig.__name__ = 'xǁPostgresDALǁinsert_item'

    def xǁPostgresDALǁinsert_event__mutmut_orig(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL event operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_event__mutmut_1(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁPostgresDALǁinsert_event__mutmut_2(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "XXPostgreSQL event operations not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_event__mutmut_3(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "postgresql event operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_event__mutmut_4(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "POSTGRESQL EVENT OPERATIONS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_event__mutmut_5(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL event operations not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁPostgresDALǁinsert_event__mutmut_6(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL event operations not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁinsert_event__mutmut_7(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL event operations not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁPostgresDALǁinsert_event__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁinsert_event__mutmut_1': xǁPostgresDALǁinsert_event__mutmut_1, 
        'xǁPostgresDALǁinsert_event__mutmut_2': xǁPostgresDALǁinsert_event__mutmut_2, 
        'xǁPostgresDALǁinsert_event__mutmut_3': xǁPostgresDALǁinsert_event__mutmut_3, 
        'xǁPostgresDALǁinsert_event__mutmut_4': xǁPostgresDALǁinsert_event__mutmut_4, 
        'xǁPostgresDALǁinsert_event__mutmut_5': xǁPostgresDALǁinsert_event__mutmut_5, 
        'xǁPostgresDALǁinsert_event__mutmut_6': xǁPostgresDALǁinsert_event__mutmut_6, 
        'xǁPostgresDALǁinsert_event__mutmut_7': xǁPostgresDALǁinsert_event__mutmut_7
    }
    
    def insert_event(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁinsert_event__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁinsert_event__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_event.__signature__ = _mutmut_signature(xǁPostgresDALǁinsert_event__mutmut_orig)
    xǁPostgresDALǁinsert_event__mutmut_orig.__name__ = 'xǁPostgresDALǁinsert_event'

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_orig(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_1(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = None
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_2(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute(None, (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_3(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", None)
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_4(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute((tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_5(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", )
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_6(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("XXSELECT * FROM item WHERE tombstone_id = %sXX", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_7(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("select * from item where tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_8(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM ITEM WHERE TOMBSTONE_ID = %S", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_9(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = None
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_10(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_11(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(None)
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_12(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = None
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_13(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(None, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_14(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, None)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_15(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_16(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, )
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_17(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute(None, (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_18(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", None)
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_19(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute((item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_20(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", )
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_21(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("XXSELECT * FROM artifact WHERE id = %sXX", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_22(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("select * from artifact where id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_23(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM ARTIFACT WHERE ID = %S", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_24(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["XXartifact_idXX"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_25(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["ARTIFACT_ID"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_26(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = None
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_27(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_28(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(None)
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_29(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['XXidXX']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_30(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['ID']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_31(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = None
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_32(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(None, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_33(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, None)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_34(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_35(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, )
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_36(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = None
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_37(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(None)
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_38(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get(None))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_39(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("XXmetadataXX"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_40(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("METADATA"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_41(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = None

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_42(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=None,
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_43(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=None,
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_44(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=None,
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_45(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=None,
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_46(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=None,
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_47(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=None,
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_48(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=None,
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_49(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=None,
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_50(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=None,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_51(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=None,
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_52(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_53(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_54(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_55(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_56(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_57(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_58(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_59(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_60(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_61(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_62(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(None),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_63(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["XXidXX"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_64(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["ID"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_65(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(None),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_66(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["XXrepoXX"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_67(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["REPO"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_68(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(None),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_69(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["XXpathXX"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_70(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["PATH"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_71(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(None),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_72(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["XXcommit_shaXX"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_73(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["COMMIT_SHA"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_74(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(None),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_75(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") and ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_76(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get(None) or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_77(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("XXlanguageXX") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_78(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("LANGUAGE") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_79(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or "XXXX"),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_80(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(None),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_81(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["XXkindXX"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_82(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["KIND"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_83(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(None),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_84(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["XXreasonXX"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_85(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["REASON"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_86(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(None),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_87(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["XXartifact_idXX"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_88(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["ARTIFACT_ID"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_89(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(None),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_90(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["XXtombstone_idXX"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_91(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["TOMBSTONE_ID"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_92(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = None
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_93(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(None)
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_94(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get(None))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_95(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("XXblob_bytesXX"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_96(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("BLOB_BYTES"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_97(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = None
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_98(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=None,
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_99(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=None,
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_100(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=None,
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_101(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=None,
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_102(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=None,
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_103(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=None,
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_104(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=None,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_105(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=None,
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_106(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_107(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_108(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_109(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_110(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_111(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_112(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_113(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_114(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(None),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_115(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["XXidXX"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_116(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["ID"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_117(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(None),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_118(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["XXcontent_sha256XX"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_119(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["CONTENT_SHA256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_120(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(None),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_121(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["XXsize_bytesXX"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_122(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["SIZE_BYTES"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_123(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(None),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_124(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["XXcompressionXX"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_125(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["COMPRESSION"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_126(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(None),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_127(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["XXmime_typeXX"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_128(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["MIME_TYPE"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_129(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(None),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_130(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["XXstorage_driverXX"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_131(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["STORAGE_DRIVER"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_132(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(None) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_133(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["XXobject_urlXX"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_134(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["OBJECT_URL"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_135(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get(None) is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_136(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("XXobject_urlXX") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_137(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("OBJECT_URL") is not None else None
            ),
        )
        return item, artifact

    def xǁPostgresDALǁfetch_by_tombstone__mutmut_138(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is None else None
            ),
        )
        return item, artifact
    
    xǁPostgresDALǁfetch_by_tombstone__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁfetch_by_tombstone__mutmut_1': xǁPostgresDALǁfetch_by_tombstone__mutmut_1, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_2': xǁPostgresDALǁfetch_by_tombstone__mutmut_2, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_3': xǁPostgresDALǁfetch_by_tombstone__mutmut_3, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_4': xǁPostgresDALǁfetch_by_tombstone__mutmut_4, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_5': xǁPostgresDALǁfetch_by_tombstone__mutmut_5, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_6': xǁPostgresDALǁfetch_by_tombstone__mutmut_6, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_7': xǁPostgresDALǁfetch_by_tombstone__mutmut_7, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_8': xǁPostgresDALǁfetch_by_tombstone__mutmut_8, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_9': xǁPostgresDALǁfetch_by_tombstone__mutmut_9, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_10': xǁPostgresDALǁfetch_by_tombstone__mutmut_10, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_11': xǁPostgresDALǁfetch_by_tombstone__mutmut_11, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_12': xǁPostgresDALǁfetch_by_tombstone__mutmut_12, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_13': xǁPostgresDALǁfetch_by_tombstone__mutmut_13, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_14': xǁPostgresDALǁfetch_by_tombstone__mutmut_14, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_15': xǁPostgresDALǁfetch_by_tombstone__mutmut_15, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_16': xǁPostgresDALǁfetch_by_tombstone__mutmut_16, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_17': xǁPostgresDALǁfetch_by_tombstone__mutmut_17, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_18': xǁPostgresDALǁfetch_by_tombstone__mutmut_18, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_19': xǁPostgresDALǁfetch_by_tombstone__mutmut_19, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_20': xǁPostgresDALǁfetch_by_tombstone__mutmut_20, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_21': xǁPostgresDALǁfetch_by_tombstone__mutmut_21, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_22': xǁPostgresDALǁfetch_by_tombstone__mutmut_22, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_23': xǁPostgresDALǁfetch_by_tombstone__mutmut_23, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_24': xǁPostgresDALǁfetch_by_tombstone__mutmut_24, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_25': xǁPostgresDALǁfetch_by_tombstone__mutmut_25, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_26': xǁPostgresDALǁfetch_by_tombstone__mutmut_26, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_27': xǁPostgresDALǁfetch_by_tombstone__mutmut_27, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_28': xǁPostgresDALǁfetch_by_tombstone__mutmut_28, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_29': xǁPostgresDALǁfetch_by_tombstone__mutmut_29, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_30': xǁPostgresDALǁfetch_by_tombstone__mutmut_30, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_31': xǁPostgresDALǁfetch_by_tombstone__mutmut_31, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_32': xǁPostgresDALǁfetch_by_tombstone__mutmut_32, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_33': xǁPostgresDALǁfetch_by_tombstone__mutmut_33, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_34': xǁPostgresDALǁfetch_by_tombstone__mutmut_34, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_35': xǁPostgresDALǁfetch_by_tombstone__mutmut_35, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_36': xǁPostgresDALǁfetch_by_tombstone__mutmut_36, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_37': xǁPostgresDALǁfetch_by_tombstone__mutmut_37, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_38': xǁPostgresDALǁfetch_by_tombstone__mutmut_38, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_39': xǁPostgresDALǁfetch_by_tombstone__mutmut_39, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_40': xǁPostgresDALǁfetch_by_tombstone__mutmut_40, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_41': xǁPostgresDALǁfetch_by_tombstone__mutmut_41, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_42': xǁPostgresDALǁfetch_by_tombstone__mutmut_42, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_43': xǁPostgresDALǁfetch_by_tombstone__mutmut_43, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_44': xǁPostgresDALǁfetch_by_tombstone__mutmut_44, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_45': xǁPostgresDALǁfetch_by_tombstone__mutmut_45, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_46': xǁPostgresDALǁfetch_by_tombstone__mutmut_46, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_47': xǁPostgresDALǁfetch_by_tombstone__mutmut_47, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_48': xǁPostgresDALǁfetch_by_tombstone__mutmut_48, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_49': xǁPostgresDALǁfetch_by_tombstone__mutmut_49, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_50': xǁPostgresDALǁfetch_by_tombstone__mutmut_50, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_51': xǁPostgresDALǁfetch_by_tombstone__mutmut_51, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_52': xǁPostgresDALǁfetch_by_tombstone__mutmut_52, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_53': xǁPostgresDALǁfetch_by_tombstone__mutmut_53, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_54': xǁPostgresDALǁfetch_by_tombstone__mutmut_54, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_55': xǁPostgresDALǁfetch_by_tombstone__mutmut_55, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_56': xǁPostgresDALǁfetch_by_tombstone__mutmut_56, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_57': xǁPostgresDALǁfetch_by_tombstone__mutmut_57, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_58': xǁPostgresDALǁfetch_by_tombstone__mutmut_58, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_59': xǁPostgresDALǁfetch_by_tombstone__mutmut_59, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_60': xǁPostgresDALǁfetch_by_tombstone__mutmut_60, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_61': xǁPostgresDALǁfetch_by_tombstone__mutmut_61, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_62': xǁPostgresDALǁfetch_by_tombstone__mutmut_62, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_63': xǁPostgresDALǁfetch_by_tombstone__mutmut_63, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_64': xǁPostgresDALǁfetch_by_tombstone__mutmut_64, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_65': xǁPostgresDALǁfetch_by_tombstone__mutmut_65, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_66': xǁPostgresDALǁfetch_by_tombstone__mutmut_66, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_67': xǁPostgresDALǁfetch_by_tombstone__mutmut_67, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_68': xǁPostgresDALǁfetch_by_tombstone__mutmut_68, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_69': xǁPostgresDALǁfetch_by_tombstone__mutmut_69, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_70': xǁPostgresDALǁfetch_by_tombstone__mutmut_70, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_71': xǁPostgresDALǁfetch_by_tombstone__mutmut_71, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_72': xǁPostgresDALǁfetch_by_tombstone__mutmut_72, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_73': xǁPostgresDALǁfetch_by_tombstone__mutmut_73, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_74': xǁPostgresDALǁfetch_by_tombstone__mutmut_74, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_75': xǁPostgresDALǁfetch_by_tombstone__mutmut_75, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_76': xǁPostgresDALǁfetch_by_tombstone__mutmut_76, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_77': xǁPostgresDALǁfetch_by_tombstone__mutmut_77, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_78': xǁPostgresDALǁfetch_by_tombstone__mutmut_78, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_79': xǁPostgresDALǁfetch_by_tombstone__mutmut_79, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_80': xǁPostgresDALǁfetch_by_tombstone__mutmut_80, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_81': xǁPostgresDALǁfetch_by_tombstone__mutmut_81, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_82': xǁPostgresDALǁfetch_by_tombstone__mutmut_82, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_83': xǁPostgresDALǁfetch_by_tombstone__mutmut_83, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_84': xǁPostgresDALǁfetch_by_tombstone__mutmut_84, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_85': xǁPostgresDALǁfetch_by_tombstone__mutmut_85, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_86': xǁPostgresDALǁfetch_by_tombstone__mutmut_86, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_87': xǁPostgresDALǁfetch_by_tombstone__mutmut_87, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_88': xǁPostgresDALǁfetch_by_tombstone__mutmut_88, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_89': xǁPostgresDALǁfetch_by_tombstone__mutmut_89, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_90': xǁPostgresDALǁfetch_by_tombstone__mutmut_90, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_91': xǁPostgresDALǁfetch_by_tombstone__mutmut_91, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_92': xǁPostgresDALǁfetch_by_tombstone__mutmut_92, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_93': xǁPostgresDALǁfetch_by_tombstone__mutmut_93, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_94': xǁPostgresDALǁfetch_by_tombstone__mutmut_94, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_95': xǁPostgresDALǁfetch_by_tombstone__mutmut_95, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_96': xǁPostgresDALǁfetch_by_tombstone__mutmut_96, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_97': xǁPostgresDALǁfetch_by_tombstone__mutmut_97, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_98': xǁPostgresDALǁfetch_by_tombstone__mutmut_98, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_99': xǁPostgresDALǁfetch_by_tombstone__mutmut_99, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_100': xǁPostgresDALǁfetch_by_tombstone__mutmut_100, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_101': xǁPostgresDALǁfetch_by_tombstone__mutmut_101, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_102': xǁPostgresDALǁfetch_by_tombstone__mutmut_102, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_103': xǁPostgresDALǁfetch_by_tombstone__mutmut_103, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_104': xǁPostgresDALǁfetch_by_tombstone__mutmut_104, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_105': xǁPostgresDALǁfetch_by_tombstone__mutmut_105, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_106': xǁPostgresDALǁfetch_by_tombstone__mutmut_106, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_107': xǁPostgresDALǁfetch_by_tombstone__mutmut_107, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_108': xǁPostgresDALǁfetch_by_tombstone__mutmut_108, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_109': xǁPostgresDALǁfetch_by_tombstone__mutmut_109, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_110': xǁPostgresDALǁfetch_by_tombstone__mutmut_110, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_111': xǁPostgresDALǁfetch_by_tombstone__mutmut_111, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_112': xǁPostgresDALǁfetch_by_tombstone__mutmut_112, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_113': xǁPostgresDALǁfetch_by_tombstone__mutmut_113, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_114': xǁPostgresDALǁfetch_by_tombstone__mutmut_114, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_115': xǁPostgresDALǁfetch_by_tombstone__mutmut_115, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_116': xǁPostgresDALǁfetch_by_tombstone__mutmut_116, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_117': xǁPostgresDALǁfetch_by_tombstone__mutmut_117, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_118': xǁPostgresDALǁfetch_by_tombstone__mutmut_118, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_119': xǁPostgresDALǁfetch_by_tombstone__mutmut_119, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_120': xǁPostgresDALǁfetch_by_tombstone__mutmut_120, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_121': xǁPostgresDALǁfetch_by_tombstone__mutmut_121, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_122': xǁPostgresDALǁfetch_by_tombstone__mutmut_122, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_123': xǁPostgresDALǁfetch_by_tombstone__mutmut_123, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_124': xǁPostgresDALǁfetch_by_tombstone__mutmut_124, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_125': xǁPostgresDALǁfetch_by_tombstone__mutmut_125, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_126': xǁPostgresDALǁfetch_by_tombstone__mutmut_126, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_127': xǁPostgresDALǁfetch_by_tombstone__mutmut_127, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_128': xǁPostgresDALǁfetch_by_tombstone__mutmut_128, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_129': xǁPostgresDALǁfetch_by_tombstone__mutmut_129, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_130': xǁPostgresDALǁfetch_by_tombstone__mutmut_130, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_131': xǁPostgresDALǁfetch_by_tombstone__mutmut_131, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_132': xǁPostgresDALǁfetch_by_tombstone__mutmut_132, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_133': xǁPostgresDALǁfetch_by_tombstone__mutmut_133, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_134': xǁPostgresDALǁfetch_by_tombstone__mutmut_134, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_135': xǁPostgresDALǁfetch_by_tombstone__mutmut_135, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_136': xǁPostgresDALǁfetch_by_tombstone__mutmut_136, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_137': xǁPostgresDALǁfetch_by_tombstone__mutmut_137, 
        'xǁPostgresDALǁfetch_by_tombstone__mutmut_138': xǁPostgresDALǁfetch_by_tombstone__mutmut_138
    }
    
    def fetch_by_tombstone(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁfetch_by_tombstone__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁfetch_by_tombstone__mutmut_mutants"), args, kwargs, self)
        return result 
    
    fetch_by_tombstone.__signature__ = _mutmut_signature(xǁPostgresDALǁfetch_by_tombstone__mutmut_orig)
    xǁPostgresDALǁfetch_by_tombstone__mutmut_orig.__name__ = 'xǁPostgresDALǁfetch_by_tombstone'

    def xǁPostgresDALǁinsert_referent__mutmut_orig(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) "
                    "ON CONFLICT DO NOTHING",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_1(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = None
            try:
                cur.execute(
                    "INSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) "
                    "ON CONFLICT DO NOTHING",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_2(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    None,
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_3(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) "
                    "ON CONFLICT DO NOTHING",
                    None,
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_4(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_5(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) "
                    "ON CONFLICT DO NOTHING",
                    )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_6(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "XXINSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) XX"
                    "ON CONFLICT DO NOTHING",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_7(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "insert into referent (item_id, ref_type, ref_value) values (%s,%s,%s) "
                    "ON CONFLICT DO NOTHING",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_8(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT INTO REFERENT (ITEM_ID, REF_TYPE, REF_VALUE) VALUES (%S,%S,%S) "
                    "ON CONFLICT DO NOTHING",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_9(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) "
                    "XXON CONFLICT DO NOTHINGXX",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁPostgresDALǁinsert_referent__mutmut_10(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s) "
                    "on conflict do nothing",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()
    
    xǁPostgresDALǁinsert_referent__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁinsert_referent__mutmut_1': xǁPostgresDALǁinsert_referent__mutmut_1, 
        'xǁPostgresDALǁinsert_referent__mutmut_2': xǁPostgresDALǁinsert_referent__mutmut_2, 
        'xǁPostgresDALǁinsert_referent__mutmut_3': xǁPostgresDALǁinsert_referent__mutmut_3, 
        'xǁPostgresDALǁinsert_referent__mutmut_4': xǁPostgresDALǁinsert_referent__mutmut_4, 
        'xǁPostgresDALǁinsert_referent__mutmut_5': xǁPostgresDALǁinsert_referent__mutmut_5, 
        'xǁPostgresDALǁinsert_referent__mutmut_6': xǁPostgresDALǁinsert_referent__mutmut_6, 
        'xǁPostgresDALǁinsert_referent__mutmut_7': xǁPostgresDALǁinsert_referent__mutmut_7, 
        'xǁPostgresDALǁinsert_referent__mutmut_8': xǁPostgresDALǁinsert_referent__mutmut_8, 
        'xǁPostgresDALǁinsert_referent__mutmut_9': xǁPostgresDALǁinsert_referent__mutmut_9, 
        'xǁPostgresDALǁinsert_referent__mutmut_10': xǁPostgresDALǁinsert_referent__mutmut_10
    }
    
    def insert_referent(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁinsert_referent__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁinsert_referent__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_referent.__signature__ = _mutmut_signature(xǁPostgresDALǁinsert_referent__mutmut_orig)
    xǁPostgresDALǁinsert_referent__mutmut_orig.__name__ = 'xǁPostgresDALǁinsert_referent'

    def xǁPostgresDALǁrecent_items__mutmut_orig(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL recent_items not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁrecent_items__mutmut_1(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁPostgresDALǁrecent_items__mutmut_2(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXPostgreSQL recent_items not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁrecent_items__mutmut_3(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "postgresql recent_items not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁrecent_items__mutmut_4(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "POSTGRESQL RECENT_ITEMS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁrecent_items__mutmut_5(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL recent_items not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁPostgresDALǁrecent_items__mutmut_6(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL recent_items not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁrecent_items__mutmut_7(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL recent_items not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁPostgresDALǁrecent_items__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁrecent_items__mutmut_1': xǁPostgresDALǁrecent_items__mutmut_1, 
        'xǁPostgresDALǁrecent_items__mutmut_2': xǁPostgresDALǁrecent_items__mutmut_2, 
        'xǁPostgresDALǁrecent_items__mutmut_3': xǁPostgresDALǁrecent_items__mutmut_3, 
        'xǁPostgresDALǁrecent_items__mutmut_4': xǁPostgresDALǁrecent_items__mutmut_4, 
        'xǁPostgresDALǁrecent_items__mutmut_5': xǁPostgresDALǁrecent_items__mutmut_5, 
        'xǁPostgresDALǁrecent_items__mutmut_6': xǁPostgresDALǁrecent_items__mutmut_6, 
        'xǁPostgresDALǁrecent_items__mutmut_7': xǁPostgresDALǁrecent_items__mutmut_7
    }
    
    def recent_items(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁrecent_items__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁrecent_items__mutmut_mutants"), args, kwargs, self)
        return result 
    
    recent_items.__signature__ = _mutmut_signature(xǁPostgresDALǁrecent_items__mutmut_orig)
    xǁPostgresDALǁrecent_items__mutmut_orig.__name__ = 'xǁPostgresDALǁrecent_items'

    def xǁPostgresDALǁsummary__mutmut_orig(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL summary not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁsummary__mutmut_1(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁPostgresDALǁsummary__mutmut_2(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXPostgreSQL summary not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁsummary__mutmut_3(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "postgresql summary not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁsummary__mutmut_4(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "POSTGRESQL SUMMARY NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁsummary__mutmut_5(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL summary not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁPostgresDALǁsummary__mutmut_6(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL summary not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁPostgresDALǁsummary__mutmut_7(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "PostgreSQL summary not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁPostgresDALǁsummary__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁsummary__mutmut_1': xǁPostgresDALǁsummary__mutmut_1, 
        'xǁPostgresDALǁsummary__mutmut_2': xǁPostgresDALǁsummary__mutmut_2, 
        'xǁPostgresDALǁsummary__mutmut_3': xǁPostgresDALǁsummary__mutmut_3, 
        'xǁPostgresDALǁsummary__mutmut_4': xǁPostgresDALǁsummary__mutmut_4, 
        'xǁPostgresDALǁsummary__mutmut_5': xǁPostgresDALǁsummary__mutmut_5, 
        'xǁPostgresDALǁsummary__mutmut_6': xǁPostgresDALǁsummary__mutmut_6, 
        'xǁPostgresDALǁsummary__mutmut_7': xǁPostgresDALǁsummary__mutmut_7
    }
    
    def summary(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁsummary__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁsummary__mutmut_mutants"), args, kwargs, self)
        return result 
    
    summary.__signature__ = _mutmut_signature(xǁPostgresDALǁsummary__mutmut_orig)
    xǁPostgresDALǁsummary__mutmut_orig.__name__ = 'xǁPostgresDALǁsummary'

    def xǁPostgresDALǁcreate_release_meta__mutmut_orig(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_1(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = None
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_2(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(None, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_3(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=None, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_4(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=None)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_5(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_6(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_7(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, )
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_8(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata and {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_9(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=True, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_10(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=False)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_11(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = None
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_12(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = None
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_13(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "XXINSERT INTO release_meta XX"
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_14(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "insert into release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_15(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO RELEASE_META "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_16(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "XX(release_id, version, created_at, actor, metadata)\nXX"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_17(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(RELEASE_ID, VERSION, CREATED_AT, ACTOR, METADATA)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_18(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "XXVALUES (%s,%s,%s,%s,%s) RETURNING idXX"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_19(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "values (%s,%s,%s,%s,%s) returning id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_20(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%S,%S,%S,%S,%S) RETURNING ID"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_21(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(None, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_22(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, None)
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_23(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute((release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_24(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_25(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = None
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_26(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_27(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError(None)
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_28(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("XXFailed to insert release_meta rowXX")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_29(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_30(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("FAILED TO INSERT RELEASE_META ROW")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_31(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = None
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_32(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(None)
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_33(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[1])
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_34(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"XXidXX": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_35(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"ID": rid, "release_id": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_36(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "XXrelease_idXX": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_37(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "RELEASE_ID": release_id, "version": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_38(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "XXversionXX": version}

    def xǁPostgresDALǁcreate_release_meta__mutmut_39(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(sql, (release_id, version, created_at, actor, payload))
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_meta row")
        rid = str(row[0])
        return {"id": rid, "release_id": release_id, "VERSION": version}
    
    xǁPostgresDALǁcreate_release_meta__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁcreate_release_meta__mutmut_1': xǁPostgresDALǁcreate_release_meta__mutmut_1, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_2': xǁPostgresDALǁcreate_release_meta__mutmut_2, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_3': xǁPostgresDALǁcreate_release_meta__mutmut_3, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_4': xǁPostgresDALǁcreate_release_meta__mutmut_4, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_5': xǁPostgresDALǁcreate_release_meta__mutmut_5, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_6': xǁPostgresDALǁcreate_release_meta__mutmut_6, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_7': xǁPostgresDALǁcreate_release_meta__mutmut_7, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_8': xǁPostgresDALǁcreate_release_meta__mutmut_8, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_9': xǁPostgresDALǁcreate_release_meta__mutmut_9, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_10': xǁPostgresDALǁcreate_release_meta__mutmut_10, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_11': xǁPostgresDALǁcreate_release_meta__mutmut_11, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_12': xǁPostgresDALǁcreate_release_meta__mutmut_12, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_13': xǁPostgresDALǁcreate_release_meta__mutmut_13, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_14': xǁPostgresDALǁcreate_release_meta__mutmut_14, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_15': xǁPostgresDALǁcreate_release_meta__mutmut_15, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_16': xǁPostgresDALǁcreate_release_meta__mutmut_16, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_17': xǁPostgresDALǁcreate_release_meta__mutmut_17, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_18': xǁPostgresDALǁcreate_release_meta__mutmut_18, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_19': xǁPostgresDALǁcreate_release_meta__mutmut_19, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_20': xǁPostgresDALǁcreate_release_meta__mutmut_20, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_21': xǁPostgresDALǁcreate_release_meta__mutmut_21, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_22': xǁPostgresDALǁcreate_release_meta__mutmut_22, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_23': xǁPostgresDALǁcreate_release_meta__mutmut_23, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_24': xǁPostgresDALǁcreate_release_meta__mutmut_24, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_25': xǁPostgresDALǁcreate_release_meta__mutmut_25, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_26': xǁPostgresDALǁcreate_release_meta__mutmut_26, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_27': xǁPostgresDALǁcreate_release_meta__mutmut_27, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_28': xǁPostgresDALǁcreate_release_meta__mutmut_28, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_29': xǁPostgresDALǁcreate_release_meta__mutmut_29, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_30': xǁPostgresDALǁcreate_release_meta__mutmut_30, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_31': xǁPostgresDALǁcreate_release_meta__mutmut_31, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_32': xǁPostgresDALǁcreate_release_meta__mutmut_32, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_33': xǁPostgresDALǁcreate_release_meta__mutmut_33, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_34': xǁPostgresDALǁcreate_release_meta__mutmut_34, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_35': xǁPostgresDALǁcreate_release_meta__mutmut_35, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_36': xǁPostgresDALǁcreate_release_meta__mutmut_36, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_37': xǁPostgresDALǁcreate_release_meta__mutmut_37, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_38': xǁPostgresDALǁcreate_release_meta__mutmut_38, 
        'xǁPostgresDALǁcreate_release_meta__mutmut_39': xǁPostgresDALǁcreate_release_meta__mutmut_39
    }
    
    def create_release_meta(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁcreate_release_meta__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁcreate_release_meta__mutmut_mutants"), args, kwargs, self)
        return result 
    
    create_release_meta.__signature__ = _mutmut_signature(xǁPostgresDALǁcreate_release_meta__mutmut_orig)
    xǁPostgresDALǁcreate_release_meta__mutmut_orig.__name__ = 'xǁPostgresDALǁcreate_release_meta'

    def xǁPostgresDALǁadd_release_component__mutmut_orig(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_1(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = None
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_2(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(None, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_3(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=None, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_4(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=None)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_5(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_6(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_7(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, )
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_8(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars and {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_9(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=True, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_10(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=False)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_11(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = None
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_12(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = None
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_13(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "XXINSERT INTO release_component XX"
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_14(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "insert into release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_15(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO RELEASE_COMPONENT "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_16(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "XX(release_id, item_id, tombstone, dest_path, mode, template_vars)\nXX"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_17(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(RELEASE_ID, ITEM_ID, TOMBSTONE, DEST_PATH, MODE, TEMPLATE_VARS)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_18(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "XXVALUES (%s,%s,%s,%s,%s,%s) RETURNING idXX"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_19(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "values (%s,%s,%s,%s,%s,%s) returning id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_20(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%S,%S,%S,%S,%S,%S) RETURNING ID"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_21(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    None,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_22(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    None,
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_23(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_24(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_25(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = None
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_26(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_27(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError(None)
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_28(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("XXFailed to insert release_component rowXX")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_29(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_30(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("FAILED TO INSERT RELEASE_COMPONENT ROW")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_31(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = None
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_32(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(None)
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_33(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[1])
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_34(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"XXidXX": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_35(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"ID": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_36(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "XXrelease_idXX": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_37(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "RELEASE_ID": release_meta_id, "tombstone": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_38(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "XXtombstoneXX": tombstone}

    def xǁPostgresDALǁadd_release_component__mutmut_39(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        self._ensure_pgcrypto()
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s) RETURNING id"
                )
                cur.execute(
                    sql,
                    (release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
                row = cur.fetchone()
            finally:
                cur.close()
        if not row:
            raise RuntimeError("Failed to insert release_component row")
        cid = str(row[0])
        return {"id": cid, "release_id": release_meta_id, "TOMBSTONE": tombstone}
    
    xǁPostgresDALǁadd_release_component__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁadd_release_component__mutmut_1': xǁPostgresDALǁadd_release_component__mutmut_1, 
        'xǁPostgresDALǁadd_release_component__mutmut_2': xǁPostgresDALǁadd_release_component__mutmut_2, 
        'xǁPostgresDALǁadd_release_component__mutmut_3': xǁPostgresDALǁadd_release_component__mutmut_3, 
        'xǁPostgresDALǁadd_release_component__mutmut_4': xǁPostgresDALǁadd_release_component__mutmut_4, 
        'xǁPostgresDALǁadd_release_component__mutmut_5': xǁPostgresDALǁadd_release_component__mutmut_5, 
        'xǁPostgresDALǁadd_release_component__mutmut_6': xǁPostgresDALǁadd_release_component__mutmut_6, 
        'xǁPostgresDALǁadd_release_component__mutmut_7': xǁPostgresDALǁadd_release_component__mutmut_7, 
        'xǁPostgresDALǁadd_release_component__mutmut_8': xǁPostgresDALǁadd_release_component__mutmut_8, 
        'xǁPostgresDALǁadd_release_component__mutmut_9': xǁPostgresDALǁadd_release_component__mutmut_9, 
        'xǁPostgresDALǁadd_release_component__mutmut_10': xǁPostgresDALǁadd_release_component__mutmut_10, 
        'xǁPostgresDALǁadd_release_component__mutmut_11': xǁPostgresDALǁadd_release_component__mutmut_11, 
        'xǁPostgresDALǁadd_release_component__mutmut_12': xǁPostgresDALǁadd_release_component__mutmut_12, 
        'xǁPostgresDALǁadd_release_component__mutmut_13': xǁPostgresDALǁadd_release_component__mutmut_13, 
        'xǁPostgresDALǁadd_release_component__mutmut_14': xǁPostgresDALǁadd_release_component__mutmut_14, 
        'xǁPostgresDALǁadd_release_component__mutmut_15': xǁPostgresDALǁadd_release_component__mutmut_15, 
        'xǁPostgresDALǁadd_release_component__mutmut_16': xǁPostgresDALǁadd_release_component__mutmut_16, 
        'xǁPostgresDALǁadd_release_component__mutmut_17': xǁPostgresDALǁadd_release_component__mutmut_17, 
        'xǁPostgresDALǁadd_release_component__mutmut_18': xǁPostgresDALǁadd_release_component__mutmut_18, 
        'xǁPostgresDALǁadd_release_component__mutmut_19': xǁPostgresDALǁadd_release_component__mutmut_19, 
        'xǁPostgresDALǁadd_release_component__mutmut_20': xǁPostgresDALǁadd_release_component__mutmut_20, 
        'xǁPostgresDALǁadd_release_component__mutmut_21': xǁPostgresDALǁadd_release_component__mutmut_21, 
        'xǁPostgresDALǁadd_release_component__mutmut_22': xǁPostgresDALǁadd_release_component__mutmut_22, 
        'xǁPostgresDALǁadd_release_component__mutmut_23': xǁPostgresDALǁadd_release_component__mutmut_23, 
        'xǁPostgresDALǁadd_release_component__mutmut_24': xǁPostgresDALǁadd_release_component__mutmut_24, 
        'xǁPostgresDALǁadd_release_component__mutmut_25': xǁPostgresDALǁadd_release_component__mutmut_25, 
        'xǁPostgresDALǁadd_release_component__mutmut_26': xǁPostgresDALǁadd_release_component__mutmut_26, 
        'xǁPostgresDALǁadd_release_component__mutmut_27': xǁPostgresDALǁadd_release_component__mutmut_27, 
        'xǁPostgresDALǁadd_release_component__mutmut_28': xǁPostgresDALǁadd_release_component__mutmut_28, 
        'xǁPostgresDALǁadd_release_component__mutmut_29': xǁPostgresDALǁadd_release_component__mutmut_29, 
        'xǁPostgresDALǁadd_release_component__mutmut_30': xǁPostgresDALǁadd_release_component__mutmut_30, 
        'xǁPostgresDALǁadd_release_component__mutmut_31': xǁPostgresDALǁadd_release_component__mutmut_31, 
        'xǁPostgresDALǁadd_release_component__mutmut_32': xǁPostgresDALǁadd_release_component__mutmut_32, 
        'xǁPostgresDALǁadd_release_component__mutmut_33': xǁPostgresDALǁadd_release_component__mutmut_33, 
        'xǁPostgresDALǁadd_release_component__mutmut_34': xǁPostgresDALǁadd_release_component__mutmut_34, 
        'xǁPostgresDALǁadd_release_component__mutmut_35': xǁPostgresDALǁadd_release_component__mutmut_35, 
        'xǁPostgresDALǁadd_release_component__mutmut_36': xǁPostgresDALǁadd_release_component__mutmut_36, 
        'xǁPostgresDALǁadd_release_component__mutmut_37': xǁPostgresDALǁadd_release_component__mutmut_37, 
        'xǁPostgresDALǁadd_release_component__mutmut_38': xǁPostgresDALǁadd_release_component__mutmut_38, 
        'xǁPostgresDALǁadd_release_component__mutmut_39': xǁPostgresDALǁadd_release_component__mutmut_39
    }
    
    def add_release_component(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁadd_release_component__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁadd_release_component__mutmut_mutants"), args, kwargs, self)
        return result 
    
    add_release_component.__signature__ = _mutmut_signature(xǁPostgresDALǁadd_release_component__mutmut_orig)
    xǁPostgresDALǁadd_release_component__mutmut_orig.__name__ = 'xǁPostgresDALǁadd_release_component'

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_orig(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_1(self, *, release_id: str) -> dict[str, Any] | None:
        cur = None
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_2(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute(None, (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_3(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", None)
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_4(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute((release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_5(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", )
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_6(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("XXSELECT * FROM release_meta WHERE release_id = %s LIMIT 1XX", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_7(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("select * from release_meta where release_id = %s limit 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_8(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM RELEASE_META WHERE RELEASE_ID = %S LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_9(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = None
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_10(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_11(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = None
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_12(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(None, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_13(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, None)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_14(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_15(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, )
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_16(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = None
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_17(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["XXidXX"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_18(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["ID"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_19(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(None)
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_20(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["XXidXX"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_21(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["ID"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_22(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = None
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_23(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["XXrelease_idXX"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_24(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["RELEASE_ID"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_25(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(None)
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_26(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["XXrelease_idXX"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_27(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["RELEASE_ID"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_28(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = None
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_29(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["XXmetadataXX"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_30(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["METADATA"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_31(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(None)
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_32(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get(None))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_33(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("XXmetadataXX"))
        return data

    def xǁPostgresDALǁget_release_meta_by_release_id__mutmut_34(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("METADATA"))
        return data
    
    xǁPostgresDALǁget_release_meta_by_release_id__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_1': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_1, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_2': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_2, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_3': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_3, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_4': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_4, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_5': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_5, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_6': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_6, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_7': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_7, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_8': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_8, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_9': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_9, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_10': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_10, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_11': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_11, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_12': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_12, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_13': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_13, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_14': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_14, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_15': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_15, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_16': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_16, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_17': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_17, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_18': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_18, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_19': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_19, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_20': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_20, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_21': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_21, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_22': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_22, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_23': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_23, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_24': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_24, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_25': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_25, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_26': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_26, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_27': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_27, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_28': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_28, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_29': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_29, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_30': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_30, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_31': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_31, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_32': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_32, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_33': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_33, 
        'xǁPostgresDALǁget_release_meta_by_release_id__mutmut_34': xǁPostgresDALǁget_release_meta_by_release_id__mutmut_34
    }
    
    def get_release_meta_by_release_id(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPostgresDALǁget_release_meta_by_release_id__mutmut_orig"), object.__getattribute__(self, "xǁPostgresDALǁget_release_meta_by_release_id__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_release_meta_by_release_id.__signature__ = _mutmut_signature(xǁPostgresDALǁget_release_meta_by_release_id__mutmut_orig)
    xǁPostgresDALǁget_release_meta_by_release_id__mutmut_orig.__name__ = 'xǁPostgresDALǁget_release_meta_by_release_id'


class MariaDbDAL(BaseDAL):
    def xǁMariaDbDALǁ__init____mutmut_orig(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_1(self, dsn: str):
        self.dsn = None
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_2(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError(None) from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_3(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("XXpymysql is required for mariadb backendXX") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_4(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("PYMYSQL IS REQUIRED FOR MARIADB BACKEND") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_5(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = None
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_6(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = None
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_7(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(None, autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_8(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=None)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_9(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_10(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), )
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_11(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(None), autocommit=False)
        self.ensure_schema()
    def xǁMariaDbDALǁ__init____mutmut_12(self, dsn: str):
        self.dsn = dsn
        try:
            import pymysql  # type: ignore
        except Exception as e:  # pragma: no cover - import guard
            raise RuntimeError("pymysql is required for mariadb backend") from e
        self.mysql = pymysql
        self.conn = self.mysql.connect(self._parse_dsn(self.dsn), autocommit=True)
        self.ensure_schema()
    
    xǁMariaDbDALǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁ__init____mutmut_1': xǁMariaDbDALǁ__init____mutmut_1, 
        'xǁMariaDbDALǁ__init____mutmut_2': xǁMariaDbDALǁ__init____mutmut_2, 
        'xǁMariaDbDALǁ__init____mutmut_3': xǁMariaDbDALǁ__init____mutmut_3, 
        'xǁMariaDbDALǁ__init____mutmut_4': xǁMariaDbDALǁ__init____mutmut_4, 
        'xǁMariaDbDALǁ__init____mutmut_5': xǁMariaDbDALǁ__init____mutmut_5, 
        'xǁMariaDbDALǁ__init____mutmut_6': xǁMariaDbDALǁ__init____mutmut_6, 
        'xǁMariaDbDALǁ__init____mutmut_7': xǁMariaDbDALǁ__init____mutmut_7, 
        'xǁMariaDbDALǁ__init____mutmut_8': xǁMariaDbDALǁ__init____mutmut_8, 
        'xǁMariaDbDALǁ__init____mutmut_9': xǁMariaDbDALǁ__init____mutmut_9, 
        'xǁMariaDbDALǁ__init____mutmut_10': xǁMariaDbDALǁ__init____mutmut_10, 
        'xǁMariaDbDALǁ__init____mutmut_11': xǁMariaDbDALǁ__init____mutmut_11, 
        'xǁMariaDbDALǁ__init____mutmut_12': xǁMariaDbDALǁ__init____mutmut_12
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁMariaDbDALǁ__init____mutmut_orig)
    xǁMariaDbDALǁ__init____mutmut_orig.__name__ = 'xǁMariaDbDALǁ__init__'

    @staticmethod
    def from_url(url: str) -> MariaDbDAL:
        return MariaDbDAL(url)

    def xǁMariaDbDALǁ_parse_dsn__mutmut_orig(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_1(self, url: str) -> dict[str, Any]:
        parsed = None
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_2(self, url: str) -> dict[str, Any]:
        parsed = urlparse(None)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_3(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_4(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError(None)

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_5(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("XXMariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/dbXX")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_6(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("mariadb dsn must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_7(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MARIADB DSN MUST INCLUDE A SCHEME, E.G. MARIADB://USER:PASS@HOST/DB")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_8(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = None

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_9(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = None
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_10(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["XXhostXX"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_11(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["HOST"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_12(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_13(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = None
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_14(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["XXportXX"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_15(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["PORT"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_16(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_17(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = None
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_18(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["XXuserXX"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_19(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["USER"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_20(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_21(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = None

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_22(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["XXpasswordXX"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_23(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["PASSWORD"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_24(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = None
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_25(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip(None)
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_26(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.rstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_27(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("XX/XX")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_28(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = None

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_29(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["XXdatabaseXX"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_30(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["DATABASE"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_31(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(None, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_32(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=None):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_33(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_34(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, ):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_35(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=False):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_36(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key != "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_37(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "XXportXX":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_38(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "PORT":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_39(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(None):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_40(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = None
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_41(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(None)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_42(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = None

        config.setdefault("host", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_43(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault(None, "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_44(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", None)
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_45(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_46(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", )
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_47(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("XXhostXX", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_48(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("HOST", "localhost")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_49(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "XXlocalhostXX")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_50(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "LOCALHOST")
        config.setdefault("database", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_51(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault(None, "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_52(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", None)

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_53(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_54(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", )

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_55(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("XXdatabaseXX", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_56(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("DATABASE", "")

        return config

    def xǁMariaDbDALǁ_parse_dsn__mutmut_57(self, url: str) -> dict[str, Any]:
        parsed = urlparse(url)
        if not parsed.scheme:
            raise ValueError("MariaDB DSN must include a scheme, e.g. mariadb://user:pass@host/db")

        config: dict[str, Any] = {}

        if parsed.hostname:
            config["host"] = parsed.hostname
        if parsed.port is not None:
            config["port"] = parsed.port
        if parsed.username is not None:
            config["user"] = parsed.username
        if parsed.password is not None:
            config["password"] = parsed.password

        database = parsed.path.lstrip("/")
        if database:
            config["database"] = database

        for key, value in parse_qsl(parsed.query, keep_blank_values=True):
            if key == "port":
                with contextlib.suppress(ValueError):
                    value = int(value)
            config[key] = value

        config.setdefault("host", "localhost")
        config.setdefault("database", "XXXX")

        return config
    
    xǁMariaDbDALǁ_parse_dsn__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁ_parse_dsn__mutmut_1': xǁMariaDbDALǁ_parse_dsn__mutmut_1, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_2': xǁMariaDbDALǁ_parse_dsn__mutmut_2, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_3': xǁMariaDbDALǁ_parse_dsn__mutmut_3, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_4': xǁMariaDbDALǁ_parse_dsn__mutmut_4, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_5': xǁMariaDbDALǁ_parse_dsn__mutmut_5, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_6': xǁMariaDbDALǁ_parse_dsn__mutmut_6, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_7': xǁMariaDbDALǁ_parse_dsn__mutmut_7, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_8': xǁMariaDbDALǁ_parse_dsn__mutmut_8, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_9': xǁMariaDbDALǁ_parse_dsn__mutmut_9, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_10': xǁMariaDbDALǁ_parse_dsn__mutmut_10, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_11': xǁMariaDbDALǁ_parse_dsn__mutmut_11, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_12': xǁMariaDbDALǁ_parse_dsn__mutmut_12, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_13': xǁMariaDbDALǁ_parse_dsn__mutmut_13, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_14': xǁMariaDbDALǁ_parse_dsn__mutmut_14, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_15': xǁMariaDbDALǁ_parse_dsn__mutmut_15, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_16': xǁMariaDbDALǁ_parse_dsn__mutmut_16, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_17': xǁMariaDbDALǁ_parse_dsn__mutmut_17, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_18': xǁMariaDbDALǁ_parse_dsn__mutmut_18, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_19': xǁMariaDbDALǁ_parse_dsn__mutmut_19, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_20': xǁMariaDbDALǁ_parse_dsn__mutmut_20, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_21': xǁMariaDbDALǁ_parse_dsn__mutmut_21, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_22': xǁMariaDbDALǁ_parse_dsn__mutmut_22, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_23': xǁMariaDbDALǁ_parse_dsn__mutmut_23, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_24': xǁMariaDbDALǁ_parse_dsn__mutmut_24, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_25': xǁMariaDbDALǁ_parse_dsn__mutmut_25, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_26': xǁMariaDbDALǁ_parse_dsn__mutmut_26, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_27': xǁMariaDbDALǁ_parse_dsn__mutmut_27, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_28': xǁMariaDbDALǁ_parse_dsn__mutmut_28, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_29': xǁMariaDbDALǁ_parse_dsn__mutmut_29, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_30': xǁMariaDbDALǁ_parse_dsn__mutmut_30, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_31': xǁMariaDbDALǁ_parse_dsn__mutmut_31, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_32': xǁMariaDbDALǁ_parse_dsn__mutmut_32, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_33': xǁMariaDbDALǁ_parse_dsn__mutmut_33, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_34': xǁMariaDbDALǁ_parse_dsn__mutmut_34, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_35': xǁMariaDbDALǁ_parse_dsn__mutmut_35, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_36': xǁMariaDbDALǁ_parse_dsn__mutmut_36, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_37': xǁMariaDbDALǁ_parse_dsn__mutmut_37, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_38': xǁMariaDbDALǁ_parse_dsn__mutmut_38, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_39': xǁMariaDbDALǁ_parse_dsn__mutmut_39, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_40': xǁMariaDbDALǁ_parse_dsn__mutmut_40, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_41': xǁMariaDbDALǁ_parse_dsn__mutmut_41, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_42': xǁMariaDbDALǁ_parse_dsn__mutmut_42, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_43': xǁMariaDbDALǁ_parse_dsn__mutmut_43, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_44': xǁMariaDbDALǁ_parse_dsn__mutmut_44, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_45': xǁMariaDbDALǁ_parse_dsn__mutmut_45, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_46': xǁMariaDbDALǁ_parse_dsn__mutmut_46, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_47': xǁMariaDbDALǁ_parse_dsn__mutmut_47, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_48': xǁMariaDbDALǁ_parse_dsn__mutmut_48, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_49': xǁMariaDbDALǁ_parse_dsn__mutmut_49, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_50': xǁMariaDbDALǁ_parse_dsn__mutmut_50, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_51': xǁMariaDbDALǁ_parse_dsn__mutmut_51, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_52': xǁMariaDbDALǁ_parse_dsn__mutmut_52, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_53': xǁMariaDbDALǁ_parse_dsn__mutmut_53, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_54': xǁMariaDbDALǁ_parse_dsn__mutmut_54, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_55': xǁMariaDbDALǁ_parse_dsn__mutmut_55, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_56': xǁMariaDbDALǁ_parse_dsn__mutmut_56, 
        'xǁMariaDbDALǁ_parse_dsn__mutmut_57': xǁMariaDbDALǁ_parse_dsn__mutmut_57
    }
    
    def _parse_dsn(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁ_parse_dsn__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁ_parse_dsn__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_dsn.__signature__ = _mutmut_signature(xǁMariaDbDALǁ_parse_dsn__mutmut_orig)
    xǁMariaDbDALǁ_parse_dsn__mutmut_orig.__name__ = 'xǁMariaDbDALǁ_parse_dsn'

    def txn(self) -> contextlib.AbstractContextManager[None]:
        @contextlib.contextmanager
        def _tx() -> Iterator[None]:
            try:
                yield
                self.conn.commit()
            except Exception:  # pragma: no cover - passthrough
                self.conn.rollback()
                raise

        return _tx()

    def xǁMariaDbDALǁensure_schema__mutmut_orig(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_1(self) -> None:
        here = None
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_2(self) -> None:
        here = Path(None).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_3(self) -> None:
        here = Path(__file__).resolve()
        root = None
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_4(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[4]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_5(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = None
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_6(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" * "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_7(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" * "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_8(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root * "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_9(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "XXdbXX" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_10(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "DB" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_11(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "XXmigrationsXX" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_12(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "MIGRATIONS" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_13(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "XXmariadbXX"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_14(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "MARIADB"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_15(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = None
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_16(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(None)
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_17(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob(None))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_18(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("XX*.sqlXX"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_19(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.SQL"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_20(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_21(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = None
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_22(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = None
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_23(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding=None)
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_24(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="XXutf-8XX")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_25(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="UTF-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_26(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(None):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_27(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split("XX;XX"):
                        if stmt.strip():
                            cur.execute(stmt)
            finally:
                cur.close()

    def xǁMariaDbDALǁensure_schema__mutmut_28(self) -> None:
        here = Path(__file__).resolve()
        root = here.parents[3]
        migrations_dir = root / "db" / "migrations" / "mariadb"
        sql_files = sorted(migrations_dir.glob("*.sql"))
        if not sql_files:
            return
        with self.txn():
            cur = self.conn.cursor()
            try:
                for path in sql_files:
                    script = path.read_text(encoding="utf-8")
                    for stmt in script.split(";"):
                        if stmt.strip():
                            cur.execute(None)
            finally:
                cur.close()
    
    xǁMariaDbDALǁensure_schema__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁensure_schema__mutmut_1': xǁMariaDbDALǁensure_schema__mutmut_1, 
        'xǁMariaDbDALǁensure_schema__mutmut_2': xǁMariaDbDALǁensure_schema__mutmut_2, 
        'xǁMariaDbDALǁensure_schema__mutmut_3': xǁMariaDbDALǁensure_schema__mutmut_3, 
        'xǁMariaDbDALǁensure_schema__mutmut_4': xǁMariaDbDALǁensure_schema__mutmut_4, 
        'xǁMariaDbDALǁensure_schema__mutmut_5': xǁMariaDbDALǁensure_schema__mutmut_5, 
        'xǁMariaDbDALǁensure_schema__mutmut_6': xǁMariaDbDALǁensure_schema__mutmut_6, 
        'xǁMariaDbDALǁensure_schema__mutmut_7': xǁMariaDbDALǁensure_schema__mutmut_7, 
        'xǁMariaDbDALǁensure_schema__mutmut_8': xǁMariaDbDALǁensure_schema__mutmut_8, 
        'xǁMariaDbDALǁensure_schema__mutmut_9': xǁMariaDbDALǁensure_schema__mutmut_9, 
        'xǁMariaDbDALǁensure_schema__mutmut_10': xǁMariaDbDALǁensure_schema__mutmut_10, 
        'xǁMariaDbDALǁensure_schema__mutmut_11': xǁMariaDbDALǁensure_schema__mutmut_11, 
        'xǁMariaDbDALǁensure_schema__mutmut_12': xǁMariaDbDALǁensure_schema__mutmut_12, 
        'xǁMariaDbDALǁensure_schema__mutmut_13': xǁMariaDbDALǁensure_schema__mutmut_13, 
        'xǁMariaDbDALǁensure_schema__mutmut_14': xǁMariaDbDALǁensure_schema__mutmut_14, 
        'xǁMariaDbDALǁensure_schema__mutmut_15': xǁMariaDbDALǁensure_schema__mutmut_15, 
        'xǁMariaDbDALǁensure_schema__mutmut_16': xǁMariaDbDALǁensure_schema__mutmut_16, 
        'xǁMariaDbDALǁensure_schema__mutmut_17': xǁMariaDbDALǁensure_schema__mutmut_17, 
        'xǁMariaDbDALǁensure_schema__mutmut_18': xǁMariaDbDALǁensure_schema__mutmut_18, 
        'xǁMariaDbDALǁensure_schema__mutmut_19': xǁMariaDbDALǁensure_schema__mutmut_19, 
        'xǁMariaDbDALǁensure_schema__mutmut_20': xǁMariaDbDALǁensure_schema__mutmut_20, 
        'xǁMariaDbDALǁensure_schema__mutmut_21': xǁMariaDbDALǁensure_schema__mutmut_21, 
        'xǁMariaDbDALǁensure_schema__mutmut_22': xǁMariaDbDALǁensure_schema__mutmut_22, 
        'xǁMariaDbDALǁensure_schema__mutmut_23': xǁMariaDbDALǁensure_schema__mutmut_23, 
        'xǁMariaDbDALǁensure_schema__mutmut_24': xǁMariaDbDALǁensure_schema__mutmut_24, 
        'xǁMariaDbDALǁensure_schema__mutmut_25': xǁMariaDbDALǁensure_schema__mutmut_25, 
        'xǁMariaDbDALǁensure_schema__mutmut_26': xǁMariaDbDALǁensure_schema__mutmut_26, 
        'xǁMariaDbDALǁensure_schema__mutmut_27': xǁMariaDbDALǁensure_schema__mutmut_27, 
        'xǁMariaDbDALǁensure_schema__mutmut_28': xǁMariaDbDALǁensure_schema__mutmut_28
    }
    
    def ensure_schema(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁensure_schema__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁensure_schema__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_schema.__signature__ = _mutmut_signature(xǁMariaDbDALǁensure_schema__mutmut_orig)
    xǁMariaDbDALǁensure_schema__mutmut_orig.__name__ = 'xǁMariaDbDALǁensure_schema'

    def xǁMariaDbDALǁensure_artifact__mutmut_orig(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB artifact operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_1(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_2(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXMariaDB artifact operations not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_3(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "mariadb artifact operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_4(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MARIADB ARTIFACT OPERATIONS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_5(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB artifact operations not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_6(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB artifact operations not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁensure_artifact__mutmut_7(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB artifact operations not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁMariaDbDALǁensure_artifact__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁensure_artifact__mutmut_1': xǁMariaDbDALǁensure_artifact__mutmut_1, 
        'xǁMariaDbDALǁensure_artifact__mutmut_2': xǁMariaDbDALǁensure_artifact__mutmut_2, 
        'xǁMariaDbDALǁensure_artifact__mutmut_3': xǁMariaDbDALǁensure_artifact__mutmut_3, 
        'xǁMariaDbDALǁensure_artifact__mutmut_4': xǁMariaDbDALǁensure_artifact__mutmut_4, 
        'xǁMariaDbDALǁensure_artifact__mutmut_5': xǁMariaDbDALǁensure_artifact__mutmut_5, 
        'xǁMariaDbDALǁensure_artifact__mutmut_6': xǁMariaDbDALǁensure_artifact__mutmut_6, 
        'xǁMariaDbDALǁensure_artifact__mutmut_7': xǁMariaDbDALǁensure_artifact__mutmut_7
    }
    
    def ensure_artifact(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁensure_artifact__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁensure_artifact__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_artifact.__signature__ = _mutmut_signature(xǁMariaDbDALǁensure_artifact__mutmut_orig)
    xǁMariaDbDALǁensure_artifact__mutmut_orig.__name__ = 'xǁMariaDbDALǁensure_artifact'

    def xǁMariaDbDALǁinsert_item__mutmut_orig(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB item operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_item__mutmut_1(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁMariaDbDALǁinsert_item__mutmut_2(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXMariaDB item operations not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_item__mutmut_3(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "mariadb item operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_item__mutmut_4(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MARIADB ITEM OPERATIONS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_item__mutmut_5(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB item operations not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁMariaDbDALǁinsert_item__mutmut_6(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB item operations not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_item__mutmut_7(self, **_: Any) -> dict[str, Any]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB item operations not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁMariaDbDALǁinsert_item__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁinsert_item__mutmut_1': xǁMariaDbDALǁinsert_item__mutmut_1, 
        'xǁMariaDbDALǁinsert_item__mutmut_2': xǁMariaDbDALǁinsert_item__mutmut_2, 
        'xǁMariaDbDALǁinsert_item__mutmut_3': xǁMariaDbDALǁinsert_item__mutmut_3, 
        'xǁMariaDbDALǁinsert_item__mutmut_4': xǁMariaDbDALǁinsert_item__mutmut_4, 
        'xǁMariaDbDALǁinsert_item__mutmut_5': xǁMariaDbDALǁinsert_item__mutmut_5, 
        'xǁMariaDbDALǁinsert_item__mutmut_6': xǁMariaDbDALǁinsert_item__mutmut_6, 
        'xǁMariaDbDALǁinsert_item__mutmut_7': xǁMariaDbDALǁinsert_item__mutmut_7
    }
    
    def insert_item(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁinsert_item__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁinsert_item__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_item.__signature__ = _mutmut_signature(xǁMariaDbDALǁinsert_item__mutmut_orig)
    xǁMariaDbDALǁinsert_item__mutmut_orig.__name__ = 'xǁMariaDbDALǁinsert_item'

    def xǁMariaDbDALǁinsert_event__mutmut_orig(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB event operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_event__mutmut_1(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁMariaDbDALǁinsert_event__mutmut_2(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "XXMariaDB event operations not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_event__mutmut_3(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "mariadb event operations not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_event__mutmut_4(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "MARIADB EVENT OPERATIONS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_event__mutmut_5(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB event operations not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁMariaDbDALǁinsert_event__mutmut_6(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB event operations not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁinsert_event__mutmut_7(self, **_: Any) -> None:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB event operations not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁMariaDbDALǁinsert_event__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁinsert_event__mutmut_1': xǁMariaDbDALǁinsert_event__mutmut_1, 
        'xǁMariaDbDALǁinsert_event__mutmut_2': xǁMariaDbDALǁinsert_event__mutmut_2, 
        'xǁMariaDbDALǁinsert_event__mutmut_3': xǁMariaDbDALǁinsert_event__mutmut_3, 
        'xǁMariaDbDALǁinsert_event__mutmut_4': xǁMariaDbDALǁinsert_event__mutmut_4, 
        'xǁMariaDbDALǁinsert_event__mutmut_5': xǁMariaDbDALǁinsert_event__mutmut_5, 
        'xǁMariaDbDALǁinsert_event__mutmut_6': xǁMariaDbDALǁinsert_event__mutmut_6, 
        'xǁMariaDbDALǁinsert_event__mutmut_7': xǁMariaDbDALǁinsert_event__mutmut_7
    }
    
    def insert_event(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁinsert_event__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁinsert_event__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_event.__signature__ = _mutmut_signature(xǁMariaDbDALǁinsert_event__mutmut_orig)
    xǁMariaDbDALǁinsert_event__mutmut_orig.__name__ = 'xǁMariaDbDALǁinsert_event'

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_orig(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_1(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = None
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_2(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute(None, (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_3(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", None)
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_4(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute((tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_5(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", )
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_6(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("XXSELECT * FROM item WHERE tombstone_id = %sXX", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_7(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("select * from item where tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_8(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM ITEM WHERE TOMBSTONE_ID = %S", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_9(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = None
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_10(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_11(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(None)
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_12(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = None
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_13(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(None, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_14(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, None)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_15(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_16(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, )
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_17(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute(None, (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_18(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", None)
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_19(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute((item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_20(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", )
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_21(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("XXSELECT * FROM artifact WHERE id = %sXX", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_22(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("select * from artifact where id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_23(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM ARTIFACT WHERE ID = %S", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_24(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["XXartifact_idXX"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_25(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["ARTIFACT_ID"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_26(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = None
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_27(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_28(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(None)
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_29(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['XXidXX']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_30(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['ID']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_31(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = None
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_32(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(None, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_33(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, None)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_34(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_35(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, )
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_36(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = None
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_37(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(None)
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_38(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get(None))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_39(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("XXmetadataXX"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_40(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("METADATA"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_41(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = None

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_42(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=None,
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_43(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=None,
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_44(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=None,
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_45(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=None,
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_46(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=None,
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_47(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=None,
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_48(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=None,
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_49(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=None,
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_50(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=None,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_51(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=None,
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_52(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_53(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_54(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_55(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_56(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_57(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_58(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_59(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_60(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_61(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_62(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(None),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_63(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["XXidXX"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_64(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["ID"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_65(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(None),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_66(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["XXrepoXX"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_67(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["REPO"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_68(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(None),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_69(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["XXpathXX"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_70(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["PATH"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_71(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(None),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_72(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["XXcommit_shaXX"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_73(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["COMMIT_SHA"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_74(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(None),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_75(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") and ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_76(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get(None) or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_77(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("XXlanguageXX") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_78(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("LANGUAGE") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_79(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or "XXXX"),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_80(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(None),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_81(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["XXkindXX"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_82(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["KIND"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_83(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(None),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_84(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["XXreasonXX"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_85(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["REASON"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_86(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(None),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_87(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["XXartifact_idXX"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_88(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["ARTIFACT_ID"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_89(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(None),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_90(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["XXtombstone_idXX"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_91(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["TOMBSTONE_ID"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_92(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = None
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_93(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(None)
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_94(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get(None))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_95(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("XXblob_bytesXX"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_96(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("BLOB_BYTES"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_97(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = None
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_98(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=None,
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_99(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=None,
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_100(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=None,
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_101(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=None,
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_102(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=None,
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_103(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=None,
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_104(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=None,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_105(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=None,
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_106(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_107(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_108(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_109(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_110(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_111(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_112(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_113(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_114(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(None),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_115(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["XXidXX"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_116(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["ID"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_117(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(None),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_118(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["XXcontent_sha256XX"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_119(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["CONTENT_SHA256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_120(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(None),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_121(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["XXsize_bytesXX"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_122(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["SIZE_BYTES"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_123(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(None),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_124(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["XXcompressionXX"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_125(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["COMPRESSION"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_126(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(None),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_127(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["XXmime_typeXX"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_128(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["MIME_TYPE"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_129(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(None),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_130(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["XXstorage_driverXX"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_131(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["STORAGE_DRIVER"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_132(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(None) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_133(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["XXobject_urlXX"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_134(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["OBJECT_URL"]) if art_data.get("object_url") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_135(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get(None) is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_136(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("XXobject_urlXX") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_137(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("OBJECT_URL") is not None else None
            ),
        )
        return item, artifact

    def xǁMariaDbDALǁfetch_by_tombstone__mutmut_138(self, tombstone_id: str) -> tuple[ItemRow, ArtifactRow]:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM item WHERE tombstone_id = %s", (tombstone_id,))
            row = cur.fetchone()
            if not row:
                raise KeyError(f"Tombstone not found: {tombstone_id}")
            item_data = _cursor_row_to_dict(cur, row)
            cur.execute("SELECT * FROM artifact WHERE id = %s", (item_data["artifact_id"],))
            art_row = cur.fetchone()
            if not art_row:
                raise KeyError(f"Artifact missing for item {item_data['id']}")
            art_data = _cursor_row_to_dict(cur, art_row)
        finally:
            cur.close()

        metadata = _decode_json_field(item_data.get("metadata"))
        item = ItemRow(
            id=str(item_data["id"]),
            repo=str(item_data["repo"]),
            path=str(item_data["path"]),
            commit_sha=str(item_data["commit_sha"]),
            language=str(item_data.get("language") or ""),
            kind=str(item_data["kind"]),
            reason=str(item_data["reason"]),
            artifact_id=str(item_data["artifact_id"]),
            metadata=metadata,
            tombstone_id=str(item_data["tombstone_id"]),
        )

        blob_bytes = _maybe_bytes(art_data.get("blob_bytes"))
        artifact = ArtifactRow(
            id=str(art_data["id"]),
            content_sha256=str(art_data["content_sha256"]),
            size_bytes=int(art_data["size_bytes"]),
            compression=str(art_data["compression"]),
            mime_type=str(art_data["mime_type"]),
            storage_driver=str(art_data["storage_driver"]),
            blob_bytes=blob_bytes,
            object_url=(
                str(art_data["object_url"]) if art_data.get("object_url") is None else None
            ),
        )
        return item, artifact
    
    xǁMariaDbDALǁfetch_by_tombstone__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁfetch_by_tombstone__mutmut_1': xǁMariaDbDALǁfetch_by_tombstone__mutmut_1, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_2': xǁMariaDbDALǁfetch_by_tombstone__mutmut_2, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_3': xǁMariaDbDALǁfetch_by_tombstone__mutmut_3, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_4': xǁMariaDbDALǁfetch_by_tombstone__mutmut_4, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_5': xǁMariaDbDALǁfetch_by_tombstone__mutmut_5, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_6': xǁMariaDbDALǁfetch_by_tombstone__mutmut_6, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_7': xǁMariaDbDALǁfetch_by_tombstone__mutmut_7, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_8': xǁMariaDbDALǁfetch_by_tombstone__mutmut_8, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_9': xǁMariaDbDALǁfetch_by_tombstone__mutmut_9, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_10': xǁMariaDbDALǁfetch_by_tombstone__mutmut_10, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_11': xǁMariaDbDALǁfetch_by_tombstone__mutmut_11, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_12': xǁMariaDbDALǁfetch_by_tombstone__mutmut_12, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_13': xǁMariaDbDALǁfetch_by_tombstone__mutmut_13, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_14': xǁMariaDbDALǁfetch_by_tombstone__mutmut_14, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_15': xǁMariaDbDALǁfetch_by_tombstone__mutmut_15, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_16': xǁMariaDbDALǁfetch_by_tombstone__mutmut_16, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_17': xǁMariaDbDALǁfetch_by_tombstone__mutmut_17, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_18': xǁMariaDbDALǁfetch_by_tombstone__mutmut_18, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_19': xǁMariaDbDALǁfetch_by_tombstone__mutmut_19, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_20': xǁMariaDbDALǁfetch_by_tombstone__mutmut_20, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_21': xǁMariaDbDALǁfetch_by_tombstone__mutmut_21, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_22': xǁMariaDbDALǁfetch_by_tombstone__mutmut_22, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_23': xǁMariaDbDALǁfetch_by_tombstone__mutmut_23, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_24': xǁMariaDbDALǁfetch_by_tombstone__mutmut_24, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_25': xǁMariaDbDALǁfetch_by_tombstone__mutmut_25, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_26': xǁMariaDbDALǁfetch_by_tombstone__mutmut_26, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_27': xǁMariaDbDALǁfetch_by_tombstone__mutmut_27, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_28': xǁMariaDbDALǁfetch_by_tombstone__mutmut_28, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_29': xǁMariaDbDALǁfetch_by_tombstone__mutmut_29, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_30': xǁMariaDbDALǁfetch_by_tombstone__mutmut_30, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_31': xǁMariaDbDALǁfetch_by_tombstone__mutmut_31, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_32': xǁMariaDbDALǁfetch_by_tombstone__mutmut_32, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_33': xǁMariaDbDALǁfetch_by_tombstone__mutmut_33, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_34': xǁMariaDbDALǁfetch_by_tombstone__mutmut_34, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_35': xǁMariaDbDALǁfetch_by_tombstone__mutmut_35, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_36': xǁMariaDbDALǁfetch_by_tombstone__mutmut_36, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_37': xǁMariaDbDALǁfetch_by_tombstone__mutmut_37, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_38': xǁMariaDbDALǁfetch_by_tombstone__mutmut_38, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_39': xǁMariaDbDALǁfetch_by_tombstone__mutmut_39, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_40': xǁMariaDbDALǁfetch_by_tombstone__mutmut_40, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_41': xǁMariaDbDALǁfetch_by_tombstone__mutmut_41, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_42': xǁMariaDbDALǁfetch_by_tombstone__mutmut_42, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_43': xǁMariaDbDALǁfetch_by_tombstone__mutmut_43, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_44': xǁMariaDbDALǁfetch_by_tombstone__mutmut_44, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_45': xǁMariaDbDALǁfetch_by_tombstone__mutmut_45, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_46': xǁMariaDbDALǁfetch_by_tombstone__mutmut_46, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_47': xǁMariaDbDALǁfetch_by_tombstone__mutmut_47, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_48': xǁMariaDbDALǁfetch_by_tombstone__mutmut_48, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_49': xǁMariaDbDALǁfetch_by_tombstone__mutmut_49, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_50': xǁMariaDbDALǁfetch_by_tombstone__mutmut_50, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_51': xǁMariaDbDALǁfetch_by_tombstone__mutmut_51, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_52': xǁMariaDbDALǁfetch_by_tombstone__mutmut_52, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_53': xǁMariaDbDALǁfetch_by_tombstone__mutmut_53, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_54': xǁMariaDbDALǁfetch_by_tombstone__mutmut_54, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_55': xǁMariaDbDALǁfetch_by_tombstone__mutmut_55, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_56': xǁMariaDbDALǁfetch_by_tombstone__mutmut_56, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_57': xǁMariaDbDALǁfetch_by_tombstone__mutmut_57, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_58': xǁMariaDbDALǁfetch_by_tombstone__mutmut_58, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_59': xǁMariaDbDALǁfetch_by_tombstone__mutmut_59, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_60': xǁMariaDbDALǁfetch_by_tombstone__mutmut_60, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_61': xǁMariaDbDALǁfetch_by_tombstone__mutmut_61, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_62': xǁMariaDbDALǁfetch_by_tombstone__mutmut_62, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_63': xǁMariaDbDALǁfetch_by_tombstone__mutmut_63, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_64': xǁMariaDbDALǁfetch_by_tombstone__mutmut_64, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_65': xǁMariaDbDALǁfetch_by_tombstone__mutmut_65, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_66': xǁMariaDbDALǁfetch_by_tombstone__mutmut_66, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_67': xǁMariaDbDALǁfetch_by_tombstone__mutmut_67, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_68': xǁMariaDbDALǁfetch_by_tombstone__mutmut_68, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_69': xǁMariaDbDALǁfetch_by_tombstone__mutmut_69, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_70': xǁMariaDbDALǁfetch_by_tombstone__mutmut_70, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_71': xǁMariaDbDALǁfetch_by_tombstone__mutmut_71, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_72': xǁMariaDbDALǁfetch_by_tombstone__mutmut_72, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_73': xǁMariaDbDALǁfetch_by_tombstone__mutmut_73, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_74': xǁMariaDbDALǁfetch_by_tombstone__mutmut_74, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_75': xǁMariaDbDALǁfetch_by_tombstone__mutmut_75, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_76': xǁMariaDbDALǁfetch_by_tombstone__mutmut_76, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_77': xǁMariaDbDALǁfetch_by_tombstone__mutmut_77, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_78': xǁMariaDbDALǁfetch_by_tombstone__mutmut_78, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_79': xǁMariaDbDALǁfetch_by_tombstone__mutmut_79, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_80': xǁMariaDbDALǁfetch_by_tombstone__mutmut_80, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_81': xǁMariaDbDALǁfetch_by_tombstone__mutmut_81, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_82': xǁMariaDbDALǁfetch_by_tombstone__mutmut_82, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_83': xǁMariaDbDALǁfetch_by_tombstone__mutmut_83, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_84': xǁMariaDbDALǁfetch_by_tombstone__mutmut_84, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_85': xǁMariaDbDALǁfetch_by_tombstone__mutmut_85, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_86': xǁMariaDbDALǁfetch_by_tombstone__mutmut_86, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_87': xǁMariaDbDALǁfetch_by_tombstone__mutmut_87, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_88': xǁMariaDbDALǁfetch_by_tombstone__mutmut_88, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_89': xǁMariaDbDALǁfetch_by_tombstone__mutmut_89, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_90': xǁMariaDbDALǁfetch_by_tombstone__mutmut_90, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_91': xǁMariaDbDALǁfetch_by_tombstone__mutmut_91, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_92': xǁMariaDbDALǁfetch_by_tombstone__mutmut_92, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_93': xǁMariaDbDALǁfetch_by_tombstone__mutmut_93, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_94': xǁMariaDbDALǁfetch_by_tombstone__mutmut_94, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_95': xǁMariaDbDALǁfetch_by_tombstone__mutmut_95, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_96': xǁMariaDbDALǁfetch_by_tombstone__mutmut_96, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_97': xǁMariaDbDALǁfetch_by_tombstone__mutmut_97, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_98': xǁMariaDbDALǁfetch_by_tombstone__mutmut_98, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_99': xǁMariaDbDALǁfetch_by_tombstone__mutmut_99, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_100': xǁMariaDbDALǁfetch_by_tombstone__mutmut_100, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_101': xǁMariaDbDALǁfetch_by_tombstone__mutmut_101, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_102': xǁMariaDbDALǁfetch_by_tombstone__mutmut_102, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_103': xǁMariaDbDALǁfetch_by_tombstone__mutmut_103, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_104': xǁMariaDbDALǁfetch_by_tombstone__mutmut_104, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_105': xǁMariaDbDALǁfetch_by_tombstone__mutmut_105, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_106': xǁMariaDbDALǁfetch_by_tombstone__mutmut_106, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_107': xǁMariaDbDALǁfetch_by_tombstone__mutmut_107, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_108': xǁMariaDbDALǁfetch_by_tombstone__mutmut_108, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_109': xǁMariaDbDALǁfetch_by_tombstone__mutmut_109, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_110': xǁMariaDbDALǁfetch_by_tombstone__mutmut_110, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_111': xǁMariaDbDALǁfetch_by_tombstone__mutmut_111, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_112': xǁMariaDbDALǁfetch_by_tombstone__mutmut_112, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_113': xǁMariaDbDALǁfetch_by_tombstone__mutmut_113, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_114': xǁMariaDbDALǁfetch_by_tombstone__mutmut_114, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_115': xǁMariaDbDALǁfetch_by_tombstone__mutmut_115, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_116': xǁMariaDbDALǁfetch_by_tombstone__mutmut_116, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_117': xǁMariaDbDALǁfetch_by_tombstone__mutmut_117, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_118': xǁMariaDbDALǁfetch_by_tombstone__mutmut_118, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_119': xǁMariaDbDALǁfetch_by_tombstone__mutmut_119, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_120': xǁMariaDbDALǁfetch_by_tombstone__mutmut_120, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_121': xǁMariaDbDALǁfetch_by_tombstone__mutmut_121, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_122': xǁMariaDbDALǁfetch_by_tombstone__mutmut_122, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_123': xǁMariaDbDALǁfetch_by_tombstone__mutmut_123, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_124': xǁMariaDbDALǁfetch_by_tombstone__mutmut_124, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_125': xǁMariaDbDALǁfetch_by_tombstone__mutmut_125, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_126': xǁMariaDbDALǁfetch_by_tombstone__mutmut_126, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_127': xǁMariaDbDALǁfetch_by_tombstone__mutmut_127, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_128': xǁMariaDbDALǁfetch_by_tombstone__mutmut_128, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_129': xǁMariaDbDALǁfetch_by_tombstone__mutmut_129, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_130': xǁMariaDbDALǁfetch_by_tombstone__mutmut_130, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_131': xǁMariaDbDALǁfetch_by_tombstone__mutmut_131, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_132': xǁMariaDbDALǁfetch_by_tombstone__mutmut_132, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_133': xǁMariaDbDALǁfetch_by_tombstone__mutmut_133, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_134': xǁMariaDbDALǁfetch_by_tombstone__mutmut_134, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_135': xǁMariaDbDALǁfetch_by_tombstone__mutmut_135, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_136': xǁMariaDbDALǁfetch_by_tombstone__mutmut_136, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_137': xǁMariaDbDALǁfetch_by_tombstone__mutmut_137, 
        'xǁMariaDbDALǁfetch_by_tombstone__mutmut_138': xǁMariaDbDALǁfetch_by_tombstone__mutmut_138
    }
    
    def fetch_by_tombstone(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁfetch_by_tombstone__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁfetch_by_tombstone__mutmut_mutants"), args, kwargs, self)
        return result 
    
    fetch_by_tombstone.__signature__ = _mutmut_signature(xǁMariaDbDALǁfetch_by_tombstone__mutmut_orig)
    xǁMariaDbDALǁfetch_by_tombstone__mutmut_orig.__name__ = 'xǁMariaDbDALǁfetch_by_tombstone'

    def xǁMariaDbDALǁinsert_referent__mutmut_orig(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s)",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_1(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = None
            try:
                cur.execute(
                    "INSERT IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s)",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_2(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    None,
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_3(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s)",
                    None,
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_4(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_5(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s)",
                    )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_6(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "XXINSERT IGNORE INTO referent (item_id, ref_type, ref_value) VALUES (%s,%s,%s)XX",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_7(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "insert ignore into referent (item_id, ref_type, ref_value) values (%s,%s,%s)",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()

    def xǁMariaDbDALǁinsert_referent__mutmut_8(self, *, item_id: str, ref_type: str, ref_value: str) -> None:
        with self.txn():
            cur = self.conn.cursor()
            try:
                cur.execute(
                    "INSERT IGNORE INTO REFERENT (ITEM_ID, REF_TYPE, REF_VALUE) VALUES (%S,%S,%S)",
                    (item_id, ref_type, ref_value),
                )
            finally:
                cur.close()
    
    xǁMariaDbDALǁinsert_referent__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁinsert_referent__mutmut_1': xǁMariaDbDALǁinsert_referent__mutmut_1, 
        'xǁMariaDbDALǁinsert_referent__mutmut_2': xǁMariaDbDALǁinsert_referent__mutmut_2, 
        'xǁMariaDbDALǁinsert_referent__mutmut_3': xǁMariaDbDALǁinsert_referent__mutmut_3, 
        'xǁMariaDbDALǁinsert_referent__mutmut_4': xǁMariaDbDALǁinsert_referent__mutmut_4, 
        'xǁMariaDbDALǁinsert_referent__mutmut_5': xǁMariaDbDALǁinsert_referent__mutmut_5, 
        'xǁMariaDbDALǁinsert_referent__mutmut_6': xǁMariaDbDALǁinsert_referent__mutmut_6, 
        'xǁMariaDbDALǁinsert_referent__mutmut_7': xǁMariaDbDALǁinsert_referent__mutmut_7, 
        'xǁMariaDbDALǁinsert_referent__mutmut_8': xǁMariaDbDALǁinsert_referent__mutmut_8
    }
    
    def insert_referent(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁinsert_referent__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁinsert_referent__mutmut_mutants"), args, kwargs, self)
        return result 
    
    insert_referent.__signature__ = _mutmut_signature(xǁMariaDbDALǁinsert_referent__mutmut_orig)
    xǁMariaDbDALǁinsert_referent__mutmut_orig.__name__ = 'xǁMariaDbDALǁinsert_referent'

    def xǁMariaDbDALǁrecent_items__mutmut_orig(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB recent_items not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁrecent_items__mutmut_1(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁMariaDbDALǁrecent_items__mutmut_2(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXMariaDB recent_items not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁrecent_items__mutmut_3(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "mariadb recent_items not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁrecent_items__mutmut_4(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "MARIADB RECENT_ITEMS NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁrecent_items__mutmut_5(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB recent_items not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁMariaDbDALǁrecent_items__mutmut_6(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB recent_items not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁrecent_items__mutmut_7(self, *_: Any, **__: Any) -> list[dict[str, Any]]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB recent_items not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁMariaDbDALǁrecent_items__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁrecent_items__mutmut_1': xǁMariaDbDALǁrecent_items__mutmut_1, 
        'xǁMariaDbDALǁrecent_items__mutmut_2': xǁMariaDbDALǁrecent_items__mutmut_2, 
        'xǁMariaDbDALǁrecent_items__mutmut_3': xǁMariaDbDALǁrecent_items__mutmut_3, 
        'xǁMariaDbDALǁrecent_items__mutmut_4': xǁMariaDbDALǁrecent_items__mutmut_4, 
        'xǁMariaDbDALǁrecent_items__mutmut_5': xǁMariaDbDALǁrecent_items__mutmut_5, 
        'xǁMariaDbDALǁrecent_items__mutmut_6': xǁMariaDbDALǁrecent_items__mutmut_6, 
        'xǁMariaDbDALǁrecent_items__mutmut_7': xǁMariaDbDALǁrecent_items__mutmut_7
    }
    
    def recent_items(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁrecent_items__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁrecent_items__mutmut_mutants"), args, kwargs, self)
        return result 
    
    recent_items.__signature__ = _mutmut_signature(xǁMariaDbDALǁrecent_items__mutmut_orig)
    xǁMariaDbDALǁrecent_items__mutmut_orig.__name__ = 'xǁMariaDbDALǁrecent_items'

    def xǁMariaDbDALǁsummary__mutmut_orig(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB summary not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁsummary__mutmut_1(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            None
        )

    def xǁMariaDbDALǁsummary__mutmut_2(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "XXMariaDB summary not yet implemented. XX"
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁsummary__mutmut_3(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "mariadb summary not yet implemented. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁsummary__mutmut_4(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "MARIADB SUMMARY NOT YET IMPLEMENTED. "
            "Use CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁsummary__mutmut_5(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB summary not yet implemented. "
            "XXUse CODEX_ARCHIVE_BACKEND=sqlite (default) for offline/local mode.XX"
        )

    def xǁMariaDbDALǁsummary__mutmut_6(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB summary not yet implemented. "
            "use codex_archive_backend=sqlite (default) for offline/local mode."
        )

    def xǁMariaDbDALǁsummary__mutmut_7(self) -> dict[str, int]:  # pragma: no cover - stub
        raise RuntimeError(
            "MariaDB summary not yet implemented. "
            "USE CODEX_ARCHIVE_BACKEND=SQLITE (DEFAULT) FOR OFFLINE/LOCAL MODE."
        )
    
    xǁMariaDbDALǁsummary__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁsummary__mutmut_1': xǁMariaDbDALǁsummary__mutmut_1, 
        'xǁMariaDbDALǁsummary__mutmut_2': xǁMariaDbDALǁsummary__mutmut_2, 
        'xǁMariaDbDALǁsummary__mutmut_3': xǁMariaDbDALǁsummary__mutmut_3, 
        'xǁMariaDbDALǁsummary__mutmut_4': xǁMariaDbDALǁsummary__mutmut_4, 
        'xǁMariaDbDALǁsummary__mutmut_5': xǁMariaDbDALǁsummary__mutmut_5, 
        'xǁMariaDbDALǁsummary__mutmut_6': xǁMariaDbDALǁsummary__mutmut_6, 
        'xǁMariaDbDALǁsummary__mutmut_7': xǁMariaDbDALǁsummary__mutmut_7
    }
    
    def summary(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁsummary__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁsummary__mutmut_mutants"), args, kwargs, self)
        return result 
    
    summary.__signature__ = _mutmut_signature(xǁMariaDbDALǁsummary__mutmut_orig)
    xǁMariaDbDALǁsummary__mutmut_orig.__name__ = 'xǁMariaDbDALǁsummary'

    def xǁMariaDbDALǁcreate_release_meta__mutmut_orig(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_1(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = None
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_2(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(None)
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_3(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = None
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_4(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(None, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_5(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=None, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_6(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=None)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_7(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_8(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_9(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, )
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_10(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata and {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_11(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=True, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_12(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=False)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_13(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = None
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_14(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = None
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_15(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "XXINSERT INTO release_meta XX"
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_16(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "insert into release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_17(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO RELEASE_META "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_18(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "XX(id, release_id, version, created_at, actor, metadata)\nXX"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_19(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(ID, RELEASE_ID, VERSION, CREATED_AT, ACTOR, METADATA)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_20(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "XXVALUES (%s,%s,%s,%s,%s,%s)XX"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_21(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "values (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_22(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%S,%S,%S,%S,%S,%S)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_23(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(None, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_24(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, None)
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_25(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute((rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_26(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, )
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_27(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"XXidXX": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_28(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"ID": rid, "release_id": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_29(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "XXrelease_idXX": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_30(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "RELEASE_ID": release_id, "version": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_31(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "XXversionXX": version}

    def xǁMariaDbDALǁcreate_release_meta__mutmut_32(
        self,
        *,
        release_id: str,
        version: str,
        created_at: str,
        actor: str,
        metadata: dict[str, Any],
    ) -> dict[str, Any]:
        rid = str(uuid.uuid4())
        payload = json.dumps(metadata or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_meta "
                    "(id, release_id, version, created_at, actor, metadata)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(sql, (rid, release_id, version, created_at, actor, payload))
            finally:
                cur.close()
        return {"id": rid, "release_id": release_id, "VERSION": version}
    
    xǁMariaDbDALǁcreate_release_meta__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁcreate_release_meta__mutmut_1': xǁMariaDbDALǁcreate_release_meta__mutmut_1, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_2': xǁMariaDbDALǁcreate_release_meta__mutmut_2, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_3': xǁMariaDbDALǁcreate_release_meta__mutmut_3, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_4': xǁMariaDbDALǁcreate_release_meta__mutmut_4, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_5': xǁMariaDbDALǁcreate_release_meta__mutmut_5, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_6': xǁMariaDbDALǁcreate_release_meta__mutmut_6, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_7': xǁMariaDbDALǁcreate_release_meta__mutmut_7, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_8': xǁMariaDbDALǁcreate_release_meta__mutmut_8, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_9': xǁMariaDbDALǁcreate_release_meta__mutmut_9, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_10': xǁMariaDbDALǁcreate_release_meta__mutmut_10, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_11': xǁMariaDbDALǁcreate_release_meta__mutmut_11, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_12': xǁMariaDbDALǁcreate_release_meta__mutmut_12, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_13': xǁMariaDbDALǁcreate_release_meta__mutmut_13, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_14': xǁMariaDbDALǁcreate_release_meta__mutmut_14, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_15': xǁMariaDbDALǁcreate_release_meta__mutmut_15, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_16': xǁMariaDbDALǁcreate_release_meta__mutmut_16, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_17': xǁMariaDbDALǁcreate_release_meta__mutmut_17, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_18': xǁMariaDbDALǁcreate_release_meta__mutmut_18, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_19': xǁMariaDbDALǁcreate_release_meta__mutmut_19, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_20': xǁMariaDbDALǁcreate_release_meta__mutmut_20, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_21': xǁMariaDbDALǁcreate_release_meta__mutmut_21, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_22': xǁMariaDbDALǁcreate_release_meta__mutmut_22, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_23': xǁMariaDbDALǁcreate_release_meta__mutmut_23, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_24': xǁMariaDbDALǁcreate_release_meta__mutmut_24, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_25': xǁMariaDbDALǁcreate_release_meta__mutmut_25, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_26': xǁMariaDbDALǁcreate_release_meta__mutmut_26, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_27': xǁMariaDbDALǁcreate_release_meta__mutmut_27, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_28': xǁMariaDbDALǁcreate_release_meta__mutmut_28, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_29': xǁMariaDbDALǁcreate_release_meta__mutmut_29, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_30': xǁMariaDbDALǁcreate_release_meta__mutmut_30, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_31': xǁMariaDbDALǁcreate_release_meta__mutmut_31, 
        'xǁMariaDbDALǁcreate_release_meta__mutmut_32': xǁMariaDbDALǁcreate_release_meta__mutmut_32
    }
    
    def create_release_meta(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁcreate_release_meta__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁcreate_release_meta__mutmut_mutants"), args, kwargs, self)
        return result 
    
    create_release_meta.__signature__ = _mutmut_signature(xǁMariaDbDALǁcreate_release_meta__mutmut_orig)
    xǁMariaDbDALǁcreate_release_meta__mutmut_orig.__name__ = 'xǁMariaDbDALǁcreate_release_meta'

    def xǁMariaDbDALǁadd_release_component__mutmut_orig(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_1(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = None
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_2(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(None)
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_3(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = None
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_4(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(None, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_5(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=None, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_6(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=None)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_7(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_8(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_9(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, )
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_10(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars and {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_11(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=True, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_12(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=False)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_13(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = None
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_14(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = None
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_15(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "XXINSERT INTO release_component XX"
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_16(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "insert into release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_17(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO RELEASE_COMPONENT "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_18(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "XX(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\nXX"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_19(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(ID, RELEASE_ID, ITEM_ID, TOMBSTONE, DEST_PATH, MODE, TEMPLATE_VARS)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_20(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "XXVALUES (%s,%s,%s,%s,%s,%s,%s)XX"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_21(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "values (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_22(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%S,%S,%S,%S,%S,%S,%S)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_23(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    None,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_24(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    None,
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_25(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_26(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_27(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"XXidXX": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_28(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"ID": cid, "release_id": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_29(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "XXrelease_idXX": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_30(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "RELEASE_ID": release_meta_id, "tombstone": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_31(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "XXtombstoneXX": tombstone}

    def xǁMariaDbDALǁadd_release_component__mutmut_32(
        self,
        *,
        release_meta_id: str,
        item_id: str | None,
        tombstone: str,
        dest_path: str,
        mode: str,
        template_vars: dict[str, Any],
    ) -> dict[str, Any]:
        cid = str(uuid.uuid4())
        payload = json.dumps(template_vars or {}, ensure_ascii=False, sort_keys=True)
        with self.txn():
            cur = self.conn.cursor()
            try:
                sql = (
                    "INSERT INTO release_component "
                    "(id, release_id, item_id, tombstone, dest_path, mode, template_vars)\n"
                    "VALUES (%s,%s,%s,%s,%s,%s,%s)"
                )
                cur.execute(
                    sql,
                    (cid, release_meta_id, item_id, tombstone, dest_path, mode, payload),
                )
            finally:
                cur.close()
        return {"id": cid, "release_id": release_meta_id, "TOMBSTONE": tombstone}
    
    xǁMariaDbDALǁadd_release_component__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁadd_release_component__mutmut_1': xǁMariaDbDALǁadd_release_component__mutmut_1, 
        'xǁMariaDbDALǁadd_release_component__mutmut_2': xǁMariaDbDALǁadd_release_component__mutmut_2, 
        'xǁMariaDbDALǁadd_release_component__mutmut_3': xǁMariaDbDALǁadd_release_component__mutmut_3, 
        'xǁMariaDbDALǁadd_release_component__mutmut_4': xǁMariaDbDALǁadd_release_component__mutmut_4, 
        'xǁMariaDbDALǁadd_release_component__mutmut_5': xǁMariaDbDALǁadd_release_component__mutmut_5, 
        'xǁMariaDbDALǁadd_release_component__mutmut_6': xǁMariaDbDALǁadd_release_component__mutmut_6, 
        'xǁMariaDbDALǁadd_release_component__mutmut_7': xǁMariaDbDALǁadd_release_component__mutmut_7, 
        'xǁMariaDbDALǁadd_release_component__mutmut_8': xǁMariaDbDALǁadd_release_component__mutmut_8, 
        'xǁMariaDbDALǁadd_release_component__mutmut_9': xǁMariaDbDALǁadd_release_component__mutmut_9, 
        'xǁMariaDbDALǁadd_release_component__mutmut_10': xǁMariaDbDALǁadd_release_component__mutmut_10, 
        'xǁMariaDbDALǁadd_release_component__mutmut_11': xǁMariaDbDALǁadd_release_component__mutmut_11, 
        'xǁMariaDbDALǁadd_release_component__mutmut_12': xǁMariaDbDALǁadd_release_component__mutmut_12, 
        'xǁMariaDbDALǁadd_release_component__mutmut_13': xǁMariaDbDALǁadd_release_component__mutmut_13, 
        'xǁMariaDbDALǁadd_release_component__mutmut_14': xǁMariaDbDALǁadd_release_component__mutmut_14, 
        'xǁMariaDbDALǁadd_release_component__mutmut_15': xǁMariaDbDALǁadd_release_component__mutmut_15, 
        'xǁMariaDbDALǁadd_release_component__mutmut_16': xǁMariaDbDALǁadd_release_component__mutmut_16, 
        'xǁMariaDbDALǁadd_release_component__mutmut_17': xǁMariaDbDALǁadd_release_component__mutmut_17, 
        'xǁMariaDbDALǁadd_release_component__mutmut_18': xǁMariaDbDALǁadd_release_component__mutmut_18, 
        'xǁMariaDbDALǁadd_release_component__mutmut_19': xǁMariaDbDALǁadd_release_component__mutmut_19, 
        'xǁMariaDbDALǁadd_release_component__mutmut_20': xǁMariaDbDALǁadd_release_component__mutmut_20, 
        'xǁMariaDbDALǁadd_release_component__mutmut_21': xǁMariaDbDALǁadd_release_component__mutmut_21, 
        'xǁMariaDbDALǁadd_release_component__mutmut_22': xǁMariaDbDALǁadd_release_component__mutmut_22, 
        'xǁMariaDbDALǁadd_release_component__mutmut_23': xǁMariaDbDALǁadd_release_component__mutmut_23, 
        'xǁMariaDbDALǁadd_release_component__mutmut_24': xǁMariaDbDALǁadd_release_component__mutmut_24, 
        'xǁMariaDbDALǁadd_release_component__mutmut_25': xǁMariaDbDALǁadd_release_component__mutmut_25, 
        'xǁMariaDbDALǁadd_release_component__mutmut_26': xǁMariaDbDALǁadd_release_component__mutmut_26, 
        'xǁMariaDbDALǁadd_release_component__mutmut_27': xǁMariaDbDALǁadd_release_component__mutmut_27, 
        'xǁMariaDbDALǁadd_release_component__mutmut_28': xǁMariaDbDALǁadd_release_component__mutmut_28, 
        'xǁMariaDbDALǁadd_release_component__mutmut_29': xǁMariaDbDALǁadd_release_component__mutmut_29, 
        'xǁMariaDbDALǁadd_release_component__mutmut_30': xǁMariaDbDALǁadd_release_component__mutmut_30, 
        'xǁMariaDbDALǁadd_release_component__mutmut_31': xǁMariaDbDALǁadd_release_component__mutmut_31, 
        'xǁMariaDbDALǁadd_release_component__mutmut_32': xǁMariaDbDALǁadd_release_component__mutmut_32
    }
    
    def add_release_component(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁadd_release_component__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁadd_release_component__mutmut_mutants"), args, kwargs, self)
        return result 
    
    add_release_component.__signature__ = _mutmut_signature(xǁMariaDbDALǁadd_release_component__mutmut_orig)
    xǁMariaDbDALǁadd_release_component__mutmut_orig.__name__ = 'xǁMariaDbDALǁadd_release_component'

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_orig(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_1(self, *, release_id: str) -> dict[str, Any] | None:
        cur = None
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_2(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute(None, (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_3(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", None)
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_4(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute((release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_5(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", )
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_6(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("XXSELECT * FROM release_meta WHERE release_id = %s LIMIT 1XX", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_7(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("select * from release_meta where release_id = %s limit 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_8(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM RELEASE_META WHERE RELEASE_ID = %S LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_9(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = None
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_10(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_11(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = None
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_12(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(None, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_13(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, None)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_14(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_15(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, )
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_16(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = None
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_17(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["XXidXX"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_18(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["ID"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_19(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(None)
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_20(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["XXidXX"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_21(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["ID"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_22(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = None
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_23(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["XXrelease_idXX"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_24(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["RELEASE_ID"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_25(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(None)
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_26(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["XXrelease_idXX"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_27(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["RELEASE_ID"])
        data["metadata"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_28(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = None
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_29(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["XXmetadataXX"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_30(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["METADATA"] = _decode_json_field(data.get("metadata"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_31(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(None)
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_32(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get(None))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_33(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("XXmetadataXX"))
        return data

    def xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_34(self, *, release_id: str) -> dict[str, Any] | None:
        cur = self.conn.cursor()
        try:
            cur.execute("SELECT * FROM release_meta WHERE release_id = %s LIMIT 1", (release_id,))
            row = cur.fetchone()
            if not row:
                return None
            data = _cursor_row_to_dict(cur, row)
        finally:
            cur.close()
        data["id"] = str(data["id"])
        data["release_id"] = str(data["release_id"])
        data["metadata"] = _decode_json_field(data.get("METADATA"))
        return data
    
    xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_1': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_1, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_2': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_2, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_3': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_3, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_4': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_4, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_5': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_5, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_6': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_6, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_7': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_7, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_8': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_8, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_9': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_9, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_10': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_10, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_11': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_11, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_12': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_12, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_13': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_13, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_14': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_14, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_15': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_15, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_16': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_16, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_17': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_17, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_18': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_18, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_19': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_19, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_20': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_20, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_21': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_21, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_22': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_22, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_23': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_23, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_24': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_24, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_25': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_25, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_26': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_26, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_27': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_27, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_28': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_28, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_29': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_29, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_30': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_30, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_31': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_31, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_32': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_32, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_33': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_33, 
        'xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_34': xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_34
    }
    
    def get_release_meta_by_release_id(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_orig"), object.__getattribute__(self, "xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_release_meta_by_release_id.__signature__ = _mutmut_signature(xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_orig)
    xǁMariaDbDALǁget_release_meta_by_release_id__mutmut_orig.__name__ = 'xǁMariaDbDALǁget_release_meta_by_release_id'
