"""
Backend Module

This module provides functionality for backend.

Usage:
    from archive.backend import ...

Classes:
    [To be documented]

Functions:
    [To be documented]

Author: Codex Team
"""

from __future__ import annotations
import logging
logger = logging.getLogger(__name__)
"""Database access layer for the Codex archive."""


import json
import os
import sqlite3
import uuid
from collections.abc import Callable, Iterable, Iterator, Mapping
from contextlib import contextmanager
from dataclasses import dataclass
from pathlib import Path
from typing import TYPE_CHECKING, Any

try:  # pragma: no cover - optional dependency
    import sqlalchemy as sa  # type: ignore
except Exception:  # pragma: no cover
    sa = None

from . import schema
from .config import ArchiveAppConfig as RuntimeArchiveConfig
from .util import ensure_directory, json_dumps_sorted, utcnow

if TYPE_CHECKING:  # pragma: no cover - typing only
    from .config import ArchiveAppConfig as SettingsArchiveConfig

Params = dict[str, Any]
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@dataclass(frozen=True)
class ArchiveConfig:
    """Runtime configuration for the archive backend."""

    url: str
    backend: str

    @classmethod
    def from_env(cls, env: Mapping[str, str] | None = None) -> ArchiveConfig:
        runtime_env: dict[str, str] = dict(os.environ)
        if env is not None:
            runtime_env.update(env)
        settings = RuntimeArchiveConfig.from_env(env=runtime_env)
        return cls(url=settings.backend.url, backend=settings.backend.backend)

    @classmethod
    def from_settings(
        cls,
        settings: RuntimeArchiveConfig | SettingsArchiveConfig,
    ) -> ArchiveConfig:
        """Create a runtime backend config from archive settings."""

        return cls(url=settings.backend.url, backend=settings.backend.backend)


def x_infer_backend__mutmut_orig(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_1(url: str) -> str:
    lowered = None
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_2(url: str) -> str:
    lowered = url.upper()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_3(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith(None):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_4(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("XXpostgresXX"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_5(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("POSTGRES"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_6(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "XXpostgresXX"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_7(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "POSTGRES"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_8(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") and lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_9(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith(None) or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_10(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("XXmariadbXX") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_11(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("MARIADB") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_12(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith(None):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_13(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("XXmysqlXX"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_14(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("MYSQL"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_15(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "XXmariadbXX"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_16(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "MARIADB"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_17(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith(None):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_18(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("XXsqliteXX"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_19(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("SQLITE"):
        return "sqlite"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_20(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "XXsqliteXX"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_21(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "SQLITE"
    raise ValueError(f"Unable to infer archive backend from URL: {url}")


def x_infer_backend__mutmut_22(url: str) -> str:
    lowered = url.lower()
    if lowered.startswith("postgres"):
        return "postgres"
    if lowered.startswith("mariadb") or lowered.startswith("mysql"):
        return "mariadb"
    if lowered.startswith("sqlite"):
        return "sqlite"
    raise ValueError(None)

x_infer_backend__mutmut_mutants : ClassVar[MutantDict] = {
'x_infer_backend__mutmut_1': x_infer_backend__mutmut_1, 
    'x_infer_backend__mutmut_2': x_infer_backend__mutmut_2, 
    'x_infer_backend__mutmut_3': x_infer_backend__mutmut_3, 
    'x_infer_backend__mutmut_4': x_infer_backend__mutmut_4, 
    'x_infer_backend__mutmut_5': x_infer_backend__mutmut_5, 
    'x_infer_backend__mutmut_6': x_infer_backend__mutmut_6, 
    'x_infer_backend__mutmut_7': x_infer_backend__mutmut_7, 
    'x_infer_backend__mutmut_8': x_infer_backend__mutmut_8, 
    'x_infer_backend__mutmut_9': x_infer_backend__mutmut_9, 
    'x_infer_backend__mutmut_10': x_infer_backend__mutmut_10, 
    'x_infer_backend__mutmut_11': x_infer_backend__mutmut_11, 
    'x_infer_backend__mutmut_12': x_infer_backend__mutmut_12, 
    'x_infer_backend__mutmut_13': x_infer_backend__mutmut_13, 
    'x_infer_backend__mutmut_14': x_infer_backend__mutmut_14, 
    'x_infer_backend__mutmut_15': x_infer_backend__mutmut_15, 
    'x_infer_backend__mutmut_16': x_infer_backend__mutmut_16, 
    'x_infer_backend__mutmut_17': x_infer_backend__mutmut_17, 
    'x_infer_backend__mutmut_18': x_infer_backend__mutmut_18, 
    'x_infer_backend__mutmut_19': x_infer_backend__mutmut_19, 
    'x_infer_backend__mutmut_20': x_infer_backend__mutmut_20, 
    'x_infer_backend__mutmut_21': x_infer_backend__mutmut_21, 
    'x_infer_backend__mutmut_22': x_infer_backend__mutmut_22
}

def infer_backend(*args, **kwargs):
    result = _mutmut_trampoline(x_infer_backend__mutmut_orig, x_infer_backend__mutmut_mutants, args, kwargs)
    return result 

infer_backend.__signature__ = _mutmut_signature(x_infer_backend__mutmut_orig)
x_infer_backend__mutmut_orig.__name__ = 'x_infer_backend'


class ArchiveDAL:
    """Archive data access layer supporting PostgreSQL, MariaDB, and SQLite."""

    def xǁArchiveDALǁ__init____mutmut_orig(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_1(self, config: ArchiveConfig | None = None, *, apply_schema: bool = False) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_2(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = None
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_3(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config and ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_4(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = None
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_5(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = None
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_6(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = ""
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_7(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = ""
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_8(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend != "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_9(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "XXsqliteXX":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_10(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "SQLITE":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_11(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = None
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_12(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(None)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_13(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(None)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_14(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = None
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_15(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(None)
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_16(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(None))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_17(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = None
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_18(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is not None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_19(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    None
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_20(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "XXsqlalchemy is required for non-sqlite archive backends. XX"
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_21(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "SQLALCHEMY IS REQUIRED FOR NON-SQLITE ARCHIVE BACKENDS. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_22(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "XXInstall sqlalchemy>=2.0XX"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_23(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_24(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "INSTALL SQLALCHEMY>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_25(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = None
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_26(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(None, future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_27(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=None)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_28(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(future=True)
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_29(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, )
        if apply_schema:
            self.ensure_schema()

    def xǁArchiveDALǁ__init____mutmut_30(self, config: ArchiveConfig | None = None, *, apply_schema: bool = True) -> None:
        self.config = config or ArchiveConfig.from_env()
        self.backend = self.config.backend
        self.url = self.config.url
        self._conn: sqlite3.Connection | None = None
        self._engine: Any | None = None
        if self.backend == "sqlite":
            path = self._sqlite_path(self.url)
            ensure_directory(path.parent)
            self._conn = sqlite3.connect(str(path))
            self._conn.row_factory = sqlite3.Row
        else:
            if sa is None:  # pragma: no cover - informative guard
                raise RuntimeError(
                    "sqlalchemy is required for non-sqlite archive backends. "
                    "Install sqlalchemy>=2.0"
                )
            self._engine = sa.create_engine(self.url, future=False)
        if apply_schema:
            self.ensure_schema()
    
    xǁArchiveDALǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ__init____mutmut_1': xǁArchiveDALǁ__init____mutmut_1, 
        'xǁArchiveDALǁ__init____mutmut_2': xǁArchiveDALǁ__init____mutmut_2, 
        'xǁArchiveDALǁ__init____mutmut_3': xǁArchiveDALǁ__init____mutmut_3, 
        'xǁArchiveDALǁ__init____mutmut_4': xǁArchiveDALǁ__init____mutmut_4, 
        'xǁArchiveDALǁ__init____mutmut_5': xǁArchiveDALǁ__init____mutmut_5, 
        'xǁArchiveDALǁ__init____mutmut_6': xǁArchiveDALǁ__init____mutmut_6, 
        'xǁArchiveDALǁ__init____mutmut_7': xǁArchiveDALǁ__init____mutmut_7, 
        'xǁArchiveDALǁ__init____mutmut_8': xǁArchiveDALǁ__init____mutmut_8, 
        'xǁArchiveDALǁ__init____mutmut_9': xǁArchiveDALǁ__init____mutmut_9, 
        'xǁArchiveDALǁ__init____mutmut_10': xǁArchiveDALǁ__init____mutmut_10, 
        'xǁArchiveDALǁ__init____mutmut_11': xǁArchiveDALǁ__init____mutmut_11, 
        'xǁArchiveDALǁ__init____mutmut_12': xǁArchiveDALǁ__init____mutmut_12, 
        'xǁArchiveDALǁ__init____mutmut_13': xǁArchiveDALǁ__init____mutmut_13, 
        'xǁArchiveDALǁ__init____mutmut_14': xǁArchiveDALǁ__init____mutmut_14, 
        'xǁArchiveDALǁ__init____mutmut_15': xǁArchiveDALǁ__init____mutmut_15, 
        'xǁArchiveDALǁ__init____mutmut_16': xǁArchiveDALǁ__init____mutmut_16, 
        'xǁArchiveDALǁ__init____mutmut_17': xǁArchiveDALǁ__init____mutmut_17, 
        'xǁArchiveDALǁ__init____mutmut_18': xǁArchiveDALǁ__init____mutmut_18, 
        'xǁArchiveDALǁ__init____mutmut_19': xǁArchiveDALǁ__init____mutmut_19, 
        'xǁArchiveDALǁ__init____mutmut_20': xǁArchiveDALǁ__init____mutmut_20, 
        'xǁArchiveDALǁ__init____mutmut_21': xǁArchiveDALǁ__init____mutmut_21, 
        'xǁArchiveDALǁ__init____mutmut_22': xǁArchiveDALǁ__init____mutmut_22, 
        'xǁArchiveDALǁ__init____mutmut_23': xǁArchiveDALǁ__init____mutmut_23, 
        'xǁArchiveDALǁ__init____mutmut_24': xǁArchiveDALǁ__init____mutmut_24, 
        'xǁArchiveDALǁ__init____mutmut_25': xǁArchiveDALǁ__init____mutmut_25, 
        'xǁArchiveDALǁ__init____mutmut_26': xǁArchiveDALǁ__init____mutmut_26, 
        'xǁArchiveDALǁ__init____mutmut_27': xǁArchiveDALǁ__init____mutmut_27, 
        'xǁArchiveDALǁ__init____mutmut_28': xǁArchiveDALǁ__init____mutmut_28, 
        'xǁArchiveDALǁ__init____mutmut_29': xǁArchiveDALǁ__init____mutmut_29, 
        'xǁArchiveDALǁ__init____mutmut_30': xǁArchiveDALǁ__init____mutmut_30
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁArchiveDALǁ__init____mutmut_orig)
    xǁArchiveDALǁ__init____mutmut_orig.__name__ = 'xǁArchiveDALǁ__init__'

    # ------------------------------------------------------------------
    # schema management
    # ------------------------------------------------------------------
    def xǁArchiveDALǁensure_schema__mutmut_orig(self) -> None:
        """Apply the schema bundle for the configured backend."""

        statements = schema.statements_for(self.backend)
        with self._transaction() as execute:
            for statement in statements:
                execute(statement)

    # ------------------------------------------------------------------
    # schema management
    # ------------------------------------------------------------------
    def xǁArchiveDALǁensure_schema__mutmut_1(self) -> None:
        """Apply the schema bundle for the configured backend."""

        statements = None
        with self._transaction() as execute:
            for statement in statements:
                execute(statement)

    # ------------------------------------------------------------------
    # schema management
    # ------------------------------------------------------------------
    def xǁArchiveDALǁensure_schema__mutmut_2(self) -> None:
        """Apply the schema bundle for the configured backend."""

        statements = schema.statements_for(None)
        with self._transaction() as execute:
            for statement in statements:
                execute(statement)

    # ------------------------------------------------------------------
    # schema management
    # ------------------------------------------------------------------
    def xǁArchiveDALǁensure_schema__mutmut_3(self) -> None:
        """Apply the schema bundle for the configured backend."""

        statements = schema.statements_for(self.backend)
        with self._transaction() as execute:
            for statement in statements:
                execute(None)
    
    xǁArchiveDALǁensure_schema__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁensure_schema__mutmut_1': xǁArchiveDALǁensure_schema__mutmut_1, 
        'xǁArchiveDALǁensure_schema__mutmut_2': xǁArchiveDALǁensure_schema__mutmut_2, 
        'xǁArchiveDALǁensure_schema__mutmut_3': xǁArchiveDALǁensure_schema__mutmut_3
    }
    
    def ensure_schema(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁensure_schema__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁensure_schema__mutmut_mutants"), args, kwargs, self)
        return result 
    
    ensure_schema.__signature__ = _mutmut_signature(xǁArchiveDALǁensure_schema__mutmut_orig)
    xǁArchiveDALǁensure_schema__mutmut_orig.__name__ = 'xǁArchiveDALǁensure_schema'

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_orig(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_1(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = None
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_2(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = None
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_3(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(None)
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_4(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = None
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_5(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(None)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_6(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = None
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_7(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop(None, 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_8(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", None)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_9(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop(0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_10(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", )
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_11(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("XXlegal_holdXX", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_12(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("LEGAL_HOLD", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_13(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 1)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_14(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = None
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_15(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(None)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_16(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = None
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_17(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop(None, None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_18(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop(None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_19(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", )
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_20(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("XXdelete_afterXX", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_21(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("DELETE_AFTER", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_22(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault(None, True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_23(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", None)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_24(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault(True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_25(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", )
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_26(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("XXlegal_holdXX", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_27(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("LEGAL_HOLD", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_28(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", False)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_29(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_30(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault(None, delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_31(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", None)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_32(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault(delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_33(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", )
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_34(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("XXdelete_afterXX", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_35(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("DELETE_AFTER", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_36(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = None
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_37(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(None, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_38(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, None)
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_39(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_40(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, )
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_41(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["XXcontent_sha256XX"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_42(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["CONTENT_SHA256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_43(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is not None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_44(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = None
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_45(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(None)
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_46(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = None
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_47(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "XXidXX": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_48(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "ID": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_49(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "XXcreated_atXX": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_50(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "CREATED_AT": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_51(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    None,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_52(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    None,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_53(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_54(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_55(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = None
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_56(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["XXidXX"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_57(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["ID"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_58(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = None
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_59(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None and artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_60(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get(None) is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_61(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("XXblob_bytesXX") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_62(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("BLOB_BYTES") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_63(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is not None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_64(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get(None) != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_65(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("XXstorage_driverXX") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_66(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("STORAGE_DRIVER") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_67(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") == artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_68(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["XXstorage_driverXX"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_69(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["STORAGE_DRIVER"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_70(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = None
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_71(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    None
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_72(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(None) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_73(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) == artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_74(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("XXsize_bytesXX", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_75(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("SIZE_BYTES", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_76(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "XXcompressionXX", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_77(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "COMPRESSION", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_78(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "XXmime_typeXX")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_79(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "MIME_TYPE")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_80(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh and metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_81(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        None,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_82(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        None,
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_83(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_84(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_85(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"XXidXX": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_86(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"ID": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_87(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = None
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_88(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(None)
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_89(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = None
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_90(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "XXidXX": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_91(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "ID": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_92(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "XXrepoXX": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_93(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "REPO": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_94(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "XXpathXX": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_95(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "PATH": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_96(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "XXcommit_shaXX": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_97(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "COMMIT_SHA": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_98(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "XXlanguageXX": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_99(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "LANGUAGE": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_100(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "XXkindXX": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_101(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "KIND": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_102(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "XXreasonXX": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_103(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "REASON": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_104(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "XXartifact_idXX": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_105(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "ARTIFACT_ID": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_106(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "XXmetadataXX": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_107(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "METADATA": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_108(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(None),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_109(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "XXarchived_byXX": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_110(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "ARCHIVED_BY": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_111(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "XXarchived_atXX": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_112(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "ARCHIVED_AT": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_113(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "XXtombstone_idXX": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_114(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "TOMBSTONE_ID": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_115(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "XXlegal_holdXX": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_116(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "LEGAL_HOLD": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_117(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "XXdelete_afterXX": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_118(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "DELETE_AFTER": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_119(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "XXrestored_atXX": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_120(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "RESTORED_AT": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_121(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                None,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_122(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                None,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_123(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_124(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_125(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = None
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_126(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "XXidXX": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_127(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "ID": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_128(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(None),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_129(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "XXitem_idXX": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_130(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "ITEM_ID": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_131(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "XXactionXX": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_132(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "ACTION": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_133(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "XXARCHIVEXX",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_134(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "archive",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_135(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "XXactorXX": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_136(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "ACTOR": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_137(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "XXcontextXX": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_138(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "CONTEXT": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_139(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(None),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_140(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "XXcreated_atXX": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_141(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "CREATED_AT": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_142(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                None,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_143(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                None,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_144(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_145(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_146(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags and []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_147(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = None
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_148(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"XXitem_idXX": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_149(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"ITEM_ID": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_150(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "XXtagXX": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_151(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "TAG": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_152(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = None
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_153(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    None,
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_154(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    None,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_155(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=None,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_156(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_157(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_158(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_159(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "XXSELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tagXX",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_160(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "select 1 from tag where item_id = :item_id and tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_161(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM TAG WHERE ITEM_ID = :ITEM_ID AND TAG = :TAG",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_162(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=False,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_163(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is not None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_164(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        None,
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_165(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        None,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_166(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_167(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_168(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "XXINSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)XX",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_169(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "insert into tag (item_id, tag) values (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_170(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO TAG (ITEM_ID, TAG) VALUES (:ITEM_ID, :TAG)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_171(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "XXtombstone_idXX": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_172(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "TOMBSTONE_ID": tombstone_id,
            "artifact_id": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_173(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "XXartifact_idXX": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_174(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "ARTIFACT_ID": artifact_id,
            "item_id": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_175(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "XXitem_idXX": item_id,
        }

    # ------------------------------------------------------------------
    # public API
    # ------------------------------------------------------------------
    def xǁArchiveDALǁrecord_archive__mutmut_176(
        self,
        *,
        repo: str,
        path: str,
        commit_sha: str,
        language: str | None,
        reason: str,
        kind: str,
        artifact_payload: dict[str, Any],
        archived_by: str,
        metadata: dict[str, Any],
        context: dict[str, Any],
        tags: Iterable[str] | None = None,
    ) -> dict[str, Any]:
        """Persist an archive record, returning the stored row."""

        now = utcnow()
        tombstone_id = str(uuid.uuid4())
        meta_copy = dict(metadata)
        legal_hold_raw = meta_copy.pop("legal_hold", 0)
        legal_hold_value = _coerce_bool(legal_hold_raw)
        delete_after_value = meta_copy.pop("delete_after", None)
        if legal_hold_value:
            meta_copy.setdefault("legal_hold", True)
        if delete_after_value is not None:
            meta_copy.setdefault("delete_after", delete_after_value)
        with self._transaction() as execute:
            artifact = self._get_artifact_by_sha(execute, artifact_payload["content_sha256"])
            if artifact is None:
                artifact_id = str(uuid.uuid4())
                artifact = {
                    "id": artifact_id,
                    **artifact_payload,
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO artifact (
                        id, content_sha256, size_bytes, compression, mime_type,
                        storage_driver, blob_bytes, object_url, created_at
                    ) VALUES (
                        :id, :content_sha256, :size_bytes, :compression, :mime_type,
                        :storage_driver, :blob_bytes, :object_url, :created_at
                    )
                    """,
                    artifact,
                )
            else:
                artifact_id = artifact["id"]
                needs_refresh = (
                    artifact.get("blob_bytes") is None
                    or artifact.get("storage_driver") != artifact_payload["storage_driver"]
                )
                metadata_changed = any(
                    artifact.get(field) != artifact_payload[field]
                    for field in ("size_bytes", "compression", "mime_type")
                )
                if needs_refresh or metadata_changed:
                    execute(
                        """
                        UPDATE artifact
                        SET size_bytes = :size_bytes,
                            compression = :compression,
                            mime_type = :mime_type,
                            storage_driver = :storage_driver,
                            blob_bytes = :blob_bytes,
                            object_url = :object_url
                        WHERE id = :id
                        """,
                        {"id": artifact_id, **artifact_payload},
                    )

            item_id = str(uuid.uuid4())
            item_payload = {
                "id": item_id,
                "repo": repo,
                "path": path,
                "commit_sha": commit_sha,
                "language": language,
                "kind": kind,
                "reason": reason,
                "artifact_id": artifact_id,
                "metadata": json_dumps_sorted(meta_copy),
                "archived_by": archived_by,
                "archived_at": now,
                "tombstone_id": tombstone_id,
                "legal_hold": legal_hold_value,
                "delete_after": delete_after_value,
                "restored_at": None,
            }
            execute(
                """
                INSERT INTO item (
                    id, repo, path, commit_sha, language, kind, reason, artifact_id,
                    metadata, archived_by, archived_at, tombstone_id, legal_hold,
                    delete_after, restored_at
                ) VALUES (
                    :id, :repo, :path, :commit_sha, :language, :kind, :reason, :artifact_id,
                    :metadata, :archived_by, :archived_at, :tombstone_id, :legal_hold,
                    :delete_after, :restored_at
                )
                """,
                item_payload,
            )

            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item_id,
                "action": "ARCHIVE",
                "actor": archived_by,
                "context": json_dumps_sorted(context),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

            for tag in tags or []:
                params = {"item_id": item_id, "tag": tag}
                existing = execute(
                    "SELECT 1 FROM tag WHERE item_id = :item_id AND tag = :tag",
                    params,
                    fetchone=True,
                )
                if existing is None:
                    execute(
                        "INSERT INTO tag (item_id, tag) VALUES (:item_id, :tag)",
                        params,
                    )

        return {
            "tombstone_id": tombstone_id,
            "artifact_id": artifact_id,
            "ITEM_ID": item_id,
        }
    
    xǁArchiveDALǁrecord_archive__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁrecord_archive__mutmut_1': xǁArchiveDALǁrecord_archive__mutmut_1, 
        'xǁArchiveDALǁrecord_archive__mutmut_2': xǁArchiveDALǁrecord_archive__mutmut_2, 
        'xǁArchiveDALǁrecord_archive__mutmut_3': xǁArchiveDALǁrecord_archive__mutmut_3, 
        'xǁArchiveDALǁrecord_archive__mutmut_4': xǁArchiveDALǁrecord_archive__mutmut_4, 
        'xǁArchiveDALǁrecord_archive__mutmut_5': xǁArchiveDALǁrecord_archive__mutmut_5, 
        'xǁArchiveDALǁrecord_archive__mutmut_6': xǁArchiveDALǁrecord_archive__mutmut_6, 
        'xǁArchiveDALǁrecord_archive__mutmut_7': xǁArchiveDALǁrecord_archive__mutmut_7, 
        'xǁArchiveDALǁrecord_archive__mutmut_8': xǁArchiveDALǁrecord_archive__mutmut_8, 
        'xǁArchiveDALǁrecord_archive__mutmut_9': xǁArchiveDALǁrecord_archive__mutmut_9, 
        'xǁArchiveDALǁrecord_archive__mutmut_10': xǁArchiveDALǁrecord_archive__mutmut_10, 
        'xǁArchiveDALǁrecord_archive__mutmut_11': xǁArchiveDALǁrecord_archive__mutmut_11, 
        'xǁArchiveDALǁrecord_archive__mutmut_12': xǁArchiveDALǁrecord_archive__mutmut_12, 
        'xǁArchiveDALǁrecord_archive__mutmut_13': xǁArchiveDALǁrecord_archive__mutmut_13, 
        'xǁArchiveDALǁrecord_archive__mutmut_14': xǁArchiveDALǁrecord_archive__mutmut_14, 
        'xǁArchiveDALǁrecord_archive__mutmut_15': xǁArchiveDALǁrecord_archive__mutmut_15, 
        'xǁArchiveDALǁrecord_archive__mutmut_16': xǁArchiveDALǁrecord_archive__mutmut_16, 
        'xǁArchiveDALǁrecord_archive__mutmut_17': xǁArchiveDALǁrecord_archive__mutmut_17, 
        'xǁArchiveDALǁrecord_archive__mutmut_18': xǁArchiveDALǁrecord_archive__mutmut_18, 
        'xǁArchiveDALǁrecord_archive__mutmut_19': xǁArchiveDALǁrecord_archive__mutmut_19, 
        'xǁArchiveDALǁrecord_archive__mutmut_20': xǁArchiveDALǁrecord_archive__mutmut_20, 
        'xǁArchiveDALǁrecord_archive__mutmut_21': xǁArchiveDALǁrecord_archive__mutmut_21, 
        'xǁArchiveDALǁrecord_archive__mutmut_22': xǁArchiveDALǁrecord_archive__mutmut_22, 
        'xǁArchiveDALǁrecord_archive__mutmut_23': xǁArchiveDALǁrecord_archive__mutmut_23, 
        'xǁArchiveDALǁrecord_archive__mutmut_24': xǁArchiveDALǁrecord_archive__mutmut_24, 
        'xǁArchiveDALǁrecord_archive__mutmut_25': xǁArchiveDALǁrecord_archive__mutmut_25, 
        'xǁArchiveDALǁrecord_archive__mutmut_26': xǁArchiveDALǁrecord_archive__mutmut_26, 
        'xǁArchiveDALǁrecord_archive__mutmut_27': xǁArchiveDALǁrecord_archive__mutmut_27, 
        'xǁArchiveDALǁrecord_archive__mutmut_28': xǁArchiveDALǁrecord_archive__mutmut_28, 
        'xǁArchiveDALǁrecord_archive__mutmut_29': xǁArchiveDALǁrecord_archive__mutmut_29, 
        'xǁArchiveDALǁrecord_archive__mutmut_30': xǁArchiveDALǁrecord_archive__mutmut_30, 
        'xǁArchiveDALǁrecord_archive__mutmut_31': xǁArchiveDALǁrecord_archive__mutmut_31, 
        'xǁArchiveDALǁrecord_archive__mutmut_32': xǁArchiveDALǁrecord_archive__mutmut_32, 
        'xǁArchiveDALǁrecord_archive__mutmut_33': xǁArchiveDALǁrecord_archive__mutmut_33, 
        'xǁArchiveDALǁrecord_archive__mutmut_34': xǁArchiveDALǁrecord_archive__mutmut_34, 
        'xǁArchiveDALǁrecord_archive__mutmut_35': xǁArchiveDALǁrecord_archive__mutmut_35, 
        'xǁArchiveDALǁrecord_archive__mutmut_36': xǁArchiveDALǁrecord_archive__mutmut_36, 
        'xǁArchiveDALǁrecord_archive__mutmut_37': xǁArchiveDALǁrecord_archive__mutmut_37, 
        'xǁArchiveDALǁrecord_archive__mutmut_38': xǁArchiveDALǁrecord_archive__mutmut_38, 
        'xǁArchiveDALǁrecord_archive__mutmut_39': xǁArchiveDALǁrecord_archive__mutmut_39, 
        'xǁArchiveDALǁrecord_archive__mutmut_40': xǁArchiveDALǁrecord_archive__mutmut_40, 
        'xǁArchiveDALǁrecord_archive__mutmut_41': xǁArchiveDALǁrecord_archive__mutmut_41, 
        'xǁArchiveDALǁrecord_archive__mutmut_42': xǁArchiveDALǁrecord_archive__mutmut_42, 
        'xǁArchiveDALǁrecord_archive__mutmut_43': xǁArchiveDALǁrecord_archive__mutmut_43, 
        'xǁArchiveDALǁrecord_archive__mutmut_44': xǁArchiveDALǁrecord_archive__mutmut_44, 
        'xǁArchiveDALǁrecord_archive__mutmut_45': xǁArchiveDALǁrecord_archive__mutmut_45, 
        'xǁArchiveDALǁrecord_archive__mutmut_46': xǁArchiveDALǁrecord_archive__mutmut_46, 
        'xǁArchiveDALǁrecord_archive__mutmut_47': xǁArchiveDALǁrecord_archive__mutmut_47, 
        'xǁArchiveDALǁrecord_archive__mutmut_48': xǁArchiveDALǁrecord_archive__mutmut_48, 
        'xǁArchiveDALǁrecord_archive__mutmut_49': xǁArchiveDALǁrecord_archive__mutmut_49, 
        'xǁArchiveDALǁrecord_archive__mutmut_50': xǁArchiveDALǁrecord_archive__mutmut_50, 
        'xǁArchiveDALǁrecord_archive__mutmut_51': xǁArchiveDALǁrecord_archive__mutmut_51, 
        'xǁArchiveDALǁrecord_archive__mutmut_52': xǁArchiveDALǁrecord_archive__mutmut_52, 
        'xǁArchiveDALǁrecord_archive__mutmut_53': xǁArchiveDALǁrecord_archive__mutmut_53, 
        'xǁArchiveDALǁrecord_archive__mutmut_54': xǁArchiveDALǁrecord_archive__mutmut_54, 
        'xǁArchiveDALǁrecord_archive__mutmut_55': xǁArchiveDALǁrecord_archive__mutmut_55, 
        'xǁArchiveDALǁrecord_archive__mutmut_56': xǁArchiveDALǁrecord_archive__mutmut_56, 
        'xǁArchiveDALǁrecord_archive__mutmut_57': xǁArchiveDALǁrecord_archive__mutmut_57, 
        'xǁArchiveDALǁrecord_archive__mutmut_58': xǁArchiveDALǁrecord_archive__mutmut_58, 
        'xǁArchiveDALǁrecord_archive__mutmut_59': xǁArchiveDALǁrecord_archive__mutmut_59, 
        'xǁArchiveDALǁrecord_archive__mutmut_60': xǁArchiveDALǁrecord_archive__mutmut_60, 
        'xǁArchiveDALǁrecord_archive__mutmut_61': xǁArchiveDALǁrecord_archive__mutmut_61, 
        'xǁArchiveDALǁrecord_archive__mutmut_62': xǁArchiveDALǁrecord_archive__mutmut_62, 
        'xǁArchiveDALǁrecord_archive__mutmut_63': xǁArchiveDALǁrecord_archive__mutmut_63, 
        'xǁArchiveDALǁrecord_archive__mutmut_64': xǁArchiveDALǁrecord_archive__mutmut_64, 
        'xǁArchiveDALǁrecord_archive__mutmut_65': xǁArchiveDALǁrecord_archive__mutmut_65, 
        'xǁArchiveDALǁrecord_archive__mutmut_66': xǁArchiveDALǁrecord_archive__mutmut_66, 
        'xǁArchiveDALǁrecord_archive__mutmut_67': xǁArchiveDALǁrecord_archive__mutmut_67, 
        'xǁArchiveDALǁrecord_archive__mutmut_68': xǁArchiveDALǁrecord_archive__mutmut_68, 
        'xǁArchiveDALǁrecord_archive__mutmut_69': xǁArchiveDALǁrecord_archive__mutmut_69, 
        'xǁArchiveDALǁrecord_archive__mutmut_70': xǁArchiveDALǁrecord_archive__mutmut_70, 
        'xǁArchiveDALǁrecord_archive__mutmut_71': xǁArchiveDALǁrecord_archive__mutmut_71, 
        'xǁArchiveDALǁrecord_archive__mutmut_72': xǁArchiveDALǁrecord_archive__mutmut_72, 
        'xǁArchiveDALǁrecord_archive__mutmut_73': xǁArchiveDALǁrecord_archive__mutmut_73, 
        'xǁArchiveDALǁrecord_archive__mutmut_74': xǁArchiveDALǁrecord_archive__mutmut_74, 
        'xǁArchiveDALǁrecord_archive__mutmut_75': xǁArchiveDALǁrecord_archive__mutmut_75, 
        'xǁArchiveDALǁrecord_archive__mutmut_76': xǁArchiveDALǁrecord_archive__mutmut_76, 
        'xǁArchiveDALǁrecord_archive__mutmut_77': xǁArchiveDALǁrecord_archive__mutmut_77, 
        'xǁArchiveDALǁrecord_archive__mutmut_78': xǁArchiveDALǁrecord_archive__mutmut_78, 
        'xǁArchiveDALǁrecord_archive__mutmut_79': xǁArchiveDALǁrecord_archive__mutmut_79, 
        'xǁArchiveDALǁrecord_archive__mutmut_80': xǁArchiveDALǁrecord_archive__mutmut_80, 
        'xǁArchiveDALǁrecord_archive__mutmut_81': xǁArchiveDALǁrecord_archive__mutmut_81, 
        'xǁArchiveDALǁrecord_archive__mutmut_82': xǁArchiveDALǁrecord_archive__mutmut_82, 
        'xǁArchiveDALǁrecord_archive__mutmut_83': xǁArchiveDALǁrecord_archive__mutmut_83, 
        'xǁArchiveDALǁrecord_archive__mutmut_84': xǁArchiveDALǁrecord_archive__mutmut_84, 
        'xǁArchiveDALǁrecord_archive__mutmut_85': xǁArchiveDALǁrecord_archive__mutmut_85, 
        'xǁArchiveDALǁrecord_archive__mutmut_86': xǁArchiveDALǁrecord_archive__mutmut_86, 
        'xǁArchiveDALǁrecord_archive__mutmut_87': xǁArchiveDALǁrecord_archive__mutmut_87, 
        'xǁArchiveDALǁrecord_archive__mutmut_88': xǁArchiveDALǁrecord_archive__mutmut_88, 
        'xǁArchiveDALǁrecord_archive__mutmut_89': xǁArchiveDALǁrecord_archive__mutmut_89, 
        'xǁArchiveDALǁrecord_archive__mutmut_90': xǁArchiveDALǁrecord_archive__mutmut_90, 
        'xǁArchiveDALǁrecord_archive__mutmut_91': xǁArchiveDALǁrecord_archive__mutmut_91, 
        'xǁArchiveDALǁrecord_archive__mutmut_92': xǁArchiveDALǁrecord_archive__mutmut_92, 
        'xǁArchiveDALǁrecord_archive__mutmut_93': xǁArchiveDALǁrecord_archive__mutmut_93, 
        'xǁArchiveDALǁrecord_archive__mutmut_94': xǁArchiveDALǁrecord_archive__mutmut_94, 
        'xǁArchiveDALǁrecord_archive__mutmut_95': xǁArchiveDALǁrecord_archive__mutmut_95, 
        'xǁArchiveDALǁrecord_archive__mutmut_96': xǁArchiveDALǁrecord_archive__mutmut_96, 
        'xǁArchiveDALǁrecord_archive__mutmut_97': xǁArchiveDALǁrecord_archive__mutmut_97, 
        'xǁArchiveDALǁrecord_archive__mutmut_98': xǁArchiveDALǁrecord_archive__mutmut_98, 
        'xǁArchiveDALǁrecord_archive__mutmut_99': xǁArchiveDALǁrecord_archive__mutmut_99, 
        'xǁArchiveDALǁrecord_archive__mutmut_100': xǁArchiveDALǁrecord_archive__mutmut_100, 
        'xǁArchiveDALǁrecord_archive__mutmut_101': xǁArchiveDALǁrecord_archive__mutmut_101, 
        'xǁArchiveDALǁrecord_archive__mutmut_102': xǁArchiveDALǁrecord_archive__mutmut_102, 
        'xǁArchiveDALǁrecord_archive__mutmut_103': xǁArchiveDALǁrecord_archive__mutmut_103, 
        'xǁArchiveDALǁrecord_archive__mutmut_104': xǁArchiveDALǁrecord_archive__mutmut_104, 
        'xǁArchiveDALǁrecord_archive__mutmut_105': xǁArchiveDALǁrecord_archive__mutmut_105, 
        'xǁArchiveDALǁrecord_archive__mutmut_106': xǁArchiveDALǁrecord_archive__mutmut_106, 
        'xǁArchiveDALǁrecord_archive__mutmut_107': xǁArchiveDALǁrecord_archive__mutmut_107, 
        'xǁArchiveDALǁrecord_archive__mutmut_108': xǁArchiveDALǁrecord_archive__mutmut_108, 
        'xǁArchiveDALǁrecord_archive__mutmut_109': xǁArchiveDALǁrecord_archive__mutmut_109, 
        'xǁArchiveDALǁrecord_archive__mutmut_110': xǁArchiveDALǁrecord_archive__mutmut_110, 
        'xǁArchiveDALǁrecord_archive__mutmut_111': xǁArchiveDALǁrecord_archive__mutmut_111, 
        'xǁArchiveDALǁrecord_archive__mutmut_112': xǁArchiveDALǁrecord_archive__mutmut_112, 
        'xǁArchiveDALǁrecord_archive__mutmut_113': xǁArchiveDALǁrecord_archive__mutmut_113, 
        'xǁArchiveDALǁrecord_archive__mutmut_114': xǁArchiveDALǁrecord_archive__mutmut_114, 
        'xǁArchiveDALǁrecord_archive__mutmut_115': xǁArchiveDALǁrecord_archive__mutmut_115, 
        'xǁArchiveDALǁrecord_archive__mutmut_116': xǁArchiveDALǁrecord_archive__mutmut_116, 
        'xǁArchiveDALǁrecord_archive__mutmut_117': xǁArchiveDALǁrecord_archive__mutmut_117, 
        'xǁArchiveDALǁrecord_archive__mutmut_118': xǁArchiveDALǁrecord_archive__mutmut_118, 
        'xǁArchiveDALǁrecord_archive__mutmut_119': xǁArchiveDALǁrecord_archive__mutmut_119, 
        'xǁArchiveDALǁrecord_archive__mutmut_120': xǁArchiveDALǁrecord_archive__mutmut_120, 
        'xǁArchiveDALǁrecord_archive__mutmut_121': xǁArchiveDALǁrecord_archive__mutmut_121, 
        'xǁArchiveDALǁrecord_archive__mutmut_122': xǁArchiveDALǁrecord_archive__mutmut_122, 
        'xǁArchiveDALǁrecord_archive__mutmut_123': xǁArchiveDALǁrecord_archive__mutmut_123, 
        'xǁArchiveDALǁrecord_archive__mutmut_124': xǁArchiveDALǁrecord_archive__mutmut_124, 
        'xǁArchiveDALǁrecord_archive__mutmut_125': xǁArchiveDALǁrecord_archive__mutmut_125, 
        'xǁArchiveDALǁrecord_archive__mutmut_126': xǁArchiveDALǁrecord_archive__mutmut_126, 
        'xǁArchiveDALǁrecord_archive__mutmut_127': xǁArchiveDALǁrecord_archive__mutmut_127, 
        'xǁArchiveDALǁrecord_archive__mutmut_128': xǁArchiveDALǁrecord_archive__mutmut_128, 
        'xǁArchiveDALǁrecord_archive__mutmut_129': xǁArchiveDALǁrecord_archive__mutmut_129, 
        'xǁArchiveDALǁrecord_archive__mutmut_130': xǁArchiveDALǁrecord_archive__mutmut_130, 
        'xǁArchiveDALǁrecord_archive__mutmut_131': xǁArchiveDALǁrecord_archive__mutmut_131, 
        'xǁArchiveDALǁrecord_archive__mutmut_132': xǁArchiveDALǁrecord_archive__mutmut_132, 
        'xǁArchiveDALǁrecord_archive__mutmut_133': xǁArchiveDALǁrecord_archive__mutmut_133, 
        'xǁArchiveDALǁrecord_archive__mutmut_134': xǁArchiveDALǁrecord_archive__mutmut_134, 
        'xǁArchiveDALǁrecord_archive__mutmut_135': xǁArchiveDALǁrecord_archive__mutmut_135, 
        'xǁArchiveDALǁrecord_archive__mutmut_136': xǁArchiveDALǁrecord_archive__mutmut_136, 
        'xǁArchiveDALǁrecord_archive__mutmut_137': xǁArchiveDALǁrecord_archive__mutmut_137, 
        'xǁArchiveDALǁrecord_archive__mutmut_138': xǁArchiveDALǁrecord_archive__mutmut_138, 
        'xǁArchiveDALǁrecord_archive__mutmut_139': xǁArchiveDALǁrecord_archive__mutmut_139, 
        'xǁArchiveDALǁrecord_archive__mutmut_140': xǁArchiveDALǁrecord_archive__mutmut_140, 
        'xǁArchiveDALǁrecord_archive__mutmut_141': xǁArchiveDALǁrecord_archive__mutmut_141, 
        'xǁArchiveDALǁrecord_archive__mutmut_142': xǁArchiveDALǁrecord_archive__mutmut_142, 
        'xǁArchiveDALǁrecord_archive__mutmut_143': xǁArchiveDALǁrecord_archive__mutmut_143, 
        'xǁArchiveDALǁrecord_archive__mutmut_144': xǁArchiveDALǁrecord_archive__mutmut_144, 
        'xǁArchiveDALǁrecord_archive__mutmut_145': xǁArchiveDALǁrecord_archive__mutmut_145, 
        'xǁArchiveDALǁrecord_archive__mutmut_146': xǁArchiveDALǁrecord_archive__mutmut_146, 
        'xǁArchiveDALǁrecord_archive__mutmut_147': xǁArchiveDALǁrecord_archive__mutmut_147, 
        'xǁArchiveDALǁrecord_archive__mutmut_148': xǁArchiveDALǁrecord_archive__mutmut_148, 
        'xǁArchiveDALǁrecord_archive__mutmut_149': xǁArchiveDALǁrecord_archive__mutmut_149, 
        'xǁArchiveDALǁrecord_archive__mutmut_150': xǁArchiveDALǁrecord_archive__mutmut_150, 
        'xǁArchiveDALǁrecord_archive__mutmut_151': xǁArchiveDALǁrecord_archive__mutmut_151, 
        'xǁArchiveDALǁrecord_archive__mutmut_152': xǁArchiveDALǁrecord_archive__mutmut_152, 
        'xǁArchiveDALǁrecord_archive__mutmut_153': xǁArchiveDALǁrecord_archive__mutmut_153, 
        'xǁArchiveDALǁrecord_archive__mutmut_154': xǁArchiveDALǁrecord_archive__mutmut_154, 
        'xǁArchiveDALǁrecord_archive__mutmut_155': xǁArchiveDALǁrecord_archive__mutmut_155, 
        'xǁArchiveDALǁrecord_archive__mutmut_156': xǁArchiveDALǁrecord_archive__mutmut_156, 
        'xǁArchiveDALǁrecord_archive__mutmut_157': xǁArchiveDALǁrecord_archive__mutmut_157, 
        'xǁArchiveDALǁrecord_archive__mutmut_158': xǁArchiveDALǁrecord_archive__mutmut_158, 
        'xǁArchiveDALǁrecord_archive__mutmut_159': xǁArchiveDALǁrecord_archive__mutmut_159, 
        'xǁArchiveDALǁrecord_archive__mutmut_160': xǁArchiveDALǁrecord_archive__mutmut_160, 
        'xǁArchiveDALǁrecord_archive__mutmut_161': xǁArchiveDALǁrecord_archive__mutmut_161, 
        'xǁArchiveDALǁrecord_archive__mutmut_162': xǁArchiveDALǁrecord_archive__mutmut_162, 
        'xǁArchiveDALǁrecord_archive__mutmut_163': xǁArchiveDALǁrecord_archive__mutmut_163, 
        'xǁArchiveDALǁrecord_archive__mutmut_164': xǁArchiveDALǁrecord_archive__mutmut_164, 
        'xǁArchiveDALǁrecord_archive__mutmut_165': xǁArchiveDALǁrecord_archive__mutmut_165, 
        'xǁArchiveDALǁrecord_archive__mutmut_166': xǁArchiveDALǁrecord_archive__mutmut_166, 
        'xǁArchiveDALǁrecord_archive__mutmut_167': xǁArchiveDALǁrecord_archive__mutmut_167, 
        'xǁArchiveDALǁrecord_archive__mutmut_168': xǁArchiveDALǁrecord_archive__mutmut_168, 
        'xǁArchiveDALǁrecord_archive__mutmut_169': xǁArchiveDALǁrecord_archive__mutmut_169, 
        'xǁArchiveDALǁrecord_archive__mutmut_170': xǁArchiveDALǁrecord_archive__mutmut_170, 
        'xǁArchiveDALǁrecord_archive__mutmut_171': xǁArchiveDALǁrecord_archive__mutmut_171, 
        'xǁArchiveDALǁrecord_archive__mutmut_172': xǁArchiveDALǁrecord_archive__mutmut_172, 
        'xǁArchiveDALǁrecord_archive__mutmut_173': xǁArchiveDALǁrecord_archive__mutmut_173, 
        'xǁArchiveDALǁrecord_archive__mutmut_174': xǁArchiveDALǁrecord_archive__mutmut_174, 
        'xǁArchiveDALǁrecord_archive__mutmut_175': xǁArchiveDALǁrecord_archive__mutmut_175, 
        'xǁArchiveDALǁrecord_archive__mutmut_176': xǁArchiveDALǁrecord_archive__mutmut_176
    }
    
    def record_archive(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁrecord_archive__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁrecord_archive__mutmut_mutants"), args, kwargs, self)
        return result 
    
    record_archive.__signature__ = _mutmut_signature(xǁArchiveDALǁrecord_archive__mutmut_orig)
    xǁArchiveDALǁrecord_archive__mutmut_orig.__name__ = 'xǁArchiveDALǁrecord_archive'

    def xǁArchiveDALǁget_restore_payload__mutmut_orig(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_1(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = None
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_2(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(None, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_3(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, None)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_4(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_5(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, )
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_6(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is not None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_7(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(None)
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_8(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = None
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_9(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(None, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_10(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, None)
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_11(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_12(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_13(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["XXartifact_idXX"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_14(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["ARTIFACT_ID"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_15(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = None
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_16(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(None)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_17(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = None
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_18(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["XXmetadataXX"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_19(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["METADATA"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_20(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(None)
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_21(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["XXmetadataXX"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_22(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["METADATA"])
        return {"item": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_23(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"XXitemXX": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_24(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"ITEM": item_dict, "artifact": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_25(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "XXartifactXX": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_26(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "ARTIFACT": dict(artifact)}

    def xǁArchiveDALǁget_restore_payload__mutmut_27(self, tombstone_id: str) -> dict[str, Any]:
        """Return the item and artifact payload for a restore operation."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            artifact = self._get_artifact_by_id(execute, item["artifact_id"])
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        return {"item": item_dict, "artifact": dict(None)}
    
    xǁArchiveDALǁget_restore_payload__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁget_restore_payload__mutmut_1': xǁArchiveDALǁget_restore_payload__mutmut_1, 
        'xǁArchiveDALǁget_restore_payload__mutmut_2': xǁArchiveDALǁget_restore_payload__mutmut_2, 
        'xǁArchiveDALǁget_restore_payload__mutmut_3': xǁArchiveDALǁget_restore_payload__mutmut_3, 
        'xǁArchiveDALǁget_restore_payload__mutmut_4': xǁArchiveDALǁget_restore_payload__mutmut_4, 
        'xǁArchiveDALǁget_restore_payload__mutmut_5': xǁArchiveDALǁget_restore_payload__mutmut_5, 
        'xǁArchiveDALǁget_restore_payload__mutmut_6': xǁArchiveDALǁget_restore_payload__mutmut_6, 
        'xǁArchiveDALǁget_restore_payload__mutmut_7': xǁArchiveDALǁget_restore_payload__mutmut_7, 
        'xǁArchiveDALǁget_restore_payload__mutmut_8': xǁArchiveDALǁget_restore_payload__mutmut_8, 
        'xǁArchiveDALǁget_restore_payload__mutmut_9': xǁArchiveDALǁget_restore_payload__mutmut_9, 
        'xǁArchiveDALǁget_restore_payload__mutmut_10': xǁArchiveDALǁget_restore_payload__mutmut_10, 
        'xǁArchiveDALǁget_restore_payload__mutmut_11': xǁArchiveDALǁget_restore_payload__mutmut_11, 
        'xǁArchiveDALǁget_restore_payload__mutmut_12': xǁArchiveDALǁget_restore_payload__mutmut_12, 
        'xǁArchiveDALǁget_restore_payload__mutmut_13': xǁArchiveDALǁget_restore_payload__mutmut_13, 
        'xǁArchiveDALǁget_restore_payload__mutmut_14': xǁArchiveDALǁget_restore_payload__mutmut_14, 
        'xǁArchiveDALǁget_restore_payload__mutmut_15': xǁArchiveDALǁget_restore_payload__mutmut_15, 
        'xǁArchiveDALǁget_restore_payload__mutmut_16': xǁArchiveDALǁget_restore_payload__mutmut_16, 
        'xǁArchiveDALǁget_restore_payload__mutmut_17': xǁArchiveDALǁget_restore_payload__mutmut_17, 
        'xǁArchiveDALǁget_restore_payload__mutmut_18': xǁArchiveDALǁget_restore_payload__mutmut_18, 
        'xǁArchiveDALǁget_restore_payload__mutmut_19': xǁArchiveDALǁget_restore_payload__mutmut_19, 
        'xǁArchiveDALǁget_restore_payload__mutmut_20': xǁArchiveDALǁget_restore_payload__mutmut_20, 
        'xǁArchiveDALǁget_restore_payload__mutmut_21': xǁArchiveDALǁget_restore_payload__mutmut_21, 
        'xǁArchiveDALǁget_restore_payload__mutmut_22': xǁArchiveDALǁget_restore_payload__mutmut_22, 
        'xǁArchiveDALǁget_restore_payload__mutmut_23': xǁArchiveDALǁget_restore_payload__mutmut_23, 
        'xǁArchiveDALǁget_restore_payload__mutmut_24': xǁArchiveDALǁget_restore_payload__mutmut_24, 
        'xǁArchiveDALǁget_restore_payload__mutmut_25': xǁArchiveDALǁget_restore_payload__mutmut_25, 
        'xǁArchiveDALǁget_restore_payload__mutmut_26': xǁArchiveDALǁget_restore_payload__mutmut_26, 
        'xǁArchiveDALǁget_restore_payload__mutmut_27': xǁArchiveDALǁget_restore_payload__mutmut_27
    }
    
    def get_restore_payload(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁget_restore_payload__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁget_restore_payload__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_restore_payload.__signature__ = _mutmut_signature(xǁArchiveDALǁget_restore_payload__mutmut_orig)
    xǁArchiveDALǁget_restore_payload__mutmut_orig.__name__ = 'xǁArchiveDALǁget_restore_payload'

    def xǁArchiveDALǁrecord_restore__mutmut_orig(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_1(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = None
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_2(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(None, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_3(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, None)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_4(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_5(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, )
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_6(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is not None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_7(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(None)
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_8(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = None
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_9(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                None,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_10(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                None,
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_11(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_12(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_13(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"XXrestored_atXX": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_14(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"RESTORED_AT": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_15(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "XXidXX": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_16(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "ID": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_17(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["XXidXX"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_18(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["ID"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_19(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = None
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_20(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "XXidXX": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_21(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "ID": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_22(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(None),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_23(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "XXitem_idXX": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_24(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "ITEM_ID": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_25(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["XXidXX"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_26(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["ID"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_27(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "XXactionXX": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_28(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "ACTION": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_29(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "XXRESTOREXX",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_30(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "restore",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_31(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "XXactorXX": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_32(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "ACTOR": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_33(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "XXcontextXX": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_34(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "CONTEXT": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_35(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted(None),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_36(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "XXcreated_atXX": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_37(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "CREATED_AT": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_38(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                None,
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_39(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                None,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_40(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                event_payload,
            )

    def xǁArchiveDALǁrecord_restore__mutmut_41(self, tombstone_id: str, *, actor: str) -> None:
        """Persist restore metadata after a successful restore."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            now = utcnow()
            execute(
                """
                UPDATE item SET restored_at = :restored_at WHERE id = :id
                """,
                {"restored_at": now, "id": item["id"]},
            )
            event_payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "RESTORE",
                "actor": actor,
                "context": json_dumps_sorted({}),
                "created_at": now,
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                )
    
    xǁArchiveDALǁrecord_restore__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁrecord_restore__mutmut_1': xǁArchiveDALǁrecord_restore__mutmut_1, 
        'xǁArchiveDALǁrecord_restore__mutmut_2': xǁArchiveDALǁrecord_restore__mutmut_2, 
        'xǁArchiveDALǁrecord_restore__mutmut_3': xǁArchiveDALǁrecord_restore__mutmut_3, 
        'xǁArchiveDALǁrecord_restore__mutmut_4': xǁArchiveDALǁrecord_restore__mutmut_4, 
        'xǁArchiveDALǁrecord_restore__mutmut_5': xǁArchiveDALǁrecord_restore__mutmut_5, 
        'xǁArchiveDALǁrecord_restore__mutmut_6': xǁArchiveDALǁrecord_restore__mutmut_6, 
        'xǁArchiveDALǁrecord_restore__mutmut_7': xǁArchiveDALǁrecord_restore__mutmut_7, 
        'xǁArchiveDALǁrecord_restore__mutmut_8': xǁArchiveDALǁrecord_restore__mutmut_8, 
        'xǁArchiveDALǁrecord_restore__mutmut_9': xǁArchiveDALǁrecord_restore__mutmut_9, 
        'xǁArchiveDALǁrecord_restore__mutmut_10': xǁArchiveDALǁrecord_restore__mutmut_10, 
        'xǁArchiveDALǁrecord_restore__mutmut_11': xǁArchiveDALǁrecord_restore__mutmut_11, 
        'xǁArchiveDALǁrecord_restore__mutmut_12': xǁArchiveDALǁrecord_restore__mutmut_12, 
        'xǁArchiveDALǁrecord_restore__mutmut_13': xǁArchiveDALǁrecord_restore__mutmut_13, 
        'xǁArchiveDALǁrecord_restore__mutmut_14': xǁArchiveDALǁrecord_restore__mutmut_14, 
        'xǁArchiveDALǁrecord_restore__mutmut_15': xǁArchiveDALǁrecord_restore__mutmut_15, 
        'xǁArchiveDALǁrecord_restore__mutmut_16': xǁArchiveDALǁrecord_restore__mutmut_16, 
        'xǁArchiveDALǁrecord_restore__mutmut_17': xǁArchiveDALǁrecord_restore__mutmut_17, 
        'xǁArchiveDALǁrecord_restore__mutmut_18': xǁArchiveDALǁrecord_restore__mutmut_18, 
        'xǁArchiveDALǁrecord_restore__mutmut_19': xǁArchiveDALǁrecord_restore__mutmut_19, 
        'xǁArchiveDALǁrecord_restore__mutmut_20': xǁArchiveDALǁrecord_restore__mutmut_20, 
        'xǁArchiveDALǁrecord_restore__mutmut_21': xǁArchiveDALǁrecord_restore__mutmut_21, 
        'xǁArchiveDALǁrecord_restore__mutmut_22': xǁArchiveDALǁrecord_restore__mutmut_22, 
        'xǁArchiveDALǁrecord_restore__mutmut_23': xǁArchiveDALǁrecord_restore__mutmut_23, 
        'xǁArchiveDALǁrecord_restore__mutmut_24': xǁArchiveDALǁrecord_restore__mutmut_24, 
        'xǁArchiveDALǁrecord_restore__mutmut_25': xǁArchiveDALǁrecord_restore__mutmut_25, 
        'xǁArchiveDALǁrecord_restore__mutmut_26': xǁArchiveDALǁrecord_restore__mutmut_26, 
        'xǁArchiveDALǁrecord_restore__mutmut_27': xǁArchiveDALǁrecord_restore__mutmut_27, 
        'xǁArchiveDALǁrecord_restore__mutmut_28': xǁArchiveDALǁrecord_restore__mutmut_28, 
        'xǁArchiveDALǁrecord_restore__mutmut_29': xǁArchiveDALǁrecord_restore__mutmut_29, 
        'xǁArchiveDALǁrecord_restore__mutmut_30': xǁArchiveDALǁrecord_restore__mutmut_30, 
        'xǁArchiveDALǁrecord_restore__mutmut_31': xǁArchiveDALǁrecord_restore__mutmut_31, 
        'xǁArchiveDALǁrecord_restore__mutmut_32': xǁArchiveDALǁrecord_restore__mutmut_32, 
        'xǁArchiveDALǁrecord_restore__mutmut_33': xǁArchiveDALǁrecord_restore__mutmut_33, 
        'xǁArchiveDALǁrecord_restore__mutmut_34': xǁArchiveDALǁrecord_restore__mutmut_34, 
        'xǁArchiveDALǁrecord_restore__mutmut_35': xǁArchiveDALǁrecord_restore__mutmut_35, 
        'xǁArchiveDALǁrecord_restore__mutmut_36': xǁArchiveDALǁrecord_restore__mutmut_36, 
        'xǁArchiveDALǁrecord_restore__mutmut_37': xǁArchiveDALǁrecord_restore__mutmut_37, 
        'xǁArchiveDALǁrecord_restore__mutmut_38': xǁArchiveDALǁrecord_restore__mutmut_38, 
        'xǁArchiveDALǁrecord_restore__mutmut_39': xǁArchiveDALǁrecord_restore__mutmut_39, 
        'xǁArchiveDALǁrecord_restore__mutmut_40': xǁArchiveDALǁrecord_restore__mutmut_40, 
        'xǁArchiveDALǁrecord_restore__mutmut_41': xǁArchiveDALǁrecord_restore__mutmut_41
    }
    
    def record_restore(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁrecord_restore__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁrecord_restore__mutmut_mutants"), args, kwargs, self)
        return result 
    
    record_restore.__signature__ = _mutmut_signature(xǁArchiveDALǁrecord_restore__mutmut_orig)
    xǁArchiveDALǁrecord_restore__mutmut_orig.__name__ = 'xǁArchiveDALǁrecord_restore'

    def xǁArchiveDALǁrecord_prune_request__mutmut_orig(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_1(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = None
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_2(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(None, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_3(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, None)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_4(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_5(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, )
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_6(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is not None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_7(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(None)
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_8(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = None
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_9(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "XXidXX": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_10(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "ID": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_11(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(None),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_12(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "XXitem_idXX": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_13(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "ITEM_ID": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_14(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["XXidXX"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_15(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["ID"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_16(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "XXactionXX": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_17(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "ACTION": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_18(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "XXPRUNE_REQUESTXX",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_19(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "prune_request",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_20(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "XXactorXX": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_21(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "ACTOR": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_22(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "XXcontextXX": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_23(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "CONTEXT": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_24(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted(None),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_25(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"XXreasonXX": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_26(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"REASON": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_27(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "XXcreated_atXX": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_28(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "CREATED_AT": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_29(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                None,
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_30(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                None,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_31(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                payload,
            )

    def xǁArchiveDALǁrecord_prune_request__mutmut_32(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        """Record a prune request event."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            payload = {
                "id": str(uuid.uuid4()),
                "item_id": item["id"],
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "context": json_dumps_sorted({"reason": reason}),
                "created_at": utcnow(),
            }
            execute(
                """
                INSERT INTO event (id, item_id, action, actor, context, created_at)
                VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                """,
                )
    
    xǁArchiveDALǁrecord_prune_request__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁrecord_prune_request__mutmut_1': xǁArchiveDALǁrecord_prune_request__mutmut_1, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_2': xǁArchiveDALǁrecord_prune_request__mutmut_2, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_3': xǁArchiveDALǁrecord_prune_request__mutmut_3, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_4': xǁArchiveDALǁrecord_prune_request__mutmut_4, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_5': xǁArchiveDALǁrecord_prune_request__mutmut_5, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_6': xǁArchiveDALǁrecord_prune_request__mutmut_6, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_7': xǁArchiveDALǁrecord_prune_request__mutmut_7, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_8': xǁArchiveDALǁrecord_prune_request__mutmut_8, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_9': xǁArchiveDALǁrecord_prune_request__mutmut_9, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_10': xǁArchiveDALǁrecord_prune_request__mutmut_10, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_11': xǁArchiveDALǁrecord_prune_request__mutmut_11, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_12': xǁArchiveDALǁrecord_prune_request__mutmut_12, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_13': xǁArchiveDALǁrecord_prune_request__mutmut_13, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_14': xǁArchiveDALǁrecord_prune_request__mutmut_14, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_15': xǁArchiveDALǁrecord_prune_request__mutmut_15, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_16': xǁArchiveDALǁrecord_prune_request__mutmut_16, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_17': xǁArchiveDALǁrecord_prune_request__mutmut_17, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_18': xǁArchiveDALǁrecord_prune_request__mutmut_18, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_19': xǁArchiveDALǁrecord_prune_request__mutmut_19, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_20': xǁArchiveDALǁrecord_prune_request__mutmut_20, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_21': xǁArchiveDALǁrecord_prune_request__mutmut_21, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_22': xǁArchiveDALǁrecord_prune_request__mutmut_22, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_23': xǁArchiveDALǁrecord_prune_request__mutmut_23, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_24': xǁArchiveDALǁrecord_prune_request__mutmut_24, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_25': xǁArchiveDALǁrecord_prune_request__mutmut_25, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_26': xǁArchiveDALǁrecord_prune_request__mutmut_26, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_27': xǁArchiveDALǁrecord_prune_request__mutmut_27, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_28': xǁArchiveDALǁrecord_prune_request__mutmut_28, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_29': xǁArchiveDALǁrecord_prune_request__mutmut_29, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_30': xǁArchiveDALǁrecord_prune_request__mutmut_30, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_31': xǁArchiveDALǁrecord_prune_request__mutmut_31, 
        'xǁArchiveDALǁrecord_prune_request__mutmut_32': xǁArchiveDALǁrecord_prune_request__mutmut_32
    }
    
    def record_prune_request(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁrecord_prune_request__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁrecord_prune_request__mutmut_mutants"), args, kwargs, self)
        return result 
    
    record_prune_request.__signature__ = _mutmut_signature(xǁArchiveDALǁrecord_prune_request__mutmut_orig)
    xǁArchiveDALǁrecord_prune_request__mutmut_orig.__name__ = 'xǁArchiveDALǁrecord_prune_request'

    def xǁArchiveDALǁrecord_delete_approval__mutmut_orig(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_1(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = True,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_2(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor != secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_3(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError(None)
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_4(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("XXPrimary and secondary approvers must be distinctXX")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_5(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_6(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("PRIMARY AND SECONDARY APPROVERS MUST BE DISTINCT")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_7(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = None
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_8(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(None, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_9(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, None)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_10(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_11(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, )
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_12(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is not None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_13(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(None)
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_14(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(None):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_15(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get(None, 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_16(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", None)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_17(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get(0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_18(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", )):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_19(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("XXlegal_holdXX", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_20(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("LEGAL_HOLD", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_21(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 1)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_22(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError(None)
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_23(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("XXItem is under legal hold and cannot be purgedXX")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_24(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_25(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("ITEM IS UNDER LEGAL HOLD AND CANNOT BE PURGED")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_26(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = None
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_27(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["XXartifact_idXX"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_28(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["ARTIFACT_ID"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_29(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = None
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_30(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = True
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_31(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = None
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_32(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 2
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_33(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = None
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_34(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    None,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_35(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    None,
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_36(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=None,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_37(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_38(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_39(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_40(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"XXartifact_idXX": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_41(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"ARTIFACT_ID": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_42(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=False,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_43(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = None
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_44(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get(None, 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_45(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", None) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_46(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get(0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_47(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", ) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_48(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("XXref_countXX", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_49(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("REF_COUNT", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_50(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 1) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_51(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 1
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_52(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = None
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_53(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(None)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_54(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = None
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_55(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count < 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_56(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 2
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_57(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = None
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_58(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "XXprimaryXX"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_59(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "PRIMARY"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_60(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "XXsecondaryXX")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_61(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "SECONDARY")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_62(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = None
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_63(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"XXroleXX": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_64(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"ROLE": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_65(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "XXreasonXX": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_66(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "REASON": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_67(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        None
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_68(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "XXapply_requestedXX": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_69(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "APPLY_REQUESTED": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_70(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": False,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_71(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "XXblob_scrubbedXX": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_72(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "BLOB_SCRUBBED": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_73(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count >= 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_74(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 2:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_75(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = None
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_76(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["XXshared_referencesXX"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_77(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["SHARED_REFERENCES"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_78(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(None, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_79(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, None)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_80(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_81(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, )
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_82(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count + 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_83(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 2, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_84(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 1)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_85(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = None
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_86(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "XXidXX": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_87(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "ID": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_88(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(None),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_89(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "XXitem_idXX": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_90(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "ITEM_ID": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_91(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["XXidXX"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_92(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["ID"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_93(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "XXactionXX": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_94(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "ACTION": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_95(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "XXDELETE_APPROVEDXX",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_96(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "delete_approved",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_97(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "XXactorXX": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_98(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "ACTOR": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_99(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "XXcontextXX": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_100(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "CONTEXT": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_101(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(None),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_102(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "XXcreated_atXX": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_103(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "CREATED_AT": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_104(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    None,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_105(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    None,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_106(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_107(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_108(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    None,
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_109(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    None,
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_110(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    {"artifact_id": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_111(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_112(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"XXartifact_idXX": artifact_id},
                )
            return blob_scrubbed

    def xǁArchiveDALǁrecord_delete_approval__mutmut_113(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        """Insert dual approvals and optionally scrub blob bytes.

        Returns ``True`` when the underlying artifact payload was scrubbed.
        ``False`` indicates that the blob bytes were left intact (for example
        because the artifact is still referenced by other tombstones).
        """

        if primary_actor == secondary_actor:
            raise ValueError("Primary and secondary approvers must be distinct")
        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            if int(item.get("legal_hold", 0)):
                raise PermissionError("Item is under legal hold and cannot be purged")
            artifact_id = item["artifact_id"]
            blob_scrubbed = False
            reference_count = 1
            if apply:
                row = execute(
                    """
                    SELECT COUNT(*) AS ref_count
                    FROM item
                    WHERE artifact_id = :artifact_id
                    """,
                    {"artifact_id": artifact_id},
                    fetchone=True,
                )
                raw_count = row.get("ref_count", 0) if row else 0
                reference_count = int(raw_count)
                blob_scrubbed = reference_count <= 1
            now = utcnow()
            for actor, tag in ((primary_actor, "primary"), (secondary_actor, "secondary")):
                context_payload = {"role": tag, "reason": reason}
                if apply:
                    context_payload.update(
                        {
                            "apply_requested": True,
                            "blob_scrubbed": blob_scrubbed,
                        }
                    )
                    if reference_count > 1:
                        context_payload["shared_references"] = max(reference_count - 1, 0)
                payload = {
                    "id": str(uuid.uuid4()),
                    "item_id": item["id"],
                    "action": "DELETE_APPROVED",
                    "actor": actor,
                    "context": json_dumps_sorted(context_payload),
                    "created_at": now,
                }
                execute(
                    """
                    INSERT INTO event (id, item_id, action, actor, context, created_at)
                    VALUES (:id, :item_id, :action, :actor, :context, :created_at)
                    """,
                    payload,
                )
            if blob_scrubbed:
                execute(
                    """
                    UPDATE artifact
                    SET blob_bytes = NULL,
                        storage_driver = 'object',
                        object_url = COALESCE(object_url, 'purged://dual-control')
                    WHERE id = :artifact_id
                    """,
                    {"ARTIFACT_ID": artifact_id},
                )
            return blob_scrubbed
    
    xǁArchiveDALǁrecord_delete_approval__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁrecord_delete_approval__mutmut_1': xǁArchiveDALǁrecord_delete_approval__mutmut_1, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_2': xǁArchiveDALǁrecord_delete_approval__mutmut_2, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_3': xǁArchiveDALǁrecord_delete_approval__mutmut_3, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_4': xǁArchiveDALǁrecord_delete_approval__mutmut_4, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_5': xǁArchiveDALǁrecord_delete_approval__mutmut_5, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_6': xǁArchiveDALǁrecord_delete_approval__mutmut_6, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_7': xǁArchiveDALǁrecord_delete_approval__mutmut_7, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_8': xǁArchiveDALǁrecord_delete_approval__mutmut_8, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_9': xǁArchiveDALǁrecord_delete_approval__mutmut_9, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_10': xǁArchiveDALǁrecord_delete_approval__mutmut_10, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_11': xǁArchiveDALǁrecord_delete_approval__mutmut_11, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_12': xǁArchiveDALǁrecord_delete_approval__mutmut_12, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_13': xǁArchiveDALǁrecord_delete_approval__mutmut_13, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_14': xǁArchiveDALǁrecord_delete_approval__mutmut_14, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_15': xǁArchiveDALǁrecord_delete_approval__mutmut_15, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_16': xǁArchiveDALǁrecord_delete_approval__mutmut_16, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_17': xǁArchiveDALǁrecord_delete_approval__mutmut_17, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_18': xǁArchiveDALǁrecord_delete_approval__mutmut_18, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_19': xǁArchiveDALǁrecord_delete_approval__mutmut_19, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_20': xǁArchiveDALǁrecord_delete_approval__mutmut_20, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_21': xǁArchiveDALǁrecord_delete_approval__mutmut_21, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_22': xǁArchiveDALǁrecord_delete_approval__mutmut_22, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_23': xǁArchiveDALǁrecord_delete_approval__mutmut_23, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_24': xǁArchiveDALǁrecord_delete_approval__mutmut_24, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_25': xǁArchiveDALǁrecord_delete_approval__mutmut_25, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_26': xǁArchiveDALǁrecord_delete_approval__mutmut_26, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_27': xǁArchiveDALǁrecord_delete_approval__mutmut_27, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_28': xǁArchiveDALǁrecord_delete_approval__mutmut_28, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_29': xǁArchiveDALǁrecord_delete_approval__mutmut_29, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_30': xǁArchiveDALǁrecord_delete_approval__mutmut_30, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_31': xǁArchiveDALǁrecord_delete_approval__mutmut_31, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_32': xǁArchiveDALǁrecord_delete_approval__mutmut_32, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_33': xǁArchiveDALǁrecord_delete_approval__mutmut_33, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_34': xǁArchiveDALǁrecord_delete_approval__mutmut_34, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_35': xǁArchiveDALǁrecord_delete_approval__mutmut_35, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_36': xǁArchiveDALǁrecord_delete_approval__mutmut_36, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_37': xǁArchiveDALǁrecord_delete_approval__mutmut_37, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_38': xǁArchiveDALǁrecord_delete_approval__mutmut_38, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_39': xǁArchiveDALǁrecord_delete_approval__mutmut_39, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_40': xǁArchiveDALǁrecord_delete_approval__mutmut_40, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_41': xǁArchiveDALǁrecord_delete_approval__mutmut_41, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_42': xǁArchiveDALǁrecord_delete_approval__mutmut_42, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_43': xǁArchiveDALǁrecord_delete_approval__mutmut_43, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_44': xǁArchiveDALǁrecord_delete_approval__mutmut_44, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_45': xǁArchiveDALǁrecord_delete_approval__mutmut_45, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_46': xǁArchiveDALǁrecord_delete_approval__mutmut_46, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_47': xǁArchiveDALǁrecord_delete_approval__mutmut_47, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_48': xǁArchiveDALǁrecord_delete_approval__mutmut_48, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_49': xǁArchiveDALǁrecord_delete_approval__mutmut_49, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_50': xǁArchiveDALǁrecord_delete_approval__mutmut_50, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_51': xǁArchiveDALǁrecord_delete_approval__mutmut_51, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_52': xǁArchiveDALǁrecord_delete_approval__mutmut_52, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_53': xǁArchiveDALǁrecord_delete_approval__mutmut_53, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_54': xǁArchiveDALǁrecord_delete_approval__mutmut_54, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_55': xǁArchiveDALǁrecord_delete_approval__mutmut_55, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_56': xǁArchiveDALǁrecord_delete_approval__mutmut_56, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_57': xǁArchiveDALǁrecord_delete_approval__mutmut_57, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_58': xǁArchiveDALǁrecord_delete_approval__mutmut_58, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_59': xǁArchiveDALǁrecord_delete_approval__mutmut_59, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_60': xǁArchiveDALǁrecord_delete_approval__mutmut_60, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_61': xǁArchiveDALǁrecord_delete_approval__mutmut_61, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_62': xǁArchiveDALǁrecord_delete_approval__mutmut_62, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_63': xǁArchiveDALǁrecord_delete_approval__mutmut_63, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_64': xǁArchiveDALǁrecord_delete_approval__mutmut_64, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_65': xǁArchiveDALǁrecord_delete_approval__mutmut_65, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_66': xǁArchiveDALǁrecord_delete_approval__mutmut_66, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_67': xǁArchiveDALǁrecord_delete_approval__mutmut_67, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_68': xǁArchiveDALǁrecord_delete_approval__mutmut_68, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_69': xǁArchiveDALǁrecord_delete_approval__mutmut_69, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_70': xǁArchiveDALǁrecord_delete_approval__mutmut_70, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_71': xǁArchiveDALǁrecord_delete_approval__mutmut_71, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_72': xǁArchiveDALǁrecord_delete_approval__mutmut_72, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_73': xǁArchiveDALǁrecord_delete_approval__mutmut_73, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_74': xǁArchiveDALǁrecord_delete_approval__mutmut_74, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_75': xǁArchiveDALǁrecord_delete_approval__mutmut_75, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_76': xǁArchiveDALǁrecord_delete_approval__mutmut_76, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_77': xǁArchiveDALǁrecord_delete_approval__mutmut_77, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_78': xǁArchiveDALǁrecord_delete_approval__mutmut_78, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_79': xǁArchiveDALǁrecord_delete_approval__mutmut_79, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_80': xǁArchiveDALǁrecord_delete_approval__mutmut_80, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_81': xǁArchiveDALǁrecord_delete_approval__mutmut_81, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_82': xǁArchiveDALǁrecord_delete_approval__mutmut_82, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_83': xǁArchiveDALǁrecord_delete_approval__mutmut_83, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_84': xǁArchiveDALǁrecord_delete_approval__mutmut_84, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_85': xǁArchiveDALǁrecord_delete_approval__mutmut_85, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_86': xǁArchiveDALǁrecord_delete_approval__mutmut_86, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_87': xǁArchiveDALǁrecord_delete_approval__mutmut_87, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_88': xǁArchiveDALǁrecord_delete_approval__mutmut_88, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_89': xǁArchiveDALǁrecord_delete_approval__mutmut_89, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_90': xǁArchiveDALǁrecord_delete_approval__mutmut_90, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_91': xǁArchiveDALǁrecord_delete_approval__mutmut_91, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_92': xǁArchiveDALǁrecord_delete_approval__mutmut_92, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_93': xǁArchiveDALǁrecord_delete_approval__mutmut_93, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_94': xǁArchiveDALǁrecord_delete_approval__mutmut_94, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_95': xǁArchiveDALǁrecord_delete_approval__mutmut_95, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_96': xǁArchiveDALǁrecord_delete_approval__mutmut_96, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_97': xǁArchiveDALǁrecord_delete_approval__mutmut_97, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_98': xǁArchiveDALǁrecord_delete_approval__mutmut_98, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_99': xǁArchiveDALǁrecord_delete_approval__mutmut_99, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_100': xǁArchiveDALǁrecord_delete_approval__mutmut_100, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_101': xǁArchiveDALǁrecord_delete_approval__mutmut_101, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_102': xǁArchiveDALǁrecord_delete_approval__mutmut_102, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_103': xǁArchiveDALǁrecord_delete_approval__mutmut_103, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_104': xǁArchiveDALǁrecord_delete_approval__mutmut_104, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_105': xǁArchiveDALǁrecord_delete_approval__mutmut_105, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_106': xǁArchiveDALǁrecord_delete_approval__mutmut_106, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_107': xǁArchiveDALǁrecord_delete_approval__mutmut_107, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_108': xǁArchiveDALǁrecord_delete_approval__mutmut_108, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_109': xǁArchiveDALǁrecord_delete_approval__mutmut_109, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_110': xǁArchiveDALǁrecord_delete_approval__mutmut_110, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_111': xǁArchiveDALǁrecord_delete_approval__mutmut_111, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_112': xǁArchiveDALǁrecord_delete_approval__mutmut_112, 
        'xǁArchiveDALǁrecord_delete_approval__mutmut_113': xǁArchiveDALǁrecord_delete_approval__mutmut_113
    }
    
    def record_delete_approval(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁrecord_delete_approval__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁrecord_delete_approval__mutmut_mutants"), args, kwargs, self)
        return result 
    
    record_delete_approval.__signature__ = _mutmut_signature(xǁArchiveDALǁrecord_delete_approval__mutmut_orig)
    xǁArchiveDALǁrecord_delete_approval__mutmut_orig.__name__ = 'xǁArchiveDALǁrecord_delete_approval'

    def xǁArchiveDALǁlist_items__mutmut_orig(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_1(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 101,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_2(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = None
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_3(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"XXlimitXX": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_4(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"LIMIT": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_5(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = None
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_6(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append(None)
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_7(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("XXrepo = :repoXX")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_8(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("REPO = :REPO")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_9(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = None
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_10(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["XXrepoXX"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_11(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["REPO"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_12(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append(None)
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_13(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("XXarchived_at >= :sinceXX")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_14(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("ARCHIVED_AT >= :SINCE")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_15(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = None
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_16(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["XXsinceXX"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_17(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["SINCE"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_18(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = None
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_19(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "XXSELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_idXX",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_20(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "select id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_21(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT ID, REPO, PATH, COMMIT_SHA, REASON, ARCHIVED_BY, ARCHIVED_AT, TOMBSTONE_ID",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_22(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "XXFROM itemXX",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_23(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "from item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_24(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM ITEM",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_25(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append(None)
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_26(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " - " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_27(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("XXWHERE XX" + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_28(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("where " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_29(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(None))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_30(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + "XX AND XX".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_31(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " and ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_32(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            None
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_33(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "XXORDER BY archived_at DESCXX",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_34(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "order by archived_at desc",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_35(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY ARCHIVED_AT DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_36(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "XXLIMIT :limitXX",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_37(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "limit :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_38(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :LIMIT",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_39(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = None
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_40(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(None)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_41(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "XX\nXX".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_42(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = None
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_43(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(None, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_44(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, None, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_45(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=None)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_46(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_47(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_48(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, )
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_49(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=False)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_50(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = None
        for row in rows or []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_51(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows and []:
            row_dict = dict(row)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_52(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = None
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_53(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(None)
            result.append(row_dict)
        return result

    def xǁArchiveDALǁlist_items__mutmut_54(
        self,
        *,
        repo: str | None = None,
        since: str | None = None,
        limit: int = 100,
    ) -> list[dict[str, Any]]:
        """Return archived items with optional filters."""

        params: Params = {"limit": limit}
        clauses: list[str] = []
        if repo:
            clauses.append("repo = :repo")
            params["repo"] = repo
        if since:
            clauses.append("archived_at >= :since")
            params["since"] = since
        query_lines = [
            "SELECT id, repo, path, commit_sha, reason, archived_by, archived_at, tombstone_id",
            "FROM item",
        ]
        if clauses:
            query_lines.append("WHERE " + " AND ".join(clauses))
        query_lines.extend(
            [
                "ORDER BY archived_at DESC",
                "LIMIT :limit",
            ]
        )
        sql = "\n".join(query_lines)
        with self._transaction() as execute:
            rows = execute(sql, params, fetchall=True)
        result: list[dict[str, Any]] = []
        for row in rows or []:
            row_dict = dict(row)
            result.append(None)
        return result
    
    xǁArchiveDALǁlist_items__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁlist_items__mutmut_1': xǁArchiveDALǁlist_items__mutmut_1, 
        'xǁArchiveDALǁlist_items__mutmut_2': xǁArchiveDALǁlist_items__mutmut_2, 
        'xǁArchiveDALǁlist_items__mutmut_3': xǁArchiveDALǁlist_items__mutmut_3, 
        'xǁArchiveDALǁlist_items__mutmut_4': xǁArchiveDALǁlist_items__mutmut_4, 
        'xǁArchiveDALǁlist_items__mutmut_5': xǁArchiveDALǁlist_items__mutmut_5, 
        'xǁArchiveDALǁlist_items__mutmut_6': xǁArchiveDALǁlist_items__mutmut_6, 
        'xǁArchiveDALǁlist_items__mutmut_7': xǁArchiveDALǁlist_items__mutmut_7, 
        'xǁArchiveDALǁlist_items__mutmut_8': xǁArchiveDALǁlist_items__mutmut_8, 
        'xǁArchiveDALǁlist_items__mutmut_9': xǁArchiveDALǁlist_items__mutmut_9, 
        'xǁArchiveDALǁlist_items__mutmut_10': xǁArchiveDALǁlist_items__mutmut_10, 
        'xǁArchiveDALǁlist_items__mutmut_11': xǁArchiveDALǁlist_items__mutmut_11, 
        'xǁArchiveDALǁlist_items__mutmut_12': xǁArchiveDALǁlist_items__mutmut_12, 
        'xǁArchiveDALǁlist_items__mutmut_13': xǁArchiveDALǁlist_items__mutmut_13, 
        'xǁArchiveDALǁlist_items__mutmut_14': xǁArchiveDALǁlist_items__mutmut_14, 
        'xǁArchiveDALǁlist_items__mutmut_15': xǁArchiveDALǁlist_items__mutmut_15, 
        'xǁArchiveDALǁlist_items__mutmut_16': xǁArchiveDALǁlist_items__mutmut_16, 
        'xǁArchiveDALǁlist_items__mutmut_17': xǁArchiveDALǁlist_items__mutmut_17, 
        'xǁArchiveDALǁlist_items__mutmut_18': xǁArchiveDALǁlist_items__mutmut_18, 
        'xǁArchiveDALǁlist_items__mutmut_19': xǁArchiveDALǁlist_items__mutmut_19, 
        'xǁArchiveDALǁlist_items__mutmut_20': xǁArchiveDALǁlist_items__mutmut_20, 
        'xǁArchiveDALǁlist_items__mutmut_21': xǁArchiveDALǁlist_items__mutmut_21, 
        'xǁArchiveDALǁlist_items__mutmut_22': xǁArchiveDALǁlist_items__mutmut_22, 
        'xǁArchiveDALǁlist_items__mutmut_23': xǁArchiveDALǁlist_items__mutmut_23, 
        'xǁArchiveDALǁlist_items__mutmut_24': xǁArchiveDALǁlist_items__mutmut_24, 
        'xǁArchiveDALǁlist_items__mutmut_25': xǁArchiveDALǁlist_items__mutmut_25, 
        'xǁArchiveDALǁlist_items__mutmut_26': xǁArchiveDALǁlist_items__mutmut_26, 
        'xǁArchiveDALǁlist_items__mutmut_27': xǁArchiveDALǁlist_items__mutmut_27, 
        'xǁArchiveDALǁlist_items__mutmut_28': xǁArchiveDALǁlist_items__mutmut_28, 
        'xǁArchiveDALǁlist_items__mutmut_29': xǁArchiveDALǁlist_items__mutmut_29, 
        'xǁArchiveDALǁlist_items__mutmut_30': xǁArchiveDALǁlist_items__mutmut_30, 
        'xǁArchiveDALǁlist_items__mutmut_31': xǁArchiveDALǁlist_items__mutmut_31, 
        'xǁArchiveDALǁlist_items__mutmut_32': xǁArchiveDALǁlist_items__mutmut_32, 
        'xǁArchiveDALǁlist_items__mutmut_33': xǁArchiveDALǁlist_items__mutmut_33, 
        'xǁArchiveDALǁlist_items__mutmut_34': xǁArchiveDALǁlist_items__mutmut_34, 
        'xǁArchiveDALǁlist_items__mutmut_35': xǁArchiveDALǁlist_items__mutmut_35, 
        'xǁArchiveDALǁlist_items__mutmut_36': xǁArchiveDALǁlist_items__mutmut_36, 
        'xǁArchiveDALǁlist_items__mutmut_37': xǁArchiveDALǁlist_items__mutmut_37, 
        'xǁArchiveDALǁlist_items__mutmut_38': xǁArchiveDALǁlist_items__mutmut_38, 
        'xǁArchiveDALǁlist_items__mutmut_39': xǁArchiveDALǁlist_items__mutmut_39, 
        'xǁArchiveDALǁlist_items__mutmut_40': xǁArchiveDALǁlist_items__mutmut_40, 
        'xǁArchiveDALǁlist_items__mutmut_41': xǁArchiveDALǁlist_items__mutmut_41, 
        'xǁArchiveDALǁlist_items__mutmut_42': xǁArchiveDALǁlist_items__mutmut_42, 
        'xǁArchiveDALǁlist_items__mutmut_43': xǁArchiveDALǁlist_items__mutmut_43, 
        'xǁArchiveDALǁlist_items__mutmut_44': xǁArchiveDALǁlist_items__mutmut_44, 
        'xǁArchiveDALǁlist_items__mutmut_45': xǁArchiveDALǁlist_items__mutmut_45, 
        'xǁArchiveDALǁlist_items__mutmut_46': xǁArchiveDALǁlist_items__mutmut_46, 
        'xǁArchiveDALǁlist_items__mutmut_47': xǁArchiveDALǁlist_items__mutmut_47, 
        'xǁArchiveDALǁlist_items__mutmut_48': xǁArchiveDALǁlist_items__mutmut_48, 
        'xǁArchiveDALǁlist_items__mutmut_49': xǁArchiveDALǁlist_items__mutmut_49, 
        'xǁArchiveDALǁlist_items__mutmut_50': xǁArchiveDALǁlist_items__mutmut_50, 
        'xǁArchiveDALǁlist_items__mutmut_51': xǁArchiveDALǁlist_items__mutmut_51, 
        'xǁArchiveDALǁlist_items__mutmut_52': xǁArchiveDALǁlist_items__mutmut_52, 
        'xǁArchiveDALǁlist_items__mutmut_53': xǁArchiveDALǁlist_items__mutmut_53, 
        'xǁArchiveDALǁlist_items__mutmut_54': xǁArchiveDALǁlist_items__mutmut_54
    }
    
    def list_items(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁlist_items__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁlist_items__mutmut_mutants"), args, kwargs, self)
        return result 
    
    list_items.__signature__ = _mutmut_signature(xǁArchiveDALǁlist_items__mutmut_orig)
    xǁArchiveDALǁlist_items__mutmut_orig.__name__ = 'xǁArchiveDALǁlist_items'

    def xǁArchiveDALǁshow_item__mutmut_orig(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_1(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = None
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_2(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(None, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_3(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, None)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_4(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_5(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, )
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_6(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is not None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_7(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(None)
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_8(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = None
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_9(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                None,
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_10(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                None,
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_11(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=None,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_12(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_13(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_14(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_15(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "XXSELECT action, actor, context, created_at XX"
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_16(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "select action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_17(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT ACTION, ACTOR, CONTEXT, CREATED_AT "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_18(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "XXFROM event WHERE item_id = :item_id XX"
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_19(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "from event where item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_20(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM EVENT WHERE ITEM_ID = :ITEM_ID "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_21(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "XXORDER BY created_atXX"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_22(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "order by created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_23(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY CREATED_AT"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_24(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"XXitem_idXX": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_25(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"ITEM_ID": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_26(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["XXidXX"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_27(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["ID"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_28(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=False,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_29(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = None
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_30(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(None)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_31(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = None
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_32(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["XXmetadataXX"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_33(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["METADATA"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_34(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(None)
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_35(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["XXmetadataXX"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_36(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["METADATA"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_37(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = None
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_38(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events and []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_39(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = None
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_40(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(None)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_41(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = None
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_42(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get(None)
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_43(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("XXcontextXX")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_44(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("CONTEXT")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_45(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = None
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_46(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["XXcontextXX"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_47(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["CONTEXT"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_48(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(None)
            events_payload.append(entry)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_49(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(None)
        item_dict["events"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_50(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["events"] = None
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_51(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["XXeventsXX"] = events_payload
        return item_dict

    def xǁArchiveDALǁshow_item__mutmut_52(self, tombstone_id: str) -> dict[str, Any]:
        """Return a full view of a single item."""

        with self._transaction() as execute:
            item = self._get_item_by_tombstone(execute, tombstone_id)
            if item is None:
                raise LookupError(f"Unknown tombstone id: {tombstone_id}")
            events = execute(
                (
                    "SELECT action, actor, context, created_at "
                    "FROM event WHERE item_id = :item_id "
                    "ORDER BY created_at"
                ),
                {"item_id": item["id"]},
                fetchall=True,
            )
        item_dict = dict(item)
        if isinstance(item_dict.get("metadata"), str):
            item_dict["metadata"] = json.loads(item_dict["metadata"])
        events_payload = []
        for row in events or []:
            entry = dict(row)
            context = entry.get("context")
            if isinstance(context, str):
                entry["context"] = json.loads(context)
            events_payload.append(entry)
        item_dict["EVENTS"] = events_payload
        return item_dict
    
    xǁArchiveDALǁshow_item__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁshow_item__mutmut_1': xǁArchiveDALǁshow_item__mutmut_1, 
        'xǁArchiveDALǁshow_item__mutmut_2': xǁArchiveDALǁshow_item__mutmut_2, 
        'xǁArchiveDALǁshow_item__mutmut_3': xǁArchiveDALǁshow_item__mutmut_3, 
        'xǁArchiveDALǁshow_item__mutmut_4': xǁArchiveDALǁshow_item__mutmut_4, 
        'xǁArchiveDALǁshow_item__mutmut_5': xǁArchiveDALǁshow_item__mutmut_5, 
        'xǁArchiveDALǁshow_item__mutmut_6': xǁArchiveDALǁshow_item__mutmut_6, 
        'xǁArchiveDALǁshow_item__mutmut_7': xǁArchiveDALǁshow_item__mutmut_7, 
        'xǁArchiveDALǁshow_item__mutmut_8': xǁArchiveDALǁshow_item__mutmut_8, 
        'xǁArchiveDALǁshow_item__mutmut_9': xǁArchiveDALǁshow_item__mutmut_9, 
        'xǁArchiveDALǁshow_item__mutmut_10': xǁArchiveDALǁshow_item__mutmut_10, 
        'xǁArchiveDALǁshow_item__mutmut_11': xǁArchiveDALǁshow_item__mutmut_11, 
        'xǁArchiveDALǁshow_item__mutmut_12': xǁArchiveDALǁshow_item__mutmut_12, 
        'xǁArchiveDALǁshow_item__mutmut_13': xǁArchiveDALǁshow_item__mutmut_13, 
        'xǁArchiveDALǁshow_item__mutmut_14': xǁArchiveDALǁshow_item__mutmut_14, 
        'xǁArchiveDALǁshow_item__mutmut_15': xǁArchiveDALǁshow_item__mutmut_15, 
        'xǁArchiveDALǁshow_item__mutmut_16': xǁArchiveDALǁshow_item__mutmut_16, 
        'xǁArchiveDALǁshow_item__mutmut_17': xǁArchiveDALǁshow_item__mutmut_17, 
        'xǁArchiveDALǁshow_item__mutmut_18': xǁArchiveDALǁshow_item__mutmut_18, 
        'xǁArchiveDALǁshow_item__mutmut_19': xǁArchiveDALǁshow_item__mutmut_19, 
        'xǁArchiveDALǁshow_item__mutmut_20': xǁArchiveDALǁshow_item__mutmut_20, 
        'xǁArchiveDALǁshow_item__mutmut_21': xǁArchiveDALǁshow_item__mutmut_21, 
        'xǁArchiveDALǁshow_item__mutmut_22': xǁArchiveDALǁshow_item__mutmut_22, 
        'xǁArchiveDALǁshow_item__mutmut_23': xǁArchiveDALǁshow_item__mutmut_23, 
        'xǁArchiveDALǁshow_item__mutmut_24': xǁArchiveDALǁshow_item__mutmut_24, 
        'xǁArchiveDALǁshow_item__mutmut_25': xǁArchiveDALǁshow_item__mutmut_25, 
        'xǁArchiveDALǁshow_item__mutmut_26': xǁArchiveDALǁshow_item__mutmut_26, 
        'xǁArchiveDALǁshow_item__mutmut_27': xǁArchiveDALǁshow_item__mutmut_27, 
        'xǁArchiveDALǁshow_item__mutmut_28': xǁArchiveDALǁshow_item__mutmut_28, 
        'xǁArchiveDALǁshow_item__mutmut_29': xǁArchiveDALǁshow_item__mutmut_29, 
        'xǁArchiveDALǁshow_item__mutmut_30': xǁArchiveDALǁshow_item__mutmut_30, 
        'xǁArchiveDALǁshow_item__mutmut_31': xǁArchiveDALǁshow_item__mutmut_31, 
        'xǁArchiveDALǁshow_item__mutmut_32': xǁArchiveDALǁshow_item__mutmut_32, 
        'xǁArchiveDALǁshow_item__mutmut_33': xǁArchiveDALǁshow_item__mutmut_33, 
        'xǁArchiveDALǁshow_item__mutmut_34': xǁArchiveDALǁshow_item__mutmut_34, 
        'xǁArchiveDALǁshow_item__mutmut_35': xǁArchiveDALǁshow_item__mutmut_35, 
        'xǁArchiveDALǁshow_item__mutmut_36': xǁArchiveDALǁshow_item__mutmut_36, 
        'xǁArchiveDALǁshow_item__mutmut_37': xǁArchiveDALǁshow_item__mutmut_37, 
        'xǁArchiveDALǁshow_item__mutmut_38': xǁArchiveDALǁshow_item__mutmut_38, 
        'xǁArchiveDALǁshow_item__mutmut_39': xǁArchiveDALǁshow_item__mutmut_39, 
        'xǁArchiveDALǁshow_item__mutmut_40': xǁArchiveDALǁshow_item__mutmut_40, 
        'xǁArchiveDALǁshow_item__mutmut_41': xǁArchiveDALǁshow_item__mutmut_41, 
        'xǁArchiveDALǁshow_item__mutmut_42': xǁArchiveDALǁshow_item__mutmut_42, 
        'xǁArchiveDALǁshow_item__mutmut_43': xǁArchiveDALǁshow_item__mutmut_43, 
        'xǁArchiveDALǁshow_item__mutmut_44': xǁArchiveDALǁshow_item__mutmut_44, 
        'xǁArchiveDALǁshow_item__mutmut_45': xǁArchiveDALǁshow_item__mutmut_45, 
        'xǁArchiveDALǁshow_item__mutmut_46': xǁArchiveDALǁshow_item__mutmut_46, 
        'xǁArchiveDALǁshow_item__mutmut_47': xǁArchiveDALǁshow_item__mutmut_47, 
        'xǁArchiveDALǁshow_item__mutmut_48': xǁArchiveDALǁshow_item__mutmut_48, 
        'xǁArchiveDALǁshow_item__mutmut_49': xǁArchiveDALǁshow_item__mutmut_49, 
        'xǁArchiveDALǁshow_item__mutmut_50': xǁArchiveDALǁshow_item__mutmut_50, 
        'xǁArchiveDALǁshow_item__mutmut_51': xǁArchiveDALǁshow_item__mutmut_51, 
        'xǁArchiveDALǁshow_item__mutmut_52': xǁArchiveDALǁshow_item__mutmut_52
    }
    
    def show_item(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁshow_item__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁshow_item__mutmut_mutants"), args, kwargs, self)
        return result 
    
    show_item.__signature__ = _mutmut_signature(xǁArchiveDALǁshow_item__mutmut_orig)
    xǁArchiveDALǁshow_item__mutmut_orig.__name__ = 'xǁArchiveDALǁshow_item'

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_orig(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_1(self, url: str) -> Path:
        prefix = None
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_2(self, url: str) -> Path:
        prefix = "XXsqlite:///XX"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_3(self, url: str) -> Path:
        prefix = "SQLITE:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_4(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(None):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_5(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = None
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_6(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith(None):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_7(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("XXsqlite://XX"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_8(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("SQLITE://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_9(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = None
        else:
            path = url
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_10(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = None
        return Path(path).expanduser().resolve()

    # ------------------------------------------------------------------
    # helpers
    # ------------------------------------------------------------------
    def xǁArchiveDALǁ_sqlite_path__mutmut_11(self, url: str) -> Path:
        prefix = "sqlite:///"
        if url.startswith(prefix):
            path = url[len(prefix) :]
        elif url.startswith("sqlite://"):
            path = url[len("sqlite://") :]
        else:
            path = url
        return Path(None).expanduser().resolve()
    
    xǁArchiveDALǁ_sqlite_path__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ_sqlite_path__mutmut_1': xǁArchiveDALǁ_sqlite_path__mutmut_1, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_2': xǁArchiveDALǁ_sqlite_path__mutmut_2, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_3': xǁArchiveDALǁ_sqlite_path__mutmut_3, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_4': xǁArchiveDALǁ_sqlite_path__mutmut_4, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_5': xǁArchiveDALǁ_sqlite_path__mutmut_5, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_6': xǁArchiveDALǁ_sqlite_path__mutmut_6, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_7': xǁArchiveDALǁ_sqlite_path__mutmut_7, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_8': xǁArchiveDALǁ_sqlite_path__mutmut_8, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_9': xǁArchiveDALǁ_sqlite_path__mutmut_9, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_10': xǁArchiveDALǁ_sqlite_path__mutmut_10, 
        'xǁArchiveDALǁ_sqlite_path__mutmut_11': xǁArchiveDALǁ_sqlite_path__mutmut_11
    }
    
    def _sqlite_path(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ_sqlite_path__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ_sqlite_path__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _sqlite_path.__signature__ = _mutmut_signature(xǁArchiveDALǁ_sqlite_path__mutmut_orig)
    xǁArchiveDALǁ_sqlite_path__mutmut_orig.__name__ = 'xǁArchiveDALǁ_sqlite_path'

    @contextmanager
    def _transaction(self) -> Iterator[Callable[[str, Params | None, bool, bool], Any]]:
        if self.backend == "sqlite":
            if self._conn is None:
                raise RuntimeError("SQLite connection is not initialised")
            cursor = self._conn.cursor()
            try:

                def execute_sql(
                    sql: str,
                    params: Params | None = None,
                    fetchone: bool = False,
                    fetchall: bool = False,
                ) -> Any:
                    return self._sqlite_execute(cursor, sql, params, fetchone, fetchall)

                yield execute_sql
                self._conn.commit()
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                self._conn.rollback()
                raise
            finally:
                cursor.close()
        else:
            if self._engine is None:
                raise RuntimeError("SQLAlchemy engine is not initialised")
            with self._engine.begin() as connection:

                def execute_sql(
                    sql: str,
                    params: Params | None = None,
                    fetchone: bool = False,
                    fetchall: bool = False,
                ) -> Any:
                    return self._sqlalchemy_execute(connection, sql, params, fetchone, fetchall)

                yield execute_sql

    def xǁArchiveDALǁ_sqlite_execute__mutmut_orig(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_1(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = None
        cursor.execute(sql, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_2(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params and {}
        cursor.execute(sql, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_3(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(None, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_4(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, None)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_5(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_6(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, )
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_7(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, parameters)
        if fetchone:
            row = None
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_8(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(None) if row is not None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_9(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is None else None
        if fetchall:
            return [dict(row) for row in cursor.fetchall()]
        return None

    def xǁArchiveDALǁ_sqlite_execute__mutmut_10(
        self,
        cursor: sqlite3.Cursor,
        sql: str,
        params: Params | None,
        fetchone: bool,
        fetchall: bool,
    ) -> Any:
        parameters = params or {}
        cursor.execute(sql, parameters)
        if fetchone:
            row = cursor.fetchone()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(None) for row in cursor.fetchall()]
        return None
    
    xǁArchiveDALǁ_sqlite_execute__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ_sqlite_execute__mutmut_1': xǁArchiveDALǁ_sqlite_execute__mutmut_1, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_2': xǁArchiveDALǁ_sqlite_execute__mutmut_2, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_3': xǁArchiveDALǁ_sqlite_execute__mutmut_3, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_4': xǁArchiveDALǁ_sqlite_execute__mutmut_4, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_5': xǁArchiveDALǁ_sqlite_execute__mutmut_5, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_6': xǁArchiveDALǁ_sqlite_execute__mutmut_6, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_7': xǁArchiveDALǁ_sqlite_execute__mutmut_7, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_8': xǁArchiveDALǁ_sqlite_execute__mutmut_8, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_9': xǁArchiveDALǁ_sqlite_execute__mutmut_9, 
        'xǁArchiveDALǁ_sqlite_execute__mutmut_10': xǁArchiveDALǁ_sqlite_execute__mutmut_10
    }
    
    def _sqlite_execute(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ_sqlite_execute__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ_sqlite_execute__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _sqlite_execute.__signature__ = _mutmut_signature(xǁArchiveDALǁ_sqlite_execute__mutmut_orig)
    xǁArchiveDALǁ_sqlite_execute__mutmut_orig.__name__ = 'xǁArchiveDALǁ_sqlite_execute'

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_orig(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_1(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = None
        result = connection.execute(statement, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_2(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(None)
        result = connection.execute(statement, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_3(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = None
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_4(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(None, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_5(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, None)
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_6(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_7(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, )
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_8(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, params and {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_9(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, params or {})
        if fetchone:
            row = None
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_10(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(None) if row is not None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_11(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is None else None
        if fetchall:
            return [dict(row) for row in result.mappings().all()]
        return None

    def xǁArchiveDALǁ_sqlalchemy_execute__mutmut_12(
        self, connection: Any, sql: str, params: Params | None, fetchone: bool, fetchall: bool
    ) -> Any:
        statement = sa.text(sql)
        result = connection.execute(statement, params or {})
        if fetchone:
            row = result.mappings().first()
            return dict(row) if row is not None else None
        if fetchall:
            return [dict(None) for row in result.mappings().all()]
        return None
    
    xǁArchiveDALǁ_sqlalchemy_execute__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_1': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_1, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_2': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_2, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_3': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_3, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_4': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_4, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_5': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_5, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_6': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_6, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_7': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_7, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_8': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_8, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_9': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_9, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_10': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_10, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_11': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_11, 
        'xǁArchiveDALǁ_sqlalchemy_execute__mutmut_12': xǁArchiveDALǁ_sqlalchemy_execute__mutmut_12
    }
    
    def _sqlalchemy_execute(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ_sqlalchemy_execute__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ_sqlalchemy_execute__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _sqlalchemy_execute.__signature__ = _mutmut_signature(xǁArchiveDALǁ_sqlalchemy_execute__mutmut_orig)
    xǁArchiveDALǁ_sqlalchemy_execute__mutmut_orig.__name__ = 'xǁArchiveDALǁ_sqlalchemy_execute'

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_orig(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", {"sha": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_1(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            None, {"sha": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_2(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", None, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_3(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", {"sha": sha}, fetchone=None
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_4(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            {"sha": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_5(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_6(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", {"sha": sha}, )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_7(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "XXSELECT * FROM artifact WHERE content_sha256 = :shaXX", {"sha": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_8(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "select * from artifact where content_sha256 = :sha", {"sha": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_9(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM ARTIFACT WHERE CONTENT_SHA256 = :SHA", {"sha": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_10(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", {"XXshaXX": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_11(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", {"SHA": sha}, fetchone=True
        )

    def xǁArchiveDALǁ_get_artifact_by_sha__mutmut_12(self, execute: Callable[..., Any], sha: str) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM artifact WHERE content_sha256 = :sha", {"sha": sha}, fetchone=False
        )
    
    xǁArchiveDALǁ_get_artifact_by_sha__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_1': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_1, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_2': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_2, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_3': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_3, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_4': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_4, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_5': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_5, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_6': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_6, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_7': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_7, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_8': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_8, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_9': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_9, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_10': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_10, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_11': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_11, 
        'xǁArchiveDALǁ_get_artifact_by_sha__mutmut_12': xǁArchiveDALǁ_get_artifact_by_sha__mutmut_12
    }
    
    def _get_artifact_by_sha(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ_get_artifact_by_sha__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ_get_artifact_by_sha__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_artifact_by_sha.__signature__ = _mutmut_signature(xǁArchiveDALǁ_get_artifact_by_sha__mutmut_orig)
    xǁArchiveDALǁ_get_artifact_by_sha__mutmut_orig.__name__ = 'xǁArchiveDALǁ_get_artifact_by_sha'

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_orig(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_1(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = None
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_2(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            None, {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_3(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", None, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_4(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"id": artifact_id}, fetchone=None
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_5(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_6(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_7(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"id": artifact_id}, )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_8(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "XXSELECT * FROM artifact WHERE id = :idXX", {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_9(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "select * from artifact where id = :id", {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_10(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM ARTIFACT WHERE ID = :ID", {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_11(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"XXidXX": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_12(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"ID": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_13(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"id": artifact_id}, fetchone=False
        )
        if artifact is None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_14(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"id": artifact_id}, fetchone=True
        )
        if artifact is not None:
            raise LookupError(f"Unknown artifact id: {artifact_id}")
        return artifact

    def xǁArchiveDALǁ_get_artifact_by_id__mutmut_15(self, execute: Callable[..., Any], artifact_id: str) -> dict[str, Any]:
        artifact = execute(
            "SELECT * FROM artifact WHERE id = :id", {"id": artifact_id}, fetchone=True
        )
        if artifact is None:
            raise LookupError(None)
        return artifact
    
    xǁArchiveDALǁ_get_artifact_by_id__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ_get_artifact_by_id__mutmut_1': xǁArchiveDALǁ_get_artifact_by_id__mutmut_1, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_2': xǁArchiveDALǁ_get_artifact_by_id__mutmut_2, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_3': xǁArchiveDALǁ_get_artifact_by_id__mutmut_3, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_4': xǁArchiveDALǁ_get_artifact_by_id__mutmut_4, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_5': xǁArchiveDALǁ_get_artifact_by_id__mutmut_5, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_6': xǁArchiveDALǁ_get_artifact_by_id__mutmut_6, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_7': xǁArchiveDALǁ_get_artifact_by_id__mutmut_7, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_8': xǁArchiveDALǁ_get_artifact_by_id__mutmut_8, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_9': xǁArchiveDALǁ_get_artifact_by_id__mutmut_9, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_10': xǁArchiveDALǁ_get_artifact_by_id__mutmut_10, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_11': xǁArchiveDALǁ_get_artifact_by_id__mutmut_11, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_12': xǁArchiveDALǁ_get_artifact_by_id__mutmut_12, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_13': xǁArchiveDALǁ_get_artifact_by_id__mutmut_13, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_14': xǁArchiveDALǁ_get_artifact_by_id__mutmut_14, 
        'xǁArchiveDALǁ_get_artifact_by_id__mutmut_15': xǁArchiveDALǁ_get_artifact_by_id__mutmut_15
    }
    
    def _get_artifact_by_id(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ_get_artifact_by_id__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ_get_artifact_by_id__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_artifact_by_id.__signature__ = _mutmut_signature(xǁArchiveDALǁ_get_artifact_by_id__mutmut_orig)
    xǁArchiveDALǁ_get_artifact_by_id__mutmut_orig.__name__ = 'xǁArchiveDALǁ_get_artifact_by_id'

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_orig(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", {"tomb": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_1(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            None, {"tomb": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_2(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", None, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_3(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", {"tomb": tombstone_id}, fetchone=None
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_4(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            {"tomb": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_5(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_6(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", {"tomb": tombstone_id}, )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_7(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "XXSELECT * FROM item WHERE tombstone_id = :tombXX", {"tomb": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_8(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "select * from item where tombstone_id = :tomb", {"tomb": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_9(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM ITEM WHERE TOMBSTONE_ID = :TOMB", {"tomb": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_10(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", {"XXtombXX": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_11(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", {"TOMB": tombstone_id}, fetchone=True
        )

    def xǁArchiveDALǁ_get_item_by_tombstone__mutmut_12(
        self, execute: Callable[..., Any], tombstone_id: str
    ) -> dict[str, Any] | None:
        return execute(
            "SELECT * FROM item WHERE tombstone_id = :tomb", {"tomb": tombstone_id}, fetchone=False
        )
    
    xǁArchiveDALǁ_get_item_by_tombstone__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_1': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_1, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_2': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_2, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_3': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_3, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_4': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_4, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_5': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_5, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_6': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_6, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_7': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_7, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_8': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_8, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_9': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_9, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_10': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_10, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_11': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_11, 
        'xǁArchiveDALǁ_get_item_by_tombstone__mutmut_12': xǁArchiveDALǁ_get_item_by_tombstone__mutmut_12
    }
    
    def _get_item_by_tombstone(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveDALǁ_get_item_by_tombstone__mutmut_orig"), object.__getattribute__(self, "xǁArchiveDALǁ_get_item_by_tombstone__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_item_by_tombstone.__signature__ = _mutmut_signature(xǁArchiveDALǁ_get_item_by_tombstone__mutmut_orig)
    xǁArchiveDALǁ_get_item_by_tombstone__mutmut_orig.__name__ = 'xǁArchiveDALǁ_get_item_by_tombstone'


def x__coerce_bool__mutmut_orig(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_1(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 2 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_2(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 1
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_3(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 2 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_4(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 1
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_5(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = None
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_6(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().upper()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_7(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 2 if lowered in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_8(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered not in {"1", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_9(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"XX1XX", "true", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_10(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "XXtrueXX", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_11(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "TRUE", "yes", "on"} else 0
    return 0


def x__coerce_bool__mutmut_12(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "XXyesXX", "on"} else 0
    return 0


def x__coerce_bool__mutmut_13(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "YES", "on"} else 0
    return 0


def x__coerce_bool__mutmut_14(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "XXonXX"} else 0
    return 0


def x__coerce_bool__mutmut_15(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "ON"} else 0
    return 0


def x__coerce_bool__mutmut_16(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 1
    return 0


def x__coerce_bool__mutmut_17(value: Any) -> int:
    """Normalise truthy inputs to 0/1 for SQL storage."""

    if isinstance(value, bool):
        return 1 if value else 0
    if isinstance(value, int | float):
        return 1 if value else 0
    if isinstance(value, str):
        lowered = value.strip().lower()
        return 1 if lowered in {"1", "true", "yes", "on"} else 0
    return 1

x__coerce_bool__mutmut_mutants : ClassVar[MutantDict] = {
'x__coerce_bool__mutmut_1': x__coerce_bool__mutmut_1, 
    'x__coerce_bool__mutmut_2': x__coerce_bool__mutmut_2, 
    'x__coerce_bool__mutmut_3': x__coerce_bool__mutmut_3, 
    'x__coerce_bool__mutmut_4': x__coerce_bool__mutmut_4, 
    'x__coerce_bool__mutmut_5': x__coerce_bool__mutmut_5, 
    'x__coerce_bool__mutmut_6': x__coerce_bool__mutmut_6, 
    'x__coerce_bool__mutmut_7': x__coerce_bool__mutmut_7, 
    'x__coerce_bool__mutmut_8': x__coerce_bool__mutmut_8, 
    'x__coerce_bool__mutmut_9': x__coerce_bool__mutmut_9, 
    'x__coerce_bool__mutmut_10': x__coerce_bool__mutmut_10, 
    'x__coerce_bool__mutmut_11': x__coerce_bool__mutmut_11, 
    'x__coerce_bool__mutmut_12': x__coerce_bool__mutmut_12, 
    'x__coerce_bool__mutmut_13': x__coerce_bool__mutmut_13, 
    'x__coerce_bool__mutmut_14': x__coerce_bool__mutmut_14, 
    'x__coerce_bool__mutmut_15': x__coerce_bool__mutmut_15, 
    'x__coerce_bool__mutmut_16': x__coerce_bool__mutmut_16, 
    'x__coerce_bool__mutmut_17': x__coerce_bool__mutmut_17
}

def _coerce_bool(*args, **kwargs):
    result = _mutmut_trampoline(x__coerce_bool__mutmut_orig, x__coerce_bool__mutmut_mutants, args, kwargs)
    return result 

_coerce_bool.__signature__ = _mutmut_signature(x__coerce_bool__mutmut_orig)
x__coerce_bool__mutmut_orig.__name__ = 'x__coerce_bool'
