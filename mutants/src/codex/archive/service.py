"""High level archive orchestration utilities."""

# ruff: noqa: I001  # import-order handled by isort configuration

from __future__ import annotations

import logging
logger = logging.getLogger(__name__)
import mimetypes
import os
import subprocess
from collections.abc import Callable, Sequence
from dataclasses import dataclass, replace
from pathlib import Path
from typing import Any

from . import backend as backend_module
from . import config as config_module
from . import retry as retry_module
from .logging_config import log_restore, setup_logging
from .perf import timer
from .util import (
    append_evidence,
    compression_codec,
    decompress_payload,
    redact_text_credentials,
    redact_url_credentials,
    sha256_hex,
    zstd_compress,
)

BackendArchiveConfig = backend_module.ArchiveConfig
ArchiveDAL = backend_module.ArchiveDAL
SettingsArchiveConfig = config_module.ArchiveAppConfig
SettingsBackendConfig = config_module.BackendConfig
RetryPolicyConfig = retry_module.RetryConfig
retry_with_backoff = retry_module.retry_with_backoff
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@dataclass(frozen=True)
class ArchiveResult:
    """Metadata returned after storing an item."""

    tombstone_id: str
    sha256: str
    size_bytes: int
    compressed_size: int
    repo: str
    path: str


class ArchiveService:
    """Service façade encapsulating archival workflows."""

    def xǁArchiveServiceǁ__init____mutmut_orig(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_1(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = False,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_2(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = None
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_3(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(None)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_4(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = None
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_5(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = None
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_6(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(None)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_7(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = None
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_8(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(None, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_9(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=None)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_10(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_11(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, )
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_12(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = None
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_13(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger and setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_14(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(None)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_15(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = ""
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_16(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = None
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_17(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = None
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_18(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = None

    def xǁArchiveServiceǁ__init____mutmut_19(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(self._retry_policy)(None)
            if self._retry_policy
            else self.dal.get_restore_payload
        )

    def xǁArchiveServiceǁ__init____mutmut_20(
        self,
        config: SettingsArchiveConfig | BackendArchiveConfig | None = None,
        *,
        apply_schema: bool = True,
        logger: logging.Logger | None = None,
    ) -> None:
        settings = self._coerce_settings(config)
        self.config = settings
        backend_config = BackendArchiveConfig.from_settings(settings)
        self.dal = ArchiveDAL(backend_config, apply_schema=apply_schema)
        self.logger = logger or setup_logging(settings.logging)
        self._retry_policy: RetryPolicyConfig | None = None
        retry_config = settings.retry.to_retry_config()
        if retry_config.enabled:
            self._retry_policy = retry_config
        self._get_restore_payload: Callable[[str], dict[str, Any]] = (
            retry_with_backoff(None)(self.dal.get_restore_payload)
            if self._retry_policy
            else self.dal.get_restore_payload
        )
    
    xǁArchiveServiceǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveServiceǁ__init____mutmut_1': xǁArchiveServiceǁ__init____mutmut_1, 
        'xǁArchiveServiceǁ__init____mutmut_2': xǁArchiveServiceǁ__init____mutmut_2, 
        'xǁArchiveServiceǁ__init____mutmut_3': xǁArchiveServiceǁ__init____mutmut_3, 
        'xǁArchiveServiceǁ__init____mutmut_4': xǁArchiveServiceǁ__init____mutmut_4, 
        'xǁArchiveServiceǁ__init____mutmut_5': xǁArchiveServiceǁ__init____mutmut_5, 
        'xǁArchiveServiceǁ__init____mutmut_6': xǁArchiveServiceǁ__init____mutmut_6, 
        'xǁArchiveServiceǁ__init____mutmut_7': xǁArchiveServiceǁ__init____mutmut_7, 
        'xǁArchiveServiceǁ__init____mutmut_8': xǁArchiveServiceǁ__init____mutmut_8, 
        'xǁArchiveServiceǁ__init____mutmut_9': xǁArchiveServiceǁ__init____mutmut_9, 
        'xǁArchiveServiceǁ__init____mutmut_10': xǁArchiveServiceǁ__init____mutmut_10, 
        'xǁArchiveServiceǁ__init____mutmut_11': xǁArchiveServiceǁ__init____mutmut_11, 
        'xǁArchiveServiceǁ__init____mutmut_12': xǁArchiveServiceǁ__init____mutmut_12, 
        'xǁArchiveServiceǁ__init____mutmut_13': xǁArchiveServiceǁ__init____mutmut_13, 
        'xǁArchiveServiceǁ__init____mutmut_14': xǁArchiveServiceǁ__init____mutmut_14, 
        'xǁArchiveServiceǁ__init____mutmut_15': xǁArchiveServiceǁ__init____mutmut_15, 
        'xǁArchiveServiceǁ__init____mutmut_16': xǁArchiveServiceǁ__init____mutmut_16, 
        'xǁArchiveServiceǁ__init____mutmut_17': xǁArchiveServiceǁ__init____mutmut_17, 
        'xǁArchiveServiceǁ__init____mutmut_18': xǁArchiveServiceǁ__init____mutmut_18, 
        'xǁArchiveServiceǁ__init____mutmut_19': xǁArchiveServiceǁ__init____mutmut_19, 
        'xǁArchiveServiceǁ__init____mutmut_20': xǁArchiveServiceǁ__init____mutmut_20
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁArchiveServiceǁ__init____mutmut_orig)
    xǁArchiveServiceǁ__init____mutmut_orig.__name__ = 'xǁArchiveServiceǁ__init__'

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_orig(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_1(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "XXcodeXX",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_2(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "CODE",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_3(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = None
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_4(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = None
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_5(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(None)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_6(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = None
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_7(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(None)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_8(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = None
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_9(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = None
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_10(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type and (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_11(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] and "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_12(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(None)[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_13(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[1] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_14(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "XXapplication/octet-streamXX")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_15(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "APPLICATION/OCTET-STREAM")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_16(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = None
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_17(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "XXsha256XX": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_18(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "SHA256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_19(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "XXsize_bytesXX": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_20(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "SIZE_BYTES": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_21(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "XXcompressed_sizeXX": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_22(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "COMPRESSED_SIZE": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_23(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "XXcompressionXX": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_24(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "COMPRESSION": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_25(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "XXmime_typeXX": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_26(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "MIME_TYPE": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_27(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "XXlanguageXX": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_28(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "LANGUAGE": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_29(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(None)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_30(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = None
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_31(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "XXcommitXX": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_32(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "COMMIT": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_33(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "XXrepoXX": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_34(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "REPO": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_35(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "XXpathXX": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_36(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "PATH": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_37(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "XXpythonXX": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_38(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "PYTHON": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_39(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") and _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_40(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv(None) or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_41(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("XXPYTHON_VERSIONXX") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_42(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("python_version") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_43(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = None
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_44(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "XXcontent_sha256XX": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_45(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "CONTENT_SHA256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_46(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "XXsize_bytesXX": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_47(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "SIZE_BYTES": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_48(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "XXcompressionXX": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_49(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "COMPRESSION": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_50(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "XXmime_typeXX": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_51(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "MIME_TYPE": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_52(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "XXstorage_driverXX": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_53(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "STORAGE_DRIVER": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_54(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "XXdbXX",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_55(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "DB",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_56(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "XXblob_bytesXX": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_57(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "BLOB_BYTES": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_58(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "XXobject_urlXX": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_59(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "OBJECT_URL": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_60(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = None
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_61(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=None,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_62(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=None,
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_63(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=None,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_64(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=None,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_65(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=None,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_66(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=None,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_67(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=None,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_68(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=None,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_69(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=None,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_70(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=None,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_71(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=None,
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_72(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_73(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_74(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_75(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_76(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_77(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_78(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_79(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_80(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_81(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_82(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_83(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(None),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_84(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags and ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_85(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            None
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_86(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "XXactionXX": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_87(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "ACTION": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_88(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "XXARCHIVEXX",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_89(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "archive",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_90(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "XXactorXX": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_91(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "ACTOR": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_92(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "XXrepoXX": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_93(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "REPO": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_94(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "XXpathXX": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_95(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "PATH": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_96(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "XXtombstoneXX": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_97(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "TOMBSTONE": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_98(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["XXtombstone_idXX"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_99(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["TOMBSTONE_ID"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_100(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "XXsha256XX": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_101(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "SHA256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_102(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "XXsizeXX": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_103(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "SIZE": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_104(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "XXcompressed_sizeXX": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_105(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "COMPRESSED_SIZE": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_106(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "XXreasonXX": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_107(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "REASON": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_108(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=None,
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_109(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=None,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_110(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=None,
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_111(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=None,
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_112(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=None,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_113(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=None,
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_114(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_115(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_116(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_117(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_118(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_119(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["tombstone_id"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_120(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["XXtombstone_idXX"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )

    # ------------------------------------------------------------------
    # entry points
    # ------------------------------------------------------------------
    def xǁArchiveServiceǁarchive_path__mutmut_121(
        self,
        *,
        repo: str,
        path: Path,
        reason: str,
        archived_by: str,
        commit_sha: str,
        kind: str = "code",
        language: str | None = None,
        mime_type: str | None = None,
        tags: Sequence[str] | None = None,
        extra_metadata: dict[str, object] | None = None,
    ) -> ArchiveResult:
        """Archive *path* and return archival metadata."""

        bytes_in = path.read_bytes()
        sha = sha256_hex(bytes_in)
        compressed = zstd_compress(bytes_in)
        codec = compression_codec()
        mime = mime_type or (mimetypes.guess_type(path.as_posix())[0] or "application/octet-stream")
        metadata = {
            "sha256": sha,
            "size_bytes": len(bytes_in),
            "compressed_size": len(compressed),
            "compression": codec,
            "mime_type": mime,
            "language": language,
        }
        if extra_metadata:
            metadata.update(extra_metadata)
        context = {
            "commit": commit_sha,
            "repo": repo,
            "path": path.as_posix(),
            "python": os.getenv("PYTHON_VERSION") or _python_version(),
        }
        artifact_payload = {
            "content_sha256": sha,
            "size_bytes": len(bytes_in),
            "compression": codec,
            "mime_type": mime,
            "storage_driver": "db",
            "blob_bytes": compressed,
            "object_url": None,
        }
        result = self.dal.record_archive(
            repo=repo,
            path=path.as_posix(),
            commit_sha=commit_sha,
            language=language,
            reason=reason,
            kind=kind,
            artifact_payload=artifact_payload,
            archived_by=archived_by,
            metadata=metadata,
            context=context,
            tags=list(tags or ()),
        )
        append_evidence(
            {
                "action": "ARCHIVE",
                "actor": archived_by,
                "repo": repo,
                "path": path.as_posix(),
                "tombstone": result["tombstone_id"],
                "sha256": sha,
                "size": len(bytes_in),
                "compressed_size": len(compressed),
                "reason": reason,
            }
        )
        return ArchiveResult(
            tombstone_id=result["TOMBSTONE_ID"],
            sha256=sha,
            size_bytes=len(bytes_in),
            compressed_size=len(compressed),
            repo=repo,
            path=path.as_posix(),
        )
    
    xǁArchiveServiceǁarchive_path__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveService�
        'xǁArchiveServiceǁarchive_path__mutmut_117': xǁArchiveServiceǁarchive_path__mutmut_117, 
        'xǁArchiveServiceǁarchive_path__mutmut_118': xǁArchiveServiceǁarchive_path__mutmut_118, 
        'xǁArchiveServiceǁarchive_path__mutmut_119': xǁArchiveServiceǁarchive_path__mutmut_119, 
        'xǁArchiveServiceǁarchive_path__mutmut_120': xǁArchiveServiceǁarchive_path__mutmut_120, 
        'xǁArchiveServiceǁarchive_path__mutmut_121': xǁArchiveServiceǁarchive_path__mutmut_121
    }
    
    def archive_path(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁarchive_path__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁarchive_path__mutmut_mutants"), args, kwargs, self)
        return result 
    
    archive_path.__signature__ = _mutmut_signature(xǁArchiveServiceǁarchive_path__mutmut_orig)
    xǁArchiveServiceǁarchive_path__mutmut_orig.__name__ = 'xǁArchiveServiceǁarchive_path'

    def xǁArchiveServiceǁrestore_to_path__mutmut_orig(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_1(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = None
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_2(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(None, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_3(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, None, None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_4(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr("backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_5(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_6(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", )
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_7(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "XXbackendXX", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_8(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "BACKEND", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_9(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = None
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_10(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(None, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_11(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, None, None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_12(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr("url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_13(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_14(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", )
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_15(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "XXurlXX", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_16(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "URL", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_17(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = None
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_18(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = ""
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_19(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = ""

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_20(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer(None) as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_21(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("XXrestoreXX") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_22(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("RESTORE") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_23(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = None
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_24(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(None)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_25(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(None)
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_26(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=None,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_27(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=None,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_28(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=None,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_29(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=None,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_30(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=None,
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_31(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_32(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_33(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_34(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_35(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_36(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = None
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_37(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(None).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_38(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(None)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_39(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = None
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_40(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" - (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_41(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(None).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_42(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "XXXX")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_43(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=None,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_44(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=None,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_45(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=None,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_46(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=None,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_47(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=None,
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_48(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=None,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_49(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_50(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_51(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_52(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_53(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_54(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_55(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    None
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_56(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = None
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_57(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["XXartifactXX"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_58(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["ARTIFACT"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_59(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = None
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_60(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get(None)
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_61(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("XXblob_bytesXX")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_62(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("BLOB_BYTES")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_63(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is not None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_64(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=None,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_65(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=None,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_66(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=None,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_67(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=None,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_68(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=None,
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_69(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_70(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_71(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_72(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_73(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_74(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="XXArtifact payload has been purged; bytes unavailableXX",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_75(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_76(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="ARTIFACT PAYLOAD HAS BEEN PURGED; BYTES UNAVAILABLE",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_77(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError(None)

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_78(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("XXArtifact payload has been purged; bytes unavailableXX")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_79(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_80(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("ARTIFACT PAYLOAD HAS BEEN PURGED; BYTES UNAVAILABLE")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_81(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = None
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_82(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") and compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_83(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get(None) or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_84(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("XXcompressionXX") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_85(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("COMPRESSION") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_86(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer(None) as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_87(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("XXdecompressXX") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_88(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("DECOMPRESS") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_89(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = None
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_90(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(None, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_91(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, None)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_92(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_93(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, )
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_94(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(None)
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_95(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=None,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_96(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=None,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_97(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=None,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_98(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=None,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_99(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=None,
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_100(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=None,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_101(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_102(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_103(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_104(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_105(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_106(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_107(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(None) from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_108(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = None

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_109(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer(None) as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_110(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("XXwriteXX") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_111(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("WRITE") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_112(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=None, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_113(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=None)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_114(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_115(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, )
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_116(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=False, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_117(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=False)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_118(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(None)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_119(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = None

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_120(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(None, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_121(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=None)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_122(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_123(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, )

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_124(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_125(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = None
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_126(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["XXduration_msXX"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_127(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["DURATION_MS"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_128(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_129(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = None
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_130(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["XXdecompression_msXX"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_131(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["DECOMPRESSION_MS"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_132(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_133(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = None

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_134(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["XXwrite_msXX"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_135(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["WRITE_MS"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_136(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = None
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_137(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "XXactionXX": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_138(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "ACTION": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_139(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "XXRESTOREXX",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_140(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "restore",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_141(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "XXactorXX": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_142(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "ACTOR": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_143(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "XXtombstoneXX": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_144(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "TOMBSTONE": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_145(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "XXpathXX": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_146(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "PATH": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_147(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "XXrepoXX": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_148(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "REPO": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_149(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get(None),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_150(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["XXitemXX"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_151(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["ITEM"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_152(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("XXrepoXX"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_153(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("REPO"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_154(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(None)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_155(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(None)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_156(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            None,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_157(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=None,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_158(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=None,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_159(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status=None,
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_160(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_161(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=None,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_162(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=None,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_163(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_164(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_165(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_166(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_167(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_168(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_169(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="SUCCESS",
            metrics=metrics or None,
            logging_config=self.config.logging,
            )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_170(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="XXSUCCESSXX",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_171(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="success",
            metrics=metrics or None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
        return output_path

    def xǁArchiveServiceǁrestore_to_path__mutmut_172(
        self,
        tombstone_id: str,
        *,
        output_path: Path,
        actor: str,
    ) -> Path:
        """Restore an archived item to *output_path*."""

        backend = getattr(self.dal, "backend", None)
        raw_url = getattr(self.dal, "url", None)
        metrics: dict[str, float] = {}
        decompress_duration: float | None = None
        write_duration: float | None = None

        with timer("restore") as restore_timer:
            try:
                payload = self._get_restore_payload(tombstone_id)
            except LookupError as exc:
                logger.debug(f"LookupError: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Tombstone not found: {exc}",
                )
                raise
            except Exception as exc:  # pragma: no cover - defensive guard
                sanitized = redact_text_credentials(str(exc)).strip()
                detail = f"{type(exc).__name__}" + (f": {sanitized}" if sanitized else "")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Backend access error: {detail}",
                    error=exc,
                )
                raise RuntimeError(
                    f"Failed to retrieve restore payload for tombstone {tombstone_id}: {detail}"
                ) from exc

            artifact = payload["artifact"]
            blob = artifact.get("blob_bytes")
            if blob is None:
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason="Artifact payload has been purged; bytes unavailable",
                )
                raise RuntimeError("Artifact payload has been purged; bytes unavailable")

            codec = artifact.get("compression") or compression_codec()
            try:
                with timer("decompress") as decompress_timer:
                    restored = decompress_payload(blob, codec)
            except (RuntimeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                self._record_restore_failure(
                    tombstone_id=tombstone_id,
                    actor=actor,
                    backend=backend,
                    url=raw_url,
                    reason=f"Decompression failed with codec '{codec}'",
                    error=exc,
                )
                raise RuntimeError(f"Unable to decompress artifact using codec '{codec}'") from exc
            else:
                decompress_duration = decompress_timer.duration_ms

            with timer("write") as write_timer:
                output_path.parent.mkdir(parents=True, exist_ok=True)
                output_path.write_bytes(restored)
            write_duration = write_timer.duration_ms

            self.dal.record_restore(tombstone_id, actor=actor)

        if self.config.performance.enabled:
            if restore_timer.duration_ms is not None:
                metrics["duration_ms"] = restore_timer.duration_ms
            if decompress_duration is not None:
                metrics["decompression_ms"] = decompress_duration
            if write_duration is not None:
                metrics["write_ms"] = write_duration

        evidence_payload: dict[str, Any] = {
            "action": "RESTORE",
            "actor": actor,
            "tombstone": tombstone_id,
            "path": output_path.as_posix(),
            "repo": payload["item"].get("repo"),
        }
        evidence_payload.update(metrics)
        append_evidence(evidence_payload)

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status
 
        'xǁArchiveServiceǁrestore_to_path__mutmut_147': xǁArchiveServiceǁrestore_to_path__mutmut_147, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_148': xǁArchiveServiceǁrestore_to_path__mutmut_148, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_149': xǁArchiveServiceǁrestore_to_path__mutmut_149, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_150': xǁArchiveServiceǁrestore_to_path__mutmut_150, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_151': xǁArchiveServiceǁrestore_to_path__mutmut_151, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_152': xǁArchiveServiceǁrestore_to_path__mutmut_152, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_153': xǁArchiveServiceǁrestore_to_path__mutmut_153, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_154': xǁArchiveServiceǁrestore_to_path__mutmut_154, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_155': xǁArchiveServiceǁrestore_to_path__mutmut_155, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_156': xǁArchiveServiceǁrestore_to_path__mutmut_156, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_157': xǁArchiveServiceǁrestore_to_path__mutmut_157, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_158': xǁArchiveServiceǁrestore_to_path__mutmut_158, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_159': xǁArchiveServiceǁrestore_to_path__mutmut_159, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_160': xǁArchiveServiceǁrestore_to_path__mutmut_160, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_161': xǁArchiveServiceǁrestore_to_path__mutmut_161, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_162': xǁArchiveServiceǁrestore_to_path__mutmut_162, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_163': xǁArchiveServiceǁrestore_to_path__mutmut_163, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_164': xǁArchiveServiceǁrestore_to_path__mutmut_164, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_165': xǁArchiveServiceǁrestore_to_path__mutmut_165, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_166': xǁArchiveServiceǁrestore_to_path__mutmut_166, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_167': xǁArchiveServiceǁrestore_to_path__mutmut_167, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_168': xǁArchiveServiceǁrestore_to_path__mutmut_168, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_169': xǁArchiveServiceǁrestore_to_path__mutmut_169, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_170': xǁArchiveServiceǁrestore_to_path__mutmut_170, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_171': xǁArchiveServiceǁrestore_to_path__mutmut_171, 
        'xǁArchiveServiceǁrestore_to_path__mutmut_172': xǁArchiveServiceǁrestore_to_path__mutmut_172
    }
    
    def restore_to_path(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁrestore_to_path__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁrestore_to_path__mutmut_mutants"), args, kwargs, self)
        return result 
    
    restore_to_path.__signature__ = _mutmut_signature(xǁArchiveServiceǁrestore_to_path__mutmut_orig)
    xǁArchiveServiceǁrestore_to_path__mutmut_orig.__name__ = 'xǁArchiveServiceǁrestore_to_path'

    def xǁArchiveServiceǁlist_items__mutmut_orig(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=repo, since=since, limit=limit)

    def xǁArchiveServiceǁlist_items__mutmut_1(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 101
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=repo, since=since, limit=limit)

    def xǁArchiveServiceǁlist_items__mutmut_2(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=None, since=since, limit=limit)

    def xǁArchiveServiceǁlist_items__mutmut_3(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=repo, since=None, limit=limit)

    def xǁArchiveServiceǁlist_items__mutmut_4(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=repo, since=since, limit=None)

    def xǁArchiveServiceǁlist_items__mutmut_5(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(since=since, limit=limit)

    def xǁArchiveServiceǁlist_items__mutmut_6(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=repo, limit=limit)

    def xǁArchiveServiceǁlist_items__mutmut_7(
        self, *, repo: str | None = None, since: str | None = None, limit: int = 100
    ) -> list[dict[str, object]]:
        return self.dal.list_items(repo=repo, since=since, )
    
    xǁArchiveServiceǁlist_items__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveServiceǁlist_items__mutmut_1': xǁArchiveServiceǁlist_items__mutmut_1, 
        'xǁArchiveServiceǁlist_items__mutmut_2': xǁArchiveServiceǁlist_items__mutmut_2, 
        'xǁArchiveServiceǁlist_items__mutmut_3': xǁArchiveServiceǁlist_items__mutmut_3, 
        'xǁArchiveServiceǁlist_items__mutmut_4': xǁArchiveServiceǁlist_items__mutmut_4, 
        'xǁArchiveServiceǁlist_items__mutmut_5': xǁArchiveServiceǁlist_items__mutmut_5, 
        'xǁArchiveServiceǁlist_items__mutmut_6': xǁArchiveServiceǁlist_items__mutmut_6, 
        'xǁArchiveServiceǁlist_items__mutmut_7': xǁArchiveServiceǁlist_items__mutmut_7
    }
    
    def list_items(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁlist_items__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁlist_items__mutmut_mutants"), args, kwargs, self)
        return result 
    
    list_items.__signature__ = _mutmut_signature(xǁArchiveServiceǁlist_items__mutmut_orig)
    xǁArchiveServiceǁlist_items__mutmut_orig.__name__ = 'xǁArchiveServiceǁlist_items'

    def xǁArchiveServiceǁshow_item__mutmut_orig(self, tombstone_id: str) -> dict[str, object]:
        return self.dal.show_item(tombstone_id)

    def xǁArchiveServiceǁshow_item__mutmut_1(self, tombstone_id: str) -> dict[str, object]:
        return self.dal.show_item(None)
    
    xǁArchiveServiceǁshow_item__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveServiceǁshow_item__mutmut_1': xǁArchiveServiceǁshow_item__mutmut_1
    }
    
    def show_item(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁshow_item__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁshow_item__mutmut_mutants"), args, kwargs, self)
        return result 
    
    show_item.__signature__ = _mutmut_signature(xǁArchiveServiceǁshow_item__mutmut_orig)
    xǁArchiveServiceǁshow_item__mutmut_orig.__name__ = 'xǁArchiveServiceǁshow_item'

    def xǁArchiveServiceǁrequest_prune__mutmut_orig(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_1(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(None, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_2(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=None, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_3(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=None)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_4(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_5(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_6(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, )
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_7(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            None
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_8(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "XXactionXX": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_9(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "ACTION": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_10(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "XXPRUNE_REQUESTXX",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_11(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "prune_request",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_12(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "XXactorXX": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_13(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "ACTOR": actor,
                "tombstone": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_14(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "XXtombstoneXX": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_15(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "TOMBSTONE": tombstone_id,
                "reason": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_16(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "XXreasonXX": reason,
            }
        )

    def xǁArchiveServiceǁrequest_prune__mutmut_17(self, tombstone_id: str, *, actor: str, reason: str) -> None:
        self.dal.record_prune_request(tombstone_id, actor=actor, reason=reason)
        append_evidence(
            {
                "action": "PRUNE_REQUEST",
                "actor": actor,
                "tombstone": tombstone_id,
                "REASON": reason,
            }
        )
    
    xǁArchiveServiceǁrequest_prune__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveServiceǁrequest_prune__mutmut_1': xǁArchiveServiceǁrequest_prune__mutmut_1, 
        'xǁArchiveServiceǁrequest_prune__mutmut_2': xǁArchiveServiceǁrequest_prune__mutmut_2, 
        'xǁArchiveServiceǁrequest_prune__mutmut_3': xǁArchiveServiceǁrequest_prune__mutmut_3, 
        'xǁArchiveServiceǁrequest_prune__mutmut_4': xǁArchiveServiceǁrequest_prune__mutmut_4, 
        'xǁArchiveServiceǁrequest_prune__mutmut_5': xǁArchiveServiceǁrequest_prune__mutmut_5, 
        'xǁArchiveServiceǁrequest_prune__mutmut_6': xǁArchiveServiceǁrequest_prune__mutmut_6, 
        'xǁArchiveServiceǁrequest_prune__mutmut_7': xǁArchiveServiceǁrequest_prune__mutmut_7, 
        'xǁArchiveServiceǁrequest_prune__mutmut_8': xǁArchiveServiceǁrequest_prune__mutmut_8, 
        'xǁArchiveServiceǁrequest_prune__mutmut_9': xǁArchiveServiceǁrequest_prune__mutmut_9, 
        'xǁArchiveServiceǁrequest_prune__mutmut_10': xǁArchiveServiceǁrequest_prune__mutmut_10, 
        'xǁArchiveServiceǁrequest_prune__mutmut_11': xǁArchiveServiceǁrequest_prune__mutmut_11, 
        'xǁArchiveServiceǁrequest_prune__mutmut_12': xǁArchiveServiceǁrequest_prune__mutmut_12, 
        'xǁArchiveServiceǁrequest_prune__mutmut_13': xǁArchiveServiceǁrequest_prune__mutmut_13, 
        'xǁArchiveServiceǁrequest_prune__mutmut_14': xǁArchiveServiceǁrequest_prune__mutmut_14, 
        'xǁArchiveServiceǁrequest_prune__mutmut_15': xǁArchiveServiceǁrequest_prune__mutmut_15, 
        'xǁArchiveServiceǁrequest_prune__mutmut_16': xǁArchiveServiceǁrequest_prune__mutmut_16, 
        'xǁArchiveServiceǁrequest_prune__mutmut_17': xǁArchiveServiceǁrequest_prune__mutmut_17
    }
    
    def request_prune(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁrequest_prune__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁrequest_prune__mutmut_mutants"), args, kwargs, self)
        return result 
    
    request_prune.__signature__ = _mutmut_signature(xǁArchiveServiceǁrequest_prune__mutmut_orig)
    xǁArchiveServiceǁrequest_prune__mutmut_orig.__name__ = 'xǁArchiveServiceǁrequest_prune'

    def xǁArchiveServiceǁapprove_delete__mutmut_orig(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_1(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = True,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_2(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = None
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_3(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            None,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_4(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=None,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_5(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=None,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_6(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=None,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_7(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=None,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_8(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_9(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_10(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_11(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_12(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_13(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            None
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_14(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "XXactionXX": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_15(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "ACTION": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_16(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "XXDELETE_APPROVEDXX",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_17(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "delete_approved",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_18(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "XXtombstoneXX": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_19(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "TOMBSTONE": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_20(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "XXprimaryXX": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_21(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "PRIMARY": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_22(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "XXsecondaryXX": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_23(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "SECONDARY": secondary_actor,
                "reason": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_24(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "XXreasonXX": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_25(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "REASON": reason,
                "apply": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_26(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "XXapplyXX": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_27(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "APPLY": apply,
                "blob_scrubbed": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_28(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "XXblob_scrubbedXX": scrubbed,
            }
        )
        return scrubbed

    def xǁArchiveServiceǁapprove_delete__mutmut_29(
        self,
        tombstone_id: str,
        *,
        primary_actor: str,
        secondary_actor: str,
        reason: str,
        apply: bool = False,
    ) -> bool:
        scrubbed = self.dal.record_delete_approval(
            tombstone_id,
            primary_actor=primary_actor,
            secondary_actor=secondary_actor,
            reason=reason,
            apply=apply,
        )
        append_evidence(
            {
                "action": "DELETE_APPROVED",
                "tombstone": tombstone_id,
                "primary": primary_actor,
                "secondary": secondary_actor,
                "reason": reason,
                "apply": apply,
                "BLOB_SCRUBBED": scrubbed,
            }
        )
        return scrubbed
    
    xǁArchiveServiceǁapprove_delete__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveServiceǁapprove_delete__mutmut_1': xǁArchiveServiceǁapprove_delete__mutmut_1, 
        'xǁArchiveServiceǁapprove_delete__mutmut_2': xǁArchiveServiceǁapprove_delete__mutmut_2, 
        'xǁArchiveServiceǁapprove_delete__mutmut_3': xǁArchiveServiceǁapprove_delete__mutmut_3, 
        'xǁArchiveServiceǁapprove_delete__mutmut_4': xǁArchiveServiceǁapprove_delete__mutmut_4, 
        'xǁArchiveServiceǁapprove_delete__mutmut_5': xǁArchiveServiceǁapprove_delete__mutmut_5, 
        'xǁArchiveServiceǁapprove_delete__mutmut_6': xǁArchiveServiceǁapprove_delete__mutmut_6, 
        'xǁArchiveServiceǁapprove_delete__mutmut_7': xǁArchiveServiceǁapprove_delete__mutmut_7, 
        'xǁArchiveServiceǁapprove_delete__mutmut_8': xǁArchiveServiceǁapprove_delete__mutmut_8, 
        'xǁArchiveServiceǁapprove_delete__mutmut_9': xǁArchiveServiceǁapprove_delete__mutmut_9, 
        'xǁArchiveServiceǁapprove_delete__mutmut_10': xǁArchiveServiceǁapprove_delete__mutmut_10, 
        'xǁArchiveServiceǁapprove_delete__mutmut_11': xǁArchiveServiceǁapprove_delete__mutmut_11, 
        'xǁArchiveServiceǁapprove_delete__mutmut_12': xǁArchiveServiceǁapprove_delete__mutmut_12, 
        'xǁArchiveServiceǁapprove_delete__mutmut_13': xǁArchiveServiceǁapprove_delete__mutmut_13, 
        'xǁArchiveServiceǁapprove_delete__mutmut_14': xǁArchiveServiceǁapprove_delete__mutmut_14, 
        'xǁArchiveServiceǁapprove_delete__mutmut_15': xǁArchiveServiceǁapprove_delete__mutmut_15, 
        'xǁArchiveServiceǁapprove_delete__mutmut_16': xǁArchiveServiceǁapprove_delete__mutmut_16, 
        'xǁArchiveServiceǁapprove_delete__mutmut_17': xǁArchiveServiceǁapprove_delete__mutmut_17, 
        'xǁArchiveServiceǁapprove_delete__mutmut_18': xǁArchiveServiceǁapprove_delete__mutmut_18, 
        'xǁArchiveServiceǁapprove_delete__mutmut_19': xǁArchiveServiceǁapprove_delete__mutmut_19, 
        'xǁArchiveServiceǁapprove_delete__mutmut_20': xǁArchiveServiceǁapprove_delete__mutmut_20, 
        'xǁArchiveServiceǁapprove_delete__mutmut_21': xǁArchiveServiceǁapprove_delete__mutmut_21, 
        'xǁArchiveServiceǁapprove_delete__mutmut_22': xǁArchiveServiceǁapprove_delete__mutmut_22, 
        'xǁArchiveServiceǁapprove_delete__mutmut_23': xǁArchiveServiceǁapprove_delete__mutmut_23, 
        'xǁArchiveServiceǁapprove_delete__mutmut_24': xǁArchiveServiceǁapprove_delete__mutmut_24, 
        'xǁArchiveServiceǁapprove_delete__mutmut_25': xǁArchiveServiceǁapprove_delete__mutmut_25, 
        'xǁArchiveServiceǁapprove_delete__mutmut_26': xǁArchiveServiceǁapprove_delete__mutmut_26, 
        'xǁArchiveServiceǁapprove_delete__mutmut_27': xǁArchiveServiceǁapprove_delete__mutmut_27, 
        'xǁArchiveServiceǁapprove_delete__mutmut_28': xǁArchiveServiceǁapprove_delete__mutmut_28, 
        'xǁArchiveServiceǁapprove_delete__mutmut_29': xǁArchiveServiceǁapprove_delete__mutmut_29
    }
    
    def approve_delete(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁapprove_delete__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁapprove_delete__mutmut_mutants"), args, kwargs, self)
        return result 
    
    approve_delete.__signature__ = _mutmut_signature(xǁArchiveServiceǁapprove_delete__mutmut_orig)
    xǁArchiveServiceǁapprove_delete__mutmut_orig.__name__ = 'xǁArchiveServiceǁapprove_delete'

    def ensure_schema(self) -> None:
        self.dal.ensure_schema()

    @staticmethod
    def _coerce_settings(
        config: SettingsArchiveConfig | BackendArchiveConfig | None,
    ) -> SettingsArchiveConfig:
        if config is None:
            return SettingsArchiveConfig.load()
        if isinstance(config, SettingsArchiveConfig):
            return config
        base_settings = SettingsArchiveConfig.load()
        backend_settings = SettingsBackendConfig(backend=config.backend, url=config.url)
        return replace(base_settings, backend=backend_settings)

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_orig(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_1(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = None
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_2(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() and reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_3(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(None).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_4(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            None
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_5(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "XXactionXX": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_6(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "ACTION": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_7(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "XXRESTORE_FAILXX",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_8(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "restore_fail",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_9(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "XXactorXX": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_10(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "ACTOR": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_11(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "XXtombstoneXX": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_12(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "TOMBSTONE": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_13(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "XXreasonXX": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_14(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "REASON": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_15(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "XXbackendXX": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_16(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "BACKEND": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_17(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "XXurlXX": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_18(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "URL": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_19(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) and None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_20(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(None) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_21(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = ""
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_22(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_23(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = None
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_24(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(None).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_25(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(None)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_26(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = None

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_27(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error and error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_28(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = None
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_29(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = None

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_30(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            None,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_31(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=None,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_32(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=None,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_33(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status=None,
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_34(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=None,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_35(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=None,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_36(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=None,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_37(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_38(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_39(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_40(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_41(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_42(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_43(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="FAILED",
            detail=detail,
            logging_config=self.config.logging,
            )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_44(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="XXFAILEDXX",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )

    def xǁArchiveServiceǁ_record_restore_failure__mutmut_45(
        self,
        *,
        tombstone_id: str,
        actor: str,
        backend: str | None,
        url: str | None,
        reason: str,
        error: Exception | None = None,
    ) -> None:
        sanitized_reason = redact_text_credentials(reason).strip() or reason
        append_evidence(
            {
                "action": "RESTORE_FAIL",
                "actor": actor,
                "tombstone": tombstone_id,
                "reason": sanitized_reason,
                "backend": backend,
                "url": redact_url_credentials(url) or None,
            }
        )
        sanitized_error: str | None = None
        if error is not None:
            redacted_error = redact_text_credentials(str(error)).strip()
            sanitized_error = redacted_error or error.__class__.__name__

        detail = sanitized_reason
        if sanitized_error:
            detail = f"{sanitized_reason}: {sanitized_error}"

        log_restore(
            self.logger,
            actor=actor,
            tombstone=tombstone_id,
            status="failed",
            detail=detail,
            logging_config=self.config.logging,
            performance_config=self.config.performance,
        )
    
    xǁArchiveServiceǁ_record_restore_failure__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁArchiveServiceǁ_record_restore_failure__mutmut_1': xǁArchiveServiceǁ_record_restore_failure__mutmut_1, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_2': xǁArchiveServiceǁ_record_restore_failure__mutmut_2, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_3': xǁArchiveServiceǁ_record_restore_failure__mutmut_3, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_4': xǁArchiveServiceǁ_record_restore_failure__mutmut_4, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_5': xǁArchiveServiceǁ_record_restore_failure__mutmut_5, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_6': xǁArchiveServiceǁ_record_restore_failure__mutmut_6, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_7': xǁArchiveServiceǁ_record_restore_failure__mutmut_7, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_8': xǁArchiveServiceǁ_record_restore_failure__mutmut_8, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_9': xǁArchiveServiceǁ_record_restore_failure__mutmut_9, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_10': xǁArchiveServiceǁ_record_restore_failure__mutmut_10, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_11': xǁArchiveServiceǁ_record_restore_failure__mutmut_11, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_12': xǁArchiveServiceǁ_record_restore_failure__mutmut_12, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_13': xǁArchiveServiceǁ_record_restore_failure__mutmut_13, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_14': xǁArchiveServiceǁ_record_restore_failure__mutmut_14, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_15': xǁArchiveServiceǁ_record_restore_failure__mutmut_15, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_16': xǁArchiveServiceǁ_record_restore_failure__mutmut_16, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_17': xǁArchiveServiceǁ_record_restore_failure__mutmut_17, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_18': xǁArchiveServiceǁ_record_restore_failure__mutmut_18, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_19': xǁArchiveServiceǁ_record_restore_failure__mutmut_19, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_20': xǁArchiveServiceǁ_record_restore_failure__mutmut_20, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_21': xǁArchiveServiceǁ_record_restore_failure__mutmut_21, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_22': xǁArchiveServiceǁ_record_restore_failure__mutmut_22, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_23': xǁArchiveServiceǁ_record_restore_failure__mutmut_23, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_24': xǁArchiveServiceǁ_record_restore_failure__mutmut_24, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_25': xǁArchiveServiceǁ_record_restore_failure__mutmut_25, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_26': xǁArchiveServiceǁ_record_restore_failure__mutmut_26, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_27': xǁArchiveServiceǁ_record_restore_failure__mutmut_27, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_28': xǁArchiveServiceǁ_record_restore_failure__mutmut_28, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_29': xǁArchiveServiceǁ_record_restore_failure__mutmut_29, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_30': xǁArchiveServiceǁ_record_restore_failure__mutmut_30, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_31': xǁArchiveServiceǁ_record_restore_failure__mutmut_31, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_32': xǁArchiveServiceǁ_record_restore_failure__mutmut_32, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_33': xǁArchiveServiceǁ_record_restore_failure__mutmut_33, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_34': xǁArchiveServiceǁ_record_restore_failure__mutmut_34, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_35': xǁArchiveServiceǁ_record_restore_failure__mutmut_35, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_36': xǁArchiveServiceǁ_record_restore_failure__mutmut_36, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_37': xǁArchiveServiceǁ_record_restore_failure__mutmut_37, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_38': xǁArchiveServiceǁ_record_restore_failure__mutmut_38, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_39': xǁArchiveServiceǁ_record_restore_failure__mutmut_39, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_40': xǁArchiveServiceǁ_record_restore_failure__mutmut_40, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_41': xǁArchiveServiceǁ_record_restore_failure__mutmut_41, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_42': xǁArchiveServiceǁ_record_restore_failure__mutmut_42, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_43': xǁArchiveServiceǁ_record_restore_failure__mutmut_43, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_44': xǁArchiveServiceǁ_record_restore_failure__mutmut_44, 
        'xǁArchiveServiceǁ_record_restore_failure__mutmut_45': xǁArchiveServiceǁ_record_restore_failure__mutmut_45
    }
    
    def _record_restore_failure(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁArchiveServiceǁ_record_restore_failure__mutmut_orig"), object.__getattribute__(self, "xǁArchiveServiceǁ_record_restore_failure__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _record_restore_failure.__signature__ = _mutmut_signature(xǁArchiveServiceǁ_record_restore_failure__mutmut_orig)
    xǁArchiveServiceǁ_record_restore_failure__mutmut_orig.__name__ = 'xǁArchiveServiceǁ_record_restore_failure'


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_orig() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_1() -> str:
    try:
        result = None
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_2() -> str:
    try:
        result = subprocess.run(
            None,
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_3() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=None,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_4() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=None,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_5() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=None,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_6() -> str:
    try:
        result = subprocess.run(
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_7() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_8() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_9() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_10() -> str:
    try:
        result = subprocess.run(
            ["XXpythonXX", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_11() -> str:
    try:
        result = subprocess.run(
            ["PYTHON", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_12() -> str:
    try:
        result = subprocess.run(
            ["python", "XX--versionXX"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_13() -> str:
    try:
        result = subprocess.run(
            ["python", "--VERSION"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_14() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=False,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_15() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=False,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_16() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=False,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_17() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() and result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_18() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv(None, "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_19() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", None)


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_20() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_21() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", )


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_22() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("XXPYTHON_VERSIONXX", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_23() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("python_version", "unknown")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_24() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "XXunknownXX")


# ----------------------------------------------------------------------
# helpers
# ----------------------------------------------------------------------


def x__python_version__mutmut_25() -> str:
    try:
        result = subprocess.run(
            ["python", "--version"],
            check=True,
            capture_output=True,
            text=True,
        )
        return result.stdout.strip() or result.stderr.strip()
    except Exception:  # pragma: no cover - best effort fallback
        return os.getenv("PYTHON_VERSION", "UNKNOWN")

x__python_version__mutmut_mutants : ClassVar[MutantDict] = {
'x__python_version__mutmut_1': x__python_version__mutmut_1, 
    'x__python_version__mutmut_2': x__python_version__mutmut_2, 
    'x__python_version__mutmut_3': x__python_version__mutmut_3, 
    'x__python_version__mutmut_4': x__python_version__mutmut_4, 
    'x__python_version__mutmut_5': x__python_version__mutmut_5, 
    'x__python_version__mutmut_6': x__python_version__mutmut_6, 
    'x__python_version__mutmut_7': x__python_version__mutmut_7, 
    'x__python_version__mutmut_8': x__python_version__mutmut_8, 
    'x__python_version__mutmut_9': x__python_version__mutmut_9, 
    'x__python_version__mutmut_10': x__python_version__mutmut_10, 
    'x__python_version__mutmut_11': x__python_version__mutmut_11, 
    'x__python_version__mutmut_12': x__python_version__mutmut_12, 
    'x__python_version__mutmut_13': x__python_version__mutmut_13, 
    'x__python_version__mutmut_14': x__python_version__mutmut_14, 
    'x__python_version__mutmut_15': x__python_version__mutmut_15, 
    'x__python_version__mutmut_16': x__python_version__mutmut_16, 
    'x__python_version__mutmut_17': x__python_version__mutmut_17, 
    'x__python_version__mutmut_18': x__python_version__mutmut_18, 
    'x__python_version__mutmut_19': x__python_version__mutmut_19, 
    'x__python_version__mutmut_20': x__python_version__mutmut_20, 
    'x__python_version__mutmut_21': x__python_version__mutmut_21, 
    'x__python_version__mutmut_22': x__python_version__mutmut_22, 
    'x__python_version__mutmut_23': x__python_version__mutmut_23, 
    'x__python_version__mutmut_24': x__python_version__mutmut_24, 
    'x__python_version__mutmut_25': x__python_version__mutmut_25
}

def _python_version(*args, **kwargs):
    result = _mutmut_trampoline(x__python_version__mutmut_orig, x__python_version__mutmut_mutants, args, kwargs)
    return result 

_python_version.__signature__ = _mutmut_signature(x__python_version__mutmut_orig)
x__python_version__mutmut_orig.__name__ = 'x__python_version'
