"""
Intent Inferer - Infer code intent using heuristics and LLM.

Combines deterministic heuristics with LLM-based semantic analysis
to produce a structured intent specification.

Author: mbaetiong
Generated: 2025-12-17

Safeguards:
- Confidence scoring for uncertainty
- Heuristic fallback when LLM unavailable
- Assumption tracking
- Conservative inference mode
"""

from __future__ import annotations

import json
import logging
from dataclasses import dataclass, field
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Literal, Optional

logger = logging.getLogger(__name__)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@dataclass
class InputSpec:
    """Specification for a code input."""
    name: str
    type: Literal["cli_arg", "stdin", "file", "env_var", "network"]
    required: bool = True


@dataclass
class OutputSpec:
    """Specification for a code output."""
    name: str
    type: Literal["stdout", "stderr", "file", "network", "return_value"]


@dataclass
class IntentSpec:
    """Inferred intent specification for code.
    
    Represents the inferred purpose, inputs, outputs, and constraints
    of analyzed code. Includes confidence score and tracking of
    assumptions made during inference.
    
    Attributes:
        snapshot_id: Reference to the analyzed snapshot
        timestamp: When inference was performed
        goal: High-level purpose of the code
        actors: Who/what interacts with this code
        inputs: list of input specifications
        outputs: list of output specifications
        constraints: Behavioral constraints
        side_effects: Potential side effects
        confidence: Confidence score (0.0-1.0)
        inference_method: How intent was inferred
        llm_provenance_ref: Reference to LLM provenance record
        assumptions: Assumptions made during inference
    """
    snapshot_id: str
    timestamp: datetime
    goal: str
    actors: list[str] = field(default_factory=list)
    inputs: list[InputSpec] = field(default_factory=list)
    outputs: list[OutputSpec] = field(default_factory=list)
    constraints: list[str] = field(default_factory=list)
    side_effects: list[str] = field(default_factory=list)
    confidence: float = 0.5
    inference_method: Literal["heuristic", "llm", "hybrid"] = "heuristic"
    llm_provenance_ref: Optional[str] = None
    assumptions: list[str] = field(default_factory=list)
    
    def to_dict(self) -> dict[str, Any]:
        """Convert to dictionary for serialization."""
        return {
            "snapshot_id": self.snapshot_id,
            "timestamp": self.timestamp.isoformat(),
            "goal": self.goal,
            "actors": self.actors,
            "inputs": [
                {"name": i.name, "type": i.type, "required": i.required}
                for i in self.inputs
            ],
            "outputs": [
                {"name": o.name, "type": o.type}
                for o in self.outputs
            ],
            "constraints": self.constraints,
            "side_effects": self.side_effects,
            "confidence": self.confidence,
            "inference_method": self.inference_method,
            "llm_provenance_ref": self.llm_provenance_ref,
            "assumptions": self.assumptions,
        }
    
    def save(self, path: Path) -> None:
        """Save intent spec to YAML file."""
        try:
            import yaml
            with path.open("w", encoding="utf-8") as f:
                yaml.dump(self.to_dict(), f, default_flow_style=False)
        except ImportError as e:
            logger.debug(f"ImportError: {e}")
            logger.warning(f"ImportError: {e}", exc_info=True)
            # Fallback to JSON
            with path.open("w", encoding="utf-8") as f:
                json.dump(self.to_dict(), f, indent=2)


def x__detect_cli_tool__mutmut_orig(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_1(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = None
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_2(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"XXargparseXX", "click", "typer", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_3(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"ARGPARSE", "click", "typer", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_4(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "XXclickXX", "typer", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_5(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "CLICK", "typer", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_6(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "XXtyperXX", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_7(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "TYPER", "fire", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_8(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "XXfireXX", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_9(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "FIRE", "docopt"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_10(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "fire", "XXdocoptXX"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_11(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "fire", "DOCOPT"}
    return bool(cli_indicators & set(imports))


def x__detect_cli_tool__mutmut_12(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "fire", "docopt"}
    return bool(None)


def x__detect_cli_tool__mutmut_13(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "fire", "docopt"}
    return bool(cli_indicators | set(imports))


def x__detect_cli_tool__mutmut_14(imports: list[str], exports: list[str]) -> bool:
    """Detect if code is a CLI tool."""
    cli_indicators = {"argparse", "click", "typer", "fire", "docopt"}
    return bool(cli_indicators & set(None))

x__detect_cli_tool__mutmut_mutants : ClassVar[MutantDict] = {
'x__detect_cli_tool__mutmut_1': x__detect_cli_tool__mutmut_1, 
    'x__detect_cli_tool__mutmut_2': x__detect_cli_tool__mutmut_2, 
    'x__detect_cli_tool__mutmut_3': x__detect_cli_tool__mutmut_3, 
    'x__detect_cli_tool__mutmut_4': x__detect_cli_tool__mutmut_4, 
    'x__detect_cli_tool__mutmut_5': x__detect_cli_tool__mutmut_5, 
    'x__detect_cli_tool__mutmut_6': x__detect_cli_tool__mutmut_6, 
    'x__detect_cli_tool__mutmut_7': x__detect_cli_tool__mutmut_7, 
    'x__detect_cli_tool__mutmut_8': x__detect_cli_tool__mutmut_8, 
    'x__detect_cli_tool__mutmut_9': x__detect_cli_tool__mutmut_9, 
    'x__detect_cli_tool__mutmut_10': x__detect_cli_tool__mutmut_10, 
    'x__detect_cli_tool__mutmut_11': x__detect_cli_tool__mutmut_11, 
    'x__detect_cli_tool__mutmut_12': x__detect_cli_tool__mutmut_12, 
    'x__detect_cli_tool__mutmut_13': x__detect_cli_tool__mutmut_13, 
    'x__detect_cli_tool__mutmut_14': x__detect_cli_tool__mutmut_14
}

def _detect_cli_tool(*args, **kwargs):
    result = _mutmut_trampoline(x__detect_cli_tool__mutmut_orig, x__detect_cli_tool__mutmut_mutants, args, kwargs)
    return result 

_detect_cli_tool.__signature__ = _mutmut_signature(x__detect_cli_tool__mutmut_orig)
x__detect_cli_tool__mutmut_orig.__name__ = 'x__detect_cli_tool'


def x__detect_gui_app__mutmut_orig(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_1(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = None
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_2(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"XXtkinterXX", "PyQt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_3(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"TKINTER", "PyQt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_4(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "XXPyQt5XX", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_5(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "pyqt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_6(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PYQT5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_7(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "XXPyQt6XX", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_8(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "pyqt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_9(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PYQT6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_10(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "XXPySide6XX", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_11(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "pyside6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_12(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PYSIDE6", "wx", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_13(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "XXwxXX", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_14(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "WX", "kivy"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_15(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "wx", "XXkivyXX"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_16(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "wx", "KIVY"}
    return bool(gui_indicators & set(imports))


def x__detect_gui_app__mutmut_17(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(None)


def x__detect_gui_app__mutmut_18(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators | set(imports))


def x__detect_gui_app__mutmut_19(imports: list[str]) -> bool:
    """Detect if code is a GUI application."""
    gui_indicators = {"tkinter", "PyQt5", "PyQt6", "PySide6", "wx", "kivy"}
    return bool(gui_indicators & set(None))

x__detect_gui_app__mutmut_mutants : ClassVar[MutantDict] = {
'x__detect_gui_app__mutmut_1': x__detect_gui_app__mutmut_1, 
    'x__detect_gui_app__mutmut_2': x__detect_gui_app__mutmut_2, 
    'x__detect_gui_app__mutmut_3': x__detect_gui_app__mutmut_3, 
    'x__detect_gui_app__mutmut_4': x__detect_gui_app__mutmut_4, 
    'x__detect_gui_app__mutmut_5': x__detect_gui_app__mutmut_5, 
    'x__detect_gui_app__mutmut_6': x__detect_gui_app__mutmut_6, 
    'x__detect_gui_app__mutmut_7': x__detect_gui_app__mutmut_7, 
    'x__detect_gui_app__mutmut_8': x__detect_gui_app__mutmut_8, 
    'x__detect_gui_app__mutmut_9': x__detect_gui_app__mutmut_9, 
    'x__detect_gui_app__mutmut_10': x__detect_gui_app__mutmut_10, 
    'x__detect_gui_app__mutmut_11': x__detect_gui_app__mutmut_11, 
    'x__detect_gui_app__mutmut_12': x__detect_gui_app__mutmut_12, 
    'x__detect_gui_app__mutmut_13': x__detect_gui_app__mutmut_13, 
    'x__detect_gui_app__mutmut_14': x__detect_gui_app__mutmut_14, 
    'x__detect_gui_app__mutmut_15': x__detect_gui_app__mutmut_15, 
    'x__detect_gui_app__mutmut_16': x__detect_gui_app__mutmut_16, 
    'x__detect_gui_app__mutmut_17': x__detect_gui_app__mutmut_17, 
    'x__detect_gui_app__mutmut_18': x__detect_gui_app__mutmut_18, 
    'x__detect_gui_app__mutmut_19': x__detect_gui_app__mutmut_19
}

def _detect_gui_app(*args, **kwargs):
    result = _mutmut_trampoline(x__detect_gui_app__mutmut_orig, x__detect_gui_app__mutmut_mutants, args, kwargs)
    return result 

_detect_gui_app.__signature__ = _mutmut_signature(x__detect_gui_app__mutmut_orig)
x__detect_gui_app__mutmut_orig.__name__ = 'x__detect_gui_app'


def x__detect_web_service__mutmut_orig(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_1(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = None
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_2(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"XXflaskXX", "fastapi", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_3(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"FLASK", "fastapi", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_4(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "XXfastapiXX", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_5(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "FASTAPI", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_6(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "XXdjangoXX", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_7(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "DJANGO", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_8(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "XXbottleXX", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_9(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "BOTTLE", "tornado", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_10(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "XXtornadoXX", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_11(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "TORNADO", "aiohttp"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_12(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "tornado", "XXaiohttpXX"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_13(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "tornado", "AIOHTTP"}
    return bool(web_indicators & set(imports))


def x__detect_web_service__mutmut_14(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "tornado", "aiohttp"}
    return bool(None)


def x__detect_web_service__mutmut_15(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators | set(imports))


def x__detect_web_service__mutmut_16(imports: list[str]) -> bool:
    """Detect if code is a web service."""
    web_indicators = {"flask", "fastapi", "django", "bottle", "tornado", "aiohttp"}
    return bool(web_indicators & set(None))

x__detect_web_service__mutmut_mutants : ClassVar[MutantDict] = {
'x__detect_web_service__mutmut_1': x__detect_web_service__mutmut_1, 
    'x__detect_web_service__mutmut_2': x__detect_web_service__mutmut_2, 
    'x__detect_web_service__mutmut_3': x__detect_web_service__mutmut_3, 
    'x__detect_web_service__mutmut_4': x__detect_web_service__mutmut_4, 
    'x__detect_web_service__mutmut_5': x__detect_web_service__mutmut_5, 
    'x__detect_web_service__mutmut_6': x__detect_web_service__mutmut_6, 
    'x__detect_web_service__mutmut_7': x__detect_web_service__mutmut_7, 
    'x__detect_web_service__mutmut_8': x__detect_web_service__mutmut_8, 
    'x__detect_web_service__mutmut_9': x__detect_web_service__mutmut_9, 
    'x__detect_web_service__mutmut_10': x__detect_web_service__mutmut_10, 
    'x__detect_web_service__mutmut_11': x__detect_web_service__mutmut_11, 
    'x__detect_web_service__mutmut_12': x__detect_web_service__mutmut_12, 
    'x__detect_web_service__mutmut_13': x__detect_web_service__mutmut_13, 
    'x__detect_web_service__mutmut_14': x__detect_web_service__mutmut_14, 
    'x__detect_web_service__mutmut_15': x__detect_web_service__mutmut_15, 
    'x__detect_web_service__mutmut_16': x__detect_web_service__mutmut_16
}

def _detect_web_service(*args, **kwargs):
    result = _mutmut_trampoline(x__detect_web_service__mutmut_orig, x__detect_web_service__mutmut_mutants, args, kwargs)
    return result 

_detect_web_service.__signature__ = _mutmut_signature(x__detect_web_service__mutmut_orig)
x__detect_web_service__mutmut_orig.__name__ = 'x__detect_web_service'


def x__detect_networked__mutmut_orig(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_1(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = None
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_2(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"XXrequestsXX", "httpx", "urllib", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_3(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"REQUESTS", "httpx", "urllib", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_4(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "XXhttpxXX", "urllib", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_5(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "HTTPX", "urllib", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_6(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "XXurllibXX", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_7(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "URLLIB", "socket", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_8(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "XXsocketXX", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_9(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "SOCKET", "aiohttp"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_10(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "socket", "XXaiohttpXX"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_11(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "socket", "AIOHTTP"}
    return bool(network_indicators & set(imports))


def x__detect_networked__mutmut_12(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "socket", "aiohttp"}
    return bool(None)


def x__detect_networked__mutmut_13(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "socket", "aiohttp"}
    return bool(network_indicators | set(imports))


def x__detect_networked__mutmut_14(imports: list[str]) -> bool:
    """Detect if code makes network calls."""
    network_indicators = {"requests", "httpx", "urllib", "socket", "aiohttp"}
    return bool(network_indicators & set(None))

x__detect_networked__mutmut_mutants : ClassVar[MutantDict] = {
'x__detect_networked__mutmut_1': x__detect_networked__mutmut_1, 
    'x__detect_networked__mutmut_2': x__detect_networked__mutmut_2, 
    'x__detect_networked__mutmut_3': x__detect_networked__mutmut_3, 
    'x__detect_networked__mutmut_4': x__detect_networked__mutmut_4, 
    'x__detect_networked__mutmut_5': x__detect_networked__mutmut_5, 
    'x__detect_networked__mutmut_6': x__detect_networked__mutmut_6, 
    'x__detect_networked__mutmut_7': x__detect_networked__mutmut_7, 
    'x__detect_networked__mutmut_8': x__detect_networked__mutmut_8, 
    'x__detect_networked__mutmut_9': x__detect_networked__mutmut_9, 
    'x__detect_networked__mutmut_10': x__detect_networked__mutmut_10, 
    'x__detect_networked__mutmut_11': x__detect_networked__mutmut_11, 
    'x__detect_networked__mutmut_12': x__detect_networked__mutmut_12, 
    'x__detect_networked__mutmut_13': x__detect_networked__mutmut_13, 
    'x__detect_networked__mutmut_14': x__detect_networked__mutmut_14
}

def _detect_networked(*args, **kwargs):
    result = _mutmut_trampoline(x__detect_networked__mutmut_orig, x__detect_networked__mutmut_mutants, args, kwargs)
    return result 

_detect_networked.__signature__ = _mutmut_signature(x__detect_networked__mutmut_orig)
x__detect_networked__mutmut_orig.__name__ = 'x__detect_networked'


def x__detect_data_processing__mutmut_orig(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_1(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = None
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_2(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"XXpandasXX", "numpy", "polars", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_3(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"PANDAS", "numpy", "polars", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_4(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "XXnumpyXX", "polars", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_5(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "NUMPY", "polars", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_6(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "XXpolarsXX", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_7(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "POLARS", "dask", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_8(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "XXdaskXX", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_9(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "DASK", "csv", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_10(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "XXcsvXX", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_11(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "CSV", "json"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_12(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "csv", "XXjsonXX"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_13(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "csv", "JSON"}
    return bool(data_indicators & set(imports))


def x__detect_data_processing__mutmut_14(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "csv", "json"}
    return bool(None)


def x__detect_data_processing__mutmut_15(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "csv", "json"}
    return bool(data_indicators | set(imports))


def x__detect_data_processing__mutmut_16(imports: list[str]) -> bool:
    """Detect if code is for data processing."""
    data_indicators = {"pandas", "numpy", "polars", "dask", "csv", "json"}
    return bool(data_indicators & set(None))

x__detect_data_processing__mutmut_mutants : ClassVar[MutantDict] = {
'x__detect_data_processing__mutmut_1': x__detect_data_processing__mutmut_1, 
    'x__detect_data_processing__mutmut_2': x__detect_data_processing__mutmut_2, 
    'x__detect_data_processing__mutmut_3': x__detect_data_processing__mutmut_3, 
    'x__detect_data_processing__mutmut_4': x__detect_data_processing__mutmut_4, 
    'x__detect_data_processing__mutmut_5': x__detect_data_processing__mutmut_5, 
    'x__detect_data_processing__mutmut_6': x__detect_data_processing__mutmut_6, 
    'x__detect_data_processing__mutmut_7': x__detect_data_processing__mutmut_7, 
    'x__detect_data_processing__mutmut_8': x__detect_data_processing__mutmut_8, 
    'x__detect_data_processing__mutmut_9': x__detect_data_processing__mutmut_9, 
    'x__detect_data_processing__mutmut_10': x__detect_data_processing__mutmut_10, 
    'x__detect_data_processing__mutmut_11': x__detect_data_processing__mutmut_11, 
    'x__detect_data_processing__mutmut_12': x__detect_data_processing__mutmut_12, 
    'x__detect_data_processing__mutmut_13': x__detect_data_processing__mutmut_13, 
    'x__detect_data_processing__mutmut_14': x__detect_data_processing__mutmut_14, 
    'x__detect_data_processing__mutmut_15': x__detect_data_processing__mutmut_15, 
    'x__detect_data_processing__mutmut_16': x__detect_data_processing__mutmut_16
}

def _detect_data_processing(*args, **kwargs):
    result = _mutmut_trampoline(x__detect_data_processing__mutmut_orig, x__detect_data_processing__mutmut_mutants, args, kwargs)
    return result 

_detect_data_processing.__signature__ = _mutmut_signature(x__detect_data_processing__mutmut_orig)
x__detect_data_processing__mutmut_orig.__name__ = 'x__detect_data_processing'


def x__infer_heuristic__mutmut_orig(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_1(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = None
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_2(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(None)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_3(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = None
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_4(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get(None, "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_5(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", None)
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_6(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_7(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", )
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_8(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("XXsnapshot_idXX", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_9(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("SNAPSHOT_ID", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_10(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "XXunknownXX")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_11(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "UNKNOWN")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_12(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = None
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_13(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get(None, []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_14(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", None):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_15(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get([]):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_16(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", ):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_17(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("XXfilesXX", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_18(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("FILES", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_19(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(None)
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_20(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get(None, []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_21(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", None))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_22(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get([]))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_23(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", ))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_24(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("XXimportsXX", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_25(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("IMPORTS", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_26(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = None
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_27(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(None)
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_28(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(None))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_29(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = None
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_30(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "XXUnknown Python codeXX"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_31(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "unknown python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_32(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "UNKNOWN PYTHON CODE"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_33(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = None
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_34(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["XXuserXX"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_35(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["USER"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_36(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = None
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_37(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = None
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_38(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = None
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_39(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = None
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_40(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = None
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_41(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 1.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_42(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = None
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_43(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(None, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_44(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, None):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_45(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool([]):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_46(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, ):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_47(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = None
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_48(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "XXCommand-line tool for processing input and producing outputXX"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_49(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_50(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "COMMAND-LINE TOOL FOR PROCESSING INPUT AND PRODUCING OUTPUT"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_51(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = None
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_52(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["XXuserXX", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_53(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["USER", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_54(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "XXshellXX"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_55(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "SHELL"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_56(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(None)
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_57(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name=None, type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_58(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type=None, required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_59(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=None))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_60(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_61(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_62(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", ))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_63(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="XXargsXX", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_64(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="ARGS", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_65(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="XXcli_argXX", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_66(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="CLI_ARG", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_67(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_68(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(None)
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_69(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name=None, type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_70(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type=None))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_71(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_72(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", ))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_73(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="XXresultXX", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_74(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="RESULT", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_75(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="XXstdoutXX"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_76(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="STDOUT"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_77(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = None
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_78(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 1.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_79(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(None):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_80(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = None
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_81(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "XXWeb service providing HTTP API endpointsXX"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_82(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "web service providing http api endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_83(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "WEB SERVICE PROVIDING HTTP API ENDPOINTS"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_84(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = None
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_85(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["XXhttp_clientXX", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_86(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["HTTP_CLIENT", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_87(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "XXuserXX"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_88(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "USER"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_89(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(None)
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_90(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name=None, type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_91(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type=None, required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_92(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=None))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_93(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_94(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_95(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", ))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_96(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="XXrequestXX", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_97(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="REQUEST", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_98(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="XXnetworkXX", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_99(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="NETWORK", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_100(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=False))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_101(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(None)
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_102(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name=None, type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_103(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type=None))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_104(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_105(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", ))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_106(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="XXresponseXX", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_107(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="RESPONSE", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_108(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="XXnetworkXX"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_109(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="NETWORK"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_110(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append(None)
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_111(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("XXListens on network portXX")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_112(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_113(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("LISTENS ON NETWORK PORT")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_114(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = None
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_115(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 1.8
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_116(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(None):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_117(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = None
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_118(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "XXGraphical user interface applicationXX"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_119(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_120(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "GRAPHICAL USER INTERFACE APPLICATION"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_121(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = None
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_122(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["XXuserXX"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_123(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["USER"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_124(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(None)
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_125(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name=None, type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_126(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type=None, required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_127(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=None))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_128(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_129(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_130(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", ))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_131(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="XXuser_inputXX", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_132(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="USER_INPUT", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_133(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="XXstdinXX", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_134(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="STDIN", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_135(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=True))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_136(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(None)
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_137(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name=None, type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_138(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type=None))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_139(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_140(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", ))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_141(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="XXdisplayXX", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_142(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="DISPLAY", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_143(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="XXstdoutXX"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_144(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="STDOUT"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_145(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append(None)
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_146(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("XXCreates GUI windowXX")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_147(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("creates gui window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_148(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("CREATES GUI WINDOW")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_149(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = None
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_150(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 1.7
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_151(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(None):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_152(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = None
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_153(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "XXData processing script for transforming datasetsXX"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_154(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_155(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "DATA PROCESSING SCRIPT FOR TRANSFORMING DATASETS"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_156(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = None
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_157(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["XXuserXX", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_158(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["USER", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_159(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "XXdata_sourceXX"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_160(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "DATA_SOURCE"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_161(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(None)
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_162(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name=None, type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_163(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type=None, required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_164(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=None))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_165(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_166(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_167(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", ))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_168(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="XXinput_dataXX", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_169(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="INPUT_DATA", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_170(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="XXfileXX", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_171(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="FILE", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_172(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=False))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_173(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(None)
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_174(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name=None, type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_175(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type=None))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_176(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_177(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", ))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_178(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="XXoutput_dataXX", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_179(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="OUTPUT_DATA", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_180(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="XXfileXX"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_181(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="FILE"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_182(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = None
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_183(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 1.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_184(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(None):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_185(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = None
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_186(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "XXNetworked application that communicates over HTTP/TCPXX"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_187(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "networked application that communicates over http/tcp"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_188(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "NETWORKED APPLICATION THAT COMMUNICATES OVER HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_189(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = None
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_190(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["XXuserXX", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_191(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["USER", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_192(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "XXremote_serverXX"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_193(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "REMOTE_SERVER"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_194(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(None)
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_195(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name=None, type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_196(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type=None, required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_197(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=None))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_198(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_199(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_200(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", ))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_201(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="XXrequest_paramsXX", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_202(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="REQUEST_PARAMS", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_203(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="XXcli_argXX", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_204(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="CLI_ARG", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_205(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_206(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(None)
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_207(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name=None, type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_208(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type=None))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_209(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_210(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", ))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_211(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="XXresponseXX", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_212(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="RESPONSE", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_213(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="XXstdoutXX"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_214(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="STDOUT"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_215(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append(None)
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_216(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("XXMakes network requestsXX")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_217(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_218(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("MAKES NETWORK REQUESTS")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_219(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append(None)
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_220(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("XXRequires network accessXX")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_221(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_222(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("REQUIRES NETWORK ACCESS")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_223(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = None
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_224(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 1.6
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_225(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = None
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_226(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "XXPython script for general-purpose computationXX"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_227(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_228(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "PYTHON SCRIPT FOR GENERAL-PURPOSE COMPUTATION"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_229(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append(None)
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_230(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("XXNo specific framework detected, assuming general scriptXX")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_231(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("no specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_232(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("NO SPECIFIC FRAMEWORK DETECTED, ASSUMING GENERAL SCRIPT")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_233(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = None
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_234(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 1.4
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_235(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "XXif __name__XX" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_236(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "IF __NAME__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_237(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" not in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_238(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append(None)
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_239(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("XXHas executable entry pointXX")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_240(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_241(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("HAS EXECUTABLE ENTRY POINT")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_242(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = None
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_243(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(None, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_244(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, None)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_245(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_246(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, )
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_247(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence - 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_248(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 1.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_249(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 2.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_250(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=None,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_251(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=None,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_252(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=None,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_253(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=None,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_254(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=None,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_255(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=None,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_256(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=None,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_257(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=None,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_258(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=None,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_259(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method=None,
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_260(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=None,
    )


def x__infer_heuristic__mutmut_261(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_262(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_263(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_264(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_265(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_266(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_267(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_268(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        confidence=confidence,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_269(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        inference_method="heuristic",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_270(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_271(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="heuristic",
        )


def x__infer_heuristic__mutmut_272(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="XXheuristicXX",
        assumptions=assumptions,
    )


def x__infer_heuristic__mutmut_273(
    static_report: dict[str, Any],
    source_excerpt: str,
) -> IntentSpec:
    """Infer intent using deterministic heuristics.
    
    Args:
        static_report: Static analysis report data
        source_excerpt: First portion of main source file
        
    Returns:
        IntentSpec from heuristic analysis
    """
    now = datetime.now(timezone.utc)
    snapshot_id = static_report.get("snapshot_id", "unknown")
    
    # Collect all imports across files
    all_imports: list[str] = []
    for file_data in static_report.get("files", []):
        all_imports.extend(file_data.get("imports", []))
    all_imports = list(set(all_imports))
    
    # Determine code type
    goal = "Unknown Python code"
    actors = ["user"]
    inputs: list[InputSpec] = []
    outputs: list[OutputSpec] = []
    constraints: list[str] = []
    side_effects: list[str] = []
    confidence = 0.5
    assumptions: list[str] = []
    
    if _detect_cli_tool(all_imports, []):
        goal = "Command-line tool for processing input and producing output"
        actors = ["user", "shell"]
        inputs.append(InputSpec(name="args", type="cli_arg", required=True))
        outputs.append(OutputSpec(name="result", type="stdout"))
        confidence = 0.75
    elif _detect_web_service(all_imports):
        goal = "Web service providing HTTP API endpoints"
        actors = ["http_client", "user"]
        inputs.append(InputSpec(name="request", type="network", required=True))
        outputs.append(OutputSpec(name="response", type="network"))
        side_effects.append("Listens on network port")
        confidence = 0.80
    elif _detect_gui_app(all_imports):
        goal = "Graphical user interface application"
        actors = ["user"]
        inputs.append(InputSpec(name="user_input", type="stdin", required=False))
        outputs.append(OutputSpec(name="display", type="stdout"))
        side_effects.append("Creates GUI window")
        confidence = 0.70
    elif _detect_data_processing(all_imports):
        goal = "Data processing script for transforming datasets"
        actors = ["user", "data_source"]
        inputs.append(InputSpec(name="input_data", type="file", required=True))
        outputs.append(OutputSpec(name="output_data", type="file"))
        confidence = 0.65
    elif _detect_networked(all_imports):
        goal = "Networked application that communicates over HTTP/TCP"
        actors = ["user", "remote_server"]
        inputs.append(InputSpec(name="request_params", type="cli_arg", required=False))
        outputs.append(OutputSpec(name="response", type="stdout"))
        side_effects.append("Makes network requests")
        constraints.append("Requires network access")
        confidence = 0.60
    else:
        goal = "Python script for general-purpose computation"
        assumptions.append("No specific framework detected, assuming general script")
        confidence = 0.40
    
    # Check for main entry point patterns
    if "if __name__" in source_excerpt:
        constraints.append("Has executable entry point")
        confidence = min(confidence + 0.1, 1.0)
    
    return IntentSpec(
        snapshot_id=snapshot_id,
        timestamp=now,
        goal=goal,
        actors=actors,
        inputs=inputs,
        outputs=outputs,
        constraints=constraints,
        side_effects=side_effects,
        confidence=confidence,
        inference_method="HEURISTIC",
        assumptions=assumptions,
    )

x__infer_heuristic__mutmut_mutants : ClassVar[MutantDict] = {
'x__infer_heuristic__mutmut_1': x__infer_heuristic__mutmut_1, 
    'x__infer_heuristic__mutmut_2': x__infer_heuristic__mutmut_2, 
    'x__infer_heuristic__mutmut_3': x__infer_heuristic__mutmut_3, 
    'x__infer_heuristic__mutmut_4': x__infer_heuristic__mutmut_4, 
    'x__infer_heuristic__mutmut_5': x__infer_heuristic__mutmut_5, 
    'x__infer_heuristic__mutmut_6': x__infer_heuristic__mutmut_6, 
    'x__infer_heuristic__mutmut_7': x__infer_heuristic__mutmut_7, 
    'x__infer_heuristic__mutmut_8': x__infer_heuristic__mutmut_8, 
    'x__infer_heuristic__mutmut_9': x__infer_heuristic__mutmut_9, 
    'x__infer_heuristic__mutmut_10': x__infer_heuristic__mutmut_10, 
    'x__infer_heuristic__mutmut_11': x__infer_heuristic__mutmut_11, 
    'x__infer_heuristic__mutmut_12': x__infer_heuristic__mutmut_12, 
    'x__infer_heuristic__mutmut_13': x__infer_heuristic__mutmut_13, 
    'x__infer_heuristic__mutmut_14': x__infer_heuristic__mutmut_14, 
    'x__infer_heuristic__mutmut_15': x__infer_heuristic__mutmut_15, 
    'x__infer_heuristic__mutmut_16': x__infer_heuristic__mutmut_16, 
    'x__infer_heuristic__mutmut_17': x__infer_heuristic__mutmut_17, 
    'x__infer_heuristic__mutmut_18': x__infer_heuristic__mutmut_18, 
    'x__infer_heuristic__mutmut_264': x__infer_heuristic__mutmut_264, 
    'x__infer_heuristic__mutmut_265': x__infer_heuristic__mutmut_265, 
    'x__infer_heuristic__mutmut_266': x__infer_heuristic__mutmut_266, 
    'x__infer_heuristic__mutmut_267': x__infer_heuristic__mutmut_267, 
    'x__infer_heuristic__mutmut_268': x__infer_heuristic__mutmut_268, 
    'x__infer_heuristic__mutmut_269': x__infer_heuristic__mutmut_269, 
    'x__infer_heuristic__mutmut_270': x__infer_heuristic__mutmut_270, 
    'x__infer_heuristic__mutmut_271': x__infer_heuristic__mutmut_271, 
    'x__infer_heuristic__mutmut_272': x__infer_heuristic__mutmut_272, 
    'x__infer_heuristic__mutmut_273': x__infer_heuristic__mutmut_273
}

def _infer_heuristic(*args, **kwargs):
    result = _mutmut_trampoline(x__infer_heuristic__mutmut_orig, x__infer_heuristic__mutmut_mutants, args, kwargs)
    return result 

_infer_heuristic.__signature__ = _mutmut_signature(x__infer_heuristic__mutmut_orig)
x__infer_heuristic__mutmut_orig.__name__ = 'x__infer_heuristic'


def x_infer_intent__mutmut_orig(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_1(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "XXXX",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_2(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = True,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_3(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = None
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_4(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(None, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_5(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, None)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_6(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_7(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, )
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_8(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm or llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_9(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_10(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = None
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_11(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "XXstatic_summaryXX": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_12(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "STATIC_SUMMARY": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_13(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get(None, {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_14(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", None),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_15(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get({}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_16(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", ),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_17(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("XXsummaryXX", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_18(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("SUMMARY", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_19(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "XXimportsXX": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_20(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "IMPORTS": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_21(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "XXsource_excerptXX": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_22(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "SOURCE_EXCERPT": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_23(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8001],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_24(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get(None, []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_25(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", None):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_26(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get([]):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_27(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", ):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_28(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("XXfilesXX", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_29(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("FILES", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_30(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(None)
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_31(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["XXimportsXX"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_32(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["IMPORTS"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_33(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get(None, []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_34(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", None))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_35(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get([]))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_36(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", ))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_37(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("XXimportsXX", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_38(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("IMPORTS", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_39(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = None
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_40(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["XXimportsXX"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_41(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["IMPORTS"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_42(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(None)
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_43(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(None))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_44(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["XXimportsXX"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_45(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["IMPORTS"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_46(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = None
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_47(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["XXruntime_observationsXX"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_48(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["RUNTIME_OBSERVATIONS"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_49(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get(None, [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_50(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", None)
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_51(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get([])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_52(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", )
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_53(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("XXexecution_resultsXX", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_54(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("EXECUTION_RESULTS", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_55(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = None
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_56(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(None)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_57(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get(None):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_58(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("XXgoalXX"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_59(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("GOAL"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_60(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = None
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_61(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["XXgoalXX"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_62(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["GOAL"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_63(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get(None):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_64(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("XXconfidenceXX"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_65(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("CONFIDENCE"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_66(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = None
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_67(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) * 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_68(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence - llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_69(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["XXconfidenceXX"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_70(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["CONFIDENCE"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_71(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 3
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_72(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = None
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_73(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "XXhybridXX"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_74(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "HYBRID"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_75(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir or llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_76(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get(None):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_77(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("XXprovenance_refXX"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_78(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("PROVENANCE_REF"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_79(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = None
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_80(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["XXprovenance_refXX"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_81(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["PROVENANCE_REF"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_82(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info(None, intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_83(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", None)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_84(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info(intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_85(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", )
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_86(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("XXEnhanced intent with LLM: confidence=%.2fXX", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_87(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("enhanced intent with llm: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_88(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("ENHANCED INTENT WITH LLM: CONFIDENCE=%.2F", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_89(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(None)
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_90(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning(None, e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_91(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", None)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_92(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning(e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_93(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", )
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_94(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("XXLLM enhancement failed, using heuristic only: %sXX", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_95(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("llm enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_96(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM ENHANCEMENT FAILED, USING HEURISTIC ONLY: %S", e)
            intent.assumptions.append(f"LLM enhancement failed: {e}")
    
    return intent


def x_infer_intent__mutmut_97(
    static_report: dict[str, Any],
    runtime_report: Optional[dict[str, Any]] = None,
    source_excerpt: str = "",
    use_llm: bool = False,
    llm_client: Optional[Any] = None,
    provenance_dir: Optional[Path] = None,
) -> IntentSpec:
    """Infer intent for analyzed code.
    
    Combines deterministic heuristics with optional LLM-based analysis
    to produce a structured intent specification.
    
    Args:
        static_report: Static analysis report dictionary
        runtime_report: Optional runtime analysis report
        source_excerpt: First portion of main source file (max 500 lines)
        use_llm: Whether to use LLM for enhanced inference
        llm_client: Optional LLM client instance
        provenance_dir: Directory for storing LLM provenance
        
    Returns:
        IntentSpec with inferred intent
        
    Example:
        >>> with open("static-report.json") as f:
        ...     static = json.load(f)
        >>> intent = infer_intent(static, source_excerpt=source[:500])
        >>> print(f"Goal: {intent.goal} (confidence: {intent.confidence})")
    """
    # Start with heuristic inference
    intent = _infer_heuristic(static_report, source_excerpt)
    
    # Optionally enhance with LLM
    if use_llm and llm_client is not None:
        try:
            
            # Build context for LLM
            context = {
                "static_summary": static_report.get("summary", {}),
                "imports": [],
                "source_excerpt": source_excerpt[:8000],  # Token budget
            }
            
            for file_data in static_report.get("files", []):
                context["imports"].extend(file_data.get("imports", []))
            context["imports"] = list(set(context["imports"]))
            
            # Add runtime observations if available
            if runtime_report:
                context["runtime_observations"] = runtime_report.get("execution_results", [])
            
            # Call LLM for enhanced inference
            llm_result = llm_client.infer_intent(context)
            
            if llm_result:
                # Merge LLM insights with heuristic base
                if llm_result.get("goal"):
                    intent.goal = llm_result["goal"]
                if llm_result.get("confidence"):
                    intent.confidence = (intent.confidence + llm_result["confidence"]) / 2
                intent.inference_method = "hybrid"
                
                # Store provenance
                if provenance_dir and llm_result.get("provenance_ref"):
                    intent.llm_provenance_ref = llm_result["provenance_ref"]
                
                logger.info("Enhanced intent with LLM: confidence=%.2f", intent.confidence)
                
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.warning("LLM enhancement failed, using heuristic only: %s", e)
            intent.assumptions.append(None)
    
    return intent

x_infer_intent__mutmut_mutants : ClassVar[MutantDict] = {
'x_infer_intent__mutmut_1': x_infer_intent__mutmut_1, 
    'x_infer_intent__mutmut_2': x_infer_intent__mutmut_2, 
    'x_infer_intent__mutmut_3': x_infer_intent__mutmut_3, 
    'x_infer_intent__mutmut_4': x_infer_intent__mutmut_4, 
    'x_infer_intent__mutmut_5': x_infer_intent__mutmut_5, 
    'x_infer_intent__mutmut_6': x_infer_intent__mutmut_6, 
    'x_infer_intent__mutmut_7': x_infer_intent__mutmut_7, 
    'x_infer_intent__mutmut_8': x_infer_intent__mutmut_8, 
    'x_infer_intent__mutmut_9': x_infer_intent__mutmut_9, 
    'x_infer_intent__mutmut_10': x_infer_intent__mutmut_10, 
    'x_infer_intent__mutmut_11': x_infer_intent__mutmut_11, 
    'x_infer_intent__mutmut_12': x_infer_intent__mutmut_12, 
    'x_infer_intent__mutmut_13': x_infer_intent__mutmut_13, 
    'x_infer_intent__mutmut_14': x_infer_intent__mutmut_14, 
    'x_infer_intent__mutmut_15': x_infer_intent__mutmut_15, 
    'x_infer_intent__mutmut_16': x_infer_intent__mutmut_16, 
    'x_infer_intent__mutmut_17': x_infer_intent__mutmut_17, 
    'x_infer_intent__mutmut_18': x_infer_intent__mutmut_18, 
    'x_infer_intent__mutmut_19': x_infer_intent__mutmut_19, 
    'x_infer_intent__mutmut_20': x_infer_intent__mutmut_20, 
    'x_infer_intent__mutmut_21': x_infer_intent__mutmut_21, 
    'x_infer_intent__mutmut_22': x_infer_intent__mutmut_22, 
    'x_infer_intent__mutmut_23': x_infer_intent__mutmut_23, 
    'x_infer_intent__mutmut_24': x_infer_intent__mutmut_24, 
    'x_infer_intent__mutmut_25': x_infer_intent__mutmut_25, 
    'x_infer_intent__mutmut_26': x_infer_intent__mutmut_26, 
    'x_infer_intent__mutmut_27': x_infer_intent__mutmut_27, 
    'x_infer_intent__mutmut_28': x_infer_intent__mutmut_28, 
    'x_infer_intent__mutmut_29': x_infer_intent__mutmut_29, 
    'x_infer_intent__mutmut_30': x_infer_intent__mutmut_30, 
    'x_infer_intent__mutmut_31': x_infer_intent__mutmut_31, 
    'x_infer_intent__mutmut_32': x_infer_intent__mutmut_32, 
    'x_infer_intent__mutmut_33': x_infer_intent__mutmut_33, 
    'x_infer_intent__mutmut_34': x_infer_intent__mutmut_34, 
    'x_infer_intent__mutmut_35': x_infer_intent__mutmut_35, 
    'x_infer_intent__mutmut_36': x_infer_intent__mutmut_36, 
    'x_infer_intent__mutmut_37': x_infer_intent__mutmut_37, 
    'x_infer_intent__mutmut_38': x_infer_intent__mutmut_38, 
    'x_infer_intent__mutmut_39': x_infer_intent__mutmut_39, 
    'x_infer_intent__mutmut_40': x_infer_intent__mutmut_40, 
    'x_infer_intent__mutmut_41': x_infer_intent__mutmut_41, 
    'x_infer_intent__mutmut_42': x_infer_intent__mutmut_42, 
    'x_infer_intent__mutmut_43': x_infer_intent__mutmut_43, 
    'x_infer_intent__mutmut_44': x_infer_intent__mutmut_44, 
    'x_infer_intent__mutmut_45': x_infer_intent__mutmut_45, 
    'x_infer_intent__mutmut_46': x_infer_intent__mutmut_46, 
    'x_infer_intent__mutmut_47': x_infer_intent__mutmut_47, 
    'x_infer_intent__mutmut_48': x_infer_intent__mutmut_48, 
    'x_infer_intent__mutmut_49': x_infer_intent__mutmut_49, 
    'x_infer_intent__mutmut_50': x_infer_intent__mutmut_50, 
    'x_infer_intent__mutmut_51': x_infer_intent__mutmut_51, 
    'x_infer_intent__mutmut_52': x_infer_intent__mutmut_52, 
    'x_infer_intent__mutmut_53': x_infer_intent__mutmut_53, 
    'x_infer_intent__mutmut_54': x_infer_intent__mutmut_54, 
    'x_infer_intent__mutmut_55': x_infer_intent__mutmut_55, 
    'x_infer_intent__mutmut_56': x_infer_intent__mutmut_56, 
    'x_infer_intent__mutmut_57': x_infer_intent__mutmut_57, 
    'x_infer_intent__mutmut_58': x_infer_intent__mutmut_58, 
    'x_infer_intent__mutmut_59': x_infer_intent__mutmut_59, 
    'x_infer_intent__mutmut_60': x_infer_intent__mutmut_60, 
    'x_infer_intent__mutmut_61': x_infer_intent__mutmut_61, 
    'x_infer_intent__mutmut_62': x_infer_intent__mutmut_62, 
    'x_infer_intent__mutmut_63': x_infer_intent__mutmut_63, 
    'x_infer_intent__mutmut_64': x_infer_intent__mutmut_64, 
    'x_infer_intent__mutmut_65': x_infer_intent__mutmut_65, 
    'x_infer_intent__mutmut_66': x_infer_intent__mutmut_66, 
    'x_infer_intent__mutmut_67': x_infer_intent__mutmut_67, 
    'x_infer_intent__mutmut_68': x_infer_intent__mutmut_68, 
    'x_infer_intent__mutmut_69': x_infer_intent__mutmut_69, 
    'x_infer_intent__mutmut_70': x_infer_intent__mutmut_70, 
    'x_infer_intent__mutmut_71': x_infer_intent__mutmut_71, 
    'x_infer_intent__mutmut_72': x_infer_intent__mutmut_72, 
    'x_infer_intent__mutmut_73': x_infer_intent__mutmut_73, 
    'x_infer_intent__mutmut_74': x_infer_intent__mutmut_74, 
    'x_infer_intent__mutmut_75': x_infer_intent__mutmut_75, 
    'x_infer_intent__mutmut_76': x_infer_intent__mutmut_76, 
    'x_infer_intent__mutmut_77': x_infer_intent__mutmut_77, 
    'x_infer_intent__mutmut_78': x_infer_intent__mutmut_78, 
    'x_infer_intent__mutmut_79': x_infer_intent__mutmut_79, 
    'x_infer_intent__mutmut_80': x_infer_intent__mutmut_80, 
    'x_infer_intent__mutmut_81': x_infer_intent__mutmut_81, 
    'x_infer_intent__mutmut_82': x_infer_intent__mutmut_82, 
    'x_infer_intent__mutmut_83': x_infer_intent__mutmut_83, 
    'x_infer_intent__mutmut_84': x_infer_intent__mutmut_84, 
    'x_infer_intent__mutmut_85': x_infer_intent__mutmut_85, 
    'x_infer_intent__mutmut_86': x_infer_intent__mutmut_86, 
    'x_infer_intent__mutmut_87': x_infer_intent__mutmut_87, 
    'x_infer_intent__mutmut_88': x_infer_intent__mutmut_88, 
    'x_infer_intent__mutmut_89': x_infer_intent__mutmut_89, 
    'x_infer_intent__mutmut_90': x_infer_intent__mutmut_90, 
    'x_infer_intent__mutmut_91': x_infer_intent__mutmut_91, 
    'x_infer_intent__mutmut_92': x_infer_intent__mutmut_92, 
    'x_infer_intent__mutmut_93': x_infer_intent__mutmut_93, 
    'x_infer_intent__mutmut_94': x_infer_intent__mutmut_94, 
    'x_infer_intent__mutmut_95': x_infer_intent__mutmut_95, 
    'x_infer_intent__mutmut_96': x_infer_intent__mutmut_96, 
    'x_infer_intent__mutmut_97': x_infer_intent__mutmut_97
}

def infer_intent(*args, **kwargs):
    result = _mutmut_trampoline(x_infer_intent__mutmut_orig, x_infer_intent__mutmut_mutants, args, kwargs)
    return result 

infer_intent.__signature__ = _mutmut_signature(x_infer_intent__mutmut_orig)
x_infer_intent__mutmut_orig.__name__ = 'x_infer_intent'
